---
milestone: v1.1
audited: 2026-07-20
status: passed
scores:
  requirements: 7/7
  phases: 3/3
  integration: 4/4
  flows: 2/2
gaps: {}
tech_debt:
  - phase: 11-mcm-facts-into-sift-analyze-golden-eval-case
    items:
      - "IN-01 (low): granted-MB figure formatted two ways (mcm_facts.py:107 vs mcm_report.py) — share one helper. Tracked: .planning/todos/pending/2026-07-20-phase11-code-review-info.md"
      - "IN-03 (low): redundant re.DOTALL / cosmetic double newline in the MCM splice (hypothesise.py:90,106). Same todo."
nyquist:
  compliant_phases: ["11"]
  partial_phases: []
  not_validated_phases: ["09", "10"]
  missing_phases: []
  overall: "phase 11 COMPLIANT (status: validated, nyquist_compliant: true); phases 9-10 predate the validate-phase status field (#2117) — draft, not a compliance failure; all their VERIFICATIONs passed"
---

# Milestone v1.1 — MCM Memory-Pressure Analysis — Audit

**Status: PASSED.** All 7 requirements satisfied, all 3 phases verified `passed`, cross-phase
integration sound, both E2E flows complete. Only low-severity cosmetic tech debt (tracked).

## Definition of Done (from ROADMAP v1.1)

> Turn a DSSErrors.log into a deterministic, evidence-cited MCM memory-pressure forensics report
> — every distinct denial episode analysed for headroom descent, memory breakdown, diagnostic
> flags, and per-object/session memory attribution — with those facts also feeding the LLM
> hypothesis pipeline (as cited evidence, never model-authored).

Met: the `sift mcm` deterministic bundle (report + CSV) ships the forensics; `sift analyze` now
injects the same deterministic facts as **citable** evidence (`cited ⊆ prompted ⊆ store`), with a
regression-gated golden case. Figures are computed by `analyse_mcm`, never authored by the model.

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | Traceability | VERIFICATION | Integration | Final |
|-----|-------|--------------|--------------|-------------|-------|
| MCM-01 detect every denial episode (full lifecycle) | 9 | Complete | passed | WIRED | ✅ satisfied |
| MCM-02 parse denial-time memory breakdown + MCM settings | 9 | Complete | passed | WIRED | ✅ satisfied |
| MCM-03 machine-independent diagnostic flags | 10 | Complete | passed | WIRED | ✅ satisfied |
| MCM-04 auto lead-up window + per-OID/Source/SID attribution | 10 | Complete | passed | WIRED | ✅ satisfied |
| MCM-05 deterministic report + CSV export (`sift mcm`) | 10 | Complete | passed | WIRED | ✅ satisfied |
| MCM-06 MCM facts feed `sift analyze` as cited evidence | 11 | Complete | passed | WIRED | ✅ satisfied |
| MCM-07 golden MCM eval case, regression-gated | 11 | Complete | passed | WIRED | ✅ satisfied |

**7/7 satisfied. No unsatisfied or orphaned requirements.**

> Note: phase SUMMARY `requirements-completed` frontmatter is empty across v1.1 (a longstanding
> project convention — requirements are marked in REQUIREMENTS.md via tooling, not SUMMARY YAML).
> The authoritative signals (per-phase VERIFICATION.md evidence tables + traceability table) agree,
> so this is a documentation-convention note, not a coverage gap.

## Phase Verification Summary

| Phase | Name | Status | Requirements |
|-------|------|--------|--------------|
| 9 | MCM Episode Detection & Denial-Time Memory Breakdown | passed | MCM-01, MCM-02 |
| 10 | Diagnostic Flags, Lead-Up Attribution & `sift mcm` Report + CSV | passed | MCM-03, MCM-04, MCM-05 |
| 11 | MCM Facts into `sift analyze` + Golden Eval Case | passed | MCM-06, MCM-07 |

Phase 11 also passed all four post-execution gates: goal-backward verification (5/5), security
(SECURED, threats_open 0), Nyquist (compliant), code-review (1 WARNING now fixed — WR-01, MCM fact
block capped at 8 episodes; 2 low INFO items tracked as a todo).

## Cross-Phase Integration (integration checker)

**All four seams WIRED, no gaps:**
1. **P9 → P10:** `sift mcm` consumes `analyse_mcm` output; report/CSV pure over `McmAnalysis`, no recompute; attribution rows carry real `event_id`s.
2. **P9/P10 → P11:** `mcm_facts` and `mcm_report` share the SAME `McmAnalysis` types — one source of truth for the numbers; `sift analyze` figures match `sift mcm`.
3. **P11 injection chokepoint:** MCM block built inside `hypothesise()` — the point both `cli.analyze` and `eval/runner._run_pipeline` funnel through — so the golden case is non-vacuous (MCM-07).
4. **E2E + no-MCM byte-identical:** denial case drives both `sift mcm` and `sift analyze` off the same ingested events; a no-MCM case leaves `sift analyze` byte-identical (regression-locked).

## Quality Gate

`uv run ruff check` clean · `uv run pyright` 0 errors · `uv run pytest` 537 passed / 8 deselected
(independently re-run by the orchestrator, not trusted from subagent self-reports).

## Deferred / Out of Scope (unchanged)

- **PERF-01** — DSSPerformanceMonitor PDH-CSV time-series correlation — deferred to v2 (SEED-001).

## Tech Debt (low, tracked)

- IN-01 / IN-03 (Phase 11 code-review INFO) — cosmetic formatting/regex tidy — tracked in
  `.planning/todos/pending/2026-07-20-phase11-code-review-info.md`. Non-blocking.
