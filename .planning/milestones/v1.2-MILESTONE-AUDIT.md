---
milestone: v1.2
audited: 2026-07-20
re_audited: 2026-07-20
status: passed
scores:
  requirements: 8/8
  phases: 3/3
  integration: 6/6
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: [12, 13, 14]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: COMPLIANT
tech_debt:
  - phase: 13-episode-correlation-hazard-flags-sift-perfmon-report-csv
    items:
      - "Code review: 13-REVIEW-FIX.md status:partial — 7/8 in-scope findings fixed, 1 skipped (non-blocking; WR-03 addressed separately in 13-REVIEW-FIX-WR03.md status:fixed)."
      - "INFO (13-VERIFICATION): PerfmonAnalysis.hazards is always () — case-level hazard branch unreachable in production; YAGNI field, deletion candidate if v2 does not claim it."
      - "INFO (13-VERIFICATION): residual reserved-prefix namespace collision (counter named 'counter.host' vs 'host') — requires attacker-controlled CSV header + contrived name pair; not worth code now."
  - phase: 12-dssperfmon-adapter-pipeline-exclusion
    items:
      - "Code review: 12-REVIEW.md status:issues_found — 0 critical, 5 warning, 2 info; phase verified passed, findings are non-blocking quality items."
resolved_since_first_audit:
  - "Phase 13 Nyquist: 13-VALIDATION.md promoted draft → validated, nyquist_compliant: true (commit efe50fe). Was the only NOT-VALIDATED phase; now COMPLIANT."
  - "PERF-02 wording: REQUIREMENTS.md line 13 reworded to the ADR-0012 amended contract ('recorded in attrs as evidence rather than applied as a shift'). Advisory closed, no code change."
  - "ROADMAP staleness: Phase 13 checkbox (line 70) ticked [x] and Progress table row (line 288) set 6/6 Complete. Cosmetic doc fix applied during this re-audit."
---

# Milestone v1.2 — DSSPerformanceMonitor Correlation — Audit

**First audited:** 2026-07-20 · **Re-audited:** 2026-07-20 (this document)
**Status:** ✅ passed
**Scope:** Phases 12–14 (PERF-01…PERF-08)

v1.2 extends v1.1's shape one layer out: **adapter → deterministic analyser/correlator →
report → LLM facts → eval**. It adds DSSPerformanceMonitor PDH-CSV as a second, independent
evidence source and correlates its memory counters against the MCM denial episodes v1.1 already
detects. The load-bearing invariant — every numeric figure computed before generation and only
then handed to the model as citable evidence (the model narrates, never authors) — is preserved
verbatim and independently verified below.

## Re-audit note

This audit was first written on 2026-07-20. Since then the only repository changes have been
`docs()` commits — **no `src/`, `tests/`, or `eval/` change since `a5b6c85` (phase-14 complete)**,
confirmed by an empty `git diff --stat a5b6c85 HEAD -- src/ tests/ eval/`. The three doc commits
closed the three items the first audit flagged "fix before archiving":

- `efe50fe` — Phase 13 `validate-phase` reconciled `13-VALIDATION.md` to `status: validated`,
  `nyquist_compliant: true`. The single NOT-VALIDATED phase is now **COMPLIANT**.
- `e69bdbb` / `c1ba1ed` — Phase 14 validation + security frontmatter filled (both green).
- REQUIREMENTS.md PERF-02 wording reworded to the amended ADR-0012 contract.
- ROADMAP.md Phase 13 checkbox + progress row corrected during this re-audit.

Because the runtime code is byte-identical to the first audit, the cross-phase integration verdict
(6/6 WIRED, traced on real data-flow) is carried forward unchanged rather than re-traced. The
quality gate was **re-run fresh at HEAD** (below) and is green.

## Verdict

All 8 requirements satisfied, all 3 phases verified passed, all 6 cross-phase seams wired end-to-end
on real data-flow, all 3 E2E flows complete, security clean (0 open threats ≥ high on all phases),
Nyquist COMPLIANT on all three phases, and the full quality gate is green. **No blockers.** A small
set of non-blocking, INFO-level tech-debt items remains for optional review.

## Requirements Coverage (3-source cross-reference)

Sources cross-referenced per REQ-ID: REQUIREMENTS.md traceability · phase VERIFICATION.md · phase
SUMMARY frontmatter. VERIFICATION.md is authoritative where SUMMARY `requirements_completed`
frontmatter is unpopulated (a pre-existing tooling gap in these phases — see note).

| REQ-ID | Phase | Traceability | VERIFICATION | Integration | Final |
|--------|-------|--------------|--------------|-------------|-------|
| PERF-01 | 12 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-02 | 12 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-03 | 12 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-04 | 13 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-05 | 13 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-06 | 13 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-07 | 14 | Complete | ✓ SATISFIED | WIRED | **satisfied** |
| PERF-08 | 14 | Complete | ✓ SATISFIED | WIRED | **satisfied** |

**SUMMARY frontmatter note:** only `14-02-SUMMARY.md` carries `requirements_completed` (PERF-07);
the other per-plan SUMMARYs left the field blank. This is a frontmatter-population gap, **not** a
delivery gap — each REQ-ID is verified SATISFIED in its phase VERIFICATION.md (12: 5/5, 13: 5/5,
14: 11/11 must-haves) and mapped Complete in REQUIREMENTS.md traceability. Two independent
authoritative sources agree; the third (SUMMARY frontmatter) is silent, not contradictory.

**Orphans:** none. REQUIREMENTS.md maps exactly PERF-01…08 to Phases 12–14; every REQ-ID is claimed
by phase plan frontmatter and verified. 0 unmapped, 0 unsatisfied.

## Phase Verification Summary

| Phase | Status | Score | Security | Nyquist |
|-------|--------|-------|----------|---------|
| 12 — `dssperfmon` Adapter & Pipeline Exclusion | ✅ passed | 5/5 truths | verified (0 open ≥ high) | COMPLIANT |
| 13 — Correlation, Hazard Flags & `sift perfmon` | ✅ passed | 5/5 truths | verified (0 open ≥ high) | **COMPLIANT** (was draft; promoted `efe50fe`) |
| 14 — Perfmon Facts into `sift analyze` + Golden Eval | ✅ passed | 11/11 truths | verified (0 open ≥ high) | COMPLIANT |

Phase 13's two escalated requirement-wording judgments (WARNING 1: full-lead-up fallback span
identity; WARNING 2: non-overlap hazard is time-only not host-aware) were both resolved and accepted
as documented D-03 / PERFV2-02-scoped limitations — `human_verification_resolved: 2026-07-20`.

## Cross-Phase Integration (6/6 WIRED — carried forward, code unchanged)

Traced end-to-end on the runtime path (not import existence) in the first audit; 42 targeted tests
pass and the runtime code is byte-identical at this re-audit:

1. **Store exclusion → correlator (PERF-03):** `EXCLUDED_FROM_RANKING={"dssperfmon"}` applied in
   exactly one place (`iter_event_summaries`); correlator reads via unfiltered `query_events` +
   in-memory `source=="dssperfmon"` filter — held out of ranking, still citable. **WIRED**
2. **Correlator → fact renderer → hypothesise splice (PERF-07):** same analysis object flows
   through; printed `[evt:]` ids unioned into `prompted_ids`; citation gate enforces
   cited ⊆ prompted ⊆ store. **WIRED**
3. **Window reuse (PERF-04):** `_resolve_span` consumes `EpisodeAnalysis.window.start_event_id` from
   Phase 10's MCM-04; never recomputes. **WIRED**
4. **E2E flow A — `sift perfmon`, perfmon CSV, no DSSErrors log (PERF-06):** file-scope path, exit 0,
   report + CSV. **WIRED**
5. **E2E flow B — `sift analyze` cites perfmon ids; no-perfmon prompt byte-identical (PERF-07):**
   empty analysis → `("", set())` → whole sentinel block stripped, pre-phase bytes restored. **WIRED**
6. **E2E flow C — `sift eval` perfmon-denial golden case, non-vacuous gate (PERF-08):** counterfactual
   test proves the citation-sensitivity gate turns red when injection is stripped. **WIRED**

## Deterministic-core-vs-LLM boundary

Independently confirmed intact: figures are a pure function of the store computed in
`analyse_perfmon`/`render_perfmon_facts` before generation, the `perfmon_facts.md` template carries
zero authored digits, and only pre-printed `[evt:]` ids enter the citable universe. Anti-authoring
proven by `test_model_cannot_alter_perfmon_figures` (a planted wrong figure 999999.999 never enters
the prompt).

## Quality Gate (independently re-run at HEAD during this re-audit)

| Gate | Result |
|------|--------|
| `ruff check` | All checks passed |
| `pyright` | 0 errors, 0 warnings, 0 informations |
| `pytest` | 658 passed, 8 deselected |

The 8 deselected tests are the live-inference cases; live `sift eval` returns 400 on `/v1/embeddings`
in this sandbox (local ONNX/OGA model lacks embeddings) — a pre-existing operator condition, not a
phase gap. The authoritative offline MockTransport eval gate is green.

## Tech Debt (non-blocking — optional review)

**Phase 13**
- **Review partial:** `13-REVIEW-FIX.md` is `status: partial` (7/8 fixed, 1 skipped, non-blocking).
  WR-03 handled separately in `13-REVIEW-FIX-WR03.md` (`status: fixed`).
- INFO: `PerfmonAnalysis.hazards` always `()` (unreachable case-level branch — YAGNI, deletion
  candidate); residual reserved-prefix namespace collision (needs attacker-controlled header).

**Phase 12**
- `12-REVIEW.md` `status: issues_found` — 0 critical / 5 warning / 2 info; phase still verified passed.

**Resolved since first audit** (were listed as debt on 2026-07-20):
- Phase 13 Nyquist NOT-VALIDATED → **validated / COMPLIANT** (`efe50fe`).
- PERF-02 REQUIREMENTS.md wording → reworded to ADR-0012 amended contract.
- ROADMAP.md Phase 13 checkbox + progress row → corrected (this re-audit).

---
*Audit first generated 2026-07-20 by /gsd-audit-milestone (integration verified by
gsd-integration-checker). Re-audited 2026-07-20 at HEAD after doc-only follow-up commits; runtime
code confirmed unchanged, quality gate re-run green.*
