---
phase: 13-episode-correlation-hazard-flags-sift-perfmon-report-csv
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/sift/pipeline/perfmon.py
  - tests/test_perfmon.py
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "D-01: the correlation span is obtained by resolving BOTH ends by `event_id` — `window.start_event_id` and `episode.denial_event_id` looked up against the passed event list and read off `Event.ts`. `McmEpisode.denial_ts` (a `str`) is never parsed for the end bound."
    - "D-02: no new window logic exists — the correlator consumes `EpisodeAnalysis.window` from the `McmAnalysis` it is handed and never calls `select_window` itself."
    - "D-03: when `window.start_event_id` is `None`, the span starts at the earliest event in `episode.event_ids` that carries a non-`None` `ts`, and still ends at the denial."
    - "D-04: when a resolved boundary event has `ts=None` or is absent from the event list, the episode yields no trend and carries a graded hazard naming the reason; the correlator never walks outward to a neighbouring timestamped event and never falls back to parsing `denial_ts`."
    - "D-05: sample selection is the closed interval `start_ts <= sample.ts <= end_ts` — a sample landing exactly on either boundary is INCLUDED — in the store's canonical order (`ORDER BY ts IS NULL, ts, source_file, line_start`), which the correlator preserves rather than re-sorting."
    - "D-07: figures are computed for every counter that parses numerically, with no allowlist — a counter set unlike Hartford's 22 is fully covered."
    - "D-08: slope is `(last - first) / seconds_elapsed` in counter-units per second, rounded at source to 4 decimal places; no least-squares fit and no accumulated summation across samples."
    - "D-09: the at-denial value is the last in-span sample at or before the denial boundary, carrying that sample's `event_id` as its citation; the figure is never interpolated."
    - "D-10: the peak carries the `event_id` of the sample it came from; on a tie the earliest sample wins, and that tie-break is stated in a comment so a later refactor cannot silently flip it."
    - "D-11: a counter value is accepted only when `math.isfinite(float(v))` holds; a non-finite or unparseable cell excludes that counter for that sample only — the row is retained — and the exclusion is reported as a per-counter `excluded_samples` count rather than silently dropped."
    - "D-12: hazards travel in a new `PerfmonHazard` model with `frozen=True, extra=\"forbid\"` and the same `event_ids` provenance discipline as `DiagnosticFlag`; `mcm.DiagnosticFlag` is neither reused nor relaxed."
    - "D-21: the correlator is pure and deterministic — no `set` iteration on any output path, all rounding at source, all ordering explicit, no model involvement; `model_dump_json` is byte-identical on re-run."
    - "A span containing exactly one sample (or several sharing a timestamp) yields `slope = None` with no hazard and no `ZeroDivisionError`; at-denial and peak remain computed and reported for that window."
    - "When several samples compare equal on timestamp, output order is the store's canonical order with `source_file` then `line_start` as tie-breakers — stable across runs."
    - "Counters appear in a deterministic, explicitly-sorted order within each trend group, so two runs emit identical sequences."
  prohibitions:
    - "The correlator must never present a fabricated or inferred alignment as a real one — it must not fill a non-overlap, an unresolvable boundary timestamp, or an excluded non-finite sample with a plausible-looking substitute figure."
  artifacts:
    - src/sift/pipeline/perfmon.py
  key_links:
    - "`analyse_perfmon(analysis, events)` ← `McmAnalysis` produced by `analyse_mcm` (mcm.py:956)"
    - "`analyse_perfmon` ← the single `store.query_events()` result, filtered in-memory on `event.source == \"dssperfmon\"`"
    - "`PerfmonAnalysis` → `render_perfmon_*` (plan 13-05) and the `perfmon` CLI command (plan 13-06)"
---

<objective>
Create `src/sift/pipeline/perfmon.py`: the frozen model layer, the finite-only numeric converter, span
resolution from MCM-04's existing window, and the three per-counter trend figures.

Purpose: PERF-04. This is the phase's load-bearing computation — every figure must be reproducible by
hand from two cited sample rows, and every figure must carry the `event_id` it came from.

Output: `PerfmonHazard`, `CounterTrend`, `TrendGroup`, `PerfmonAnalysis`, `analyse_perfmon`, and the
six PERF-04 unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-CONTEXT.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-RESEARCH.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-PATTERNS.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-01-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan (new symbols — not pre-existing):

| Symbol | File | Kind |
|--------|------|------|
| `PerfmonHazard` | `src/sift/pipeline/perfmon.py` | Pydantic model |
| `CounterTrend` | `src/sift/pipeline/perfmon.py` | Pydantic model |
| `TrendGroup` | `src/sift/pipeline/perfmon.py` | Pydantic model |
| `PerfmonAnalysis` | `src/sift/pipeline/perfmon.py` | Pydantic model |
| `analyse_perfmon` | `src/sift/pipeline/perfmon.py` | function |
| `_numeric` | `src/sift/pipeline/perfmon.py` | private function |
| `_resolve_span` | `src/sift/pipeline/perfmon.py` | private function |
| `_counter_trends` | `src/sift/pipeline/perfmon.py` | private function |
| `SLOPE_DP` | `src/sift/pipeline/perfmon.py` | module constant (value `4`) |

Created by OTHER plans in this phase (excluded from drift verification):
`src/sift/render/perfmon_report.py`, `render_perfmon_markdown`, `render_perfmon_json`,
`write_perfmon_trend_csv`, `PERFMON_CSV_HEADER`, `_csv_safe`, `PerfmonFormat`, the `perfmon` CLI
command, `_DRIFT_ATTR`, `_NOTE_CAP`, `tests/_perfmon_fixtures.py` and its builders.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frozen model layer and the finite-only numeric converter (D-07, D-11, D-12, D-21)</name>
  <files>src/sift/pipeline/perfmon.py, tests/test_perfmon.py</files>
  <read_first>
    - src/sift/pipeline/mcm.py lines 181-306 — `McmEpisode`, `EpisodeWindow`, `DiagnosticFlag`,
      `EpisodeAnalysis`, `McmAnalysis`. Copy the house model conventions exactly: `model_config`
      declared first, `frozen=True, extra="forbid"`, tuples never lists, one inline `#` comment per
      field naming its allowed values and its rounding, and a docstring stating the invariant.
    - src/sift/pipeline/mcm.py lines 609-624 — `_grade`, the two-cut-point grader. Read it ONLY to
      confirm the divergence is deliberate; it is not called from this module.
    - src/sift/adapters/dssperfmon.py lines 130-155 — `_bad_cells`. Note that its bare `float()`
      probe ACCEPTS non-finite tokens, which is exactly the gap `_numeric` closes.
    - tests/_perfmon_fixtures.py — `write_non_finite_csv` from plan 13-01.
    - tests/test_perfmon.py — the `ingest_perfmon_slice` and `log_boundary_event` helpers.
  </read_first>
  <behavior>
    - `_numeric("266042")` returns `266042.0`; `_numeric("N/A")` returns `None`;
      `_numeric("nan")`, `_numeric("inf")`, `_numeric("-Infinity")` and `_numeric("NAN")` each
      return `None`.
    - A `PerfmonHazard` rejects an unknown field (`extra="forbid"`) and rejects mutation
      (`frozen=True`).
    - `CounterTrend` accepts `at_denial=None`, `slope_per_second=None` and `peak=None`
      independently, so a partially-computable counter is representable without a sentinel number.
    - `PerfmonAnalysis(groups=(), hazards=())` constructs successfully — the empty case is a valid
      value, never an error.
  </behavior>
  <action>
    Create `src/sift/pipeline/perfmon.py` with `from __future__ import annotations`, importing only
    `math` from the standard library plus `BaseModel`/`ConfigDict` from pydantic and `Event` from
    `sift.models` under `if TYPE_CHECKING`. The module must remain SQL-free, typer-free and
    print-free per the pipeline-tier convention.

    Define `SLOPE_DP = 4` as a module constant and use it as the second argument to `round` wherever
    a slope is produced (D-08's round-at-source discipline, mirroring `_mb_bytes`'s 3 dp at
    `mcm_report.py:79`).

    Define `_numeric(value: str) -> float | None`: attempt `float(value)` inside a `try` catching
    `ValueError`, then return the parsed value only when `math.isfinite(parsed)` holds, else `None`.
    Its docstring must state that `dssperfmon._bad_cells` probes with a bare `float()` which accepts
    non-finite tokens, so such a cell reaches the store on a clean `severity="info"` row — this
    function is the guard that stops it reaching slope, peak and at-denial (D-11).

    Define four frozen models, all `model_config = ConfigDict(frozen=True, extra="forbid")`:

    `PerfmonHazard` — `dimension: str`, `severity: str`, `message: str`,
    `event_ids: tuple[str, ...]`, `value: float | None = None`. Its docstring must state that
    severities are categorical string literals fixed in code and that `mcm._grade` is deliberately
    not called, so the omission does not read as an oversight; and that this model deliberately does
    not reuse `mcm.DiagnosticFlag`, whose `value_pct` is locked as a ratio (D-12).

    `CounterTrend` — `counter: str`, `at_denial: float | None`, `at_denial_event_id: str | None`,
    `slope_per_second: float | None`, `peak: float | None`, `peak_event_id: str | None`,
    `sample_count: int`, `excluded_samples: int`. `excluded_samples` is D-11's reporting channel:
    the count of in-span samples whose value for this counter was rejected as non-finite or
    unparseable.

    `TrendGroup` — `scope: str` (allowed values `"episode"` and `"file"`), `key: str` (the
    `denial_event_id` for episode scope, the `source_file` for file scope), `label: str` (the window
    label, or a full-sample-range label for file scope), `start_ts: str | None`, `end_ts: str | None`,
    `boundary_event_ids: tuple[str, ...]`, `sample_count: int`,
    `counters: tuple[CounterTrend, ...]`, `hazards: tuple[PerfmonHazard, ...]`. One model serves
    both D-19's per-episode shape and D-20's per-file shape; state that choice in the docstring so a
    reviewer does not read `scope` as speculative generality.

    `PerfmonAnalysis` — `groups: tuple[TrendGroup, ...]`, `hazards: tuple[PerfmonHazard, ...]`
    (case-level hazards not attributable to one group). Its docstring must carry the equivalent of
    `McmAnalysis`'s guarantee: an empty case yields `groups=()` and never a crash; and must state
    the determinism contract (D-21) in the same words `analyse_mcm` uses at `mcm.py:956` —
    `model_dump_json` is byte-identical on re-run, with no `set` iteration anywhere on the path.

    Add the unit tests from `<behavior>` to `tests/test_perfmon.py` as
    `test_numeric_rejects_non_finite`, `test_hazard_model_frozen_and_strict` and
    `test_empty_analysis_constructs`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perfmon.py -x -q -k "numeric or hazard_model or empty_analysis"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon.py -x -q -k "numeric or hazard_model or empty_analysis"` passes.
    - `_numeric` returns `None` for each of `"nan"`, `"NAN"`, `"inf"`, `"-Infinity"`, `"N/A"`, `""`,
      and returns `266042.0` for `"266042"` — all six asserted in the test.
    - `PerfmonHazard(dimension="x", severity="warn", message="m", event_ids=(), extra_field=1)`
      raises a pydantic `ValidationError`, asserted in the test.
    - Mutating a constructed `PerfmonHazard` attribute raises, asserted in the test.
    - `PerfmonAnalysis(groups=(), hazards=())` constructs without error, asserted in the test.
    - The import block of `src/sift/pipeline/perfmon.py` contains only `math` from the standard
      library, `pydantic`, and `sift.*` names — no third-party numeric library appears, since only
      `httpx`, `pydantic`, `pyyaml`, `rich`, `scikit-learn`, `sqlite-vec`, `typer` and `zstandard`
      are declared runtime dependencies in `pyproject.toml`.
    - `SLOPE_DP` is defined at module level with value `4`.
    - `uv run ruff check` clean; `uv run pyright` 0 errors; `uv run pytest` full suite green.
    - Fail-fast: if any of these three tests passes on its first run, temporarily invert the guard it
      exercises (for example make `_numeric` skip the `math.isfinite` check), confirm the test fails,
      then restore the file byte-identically before committing.
  </acceptance_criteria>
  <done>The model layer and the finite-only converter exist, each proven by a test that fails without them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Span resolution from MCM-04's existing window (D-01, D-02, D-03, D-04, D-05)</name>
  <files>src/sift/pipeline/perfmon.py, tests/test_perfmon.py</files>
  <read_first>
    - src/sift/pipeline/mcm.py lines 541-575 — `select_window`, including the fallback branch at
      558-566 that returns `threshold_pct=0, start_event_id=None, label="full available lead-up"`.
      This function is CONSUMED, never called from this module (D-02).
    - src/sift/pipeline/mcm.py lines 895-950 — `attribute_window`, whose head resolution
      `window.start_event_id or (ep.event_ids[0] if ep.event_ids else None)` is the shape to mirror
      but NOT the rule to copy: D-03 requires the earliest `event_ids` entry whose resolved
      `Event.ts` is not `None`, which `event_ids[0]` does not guarantee.
    - src/sift/store.py lines 570-600 — `query_events`, specifically the
      `ORDER BY ts IS NULL, ts, source_file, line_start` clause. That clause IS D-05's canonical
      ordering; preserve it rather than re-sorting.
    - src/sift/models.py lines 18-47 — `Event.ts` is `datetime | None`, UTC-aware.
    - tests/test_perfmon.py — `log_boundary_event` from plan 13-01, which supplies boundary events
      with a controllable `ts` including `None`.
  </read_first>
  <behavior>
    - `span_from_event_ids`: with `window.start_event_id` and `episode.denial_event_id` both
      resolving to timestamped events, the span is exactly those two `Event.ts` values — neither is
      derived from `McmEpisode.denial_ts`.
    - `span_full_leadup_fallback`: with `window.start_event_id = None` and an `episode.event_ids`
      tuple whose FIRST entry carries `ts=None` and whose second carries a real `ts`, the span
      starts at the second entry's `ts` — proving the scan is for the first timestamped entry, not
      for `event_ids[0]`.
    - `span_missing_ts_hazard`: with the denial event resolving but carrying `ts=None`, the group
      has `counters=()` and exactly one hazard naming the unresolvable boundary; `start_ts` and
      `end_ts` are `None`. No neighbouring event's timestamp is substituted.
    - Boundary inclusivity: a perfmon sample whose `ts` equals `start_ts` exactly, and one whose
      `ts` equals `end_ts` exactly, are BOTH selected (D-05's closed interval).
  </behavior>
  <action>
    Add `_resolve_span(ea, by_id)` to `src/sift/pipeline/perfmon.py`, returning the resolved start
    `Event`, end `Event` and a failure reason string, in whichever small return shape reads
    cleanest — a named tuple or a 3-tuple is fine, but it must distinguish "resolved" from "could
    not resolve, and here is why" without raising.

    Build the id index once as `by_id = {e.event_id: e for e in events}` at the top of
    `analyse_perfmon`, mirroring the `attribute_window` precedent — do not issue any store query
    from this module.

    End bound: `by_id.get(ea.episode.denial_event_id)`. If absent or `ts is None`, that is D-04's
    failure — return the reason. Do NOT parse `ea.episode.denial_ts` as a consolation; add a comment
    saying so, because that fallback is the easy mistake here and D-01 forbids it.

    Start bound: when `ea.window.start_event_id` is not `None`, resolve it via `by_id`. When it IS
    `None`, scan `ea.episode.event_ids` in order for the first entry that both resolves in `by_id`
    and carries a non-`None` `ts` (D-03). If neither path yields a timestamped event, that is D-04's
    failure with its own reason string.

    Add sample selection: filter the passed events to `event.source == "dssperfmon"` — do not import
    `EXCLUDED_FROM_RANKING` from the store, which means "held out of ranking" and is a different
    concept — then keep those whose `ts` is not `None` and satisfies `start_ts <= ts <= end_ts`.
    Preserve input order; do not re-sort. Add a comment that the closed interval is deliberate
    (D-05) so Hartford's last-sample-before-denial case is unambiguous.

    On a D-04 failure, emit a `TrendGroup` with `scope="episode"`, the episode's
    `denial_event_id` as `key`, `start_ts=None`, `end_ts=None`, `counters=()`, `sample_count=0`, and
    exactly one `PerfmonHazard` whose `message` names the reason in British English and whose
    `event_ids` cite the boundary ids that were attempted. The episode still appears — nothing
    disappears silently.

    Add the four tests from `<behavior>` to `tests/test_perfmon.py` as `test_span_from_event_ids`,
    `test_span_full_leadup_fallback`, `test_span_missing_ts_hazard` and
    `test_span_boundaries_are_inclusive`. Construct `McmEpisode`/`EpisodeWindow`/`EpisodeAnalysis`
    directly in the tests and pass real ingested perfmon events from `ingest_perfmon_slice()`
    alongside synthetic boundary events from `log_boundary_event` — the shipped MCM log fixtures
    cannot be used for this because their timestamps do not overlap the CSV slice (see the module
    docstring note from plan 13-01).
  </action>
  <verify>
    <automated>uv run pytest tests/test_perfmon.py -x -q -k span</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon.py -x -q -k span` passes with 4 tests collected.
    - `test_span_from_event_ids` asserts the resolved `start_ts`/`end_ts` equal the two boundary
      events' `Event.ts` values, and additionally asserts that a deliberately WRONG
      `episode.denial_ts` string (differing from the denial event's real `Event.ts`) does not appear
      in the resolved span — proving `denial_ts` is not parsed.
    - `test_span_full_leadup_fallback` uses an `event_ids` tuple whose first entry has `ts=None` and
      asserts the span starts at the SECOND entry's timestamp.
    - `test_span_missing_ts_hazard` asserts `group.counters == ()`, `group.start_ts is None`,
      `len(group.hazards) == 1`, and that the hazard's `event_ids` is non-empty.
    - `test_span_boundaries_are_inclusive` asserts a sample at exactly `start_ts` and a sample at
      exactly `end_ts` are both counted in `group.sample_count`.
    - `grep -n 'denial_ts' src/sift/pipeline/perfmon.py` shows only comment lines, never an
      expression that parses or converts it.
    - `src/sift/pipeline/perfmon.py` contains no `import` of `select_window` and no call to it.
    - `uv run ruff check` clean; `uv run pyright` 0 errors; `uv run pytest` full suite green.
  </acceptance_criteria>
  <done>Spans resolve by event_id at both ends, with both documented fallbacks and a graded failure that never invents a bound.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The three trend figures per counter (D-07, D-08, D-09, D-10, D-11, D-21)</name>
  <files>src/sift/pipeline/perfmon.py, tests/test_perfmon.py</files>
  <read_first>
    - src/sift/pipeline/perfmon.py — the models and `_resolve_span` from tasks 1 and 2.
    - src/sift/render/mcm_report.py lines 75-85 — `_mb_bytes`, the round-at-source precedent whose
      docstring wording (`rounded deterministically to N dp`) the slope docstring should echo.
    - src/sift/pipeline/mcm.py lines 930-955 — the `tuple(dict.fromkeys(...))` deduplication idiom
      and the explicit-tie-breaker sort `out.sort(key=lambda r: (-r.granted_bytes, r.key))`. These
      are what D-21's no-`set` rule means concretely.
    - src/sift/adapters/dssperfmon.py lines 280-320 — how counter keys land in `attrs`, and which
      keys are provenance rather than counters (`byte_offset`, `byte_len`, `host`, `pdh_version`,
      `tz_name`, `tz_offset_min`, `unparsed_columns`) and must be excluded from the counter sweep.
    - tests/test_perfmon.py — `ingest_perfmon_slice`, whose last sample carries the milestone
      figures and whose first carries `27` / `186503`.
  </read_first>
  <behavior>
    - `golden_trend_figures`: over a span covering all 20 slice samples, the counter
      `Working set cache RAM usage(MB)` reports `at_denial == 266042.0` and `peak == 266042.0`;
      `RAM used(MB)` reports `at_denial == 463915.0`; `Size(MB)` reports `401603.0`;
      `Open Sessions` reports `1488.0`; `Total MCM Denial` reports `0.0`. Each at-denial figure
      carries the LAST sample's `event_id` as `at_denial_event_id`.
    - Slope is `(last - first) / seconds_elapsed` rounded to 4 dp, asserted against a value computed
      by hand in the test from the two boundary samples — not by re-running the implementation.
    - `single_sample_no_zero_division`: a span containing exactly one sample yields
      `slope_per_second is None`, `at_denial` and `peak` both equal to that sample's value, and
      `sample_count == 1`. No exception is raised.
    - `non_finite_excluded`: with a `nan` cell present, the affected counter's figures are computed
      from the remaining samples only, `excluded_samples == 1` for that counter, the row's other
      counters are unaffected, and no figure anywhere in the analysis is a non-finite float.
    - Peak ties resolve to the earliest sample: given two samples sharing the maximum value, the
      reported `peak_event_id` is the earlier one's.
  </behavior>
  <action>
    Add `_counter_trends(samples)` to `src/sift/pipeline/perfmon.py`, returning
    `tuple[CounterTrend, ...]`.

    Build the counter name set by sweeping every in-span sample's `attrs` keys and excluding the
    provenance keys listed in `<read_first>`. Collect names with `dict.fromkeys` over the
    canonically-ordered samples so first-appearance order is deterministic, then emit the counters
    sorted by name so the output sequence is explicit and stable (D-21). No allowlist — every
    numerically-parsing counter is covered (D-07).

    For each counter, walk the in-span samples in order, converting each cell through `_numeric`.
    Count rejections into `excluded_samples` (D-11) and build the accepted `(value, event_id)`
    sequence.

    At-denial (D-09): the LAST accepted pair's value and `event_id`. Never interpolate; add a
    comment that an interpolated figure cites no real event and cannot be checked against the CSV.

    Slope (D-08): compute `seconds_elapsed` as
    `(last_accepted.ts - first_accepted.ts).total_seconds()`. When that value is `0.0` — one
    accepted sample, or several sharing a timestamp — set `slope_per_second = None` and raise NO
    hazard: one sample inside a narrow window is normal at a 30 s sampling interval against a short
    MCM descent, not a correlation failure. Otherwise
    `round((last - first) / seconds_elapsed, SLOPE_DP)`. Guard the zero case before dividing, not
    with an exception handler.

    Peak (D-10): use `max` over the accepted pairs keyed on value. Add a comment stating that
    `max()` returns the FIRST maximal element so ties already resolve to the earliest sample, and
    that a later refactor to `sorted(...)[-1]` would silently flip this.

    Round `at_denial` and `peak` to 3 dp at source so float repr noise cannot vary the rendered
    output between runs.

    When the accepted sequence for a counter is empty, emit a `CounterTrend` with all three figures
    `None`, both citation ids `None`, and `excluded_samples` equal to the number of samples that
    carried the key — the counter is reported as uncomputable, not omitted.

    Guard every figure computation on a non-empty sample list. `samples[-1]` or `samples[0]` on an
    empty in-span list raises `IndexError`; the empty case must return `()` cleanly.

    Wire `analyse_perfmon(analysis: McmAnalysis, events: list[Event]) -> PerfmonAnalysis` to iterate
    `analysis.episodes`, resolve each span, select samples, compute trends and emit one
    `TrendGroup` per episode with `scope="episode"` and `label=ea.window.label`. Two things are
    deliberately deferred: hazards are added in plan 13-04, and D-20's `scope="file"`
    full-sample-range path is added in plan 13-06 alongside the criterion-5 CLI test that proves it.
    Leave `PerfmonAnalysis.hazards` as an empty tuple for now and add a comment naming both
    follow-on plans, so neither omission reads as an oversight.

    Add the four tests from `<behavior>` to `tests/test_perfmon.py` as `test_golden_trend_figures`,
    `test_single_sample_no_zero_division`, `test_non_finite_excluded` and
    `test_peak_tie_takes_earliest_sample`. The non-finite test ingests
    `write_non_finite_csv(tmp_path)` from `tests/_perfmon_fixtures.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perfmon.py -x -q -k "golden_trend_figures or single_sample_no_zero_division or non_finite_excluded or peak_tie"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon.py -x -q -k "golden_trend_figures or single_sample_no_zero_division or non_finite_excluded or peak_tie"` passes with 4 tests collected.
    - `test_golden_trend_figures` asserts the literal values `266042.0`, `463915.0`, `401603.0`,
      `1488.0` and `0.0` for the five named counters, and asserts each `at_denial_event_id` equals
      the last slice sample's `event_id`.
    - The expected slope in `test_golden_trend_figures` is written as a hand-computed literal
      derived from the first and last sample values and their timestamp difference, with the
      arithmetic shown in a comment — not as a call back into `_counter_trends`.
    - `test_single_sample_no_zero_division` asserts `slope_per_second is None` and that
      `at_denial` and `peak` are both non-`None`, and completes without raising.
    - `test_non_finite_excluded` asserts `excluded_samples == 1` on the affected counter, asserts a
      sibling counter on the same row has `excluded_samples == 0`, and asserts
      `math.isfinite(v)` holds for every non-`None` figure in the whole analysis.
    - `test_peak_tie_takes_earliest_sample` asserts `peak_event_id` equals the earlier of two
      equal-valued samples' ids.
    - Running `analyse_perfmon` twice on the same inputs produces equal
      `PerfmonAnalysis.model_dump_json()` output, asserted in `test_golden_trend_figures`.
    - `uv run ruff check` clean; `uv run pyright` 0 errors; `uv run pytest` full suite green.
    - Fail-fast: if `test_single_sample_no_zero_division` passes on its first run, temporarily remove
      the `seconds_elapsed == 0.0` guard, confirm the test fails with `ZeroDivisionError`, then
      restore the file byte-identically before committing.
  </acceptance_criteria>
  <done>At-denial, slope and peak are computed per counter with citations, a zero-duration guard, and non-finite exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `case.db` `attrs` values → computed figures | Counter values are customer-supplied strings stored verbatim by the Phase 12 adapter; conversion is where validation belongs |
| `case.db` `attrs` keys → counter name set | Counter names originate in the customer CSV header and are attacker-influenceable |
| `McmAnalysis` boundary ids → resolved timestamps | A tampered or partially-ingested store can present an id that does not resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-NONFINITE | Tampering | `_numeric` in `pipeline/perfmon.py` | high | mitigate | `math.isfinite(float(v))` gate at conversion (D-11). `dssperfmon._bad_cells` probes with a bare `float()` which accepts `nan`/`inf`, so such a cell reaches the store on a clean `severity="info"` row and would otherwise poison `max()`, slope and at-denial, then serialise into the JSON report as the invalid-JSON token `NaN`. Verified by `test_non_finite_excluded`, which asserts every non-`None` figure in the analysis is finite. |
| T-13-BOUNDARY | Spoofing | `_resolve_span` in `pipeline/perfmon.py` | medium | mitigate | A boundary id that does not resolve, or resolves to an event with `ts=None`, yields a graded hazard and no trend (D-04). The correlator never substitutes a neighbouring event's timestamp and never parses `McmEpisode.denial_ts` as a consolation, so a tampered store cannot induce a fabricated-but-plausible span. Verified by `test_span_missing_ts_hazard`. |
| T-13-ATTRSWEEP | Tampering | counter-name sweep in `_counter_trends` | medium | mitigate | Provenance keys (`byte_offset`, `byte_len`, `host`, `pdh_version`, `tz_name`, `tz_offset_min`, `unparsed_columns`) are excluded from the counter sweep, so a crafted counter name cannot cause provenance metadata to be reported as a counter trend. Complements the adapter-side `_RESERVED_ATTRS` guard hardened in plan 13-03. |
| T-13-TYPECOERCE | Tampering | `_numeric` input | low | accept | A tampered `case.db` could hold a non-`str` `attrs` value despite the Phase 12 `dict[str, str]` contract. `float()` inside a `try` already degrades such a value to `None` without raising, matching the `store._coerce_str_list` precedent; no additional control is warranted at ASVS L1. |

No package-manager installs occur in this plan, so no `T-13-SC` supply-chain checkpoint applies.
</threat_model>

<verification>
- `uv run pytest tests/test_perfmon.py -q` — all 13 correlator tests pass
- `uv run ruff check` clean
- `uv run pyright` 0 errors
- `uv run pytest` full suite green
</verification>

<success_criteria>
- `analyse_perfmon(analysis, events)` returns a `PerfmonAnalysis` with one `TrendGroup` per MCM episode.
- Spans resolve by `event_id` at both ends, with the D-03 fallback and the D-04 graded failure.
- At-denial, slope and peak are computed per counter, each carrying its source `event_id`.
- The Hartford milestone figures are reproduced exactly from the slice fixture.
- A single-sample span yields `slope=None`, not a `ZeroDivisionError`.
- A `nan` cell excludes one counter for one sample and is counted, not silently propagated.
</success_criteria>

<output>
Create `.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-02-SUMMARY.md` when done
</output>
