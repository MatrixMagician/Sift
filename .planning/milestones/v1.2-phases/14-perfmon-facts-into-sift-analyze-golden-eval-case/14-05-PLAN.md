---
phase: 14-perfmon-facts-into-sift-analyze-golden-eval-case
plan: 05
type: execute
wave: 3
depends_on: [14-01, 14-04]
files_modified:
  - eval/cases/perfmon-denial/truth.yaml
  - eval/cases/perfmon-denial/README.md
  - tests/test_eval_cases.py
autonomous: true
requirements: [PERF-08]
must_haves:
  truths:
    - "D-07: the regression gate is anchored on the Hartford deny signal — the perfmon-denial golden case (whose input/ is the 14-01 overlapping CSV+log pair, authored within D-07's reuse-synthetic/real-slice discretion because the raw Hartford pair is non-overlapping); the snapshot pair stays a documented future second candidate, not built this phase."
    - "PERF-08: sift eval discovers perfmon-denial and scores it positive, and exits non-zero when correlation output regresses — proven by a citation-sensitivity test (mirror of the MCM one): a hypothesis cites a perfmon event_id ONLY because injection unions it into prompted_ids; monkeypatch render_perfmon_facts to ('', set()) and the same citation is FLAGGED, dropping citation_validity_rate below 1.0 (T-11-06 analogue — the gate is not vacuous)."
    - "PERF-08 [edge: unclassified→covered]: the regression gate's teeth are the citation_validity_rate floor — a correlation-output regression that stops perfmon ids being cited turns the case red and sift eval exits non-zero (the eval CI contract, EVAL-03)."
  artifacts:
    - "eval/cases/perfmon-denial/truth.yaml — frozen ground truth (authored BEFORE any prompt tuning; a regression must fail, never be edited away)"
    - "eval/cases/perfmon-denial/README.md — origin + the citation-sensitivity guard explanation"
    - "tests/test_eval_cases.py — perfmon-denial added to _EXPECTED_CASES (count 7 -> 8), discovery test, citation-sensitivity test"
  key_links:
    - "run_case reuses the exact analyze pipeline (hypothesise), so the 14-04 injection is exercised end-to-end; the perfmon-sensitive metric is citation_validity_rate (required_evidence/retrieval is matched against raw exemplars and is insensitive to injection by construction — Assumption A2)."
  prohibitions:
    - "editing truth.yaml to make a regressed run pass"
    - "a golden case that passes with an empty/critical-hazard perfmon block (a vacuous gate)"
---

<objective>
Wire the overlapping perfmon pair into `sift eval` as a frozen golden case and
prove the gate has teeth. Mirror Phase 11's `mcm-denial` case exactly, including
the citation-sensitivity guard that makes the gate non-vacuous.

Purpose: deliver PERF-08 — `sift eval` exits non-zero when perfmon correlation
output degrades. The MCM-sensitive lesson applies verbatim: `retrieval_hit_rate`
is insensitive to injection (matched against raw exemplars), so the
perfmon-sensitive metric is `citation_validity_rate`.
Output: `eval/cases/perfmon-denial/{truth.yaml,README.md}` and the eval-suite
tests (discovery + citation-sensitivity).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-04-SUMMARY.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the frozen truth.yaml + README for perfmon-denial</name>
  <read_first>
    - eval/cases/mcm-denial/truth.yaml (the frozen-ground-truth shape to mirror: root_cause, required_evidence regexes matched vs cluster exemplars, acceptable_keywords vs hypothesis title+narrative, expect_no_incident) and eval/cases/mcm-denial/README.md (origin + MCM-sensitivity guard prose to mirror as a PERFMON-sensitivity guard)
    - src/sift/eval/truth.py (the Truth model with extra="forbid" — a typo'd key fails loudly; keys must match exactly)
    - eval/cases/perfmon-denial/input/ (the 14-01 pair — root_cause + required_evidence regexes must match text actually present in the denial log)
  </read_first>
  <files>eval/cases/perfmon-denial/truth.yaml, eval/cases/perfmon-denial/README.md</files>
  <action>
    Create eval/cases/perfmon-denial/truth.yaml mirroring mcm-denial: a British-English root_cause describing
    the memory-pressure denial the overlapping log carries; required_evidence regexes that match the denial
    log's exemplar text (verify each against the committed log — do not author a regex that never matches);
    acceptable_keywords (memory / MCM / working set / denial / counter). Set expect_no_incident: false. Add a
    header comment stating the file is frozen before any prompt tuning and must never be edited to make a run
    pass (a regression must fail). Create README.md mirroring mcm-denial/README.md: origin of the overlapping
    pair, and the PERFMON-sensitivity note — citation_validity_rate is the sensitive metric because a
    perfmon-citing hypothesis is valid ONLY via injection; strip injection and the case regresses.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_eval_cases.py -k every_truth_yaml_loads` exits 0 (truth.yaml validates under extra="forbid").
    - Each required_evidence regex matches text present in eval/cases/perfmon-denial/input/ (verified, not assumed).
    - truth.yaml carries the frozen-before-tuning header comment.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_cases.py -k every_truth_yaml_loads -x</automated>
  </verify>
  <done>A frozen, schema-valid truth.yaml + README exist for perfmon-denial, anchored on the overlapping pair.</done>
</task>

<task type="auto">
  <name>Task 2: Register the case + prove the gate is citation-sensitive</name>
  <read_first>
    - tests/test_eval_cases.py:32-40 (_EXPECTED_CASES — add "perfmon-denial") and :84-85 (test_suite_is_exactly_the_seven_cases — rename/retune to eight)
    - tests/test_eval_cases.py:142-268 (the MCM precedent verbatim: _ingest_case, _mcm_hypset, test_mcm_denial_case_discovered_and_scored_positive, and especially test_mcm_denial_citation_validity_is_mcm_sensitive — monkeypatch render_mcm_facts -> ("", set()), citation_validity_rate drops below 1.0)
    - src/sift/eval/runner.py (run_case — reuses the analyze pipeline; how citation_validity_rate is computed) and src/sift/eval/metrics.py (CaseResult fields)
    - src/sift/pipeline/perfmon_facts.py (render_perfmon_facts — the monkeypatch target) and the 14-01 _ingest_perfmon_case helper for selecting a printed citable perfmon id
    - .claude/skills (frozen-truth rule: never edit truth.yaml to make a run pass; TDD fail-fast rule for the sensitivity test)
  </read_first>
  <files>tests/test_eval_cases.py</files>
  <action>
    In tests/test_eval_cases.py: add "perfmon-denial" to _EXPECTED_CASES and update the suite-count test
    (name + assertion) from seven to eight cases. Add a discovery/positive test mirroring
    test_mcm_denial_case_discovered_and_scored_positive (case found in _case_dirs, expect_no_incident False,
    run_failed False). Add the citation-sensitivity test mirroring
    test_mcm_denial_citation_validity_is_mcm_sensitive: pick a perfmon event_id that render_perfmon_facts
    prints for this case (via analyse_perfmon over the ingested pair), drive the MockTransport hypset to cite
    it — with injection ON citation_validity_rate == 1.0; then monkeypatch hypothesise.render_perfmon_facts to
    return ("", set()) and assert the SAME cited perfmon id now drops citation_validity_rate below 1.0. Choose
    a perfmon id NOT already in the MCM id set so the sensitivity isolates the perfmon path.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_eval_cases.py -k perfmon` exits 0.
    - The suite-count test asserts exactly eight cases including perfmon-denial.
    - With render_perfmon_facts stripped, citation_validity_rate < 1.0 for the perfmon-citing run (source assertion), proving the gate is non-vacuous.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_cases.py -x</automated>
  </verify>
  <done>perfmon-denial is a discovered, positive-scored golden case whose gate turns red when perfmon injection is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden truth.yaml → eval loader | truth files are project-authored; load via yaml.safe_load only (T-07-01) — this plan adds one more case, no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09 | Tampering | truth.yaml custom-tag RCE vector | low | mitigate | `load_truth` uses `yaml.safe_load` + `extra="forbid"` (inherited); no new loader |
| T-14-10 | Repudiation | a vacuous gate silently passing a regression | high | mitigate | citation-sensitivity test proves the gate turns red when perfmon injection is removed (non-vacuous); truth.yaml frozen |

No package installs this phase — no supply-chain (SC) checkpoint applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `eval/cases/perfmon-denial/truth.yaml`, `eval/cases/perfmon-denial/README.md`
- `tests/test_eval_cases.py`: `perfmon-denial` in `_EXPECTED_CASES`, the suite-count update, the discovery/positive test, and the perfmon citation-sensitivity test function
</artifacts_produced>

<verification>
- `uv run pytest tests/test_eval_cases.py -q` green.
- Phase gate: full `uv run pytest` + `uv run ruff check` + `uv run pyright` clean; `sift eval` exits 0 on the real suite before verify.
</verification>

<success_criteria>
sift eval discovers and positively scores perfmon-denial, and exits non-zero when
perfmon correlation output regresses (citation_validity_rate floor), with truth.yaml
frozen.
</success_criteria>

<output>
Create `.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-05-SUMMARY.md` when done
</output>
