---
phase: 14-perfmon-facts-into-sift-analyze-golden-eval-case
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/cases/perfmon-denial/input/perfmon_overlap.csv
  - eval/cases/perfmon-denial/input/perfmon_denial.log
  - tests/test_perfmon_analyze.py
  - tests/_perfmon_fixtures.py
autonomous: true
requirements: [PERF-07, PERF-08]
must_haves:
  truths:
    - "D-07: the golden regression gate is anchored on the Hartford deny signal, but because the shipped Hartford CSV ends ~12:39:39 and the denial is ~12:39:47 (RESEARCH Pitfall 1: ZERO in-span samples), this plan authors an OVERLAPPING re-timed/synthetic CSV+log pair so >=1 perfmon sample falls inside the MCM lead-up window — within D-07's sanctioned reuse-synthetic-builders / real-Hartford-slice discretion, NOT a reversal of D-07."
    - "PERF-08: the fixture makes the gate non-vacuous — analyse_perfmon over the pair yields an episode group with >=1 non-None CounterTrend.at_denial_event_id, asserted by a self-verifying overlap guard so a silent regression back to non-overlap fails loudly (RESEARCH: single most important Wave 0 item)."
    - "PERF-07: this overlapping pair is the precondition for citable perfmon facts — without it PERF-07 criterion 1 (hypotheses cite perfmon evidence by event_id) is silently vacuous."
  artifacts:
    - "eval/cases/perfmon-denial/input/perfmon_overlap.csv — overlapping PDH-CSV slice (tens of samples, not the 13,596-row full file — RESEARCH Open Q2)"
    - "eval/cases/perfmon-denial/input/perfmon_denial.log — dsserrors denial log whose MCM lead-up window brackets the CSV samples"
    - "tests/test_perfmon_analyze.py — created here with the shared ingest helper _ingest_perfmon_case and the fixture-overlap guard test_fixture_overlaps"
  key_links:
    - "analyse_perfmon(analyse_mcm(events), events) over the ingested pair must produce an episode-scope TrendGroup with a CounterTrend carrying a non-None at_denial_event_id — the mechanical proof the fixture overlaps."
  prohibitions:
    - "the golden fixture yielding only a critical non_overlap hazard with zero perfmon event_ids"
    - "committing the full 13,596-row Hartford CSV as the eval input (slows every sift eval for no added signal)"
---

<objective>
Author the OVERLAPPING perfmon CSV + dsserrors denial-log pair that PERF-07 and
PERF-08 both depend on, and a self-verifying overlap guard. This is the single
largest planning risk in the phase (RESEARCH Pitfall 1 / Open Q1): the shipped
Hartford `tests/fixtures/{dssperfmon,mcm}/hartford_deny_slice.*` pair does not
overlap in time (CSV ends ~12:39:39, denial ~12:39:47), so reusing it would make
the whole downstream integration silently vacuous.

Purpose: give Waves 2-3 a fixture over which `analyse_perfmon` genuinely produces
citable perfmon `event_id`s, and a guard that fails loudly if that ever stops
being true.
Output: `eval/cases/perfmon-denial/input/` CSV+log pair; `tests/test_perfmon_analyze.py`
scaffold (ingest helper + overlap guard). Later waves populate `truth.yaml`
(14-05) and append integration tests (14-04) to this same test module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-RESEARCH.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-PATTERNS.md
</context>

<source_audit>
## Multi-Source Coverage Audit (phase-wide, authored once here)

| Source | Item | Covered by |
|--------|------|------------|
| GOAL (ROADMAP §14) | corroborating counter evidence the model can cite but cannot author | 14-03 renderer + 14-04 splice |
| GOAL | regression gate stops quiet degradation | 14-05 eval gate |
| REQ | PERF-07 (cited perfmon evidence, `cited ⊆ prompted ⊆ store`, byte-identical no-data prompt) | 14-03, 14-04 |
| REQ | PERF-08 (regression-gated golden perfmon eval case) | 14-01 (fixture), 14-05 |
| RESEARCH | Pitfall 1 fixture blocker → overlapping pair + overlap guard | 14-01 (this plan) |
| RESEARCH | Pitfall 2 counter short/qualified-name final-segment match | 14-03 |
| RESEARCH | Pitfall 3 D-08 invariant preservation | 14-02 |
| RESEARCH | Pitfall 4 no-data baseline hash must not move | 14-04 |
| CONTEXT | D-01..D-08 | D-01/D-02/D-05 → 14-04; D-03/D-04/D-05/D-06 → 14-03; D-07 → 14-01/14-05; D-08 → 14-02 |

No MISSING items. Exclusions (not gaps): snapshot golden case (Deferred), PERFV2-01/02/03
(Deferred), any `sift perfmon` report/CSV reshape (Phase 13, shipped).
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Author the overlapping CSV + denial-log pair</name>
  <read_first>
    - tests/_perfmon_fixtures.py (PDH-CSV builder conventions: PDH_HEADER_PREFIX, `_HOST`, QUOTE_ALL + LF terminator via `_write`, `_short_counter_name` import, `_STAMPS` 30 s spacing)
    - eval/cases/mcm-denial/input/hartford_deny_slice.log (the shipped denial log that analyse_mcm parses into an episode — the shape to re-time from) and eval/cases/mcm-denial/README.md (how the mcm-denial case reuses a real slice verbatim)
    - src/sift/pipeline/perfmon.py:200-265 (`_resolve_span`, `_in_span`) and :557-737 (episode path in `analyse_perfmon`) — proves WHICH samples count as in-span so the CSV can be timed into `[window_start, denial_ts]`
    - .planning/REQUIREMENTS.md § Reference Data (observed lead-in counter trend + salient counter names: Working set cache RAM usage(MB), System\RAM used(MB), Process(MSTRSvr)\Size(MB), Open Sessions, Total MCM Denial)
  </read_first>
  <files>eval/cases/perfmon-denial/input/perfmon_overlap.csv, eval/cases/perfmon-denial/input/perfmon_denial.log, tests/_perfmon_fixtures.py</files>
  <action>
    Create eval/cases/perfmon-denial/input/ with two committed files whose timelines
    OVERLAP (the whole point — RESEARCH Pitfall 1): (1) a dsserrors denial log
    (perfmon_denial.log) that analyse_mcm parses into exactly one MCM episode with a
    RESOLVABLE lead-up window (`_resolve_span` returns start AND end, no reason) — re-time
    or trim the shipped hartford_deny_slice.log so its AvailableMCM lead-up lines and the
    denial banner span a window several seconds wide; (2) a small PDH-CSV (perfmon_overlap.csv,
    tens of samples not thousands — RESEARCH Open Q2) built with the tests/_perfmon_fixtures.py
    conventions (PDH_HEADER_PREFIX, QUOTE_ALL, LF terminator, same _HOST), carrying the D-04
    salient counters (Working set cache RAM usage(MB), System\RAM used(MB),
    Process(MSTRSvr)\Size(MB), Open Sessions, Total MCM Denial), whose sample timestamps land
    INSIDE that window so `_in_span` keeps >=1 of them. Add a reusable overlapping builder to
    tests/_perfmon_fixtures.py (e.g. write_overlapping_csv) if you take the synthetic route, and
    verify overlap empirically with a scratch `uv run python -c` call to analyse_perfmon before
    committing the files — do NOT trust the timestamps by eye (Pitfall 1 is a timestamp trap).
    Keep files British-English, LF-terminated, zero-network.
  </action>
  <acceptance_criteria>
    - `uv run python -c "..."` ingesting eval/cases/perfmon-denial/input/ and running
      analyse_perfmon(analyse_mcm(events), events) prints a group whose scope == "episode" and
      whose counters include at least one CounterTrend with at_denial_event_id is not None.
    - eval/cases/perfmon-denial/input/perfmon_overlap.csv is < 100 rows and LF-terminated.
    - The denial log auto-sniffs as dsserrors (no --adapter override) and analyse_mcm returns >=1 episode.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/_perfmon_fixtures.py -q</automated>
  </verify>
  <done>An overlapping, empirically-verified CSV+log pair exists under eval/cases/perfmon-denial/input/.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_perfmon_analyze.py with the ingest helper + fixture-overlap guard</name>
  <read_first>
    - tests/test_eval_cases.py:142-172 (`_ingest_case` — the real sniff+ingest-into-temp-case.db helper to mirror as `_ingest_perfmon_case`) and :83-120 (`_offline_client` / MockTransport pattern for later waves)
    - tests/test_mcm_analyze.py (autouse `_no_network` conftest usage; CliRunner and MockTransport shape — the module this file will grow to mirror)
    - src/sift/pipeline/perfmon.py:125-145 (CounterTrend fields: at_denial_event_id, peak_event_id) and :648-737 (analyse_perfmon episode path)
    - .claude/skills (TDD fail-fast rule: a guard that passes on first run must be counterfactually broken to prove it fails without a genuine overlap, then restored byte-identically before commit)
  </read_first>
  <files>tests/test_perfmon_analyze.py</files>
  <action>
    Create tests/test_perfmon_analyze.py. Add a module-level `_ingest_perfmon_case(config, case_dir)`
    helper mirroring test_eval_cases._ingest_case: ingest eval/cases/perfmon-denial/input/ via the
    real dsserrors+dssperfmon sniff/ingest path into a temp case.db, return the stored events (or the
    CaseStore) plus the analyse_mcm result so later-wave tests can reuse it. Add `test_fixture_overlaps`:
    build PerfmonAnalysis via analyse_perfmon(analyse_mcm(events), events) over the ingested pair and
    assert at least one episode-scope group carries a CounterTrend whose at_denial_event_id is not None
    (the anti-vacuous overlap proof). Prove the guard fails without a real overlap: temporarily point the
    ingest at the shipped non-overlapping hartford_deny_slice pair, confirm the assertion fails, then
    restore. Do NOT add the byte-identity / anti-hallucination tests here — 14-04 appends those.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon_analyze.py -k fixture_overlaps` exits 0.
    - The guard asserts a non-None at_denial_event_id (source assertion), not merely a non-empty groups tuple.
    - Counterfactual check recorded in the SUMMARY: guard fails when pointed at the shipped non-overlapping pair.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_perfmon_analyze.py -k fixture_overlaps -x</automated>
  </verify>
  <done>test_fixture_overlaps is green over the authored fixture and demonstrably red over the shipped non-overlapping pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer CSV/log → case.db → analyser | untrusted diagnostic text crosses into the pipeline; this plan only authors fixtures, adds no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | fixture builders in tests/_perfmon_fixtures.py | low | mitigate | builders write strictly beneath tmp_path / the fixed eval case input dir (T-13-FIXPATH inherited); no caller-chosen destination |
| T-14-02 | Information disclosure | committed fixture content | low | accept | synthetic/redacted host + counters only; no secrets. Rationale: reference host string already public in shipped fixtures |

No package installs this phase (zero new dependencies) — no supply-chain (SC) checkpoint applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `eval/cases/perfmon-denial/` (eval case id: `perfmon-denial`)
- `eval/cases/perfmon-denial/input/perfmon_overlap.csv`, `eval/cases/perfmon-denial/input/perfmon_denial.log`
- `tests/test_perfmon_analyze.py` (NEW): `_ingest_perfmon_case`, `test_fixture_overlaps`
- `tests/_perfmon_fixtures.py`: optional `write_overlapping_csv` builder (+ its guard test) if the synthetic route is taken
</artifacts_produced>

<verification>
- `uv run pytest tests/test_perfmon_analyze.py tests/_perfmon_fixtures.py -q` green.
- `uv run ruff check` and `uv run pyright` clean.
</verification>

<success_criteria>
The overlapping fixture exists, is empirically verified to yield a non-None
at_denial_event_id, and the overlap guard is green (and demonstrably red on the
non-overlapping shipped pair).
</success_criteria>

<output>
Create `.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-01-SUMMARY.md` when done
</output>
