---
phase: 16-saturation-contention-signature-collapse
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/sift/pipeline/eustack.py
  - tests/test_eustack_rules.py
autonomous: true
requirements: [EUS-04]
must_haves:
  truths:
    - "A lock site is the ENCLOSING APPLICATION FRAME, never the leaf: the walk starts above the classification's reported frame_index, which points at glibc because the shipped rule is a `contains` match on `__lll_lock_wait` (D-03)."
    - "The lock-site frame walk skips `std::`, `boost::`, `__gnu_cxx::` and `abi::` frames, matching on the symbol's LEADING namespace via `str.startswith(tuple)` — never a substring test, or a genuine `MBase::` frame nested inside a `std::` template argument list is misjudged (D-04 edge case 3)."
    - "\"Above\" the leaf means INCREASING frame index — `iter_frames()` yields `#1`, `#2`, `#3`… from leaf toward thread entry point, so the walk iterates `frame_index + 1`, `frame_index + 2`, … (D-04)."
    - "Unresolvable (`??`/bare-address) frames are walked PAST, not treated as a stopping point; the walk reuses the shipped `_is_resolvable()` rather than re-implementing it (D-04 edge case 2)."
    - "No qualifying frame above the leaf means the site is unknown-but-counted: it is never dropped and never attributed to the leaf (D-04 edge case 1)."
    - "The leaf being the last frame in the tuple falls to the unknown-but-counted case, with no IndexError (D-04 edge case 4)."
    - "Multiple `__lll_lock_wait` frames in one stack: the walk starts from the classification's reported `frame_index`, not from a fresh re-scan of the tuple (D-04 edge case 5)."
    - "Every lock output is labelled ownership-blind at the point of reporting and reports the site with its thread count; no emitted string contains the three prohibited terms (D-05)."
    - "The `blocked-on-lock` path is exercised by a hand-authored synthetic scenario, clearly labelled synthetic, because the healthy reference capture matches the lock rule zero times by design (D-11)."
    - "Frames reaching the walk are already `normalise()`d by `signature_of()`; the walk never re-normalises them."
  artifacts:
    - "src/sift/pipeline/eustack.py :: enclosing_application_frame"
    - "src/sift/pipeline/eustack.py :: _RUNTIME_NAMESPACES"
    - "src/sift/pipeline/eustack.py :: UNKNOWN_LOCK_SITE"
    - "src/sift/pipeline/eustack.py :: LOCK_FINDING_NOTE"
    - "src/sift/pipeline/eustack.py :: LockSite"
    - "src/sift/pipeline/eustack.py :: SaturationAnalysis.lock_sites"
    - "tests/test_eustack_rules.py :: test_lock_site_skips_runtime_namespace"
    - "tests/test_eustack_rules.py :: test_synthetic_lock_convergence"
  key_links:
    - "SignatureGroup.frame_index (set only when a rule matched) -> enclosing_application_frame(frames, frame_index) -> LockSite.site: the sole attribution path; a None frame_index can never reach the walk because only rule-matched groups carry role == 'blocked-on-lock'."
    - "eustack._is_resolvable -> enclosing_application_frame: one definition of 'no symbol resolved here' for the whole eu-stack path."
    - "thresholds.lock_convergence_count -> _grade() -> SaturationFlag(unit='threads'): the count flag that DiagnosticFlag.value_pct could not carry (D-08 AMENDED)."
---

<objective>
Deliver EUS-04: threads converging on a lock-acquisition path, reported with
site and count, always ownership-blind. This is the one piece of genuinely new
logic in Phase 16 — the D-04 enclosing-frame walk — plus the `LockSite` grouping
it feeds and the count-based flag `DiagnosticFlag` structurally could not carry.

Purpose: the shipped rule matches `__lll_lock_wait` (a `contains` match on a
glibc leaf), so `SignatureGroup.frame_index` points at glibc. Reporting the leaf
would attribute every contention finding to the same futex symbol and tell an
engineer nothing. Walking up to the enclosing application frame is what makes
the finding actionable.

Output: `enclosing_application_frame()`, `_RUNTIME_NAMESPACES`,
`UNKNOWN_LOCK_SITE`, `LOCK_FINDING_NOTE`, `LockSite`,
`SaturationAnalysis.lock_sites`, the `lock_convergence_count` flag, the D-11
synthetic scenario, and the extended ownership-blind guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-saturation-contention-signature-collapse/16-CONTEXT.md
@.planning/phases/16-saturation-contention-signature-collapse/16-PATTERNS.md
@.planning/phases/16-saturation-contention-signature-collapse/16-01-SUMMARY.md
@src/sift/pipeline/eustack.py
@docs/decisions/0015-eustack-thread-role-taxonomy.md
</context>

## Settled design decisions carried from 16-01

S-1 (extend `eustack.py` in place), S-2 (import `mcm._grade` as-is), S-3 (one
`SaturationFlag` record for all three flag families, calling `_grade()` at
construction), S-4 (`lock_convergence_count` warn 5.0 / critical 20.0) and S-6
(flags emitted at every severity; a signal with nothing to measure emits no
flag) are settled in `16-01-PLAN.md` and are NOT reopened here. Read that plan's
"Settled design decisions" section before starting.

## S-7 — The ownership-blind guard is extended over EMITTED OUTPUT, not source text

D-05 prohibits three terms — the milestone's own permanently-forbidden word plus
"owner" and "holder" — from the **output vocabulary**. The shipped
`test_no_ownership_attributed_lock_language_in_shipped_surface` greps
`eustack.py`'s whole source for the one term REQUIREMENTS.md names, and because
S-1 keeps Phase 16 inside `eustack.py`, that grep already covers this plan's
additions with zero changes.

It cannot simply be widened to all three terms over source text:
`src/sift/rules/eustack_roles.toml` already contains the word "holder" in a
prose comment explaining the non-goal ("contention can be observed but never
attributed to a ..."), which is documentation, not output. Rewriting a shipped
Phase 15 comment to satisfy a Phase 16 test would be the tail wagging the dog.

Settled: keep the existing source grep exactly as shipped, and ADD a second
assertion inside that same test which checks the three terms, word-boundary
matched and case-insensitively, against the strings this phase actually EMITS —
`LOCK_FINDING_NOTE`, `UNKNOWN_LOCK_SITE`, every `LockSite.site`, and every
`SaturationFlag.message` produced by running both the committed derivative
fixture and the synthetic lock scenario through `analyse_saturation()`. A
behaviour assertion over real emitted strings is strictly stronger than a source
grep, and it does not touch Phase 15's shipped rules file. Word-boundary matching
is required so an innocuous word such as "placeholder" cannot produce a false
positive on the substring "holder".

## Artifacts this plan produces

New symbols (excluded from drift verification):

**`src/sift/pipeline/eustack.py`**
- constant `_RUNTIME_NAMESPACES` = `("std::", "boost::", "__gnu_cxx::", "abi::")`
- constant `UNKNOWN_LOCK_SITE` (the sentinel string for the unknown-but-counted case)
- constant `LOCK_FINDING_NOTE` (the ownership-blind label carried on the analysis)
- function `enclosing_application_frame`
- class `LockSite`, fields: `site`, `thread_count`, `signature_count`
- field `SaturationAnalysis.lock_sites`
- field `SaturationAnalysis.lock_finding_note`

**Tests (`tests/test_eustack_rules.py`)**
- `test_lock_site_walk_finds_enclosing_application_frame`
- `test_lock_site_walk_starts_at_reported_frame_index`
- `test_lock_site_skips_runtime_namespace`
- `test_lock_site_template_arg_not_misjudged`
- `test_lock_site_unknown_but_counted`
- `test_synthetic_lock_convergence`
- second assertion added inside the shipped `test_no_ownership_attributed_lock_language_in_shipped_surface`, reachable under `-k ownership_blind` (rename to `test_ownership_blind_vocabulary_absent_from_source_and_emitted_output` so the `-k ownership_blind` selector 16-VALIDATION.md specifies matches it)

<tasks>

<task type="auto">
  <name>Task 1: `enclosing_application_frame()` — the D-04 walk and its six edge cases</name>

  <files>src/sift/pipeline/eustack.py, tests/test_eustack_rules.py</files>

  <read_first>
    - `src/sift/pipeline/eustack.py` lines 200-268 — `_BARE_ADDRESS_RE`, `_is_resolvable()` (the resolvability test this walk reuses, never re-implements) and `classify_signature()`, whose `enumerate(signature)` iteration is what sets the `frame_index` this walk starts from.
    - `src/sift/pipeline/eustack.py` lines 140-161 — `normalise()` and `signature_of()`, which prove `SignatureGroup.frames` entries arrive already normalised, so the walk must NOT re-normalise.
    - `tests/test_eustack_rules.py` lines 33-60 — the tracer block and `test_tracer_thread_block_classifies_via_packaged_rules`. This is the canonical worked example of the frame-index direction: `#0 pthread_cond_timedwait` (leaf) through `#3 MSIQTask::GetNextPreferredJob` (the classifying frame) to `#4 MSIThread::Run()` (the entry point). Cite it in the new helper's docstring.
    - `tests/fixtures/eustack/reference_capture_derivative.txt` lines 114-121 (TID 100014) — the real `pthread_cond_wait` -> `std::condition_variable::wait(std::unique_lock<std::mutex>&)` -> `ParallelBursting::ThreadPool::WorkLoop(unsigned long)` shape, and lines 86-93 (TID 100011) for the equivalent `boost::asio::detail::scheduler::do_run_one(...)` shape. These are the evidence for the D-04 AMENDED denylist and the source material the denylist test synthesises from.
    - `.planning/phases/16-saturation-contention-signature-collapse/16-RESEARCH.md` § Pitfall 2 (walk direction) and § Pitfall 3 (the `std::`/`boost::` misfire).
  </read_first>

  <action>
Add to `src/sift/pipeline/eustack.py`, below the existing `_is_resolvable()`
so the two sit together as one resolvability-and-attribution unit.

Declare `_RUNTIME_NAMESPACES: tuple[str, ...] = ("std::", "boost::",
"__gnu_cxx::", "abi::")`. A comment records D-04 AMENDED's counting evidence:
roughly 110 distinct top-level namespaces appear in the reference capture and
exactly two are third-party (`std::` 14 frames, `boost::` 10); `__gnu_cxx::` and
`abi::` are defensive entries for the same libstdc++/libgcc family. Every other
top-level namespace — `MSynch::`, `CDSSQueryEngine::`, `MSIThread::`,
`MSIThreadPoolTask::`, `MCE::`, `MDb::` and so on — is MicroStrategy, which is
why this is a denylist of four rather than an allowlist of 110 that would grow
with every build.

Declare `UNKNOWN_LOCK_SITE: str` as a fixed sentinel describing an
application frame that could not be resolved above the leaf. It must read as a
plain British-English phrase, contain no digits, and contain none of the three
D-05-prohibited terms. It exists because D-04 edge case 1 demands the site be
unknown-but-counted rather than dropped, AND because a `str | None` site field
would make `(-thread_count, site)` raise `TypeError` when `None` and `str` are
compared (16-RESEARCH.md Pitfall 4) — substituting the sentinel at construction
keeps the field typed `str` and the sort key total.

Declare `LOCK_FINDING_NOTE: str`, the ownership-blind label D-05 requires at the
point of reporting. It states that the finding is a count of threads observed at
a site and that eu-stack output carries no lock-acquisition edges, so nothing
beyond the site itself can be established. It must contain none of the three
prohibited terms — write around them, do not paraphrase them.

Write `enclosing_application_frame(frames: tuple[str, ...], frame_index: int)
-> str | None`, public (no leading underscore) so its correctness — which is
subtle enough to have needed two research pitfalls — can be unit-tested directly
rather than only through the full aggregation. Iterate `frames[frame_index + 1:]`
and return the first frame satisfying all three of: `_is_resolvable(frame)` is
true, `"::"` is in the frame, and `frame.startswith(_RUNTIME_NAMESPACES)` is
false. Return `None` when the slice yields no such frame.

Three properties of that loop are load-bearing and each needs its own comment.
(1) `frames[frame_index + 1:]` is INCREASING index: `iter_frames()` yields
`#1`, `#2`, `#3`… from leaf toward the thread entry point, so "above the leaf"
means larger index, not smaller — cite `tests/test_eustack_rules.py`'s tracer
block by name as the worked example. A slice past the end of the tuple is empty,
not an error, which is exactly how D-04 edge case 4 (the leaf is the last frame)
resolves to `None` with no bounds check. (2) An unresolvable frame is `continue`d
past, never a stopping point (D-04 edge case 2). (3)
`str.startswith(tuple)` gives the LEADING-namespace test for free — this is a
prefix test, never a substring test, so a genuine `MBase::` frame nested inside a
`std::` template argument list such as
`std::thread::_State_impl<std::tuple<MBase::ThreadedRepeater...>>` is judged on
its leading `std::` (correctly a runtime frame) while a frame whose own leading
namespace is `MBase::` and which merely mentions `std::` inside its template
arguments is correctly kept (D-04 edge case 3).

The docstring also records the known, accepted imprecision: the denylist covers
the two third-party namespaces measured in the reference capture, so a
C++-runtime namespace outside those four would still be reported as a site. This
is recorded openly rather than silently accepted, following ADR 0015's own
precedent for a known single-thread misclassification. Add the "frames are
already `normalise()`d — do not re-normalise here" note: two code paths that
should agree but are never compared is how a normalisation bug hides.

Add five focused unit tests to `tests/test_eustack_rules.py`, calling
`enclosing_application_frame()` directly on hand-built tuples.
`test_lock_site_walk_finds_enclosing_application_frame` — a tuple shaped
`("__lll_lock_wait", "pthread_mutex_lock", "MSynch::CriticalSection::Enter",
"MSIQTask::Execute")` with `frame_index=0` returns
`"MSynch::CriticalSection::Enter"`, proving the walk goes toward INCREASING index
and stops at the first qualifying frame rather than the outermost.
`test_lock_site_skips_runtime_namespace` — a tuple synthesised from the real
TID 100014 shape with `__lll_lock_wait` substituted at index 0:
`("__lll_lock_wait", "pthread_cond_wait",
"std::condition_variable::wait(std::unique_lock<std::mutex>&)",
"ParallelBursting::ThreadPool::WorkLoop(unsigned long)")` with `frame_index=0`
returns the `ParallelBursting::` frame, not the `std::` one. Add the equivalent
`boost::asio::detail::scheduler::do_run_one(...)` case from TID 100011 in the
same test, and put the fixture line numbers in the test docstring so a reader can
confirm the shapes are real, not invented.
`test_lock_site_template_arg_not_misjudged` — a tuple containing
`"MBase::ThreadedRepeater<std::chrono::duration<long> >::Run()"` above the leaf
returns that frame, proving the denylist matched the leading namespace and not
the `std::` inside the template argument list; and a second assertion that
`"std::thread::_State_impl<std::tuple<MBase::ThreadedRepeater> >::_M_run()"` is
SKIPPED, proving the same prefix rule in the opposite direction.
`test_lock_site_unknown_but_counted` — three assertions returning `None`: a tuple
whose frames above the leaf are all unqualified C symbols; a tuple where the leaf
is the last element (empty slice, no `IndexError`); and a tuple whose only
frames above the leaf are `"??"` and a bare `0x` address, proving unresolvable
frames are walked past and still yield `None` rather than being returned.
`test_lock_site_walk_starts_at_reported_frame_index` — a tuple carrying TWO
`__lll_lock_wait` frames with a distinct application frame above each; calling
with the SECOND occurrence's index returns the second application frame, proving
the walk starts from the classification's reported `frame_index` and never
re-scans the tuple for a fresh leaf (D-04 edge case 5).
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_rules.py -k "lock_site_walk or lock_site_skips_runtime_namespace or lock_site_template_arg or lock_site_unknown" -x</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_eustack_rules.py -k "lock_site_walk or lock_site_skips_runtime_namespace or lock_site_template_arg or lock_site_unknown" -x` exits 0 and collects at least 5 tests.
    - `src/sift/pipeline/eustack.py` contains `def enclosing_application_frame(`.
    - `uv run python -c "from sift.pipeline.eustack import enclosing_application_frame as f; print(f(('__lll_lock_wait','pthread_cond_wait','std::condition_variable::wait(std::unique_lock<std::mutex>&)','ParallelBursting::ThreadPool::WorkLoop(unsigned long)'), 0))"` prints `ParallelBursting::ThreadPool::WorkLoop(unsigned long)`.
    - `uv run python -c "from sift.pipeline.eustack import enclosing_application_frame as f; print(f(('__lll_lock_wait',), 0), f(('__lll_lock_wait','??','0x00007f0000000001'), 0))"` prints `None None` and raises no `IndexError`.
    - `uv run ruff check` and `uv run pyright` both exit 0.
  </acceptance_criteria>

  <done>The enclosing-frame walk is a directly unit-tested public function whose six D-04 edge cases each have a named test, with the runtime-namespace denylist proven against two real fixture stack shapes.</done>
</task>

<task type="auto">
  <name>Task 2: `LockSite` grouping, the count flag, and the D-11 synthetic scenario</name>

  <files>src/sift/pipeline/eustack.py, tests/test_eustack_rules.py</files>

  <read_first>
    - `src/sift/pipeline/eustack.py` — `analyse_saturation()` and `SaturationFlag` as written in 16-01, plus `enclosing_application_frame()` from Task 1.
    - `src/sift/rules/eustack_roles.toml` — Rule 6, the only `blocked-on-lock` rule (`contains` match on `__lll_lock_wait`, subsystem `lock`), and its comment stating the healthy capture matches it zero times BY DESIGN. That comment is why D-11 requires a hand-authored synthetic scenario.
    - `src/sift/pipeline/mcm.py` `compute_flags` (from line 627) — the fixed authored append order and the guard-before-dividing discipline the flag pass mirrors.
    - `tests/test_eustack_rules.py` lines 300-345 — `_thread_raw()` and `_event()`, which synthesise the D-11 scenario inline; 16-VALIDATION.md's Wave 0 note records that inline synthesis is preferred over a fixture file that would have to look synthetic and parse like a real capture at the same time.
  </read_first>

  <action>
Extend `src/sift/pipeline/eustack.py`.

Declare `LockSite(BaseModel)`, frozen and `extra="forbid"`, fields
`site: str`, `thread_count: int`, `signature_count: int`. `site` is typed `str`,
never `str | None` — the `UNKNOWN_LOCK_SITE` sentinel is substituted at
construction so the sort key stays total (16-RESEARCH.md Pitfall 4). The
docstring cites D-03 (the site is the enclosing application frame, not the leaf)
and D-05, and states that the record carries a count of threads observed at a
site and nothing more.

Add two fields to `SaturationAnalysis`: `lock_sites: tuple[LockSite, ...] = ()`
and `lock_finding_note: str = LOCK_FINDING_NOTE`. Defaults keep the addition
purely additive over 16-01's model. `lock_finding_note` lives on the analysis
rather than on each `LockSite` row so the label appears exactly once per report
and Phase 17 cannot render the table without it — that is the mechanism
satisfying D-05's "labelled ownership-blind at the point of reporting".

Extend `analyse_saturation()` with the lock pass, placed after the pool pass.
Filter `analysis.signatures` to `group.role == "blocked-on-lock"`. For each such
group call `enclosing_application_frame(group.frames, group.frame_index)`,
substituting `UNKNOWN_LOCK_SITE` when it returns `None`. `frame_index` is
structurally non-`None` for this role because `classify_signature()` sets it only
on a rule match and `unclassified` is the sole role without one — assert that
with a narrow guard that satisfies pyright rather than silently skipping the
group, and add a comment saying so. Accumulate `thread_count` and
`signature_count` per site into plain dicts, then build `LockSite` rows sorted on
the explicit total order `(-thread_count, site)` — thread count descending, ties
broken ascending on the site string. Add the same forbidding comment
`analyse_eustack()` carries: never `Counter.most_common()`, never set iteration.

Add the lock-convergence flag as the THIRD check in the fixed authored flag
order established in 16-01 (unclassified share, then no-resolvable-frame share
which 16-03 adds, then lock convergence). Emit one `SaturationFlag` per
`LockSite` row, iterating `lock_sites` in its already-sorted order so the flag
sub-list inherits that explicit ordering rather than needing a second sort key.
Each flag carries `dimension="lock_convergence_count"`,
`value=float(site.thread_count)`, `unit="threads"`,
`severity=_grade(site.thread_count, thresholds.lock_convergence_count.warn,
thresholds.lock_convergence_count.critical)`, `warn`/`critical` from that same
pair, and a British-English `message` naming the site and its thread count.
Zero lock sites therefore emits zero lock flags — which is exactly why the
healthy reference capture raises none (D-09), since Rule 6 matches it zero
times. `_grade()` takes any float and does not care that this input is a count
rather than a ratio; that is why it is reused unchanged (D-08 AMENDED) while
`DiagnosticFlag.value_pct`'s locked ratio contract is left untouched.

Add `test_synthetic_lock_convergence` to `tests/test_eustack_rules.py`. Its
docstring must state plainly that the scenario is HAND-AUTHORED and SYNTHETIC,
not a redacted real capture, and give the reason: the healthy reference capture
matches Rule 6 zero times by design, so no real capture can exercise this path
(D-11). Build the scenario with `_thread_raw()`/`_event()` inline — no new
fixture file, per 16-VALIDATION.md's Wave 0 note. Synthesise threads converging
on ONE site: N events whose stacks are `__lll_lock_wait` at `#0`, a glibc
`pthread_mutex_lock` at `#1`, one shared application frame such as
`MSynch::CriticalSection::Enter` at `#2`, and DIFFERENT outer frames per group so
several distinct signatures collapse to the same site — that convergence, not
the raw thread count, is what EUS-04 reports. Assert: exactly one `LockSite`
row; its `site` is the shared application frame, not `__lll_lock_wait`; its
`thread_count` is the total across all contributing signatures; its
`signature_count` is greater than 1. Exercise the grading at three counts —
below warn, at warn, at or above critical — by parameterising the thread count
and asserting the emitted flag's `severity` is `info`, `warn` and `critical`
respectively, since S-4 records these cut-points as uncalibrated round numbers
with no real hung-server capture behind them. Add one more case whose stacks
carry `__lll_lock_wait` with no qualifying frame above it, and assert its
`LockSite.site` equals `UNKNOWN_LOCK_SITE` with a non-zero `thread_count` — the
unknown-but-counted contract, never dropped, never attributed to the leaf.
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_rules.py -k synthetic_lock_convergence -x</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_eustack_rules.py -k synthetic_lock_convergence -x` exits 0.
    - `src/sift/pipeline/eustack.py` contains `class LockSite(` and `SaturationAnalysis` exposes `lock_sites` and `lock_finding_note`.
    - The synthetic scenario yields a `LockSite` whose `site` is the shared application frame and whose `signature_count` is greater than 1 — several signatures converging on one site.
    - A synthetic scenario with no qualifying frame above the leaf yields a `LockSite` whose `site` equals `UNKNOWN_LOCK_SITE` and whose `thread_count` is greater than 0.
    - Analysing `tests/fixtures/eustack/reference_capture_derivative.txt` yields `lock_sites == ()` and zero flags whose `dimension` is `lock_convergence_count` — the healthy capture matches Rule 6 zero times.
    - `uv run pytest tests/test_eustack_rules.py -x` exits 0 — Phase 15's shipped tests still green.
    - `uv run ruff check` and `uv run pyright` both exit 0.
  </acceptance_criteria>

  <done>Converging threads are reported by enclosing site with a thread count and a signature count, graded against a config cut-point, proven on a clearly-labelled synthetic scenario at three severity tiers plus the unknown-but-counted case.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the ownership-blind guard over emitted output (D-05, V11)</name>

  <files>tests/test_eustack_rules.py</files>

  <read_first>
    - `tests/test_eustack_rules.py` lines 569-591 — the shipped `test_no_ownership_attributed_lock_language_in_shipped_surface`, including the runtime `re.search(r'the word "(\w+)"', requirements_text)` extraction that keeps the term out of the test source.
    - `.planning/REQUIREMENTS.md` § Out of Scope — the "Deadlock detection" row, which is the runtime source of the extracted term.
    - `.planning/phases/16-saturation-contention-signature-collapse/16-CONTEXT.md` D-05 — the full three-term prohibition, and this plan's S-7 explaining why the guard is extended over emitted output rather than over source text.
  </read_first>

  <action>
Rename the shipped test to
`test_ownership_blind_vocabulary_absent_from_source_and_emitted_output`, so the
`-k ownership_blind` selector 16-VALIDATION.md specifies matches it. Keep both
existing assertions — the runtime extraction from `.planning/REQUIREMENTS.md` and
the two source greps over `src/sift/rules/eustack_roles.toml` and
`src/sift/pipeline/eustack.py` — byte-for-byte unchanged. Because S-1 keeps
Phase 16 inside `eustack.py`, that second grep already covers this phase's
additions with no edit.

Append a second half asserting the full D-05 three-term prohibition against
strings the code actually EMITS, not against source text. Build the candidate
string set from: `LOCK_FINDING_NOTE`; `UNKNOWN_LOCK_SITE`; every `LockSite.site`
and every `SaturationFlag.message` from `analyse_saturation()` run over the
committed derivative fixture; and the same fields from the synthetic
lock-convergence scenario Task 2 builds, so the lock-specific strings are
actually present in the set rather than trivially absent. Extract the shipped
whole-source term from REQUIREMENTS.md as the existing half already does, then
assert that term plus the literal terms for lock ownership and lock holding are
each absent from every candidate string, matched case-insensitively with
`re.search` using `\b...\b` word boundaries.

Word boundaries are load-bearing and need an inline comment saying so: a bare
substring test would fail on any innocuous word ending in the same letters, and
a guard that produces false positives gets weakened by the next person who trips
it. Assert the candidate set is non-empty before the loop, so a refactor that
stops emitting these strings cannot turn the test vacuously green — the same
non-vacuity discipline the eval gate applies to an empty positive set.

Add a short comment recording S-7: `src/sift/rules/eustack_roles.toml` carries
one of the three terms in a prose comment describing the non-goal, which is
documentation and not output, which is why the whole-source grep stays scoped to
the single term REQUIREMENTS.md names while the three-term prohibition is
enforced over emitted strings.
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_rules.py -k ownership_blind -x</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_eustack_rules.py -k ownership_blind -x` exits 0 and collects exactly 1 test.
    - The test asserts its candidate string set is non-empty before iterating, so it cannot pass vacuously.
    - The candidate set includes at least one `LockSite.site` and one `SaturationFlag.message` from the synthetic lock scenario, so lock-specific emitted strings are genuinely covered.
    - `uv run pytest tests/test_eustack_rules.py -x` exits 0.
    - `uv run ruff check` and `uv run pyright` both exit 0.
  </acceptance_criteria>

  <done>The three-term ownership-blind prohibition is mechanically enforced over the strings Phase 16 emits, with word-boundary matching and a non-vacuity guard, in one extended test rather than a second parallel one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested capture text -> `SignatureGroup.frames` -> `LockSite.site` | Customer C++ symbol text crosses from the case store into an attribution field an engineer will act on |
| shipped rules TOML -> `Rule.subsystem`/`frame_index` -> the walk's starting point | A curated rule change silently relocates every lock finding's starting frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Repudiation | `LockSite.site` presented as an authoritative call site | high | mitigate | Two controls. (a) `UNKNOWN_LOCK_SITE` is substituted whenever the walk resolves nothing, so an engineer is never shown a fabricated or leaf-attributed site (D-04 edge case 1) — pinned by `test_lock_site_unknown_but_counted`. (b) `LOCK_FINDING_NOTE` travels on `SaturationAnalysis` so Phase 17 cannot render the table without the ownership-blind label (D-05), and the three-term prohibition is asserted over emitted strings by `-k ownership_blind` (Task 3). The denylist's known imprecision — a C++ runtime namespace outside the four listed would still be reported — is recorded openly in the helper's docstring rather than silently accepted, following ADR 0015's precedent. |
| T-16-02 | Tampering | `enclosing_application_frame` walk direction and prefix test | high | mitigate | Walking toward decreasing index, or using a substring rather than prefix denylist test, both silently mis-attribute contention to a runtime frame. Each is pinned by a dedicated test with real fixture-derived shapes: `test_lock_site_walk_finds_enclosing_application_frame` (direction) and `test_lock_site_skips_runtime_namespace` plus `test_lock_site_template_arg_not_misjudged` (prefix, both directions). |
| T-16-03 | Information disclosure | Verbatim customer C++ symbol text in `LockSite.site` | low | accept | The symbol text originates from an artefact the operator already holds, and this phase performs no rendering, no file write, no CSV emission and no network call. Output-boundary guarding (`_csv_safe`, sanitise) is Phase 17's responsibility. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan — zero new dependencies (16-RESEARCH.md § Package Legitimacy Audit records the gate as not applicable). No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `uv run pytest tests/test_eustack_rules.py -x` — every new lock test plus all Phase 15 and 16-01 tests green.
- `uv run pytest` — full suite, per 16-VALIDATION.md's per-wave sampling rule.
- `uv run ruff check` and `uv run pyright` clean.
- Manual sanity check on the committed derivative fixture: `lock_sites` is empty and no `lock_convergence_count` flag is emitted, matching Rule 6's documented zero matches on a healthy capture.
</verification>

<success_criteria>
- Threads converging on a lock-acquisition path are reported with site and count
  (Roadmap Success Criterion 2, EUS-04).
- The reported site is the enclosing application frame, found by walking toward
  increasing frame index from the classification's own `frame_index`, skipping
  unresolvable frames and the four runtime namespaces by leading-namespace prefix
  (D-03, D-04).
- No qualifying frame yields `UNKNOWN_LOCK_SITE` with the thread count intact —
  never dropped, never the leaf.
- Every emitted lock string is free of all three D-05-prohibited terms, asserted
  mechanically over real emitted output with a non-vacuity guard.
- The `blocked-on-lock` path is exercised by a clearly-labelled synthetic
  scenario at three severity tiers (D-11); the healthy capture emits zero lock
  sites and zero lock flags.
- Figures computed model-free (D-12); zero new dependencies.
- `ruff check`, `pyright` and `pytest` all clean.
</success_criteria>

<output>
Create `.planning/phases/16-saturation-contention-signature-collapse/16-02-SUMMARY.md` when done.
</output>
</content>
