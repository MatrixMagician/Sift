---
phase: 19-ranking-exclusion-regression-gated-golden-eval
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/sift/eval/thresholds.py
  - src/sift/eval/report.py
  - eval/thresholds.toml
  - tests/fixtures/eustack/derive_reference_capture_derivative.py
  - eval/cases/eustack-healthy/README.md
  - eval/cases/eustack-healthy/truth.yaml
  - eval/cases/eustack-healthy/input/threaddump.txt
  - tests/test_eval_cases.py
  - tests/test_eval_thresholds.py

autonomous: true
requirements: [EUS-12]

must_haves:
  truths:
    - "EUS-12: `sift eval` gates a fifth floor, eustack_detection_rate = 1.00, in the same higher-is-better lower-bound shape as the existing four, so one comparison direction still covers every metric (D-19-07)."
    - "EUS-12: a suite containing zero scorable eu-stack cases can never report a pass, even though the new aggregate reads a vacuous 1.00 on an empty list — deleting or skipping the eu-stack cases cannot turn the gate green (D-19-13)."
    - "EUS-12: the real healthy reference capture, run as a golden case, reports hang_detected false and raises zero graded flags, both asserted independently (D-19-15)."
    - "EUS-12: the healthy case is machine-marked provenance observed and is the only case in the suite so marked, so the synthetic-versus-observed evidence gap stays visible inside the harness rather than only in planning documents (D-19-10)."
    - "EUS-12: the healthy case lives in its own `eval/cases/eustack-healthy/` directory rather than being folded into a combined multi-dump case, so the suite table shows its verdict independently and a regression names its own case (D-19-08); its input stays signature-derived and small, following the discipline already used for `tests/fixtures/eustack/` rather than shipping a full-size realistic dump (D-19-12)."
    - "EUS-12: the eu-stack column and its floor verdict appear in both the text and JSON metric tables, so a regression names itself in `sift eval` output rather than only in an exit code."
  prohibitions:
    - statement: "MUST NOT present authored fixtures as observed evidence, and MUST NOT describe a synthetic-positive pass as validated hang-detection recall — the reference capture proves only that the analyser does not cry wolf."
      category: transparency
      requirement_id: EUS-12
      status: unresolved
      verification: null
      resolution: null
      reason: null
  artifacts:
    - path: "src/sift/eval/thresholds.py"
      provides: "the fifth METRIC_KEYS entry and the no_eustack_cases vacuity flag on GateResult"
      contains: "eustack_detection_rate"
    - path: "eval/thresholds.toml"
      provides: "the eustack_detection_rate floor"
      contains: "eustack_detection_rate"
    - path: "src/sift/eval/report.py"
      provides: "the eu-stack verdict column and the no-eu-stack-case gate line"
    - path: "eval/cases/eustack-healthy/truth.yaml"
      provides: "the frozen negative golden truth, provenance observed"
      contains: "provenance"
    - path: "eval/cases/eustack-healthy/input/threaddump.txt"
      provides: "a thread-proportion-faithful derivative of the real healthy reference capture"
    - path: "eval/cases/eustack-healthy/README.md"
      provides: "provenance narrative, derivation invocation, and the measured figures the truth block pins"
  key_links:
    - from: "src/sift/eval/thresholds.py::gate"
      to: "src/sift/eval/metrics.py::SuiteResult.mean_eustack_detection_rate"
      via: "the fifth entry in the values map, consumed by the existing MetricVerdict loop"
      pattern: "eustack_detection_rate"
    - from: "src/sift/eval/thresholds.py::gate"
      to: "src/sift/eval/metrics.py::CaseResult.is_eustack"
      via: "no_eustack_cases, folded into passed alongside the existing three negative terms"
      pattern: "no_eustack_cases"
    - from: "eval/cases/eustack-healthy/truth.yaml"
      to: "src/sift/eval/runner.py::_run_eustack_case"
      via: "expect_eustack figures compared against analyse_eustack_bundle's output"
      pattern: "expect_eustack"
---

<objective>
Wire the eu-stack metric into the `sift eval` gate and land the negative golden case: the real healthy
reference capture, derived down to a committable thread-proportion-faithful fixture, asserting no hang and
zero graded flags.

Purpose: this is the half of EUS-12 that can be proven against observed evidence. It also installs the
vacuity guard, so the synthetic positives landing in 19-04 cannot be deleted without turning the gate red.

Output: a fifth gated floor, a `no_eustack_cases` guard, an eu-stack column in both report formats, and
`eval/cases/eustack-healthy/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-ranking-exclusion-regression-gated-golden-eval/19-CONTEXT.md
@.planning/phases/19-ranking-exclusion-regression-gated-golden-eval/19-PATTERNS.md
@.planning/phases/19-ranking-exclusion-regression-gated-golden-eval/19-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the shipped source this planning session. -->

From `src/sift/eval/thresholds.py`:
- `METRIC_KEYS: tuple[str, ...] = ("retrieval_hit_rate", "hypothesis_hit_at_k", "citation_validity_rate", "determinism_stability")`
  — order is display order; `load_thresholds` builds `{key: float(raw[key]) for key in METRIC_KEYS}`, so a
  fifth entry automatically becomes a REQUIRED float in `eval/thresholds.toml` and a missing one raises
  `ValueError` (mapped by the CLI to exit 2).
- `GateResult` fields: `metrics: list[MetricVerdict]`, `run_failed_cases: list[str]`,
  `false_positive_cases: list[str]`, `no_positive_cases: bool`, `passed: bool`, plus `as_dict()`.
- `gate()` builds a `values` dict of four `suite.mean_*()` calls, then the `metrics` list comprehension
  over `METRIC_KEYS`, then:
```
no_positive_cases = not any(
    not c.expect_no_incident and not c.run_failed for c in suite.cases
)
passed = (
    all(m.passed for m in metrics)
    and not run_failed_cases
    and not false_positive_cases
    and not no_positive_cases
)
```

From `src/sift/eval/report.py`:
- `render_text_table` builds `header` from fixed-width column labels
  (`case` 32, `retrieval` 9, `hit@k` 6, `citation` 8, `drift` 6, optional `judge` 6, then `status`),
  one row per case via `_status(case)`, an aggregate row, then — when `gate` is supplied — a
  `gate (floors):` block looping `gate.metrics`, followed by conditional lines for
  `run_failed_cases`, `false_positive_cases`, `no_positive_cases`, then `GATE: PASS|FAIL`.
  The existing no-positive line reads `"  no scorable positive case — gate cannot pass"`.
- `_status(case)` returns `FAILED` / `PASS` / `FAIL` / `ok`.
- `_case_dict(case)` is the per-case JSON projection; `render_json_table` emits
  `{"cases": [...], "aggregate": {...}, "gate": gate.as_dict()}` with `sort_keys=True`.

From `eval/thresholds.toml` — four bare `key = value` lines with aligned inline comments, under a
12-line header comment block explaining the uniform lower-bound direction (ADR 0010).

From `tests/test_eval_cases.py:27-90`:
- `_SUITE = _REPO_ROOT / "eval" / "cases"`; `_case_dirs()` returns sorted dirs containing `truth.yaml`.
- `_EXPECTED_CASES` is a hardcoded set of 8 names; `test_suite_is_exactly_the_eight_cases` asserts
  `dirs == _EXPECTED_CASES`, `len(dirs) == 8`, and `"perfmon-denial" in dirs`.
- `_offline_client(config, *, hyp_content=...)` builds an `InferenceClient` over an `httpx.MockTransport`.

From `tests/fixtures/eustack/derive_reference_capture_derivative.py`:
- A provenance tool, never collected by pytest. `argparse` CLI: `<input> <output>`.
- Module constants `_HIGH_POPULATION_CAP = 5`, `_DEFAULT_CAP = 1`, `_HIGH_POPULATION_COUNT = 3`,
  `_SYNTHETIC_PID_HEADER`, `_SYNTHETIC_TID_START = 100_001`.
- `iter_thread_blocks(text)` splits on `_TID_RE`; grouping uses `signature_of`, imported from
  `sift.pipeline.eustack` rather than re-derived. The tool is deliberately meaning-blind: no role, no
  rule, no pool concept — that blindness is what makes its output immune to PITFALLS Pitfall 5.

Measured 2026-07-27 against the real capture at
`/home/oliverh/Downloads/iserver1_stacks_1-minute_diff/` (first dump, `..._160739 (12_07 PM EST).txt`):
```
total_threads 3902   total_signatures 93
flags: unclassified_thread_pct info 1.3 percent ; no_resolvable_frame_pct info 0.0 percent
lock_sites: []
dependencies: warehouse 92 ; http 96 ; ipc 6      (rendered order: http, warehouse, ipc)
pools (top 6): job-queue 1716 total/1716 idle/0 busy ; evaluation 1126/1126/0 ;
               command-queue 247/247/0 ; ipc 218/212/6 ; generic-pool 110/110/0 ; http 96/0/96
```
Measured on the committed `tests/fixtures/eustack/reference_capture_derivative.txt` (105 threads):
`unclassified_thread_pct` = **38.1 critical**. That fixture therefore CANNOT be the negative case input.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The fifth gated floor and the zero-eu-stack-cases vacuity guard</name>
  <reversibility rating="costly">`eustack_detection_rate` becomes a required key in `eval/thresholds.toml`; because `load_thresholds` raises on a missing floor, any deployment or CI checkout carrying an older thresholds file starts exiting 2 until the key is added. Renaming it later means a coordinated edit of the config file, `METRIC_KEYS`, and both report projections.</reversibility>
  <read_first>
    - `src/sift/eval/thresholds.py` — the whole file (135 lines); the `no_positive_cases` construction at
      lines 119-127 is the exact shape to mirror.
    - `src/sift/eval/report.py` — the whole file (134 lines); note the fixed-width header construction and
      the conditional gate lines.
    - `eval/thresholds.toml` — the header comment block and the four aligned entries.
    - `src/sift/eval/metrics.py` — `mean_eustack_detection_rate` as landed in 19-02, and the `_mean([])`
      vacuous `1.0`.
    - `tests/test_eval_thresholds.py` — the existing gate tests, including how they fabricate
      `CaseResult`/`SuiteResult` records without I/O.
  </read_first>
  <files>src/sift/eval/thresholds.py, src/sift/eval/report.py, eval/thresholds.toml, tests/test_eval_thresholds.py</files>
  <behavior>
    - A suite with one passing eu-stack case and the eight ordinary cases gates PASS.
    - A suite with one FAILING eu-stack case gates FAIL, and the failing metric named in the output is
      `eustack_detection_rate`.
    - A suite with ZERO eu-stack cases gates FAIL even though every metric aggregate reads 1.00 — this is
      the vacuity guard, and it must be asserted with all five metric verdicts passing, so the test
      genuinely isolates the new term.
    - A suite whose only eu-stack case is `run_failed` also gates FAIL (it is not a scorable eu-stack
      case), proving the guard counts scorable cases rather than directory entries.
    - `load_thresholds` on a thresholds file lacking `eustack_detection_rate` raises `ValueError`.
    - `render_text_table` with a failing gate prints an `eustack_detection_rate` floor line, and prints an
      explicit line when no scorable eu-stack case exists.
    - `render_json_table`'s per-case objects carry the eu-stack fields, and its `gate` object carries the
      new flag.
  </behavior>
  <action>
1. In `src/sift/eval/thresholds.py`: append `"eustack_detection_rate"` as the fifth entry of
   `METRIC_KEYS` (last, so display order stays stable for the existing four). Add
   `"eustack_detection_rate": suite.mean_eustack_detection_rate()` to `gate()`'s `values` dict — the
   `metrics` comprehension and `MetricVerdict` need no other change. Add, immediately after the existing
   `no_positive_cases` assignment and in the identical shape:
   `no_eustack_cases = not any(c.is_eustack and not c.run_failed for c in suite.cases)`.
   Add `no_eustack_cases: bool` to `GateResult` (placed after `no_positive_cases`), add it to `as_dict()`,
   and add `and not no_eustack_cases` to the `passed` expression as a fourth negative term. Update the
   module docstring and `gate()`'s docstring so both say "five floors" and name the new invariant in the
   same voice as the existing empty-positive-set sentence, citing D-19-13.

2. In `eval/thresholds.toml`: add `eustack_detection_rate = 1.00` with an aligned inline comment in the
   file's existing British-English voice, stating that every eu-stack golden case must reproduce its
   declared figures. Extend the header comment block so it says five floors rather than four.

3. In `src/sift/eval/report.py`: add an `eustack` verdict cell to `render_text_table`'s per-case rows
   (rendering the tri-state: the pass/fail verdict for an eu-stack case, `n/a` for every other case,
   mirroring `_judge_cell`'s `n/a` idiom), widen the header accordingly, and add a conditional gate line
   for the new flag mirroring the shipped `"  no scorable positive case — gate cannot pass"` line. Add
   `is_eustack` and `eustack_case_pass` to `_case_dict`. The `gate` object in `render_json_table` picks up
   the new flag automatically via `as_dict()`.

4. Add the `<behavior>` cases to `tests/test_eval_thresholds.py`, fabricating `CaseResult` records
   directly (no I/O, no client). The zero-eu-stack-cases test must additionally assert that all five
   `MetricVerdict`s passed, so it proves the FAIL came from the vacuity term and not from a floor.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_eval_thresholds.py -q` passes.
    - `uv run python -c "from sift.eval.thresholds import METRIC_KEYS; assert METRIC_KEYS[-1]=='eustack_detection_rate' and len(METRIC_KEYS)==5, METRIC_KEYS"` exits 0.
    - `uv run python -c "
      from pathlib import Path
      from sift.eval.thresholds import load_thresholds
      f = load_thresholds(Path('eval/thresholds.toml'))
      assert f['eustack_detection_rate'] == 1.00, f"` exits 0.
    - The vacuity term is folded into `passed`:
      `grep -c 'and not no_eustack_cases' src/sift/eval/thresholds.py` returns 1.
    - The zero-eu-stack-cases test asserts `all(m.passed for m in result.metrics)` alongside
      `result.passed is False`.
    - `uv run pytest tests/test_eval_cases.py -q` — the eight shipped cases are unaffected by the report
      column change (this is the guard against a widened header breaking a shipped output assertion).
    - `uv run ruff check src/sift/eval/ tests/test_eval_thresholds.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_thresholds.py tests/test_eval_cases.py -q</automated>
  </verify>
  <done>The gate carries a fifth floor and a vacuity guard that no empty eu-stack set can slip past, and both report formats name the new metric.</done>
</task>

<task type="auto">
  <name>Task 2: Derive and commit the negative golden case from the real healthy capture</name>
  <precondition>The real reference capture is readable at `/home/oliverh/Downloads/iserver1_stacks_1-minute_diff/` — two files, roughly 2.4 MB each; the earlier `160739` dump is the one to derive from.</precondition>
  <read_first>
    - `tests/fixtures/eustack/derive_reference_capture_derivative.py` — the whole file. Its docstring
      states why the tool is meaning-blind and records the existing invocation; its `argparse` block, the
      three cap constants and `iter_thread_blocks` are what this task extends.
    - `eval/cases/perfmon-denial/README.md` and `eval/cases/perfmon-denial/truth.yaml` — the three-file
      case-directory shape and, critically, the frozen-truth header comment ("Do not edit this file to
      make a run pass — a regression must fail, not be silently accommodated") that every new truth.yaml
      must carry.
    - `src/sift/adapters/eustack.py:39-58` — `MAX_EVENT_LINES`, `MAX_EVENT_BYTES`, `_SNIFF_TID_RE`
      (`^TID \d+:`) and `_SNIFF_FRAME_RE` (`^#\d+\s+0x`). BOTH sniff regexes must match in the sniffed
      head or the file falls through to `genericlog` and the whole case silently proves nothing.
    - This plan's `<interfaces>` measured figures for the real capture.
  </read_first>
  <files>tests/fixtures/eustack/derive_reference_capture_derivative.py, eval/cases/eustack-healthy/input/threaddump.txt, eval/cases/eustack-healthy/truth.yaml, eval/cases/eustack-healthy/README.md</files>
  <action>
The committed CI fixture `tests/fixtures/eustack/reference_capture_derivative.txt` CANNOT be reused here:
it is signature-faithful but deliberately not thread-count-faithful (105 threads standing in for 3,902),
which inflates its unclassified share to a measured 38.1% and grades it `critical`. The negative case
needs a thread-PROPORTION-faithful derivative instead.

1. Extend `derive_reference_capture_derivative.py` with an optional `--scale N` argument. When supplied,
   the per-signature keep count becomes `round(count / N)` and a signature rounding to zero contributes no
   blocks — that zero is the whole point, and it is what preserves real thread proportions. When omitted,
   behaviour is byte-identical to today, so the existing committed fixture stays reproducible from its
   recorded invocation. Keep the tool meaning-blind: `--scale` reads only per-signature block counts;
   introduce no role, rule, pool or dependency concept, and import nothing from `sift.pipeline.eustack`
   beyond the `signature_of` the tool already uses. Append the new invocation to the module docstring
   alongside the existing one.

2. Run it against the earlier (`160739`) real dump, writing to
   `eval/cases/eustack-healthy/input/threaddump.txt` — its own case directory, per D-19-08, never folded
   into a combined multi-dump case. The 250 KB ceiling below is D-19-12's "small, not a full-size
   realistic dump" discipline made mechanical. Start at `--scale 26` (≈150 threads). Then measure
   the result by calling `load_rules(None)` and `analyse_eustack_bundle` over the parsed events and
   iterate the scale until ALL of these hold: at least 100 threads; no flag at `warn` or `critical`
   severity; the file is at most 250 KB; and both sniff regexes match within the first 4 KB. The scale
   value is not the acceptance criterion — the measured flag severities are.

3. Write `eval/cases/eustack-healthy/truth.yaml`. Carry the frozen-truth header comment verbatim in shape
   from `perfmon-denial/truth.yaml`. Set `root_cause` to a British-English statement that this capture
   shows a healthy, near-idle server with no incident. Set `required_evidence: []` and
   `acceptable_keywords: []` (D-19-06: no LLM metric is scored vacuously on this case) and
   `expect_no_incident: false` — this case is scored by the eu-stack path, not by the negative-hypothesis
   predicate, and setting the older flag would double-count it as a second kind of negative. Add the
   `expect_eustack` block with `provenance: observed`, `hang_detected: false`, the flag field at `0` under
   the semantics chosen at 19-02's checkpoint, `total_threads` set to the MEASURED thread count of the
   derivative you just wrote, and `pools` / `dependencies` entries copied from the MEASURED bundle. Every
   number in this file must come from a run against the committed fixture — declaring a figure that was
   not measured is exactly what the plan's integrity prohibition forbids.

4. Write `eval/cases/eustack-healthy/README.md` covering: that the input is a mechanically derived subset
   of a real, observed healthy Intelligence Server capture, with the derivation invocation quoted; the
   scale factor and the resulting thread count against the real 3,902; the measured flag list with
   severities; and — stated plainly — that this case proves only that the analyser does not raise false
   alarms on a healthy server, and cannot demonstrate hang-detection recall, for which the only evidence
   is the authored fixtures in `eustack-hang-*`.
  </action>
  <acceptance_criteria>
    - `eval/cases/eustack-healthy/` contains exactly `README.md`, `truth.yaml` and `input/threaddump.txt`.
    - The input sniffs as eu-stack and parses to only eu-stack events:
      `uv run python -c "
      from pathlib import Path
      from sift.adapters.eustack import EustackAdapter
      d = Path('eval/cases/eustack-healthy/input')
      a = EustackAdapter(); a.input_root = d
      evs = list(a.parse(d / 'threaddump.txt', 'healthy'))
      assert {e.source for e in evs} == {'eustack'}, {e.source for e in evs}
      assert sum(1 for e in evs if e.thread is not None) >= 100"` exits 0.
    - Zero graded flags, measured:
      `uv run python -c "
      from pathlib import Path
      from sift.adapters.eustack import EustackAdapter
      from sift.config import EustackThresholdsConfig
      from sift.pipeline.eustack import load_rules
      from sift.pipeline.eustack_progression import analyse_eustack_bundle
      d = Path('eval/cases/eustack-healthy/input')
      a = EustackAdapter(); a.input_root = d
      evs = list(a.parse(d / 'threaddump.txt', 'healthy'))
      r, h = load_rules(None)
      b = analyse_eustack_bundle(evs, r, h, EustackThresholdsConfig())
      graded = [f for f in b.saturation.flags if f.severity != 'info']
      assert graded == [], graded
      assert b.saturation.lock_sites == ()"` exits 0.
    - `test -s eval/cases/eustack-healthy/input/threaddump.txt && [ "$(stat -c%s eval/cases/eustack-healthy/input/threaddump.txt)" -le 256000 ]` succeeds.
    - `uv run python -c "
      from pathlib import Path
      from sift.eval.truth import load_truth
      t = load_truth(Path('eval/cases/eustack-healthy/truth.yaml'))
      assert t.expect_eustack is not None
      assert t.expect_eustack.provenance == 'observed'
      assert t.expect_eustack.hang_detected is False
      assert t.required_evidence == [] and t.acceptable_keywords == []"` exits 0.
    - The derivation tool's default behaviour is unchanged: rerunning its ORIGINAL recorded invocation
      reproduces `tests/fixtures/eustack/reference_capture_derivative.txt` byte-for-byte
      (`cmp` against the committed file succeeds).
    - `README.md` states the recall limitation explicitly and quotes the derivation invocation.
    - `uv run ruff check tests/fixtures/eustack/derive_reference_capture_derivative.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_truth.py -q</automated>
  </verify>
  <done>A thread-proportion-faithful derivative of the real healthy capture is committed as a golden case whose measured figures raise zero graded flags, with its provenance and its recall limitation written down beside it.</done>
</task>

<task type="auto">
  <name>Task 3: Register the healthy case in the suite and pin its verdict and provenance</name>
  <read_first>
    - `tests/test_eval_cases.py:27-120` — `_EXPECTED_CASES`, `test_suite_is_exactly_the_eight_cases`,
      `test_every_truth_yaml_loads`, `test_special_shapes_present`, and the
      `test_negative_case_passes_offline_and_is_excluded_from_positive_aggregate` shape (the closest
      analog for an offline case-scoring test).
    - `eval/cases/eustack-healthy/truth.yaml` as written in Task 2 — the exact declared figures.
    - `.planning/phases/19-ranking-exclusion-regression-gated-golden-eval/19-RESEARCH.md` "Pitfall 3" —
      the hardcoded-count test failing here is an expected expectation update, not a discovered regression.
  </read_first>
  <files>tests/test_eval_cases.py</files>
  <action>
1. Add `"eustack-healthy"` to `_EXPECTED_CASES`, change the count assertion from 8 to 9, and rename
   `test_suite_is_exactly_the_eight_cases` to match the new count so the name does not lie. Update the
   module docstring's case-count sentence and the `_EXPECTED_CASES` comment to record what the ninth case
   is. This is an expected expectation update (RESEARCH Pitfall 3), not a regression — record that in the
   commit message.

2. Add `test_eustack_healthy_case_scores_pass_offline`: run `run_case` on `eval/cases/eustack-healthy`
   through the mocked-transport client, and assert `run_failed is False`, `is_eustack is True`,
   `eustack_case_pass is True`, and that the recorded request list is EMPTY — the D-19-16 no-endpoint
   claim, proven per case rather than only in the unit test from 19-02.

3. Add `test_eustack_healthy_raises_no_graded_flag`: assert directly on the bundle produced from the
   committed fixture that `hang_detected` is false per the truth block AND that no flag reaches `warn`.
   Assert BOTH — under D-19-17 these are genuinely independent claims (a fixture can reproduce hang-shaped
   figures without tripping any graded dimension, and vice versa), so a single assertion would leave half
   of D-19-15 unproven. Include a non-vacuity guard: the flag tuple is non-empty (the analyser really did
   grade the two percentage dimensions and reported them `info`), so a future change that stopped emitting
   flags entirely cannot pass this test by accident.

4. Add `test_only_the_healthy_case_is_marked_observed`: iterate every case directory whose truth carries
   an eu-stack block and assert exactly one is `provenance == "observed"`, and that it is
   `eustack-healthy`. Written now so it is already in place when 19-04 adds the authored positives, and
   guard it with an assertion that at least one eu-stack case was examined.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_eval_cases.py -q` passes and the suite-shape test asserts `len(dirs) == 9`.
    - `grep -c 'eight_cases' tests/test_eval_cases.py` returns 0 — the renamed test carries no stale count
      in its name.
    - `test_eustack_healthy_case_scores_pass_offline` asserts the recorded request list `== []`.
    - `test_eustack_healthy_raises_no_graded_flag` contains all three assertions: no flag at warn or above,
      `hang_detected` false, and a non-empty flag tuple.
    - `test_only_the_healthy_case_is_marked_observed` asserts at least one eu-stack case was examined
      before asserting the observed count is 1.
    - `uv run ruff check tests/test_eval_cases.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_cases.py -q</automated>
  </verify>
  <done>The healthy case is part of the frozen suite, scores a pass with no endpoint contact, is proven to raise no graded flag, and is machine-marked as the sole observed case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `eval/thresholds.toml` → `load_thresholds` | The config file that controls the pass/fail decision; parsed with stdlib `tomllib` in binary mode (T-07-02), no code-execution surface. |
| real out-of-repo capture → committed fixture | A customer-environment capture is mechanically reduced and redacted on its way into the repository. |
| `eval/cases/eustack-healthy/input/` → eu-stack adapter | Fixture text is untrusted parser input, bounded by the shipped `MAX_EVENT_LINES` / `MAX_EVENT_BYTES` caps. |
| case name / error text → terminal | Rendered through the existing `sanitise` path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-09 | Information disclosure | the derived healthy fixture carrying customer-environment identifiers | high | mitigate | The derivation tool already redacts and renumbers identifying header fields (`_SYNTHETIC_PID_HEADER`, `_SYNTHETIC_TID_START = 100_001`) and writes its own synthetic preamble rather than copying the capture's. Task 2 changes only the per-signature keep count via `--scale`, leaving every redaction path untouched, and Task 2's acceptance requires the tool's default output to still reproduce the existing committed fixture byte-for-byte — which is a regression test on the redaction path itself. The real capture's filename carries an environment identifier and is never written into the repository; the README quotes the invocation with a placeholder input path, as the existing docstring already does. |
| T-19-10 | Tampering | `eval/thresholds.toml` as the pass/fail authority | medium | mitigate | Unchanged parse path: `load_thresholds` keeps its binary-mode `tomllib` read and its `float()` coercion, and adding a `METRIC_KEYS` entry makes the new floor REQUIRED rather than defaultable — a stripped or truncated thresholds file raises `ValueError` and exits 2 rather than silently gating on four metrics. Acceptance asserts the loaded value is `1.00`. |
| T-19-11 | Repudiation | a golden case whose declared figures were never measured | medium | mitigate | Task 2 requires every figure in `truth.yaml` to come from a run against the committed fixture, and Task 3's `test_eustack_healthy_case_scores_pass_offline` fails if any declared figure diverges — a fabricated figure cannot reach a green suite. The README records the derivation invocation as the audit trail. |
| T-19-12 | Denial of service | a large committed fixture slowing every CI run | low | mitigate | Task 2 caps the fixture at 250 KB, asserted with `stat`, and requires at least 100 threads — bounding both directions. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero packages installed. `uv.lock` and `pyproject.toml` untouched; the wave gate confirms via `git diff --stat`. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 19-03 slice)

**New config keys**
- `eval/thresholds.toml` → `eustack_detection_rate = 1.00`

**New model fields**
- `sift.eval.thresholds.GateResult.no_eustack_cases: bool` (also a new key in `as_dict()`)

**Changed values**
- `sift.eval.thresholds.METRIC_KEYS` — gains a fifth entry, `"eustack_detection_rate"`, in last position.

**New file paths**
- `eval/cases/eustack-healthy/README.md`
- `eval/cases/eustack-healthy/truth.yaml`
- `eval/cases/eustack-healthy/input/threaddump.txt`

**New CLI arguments**
- `tests/fixtures/eustack/derive_reference_capture_derivative.py` → `--scale N` (optional; omitted
  reproduces today's output byte-for-byte)

**New test symbols**
- `tests/test_eval_cases.py::test_eustack_healthy_case_scores_pass_offline`
- `tests/test_eval_cases.py::test_eustack_healthy_raises_no_graded_flag`
- `tests/test_eval_cases.py::test_only_the_healthy_case_is_marked_observed`
- `tests/test_eval_cases.py::_EXPECTED_CASES` — gains `"eustack-healthy"` (now 9 entries)
- `tests/test_eval_cases.py::test_suite_is_exactly_the_eight_cases` — renamed to the new count

**Changed rendering**
- `sift.eval.report.render_text_table` — one new column plus one new conditional gate line
- `sift.eval.report._case_dict` — gains `is_eustack` and `eustack_case_pass`
</artifacts_produced>

<verification>
- `uv run pytest tests/test_eval_thresholds.py tests/test_eval_cases.py tests/test_eval_truth.py tests/test_eval_harness.py -q` — green.
- `uv run pytest` — full suite green.
- `uv run ruff check` — clean.
- `uv run pyright` — no NEW errors beyond the 31-error baseline.
- `sift eval`'s overall gate is still expected to be RED at the end of this plan, because the vacuity guard
  is satisfied but the eu-stack positives do not exist until 19-04. Confirm the failure names
  `eustack-hang` absence or the positive-case terms — not a floor regression on the eight shipped cases.
</verification>

<success_criteria>
- `eustack_detection_rate = 1.00` is a required, gated, higher-is-better floor (D-19-07).
- A suite with zero scorable eu-stack cases cannot pass, proven with all five metric verdicts green
  (D-19-13).
- The healthy case asserts `hang_detected` false AND zero graded flags as two independent claims (D-19-15,
  under the semantics amended at 19-02's checkpoint).
- The healthy case is the only case machine-marked `provenance: observed`, and its README states the recall
  limitation plainly (D-19-10).
- Every figure in `eval/cases/eustack-healthy/truth.yaml` was measured from the committed fixture.
- `uv run ruff check`, `uv run pyright` (no new errors), `uv run pytest` all green.
</success_criteria>

<output>
Create `.planning/phases/19-ranking-exclusion-regression-gated-golden-eval/19-03-SUMMARY.md` when done.
</output>
