---
phase: 08-packaging-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_packaging.py
  - src/sift/cli.py
autonomous: true
requirements: [PKG-01]
must_haves:
  truths:
    - "uv build + offline install of the built wheel yields a `sift` console script that runs (--help, --version, new)"
    - "The build and install happen fully offline — no PyPI/index fetch (the zero-egress invariant holds across the uv/sift subprocess boundary)"
    - "The new packaging test is excluded from the default fast suite and only runs via `-m packaging`"
  artifacts:
    - "pyproject.toml with a `packaging` pytest marker and addopts `-m 'not perf and not live and not packaging'`"
    - "tests/test_packaging.py with a @pytest.mark.packaging offline install smoke test (PKG-01)"
    - "`sift --version` affordance printing the package version (0.1.0)"
  key_links:
    - "--offline / --no-index --find-links dist/ + UV_OFFLINE=1 on every uv subprocess (else the test is not actually offline — RESEARCH Pitfall 1)"
    - "addopts exclusion of `packaging` (else the slow smoke test regresses default-suite time)"
---

<objective>
Prove PKG-01 (D-01, D-04): a clean-checkout `uv tool install` / wheel install yields a working `sift`, entirely offline. The wheel already builds and the console script is already wired (D-02: `uv_build` backend + `[project.scripts] sift = "sift.cli:app"`), and the version stays at 0.1.0 (D-03) — so this plan *proves* PKG-01, it does not build it.

Purpose: lock the install path with an automated offline regression test, gated behind an opt-in `packaging` marker so the default fast suite is untouched. Add a small `sift --version` affordance (Claude's discretion, D-10 context) so a freshly-installed user can confirm the install and so the smoke test has a second console-script assertion.
Output: `packaging` marker + addopts extension in `pyproject.toml`, `tests/test_packaging.py` offline smoke test, and a `sift --version` Typer callback.
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `packaging` pytest marker | config (pyproject.toml) | new marker string + addopts extension |
| `tests/test_packaging.py` | test module (NEW) | `@pytest.mark.packaging` offline install smoke; PKG-02 tests appended by 08-02 |
| `sift --version` | CLI affordance (src/sift/cli.py) | Typer `@app.callback` printing `importlib.metadata.version("sift")` |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-packaging-deploy/08-RESEARCH.md
@.planning/phases/08-packaging-deploy/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `packaging` marker + addopts exclusion, and a `sift --version` affordance</name>
  <read_first>
    - pyproject.toml (lines 44-53: `[tool.pytest.ini_options]` — the existing `perf`/`live` markers + `addopts` are the exact analog to mirror; 08-PATTERNS.md §pyproject.toml)
    - src/sift/cli.py (lines 40-42: `app = typer.Typer(no_args_is_help=True)` — where the `@app.callback` goes; 08-RESEARCH.md §Code Examples "Optional sift --version")
  </read_first>
  <files>pyproject.toml, src/sift/cli.py</files>
  <action>
    In `pyproject.toml` `[tool.pytest.ini_options]`: change `addopts` from `-m 'not perf and not live'` to exactly `-m 'not perf and not live and not packaging'`, and append one `markers` entry `packaging: offline install smoke + Quadlet dry-run, run explicitly via -m packaging` (mirror the register style of the existing `perf`/`live` lines). Change nothing else in `pyproject.toml` — `[project.scripts]`, the `uv_build` backend, `[project.optional-dependencies] pdf`, and `version = "0.1.0"` are all carried forward unchanged (D-02, D-03). Do NOT bump the version.
    In `src/sift/cli.py`: add a Typer `@app.callback()` that takes a `version: bool` option bound to `--version` with `is_eager=True`; when true, echo the package version and `raise typer.Exit()`. Import `version` and `PackageNotFoundError` from `importlib.metadata` at module top, and guard the lookup: `try: v = version("sift") except PackageNotFoundError: v = "0.1.0"` (a bare `version("sift")` raises `PackageNotFoundError` when run from an uninstalled checkout, e.g. `python -m sift.cli` — the fallback keeps `--version` robust). Keep it type-hinted (strict pyright) and British-English in any help text (D-10). This is the only production-code change this phase (D-04: PKG-01 is proven, not built).
  </action>
  <acceptance_criteria>
    - `grep -F "not packaging" pyproject.toml` matches the addopts line and the full string reads `-m 'not perf and not live and not packaging'`.
    - `uv run python -c "import tomllib,pathlib; m=tomllib.loads(pathlib.Path('pyproject.toml').read_text())['tool']['pytest']['ini_options']['markers']; assert any(x.startswith('packaging:') for x in m)"` exits 0.
    - `uv run sift --version` prints `0.1.0` and exits 0.
    - `uv run pytest -q` (default suite) collects and passes with no new failures; `ruff check` and `pyright` are clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run sift --version && uv run ruff check && uv run pyright && uv run pytest -q</automated>
  </verify>
  <done>addopts excludes `packaging`; the `packaging` marker is registered; `sift --version` prints 0.1.0; version stays 0.1.0; ruff/pyright/default pytest clean.</done>
</task>

<task type="auto">
  <name>Task 2: Offline install smoke test — build a wheel, install it offline into a throwaway env, run the real `sift` (PKG-01 e2e proof)</name>
  <read_first>
    - tests/perf/test_perf_ingest.py (lines 1-6, 31: opt-in `@pytest.mark.perf` + module-docstring convention — the exact analog for a marker-gated slow integration test; 08-PATTERNS.md §tests/test_packaging.py)
    - tests/conftest.py (lines 34-60: the autouse `_no_network` guard patches `socket.socket.connect` IN-PROCESS ONLY — it does NOT reach uv/sift subprocesses; 08-RESEARCH.md Pitfall 1)
    - 08-RESEARCH.md (§Pattern 1 / §Code Examples lines 140-164: the build→offline-install→run-console-script shape; Assumptions A1/A2 and the `--find-links dist/` fallback)
  </read_first>
  <files>tests/test_packaging.py</files>
  <action>
    Create `tests/test_packaging.py` with a module docstring stating the module runs explicitly via `uv run pytest -m packaging` (mirroring the perf module) AND spelling out the subprocess carve-out: the autouse `_no_network` conftest guard patches the in-process socket only, so it does NOT reach uv/sift subprocesses — offline-ness is enforced with uv's own flags, and this marker does NOT need a `_no_network` exemption (unlike `live`); do NOT add a `packaging` branch to conftest.
    Write one `@pytest.mark.packaging` test using `tmp_path`: (1) `uv build --offline --wheel -o <dist>` (a subdir of tmp_path); resolve the wheel via `next(Path(dist).glob("sift-*.whl"))`. (2) `uv tool install --offline --no-index --find-links <dist> <wheel>` — exercising PKG-01's EXACT `uv tool install` code path (tool-venv creation + PATH-injection), not an equivalent `uv pip install`. Redirect the tool location to a HERMETIC dir so the real user tool state is never touched: set `UV_TOOL_DIR=<tmp>/tools` and `UV_TOOL_BIN_DIR=<tmp>/bin` in the subprocess env. (3) assert the injected entry-point script `<tmp>/bin/sift` exists (proves `uv tool install` PATH-injection worked), then run `<tmp>/bin/sift --help`, `<tmp>/bin/sift --version`, and a trivial `<tmp>/bin/sift new smoke --input <scratch>` (scratch dir under tmp_path with one tiny .log). Pass `env={"UV_OFFLINE": "1", "UV_TOOL_DIR": str(<tmp>/tools), "UV_TOOL_BIN_DIR": str(<tmp>/bin), **os.environ}` on every `subprocess.run` as belt-and-braces (RESEARCH Pitfall 1). Assert each subprocess exits 0, that `--help` stdout contains `ingest`, and that `--version` stdout contains `0.1.0`. Use `check=True` and `capture_output=True, text=True`. Runtime deps resolve from the warm uv cache after a prior `uv sync` (RESEARCH A1); if the cache is cold in a given environment the documented fallback is to vendor the runtime wheels into `<dist>` via `--find-links` — note this in a comment but implement the cache path.
  </action>
  <acceptance_criteria>
    - `uv run pytest -m packaging tests/test_packaging.py -q` passes (the smoke test builds, installs offline, and runs the console script).
    - The test issues zero index/PyPI fetches: every uv subprocess carries `--offline` (and `UV_OFFLINE=1`), and `uv tool install` carries `--no-index --find-links <dist>`.
    - The test uses `uv tool install` (not `uv pip install`) into a hermetic `UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` under tmp_path, and asserts the injected `<tmp>/bin/sift` entry point exists before running it — proving PKG-01's exact `uv tool install` path, not an equivalent.
    - `uv run pytest -q` (default suite, no `-m packaging`) does NOT collect this test (proven by the run time staying at baseline and `--collect-only -q` not listing it).
    - `ruff check` and `pyright` clean on the new module.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -m packaging tests/test_packaging.py -q && uv run ruff check tests/test_packaging.py && uv run pyright tests/test_packaging.py</automated>
  </verify>
  <done>Building a wheel and installing it offline into a throwaway env yields a `sift` that runs `--help`/`--version`/`new`; the test is opt-in (`-m packaging`) and enforces offline via uv flags + `UV_OFFLINE=1`, not the socket guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → package index (PyPI) | `uv build`/`uv pip install` run as subprocesses and could fetch index metadata, bypassing the in-process socket guard |
| source tree → wheel | the checked-out source is the only trusted build input for the distributed wheel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Information Disclosure | tests/test_packaging.py uv subprocesses | medium | mitigate | Force `--offline` (+ `UV_OFFLINE=1`) on every uv subprocess and `--no-index --find-links dist/` on install; the conftest socket guard does not reach subprocesses (RESEARCH Pitfall 1), so offline-ness is enforced by uv flags |
| T-08-03 | Tampering | wheel build / dependency set | high | mitigate | No new runtime deps this phase (D-02/D-03); deps stay pinned in pyproject; the wheel is built only from the checked-out source; `uv build` uses the native `uv_build` backend (no build-frontend fetch) |
| T-08-SC | Tampering | pip/uv package installs | low | accept | No package-manager installs occur this phase — RESEARCH Package Legitimacy Audit records zero new packages, so the supply-chain surface is unchanged and there is nothing to verify at a legitimacy checkpoint |
</threat_model>

<verification>
- `uv run pytest -m packaging` passes (offline install smoke green).
- `uv run pytest` default suite unchanged (packaging excluded via addopts).
- `uv run sift --version` → `0.1.0`; `ruff check` + `pyright` clean.
</verification>

<success_criteria>
PKG-01 has an automated, fully-offline proof: `uv build` + offline install yields a runnable `sift` console script (--help/--version/new), the proof is opt-in behind `-m packaging`, and the default fast suite is untouched. Version stays 0.1.0 (D-03).
</success_criteria>

<output>
Create `.planning/phases/08-packaging-deploy/08-01-SUMMARY.md` when done.
</output>
