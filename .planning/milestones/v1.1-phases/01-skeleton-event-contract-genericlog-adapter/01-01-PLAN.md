---
phase: 01-skeleton-event-contract-genericlog-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - LICENSE
  - src/sift/__init__.py
  - src/sift/cli.py
  - tests/conftest.py
  - tests/test_cli.py
autonomous: false
requirements: [CLI-01, INGST-01]
user_setup: []

must_haves:
  truths:
    - "`uv run sift --help` exits 0 and lists exactly seven subcommands: new, ingest, analyze, report, show, eval, doctor (CLI-01)"
    - "The test suite cannot open network connections: an autouse pytest fixture patches socket.socket.connect to raise"
    - "Tests never touch the real home directory: an autouse fixture redirects XDG_DATA_HOME, XDG_CONFIG_HOME and clears SIFT_* env vars to tmp_path"
    - "An end-to-end CliRunner test covering new -> ingest -> show events exists in tests/test_cli.py (the walking-skeleton happy path, INGST-01)"
    - "`uv run ruff check` and `uv run pyright` exit 0 on the scaffold"
  artifacts:
    - path: pyproject.toml
      provides: "uv-managed project, deps typer/pydantic/zstandard, dev deps pytest/ruff/pyright, sift entry point"
      contains: "sift.cli:app"
    - path: src/sift/cli.py
      provides: "Typer app with 7 subcommands (stub bodies)"
      contains: "typer.Typer"
    - path: tests/conftest.py
      provides: "XDG isolation + socket guard autouse fixtures"
      contains: "autouse"
    - path: tests/test_cli.py
      provides: "walking-skeleton end-to-end test (RED at end of this plan)"
      contains: "test_walking_skeleton_happy_path"
    - path: LICENSE
      provides: "Apache-2.0 licence text"
      contains: "Apache License"
  key_links:
    - from: pyproject.toml
      to: src/sift/cli.py
      via: "[project.scripts] sift = sift.cli:app"
      pattern: "sift\\.cli:app"
    - from: tests/test_cli.py
      to: src/sift/cli.py
      via: "typer.testing.CliRunner invoking app"
      pattern: "CliRunner"
  prohibitions:
    - statement: "Zero network egress: this phase installs no HTTP client dependency and no module under src/sift/ opens a socket at runtime or in tests"
      status: flagged-unverified
---

<objective>
Bootstrap the Sift walking skeleton: uv project scaffold with quality gates, the seven-subcommand Typer CLI surface (D-01), test isolation infrastructure, and the RED end-to-end test that defines the phase's happy path (`sift new` -> `sift ingest` -> `sift show events`). Plan 01-02 makes this test GREEN.

Purpose: Everything later in the phase (and project) builds on this scaffold. The failing e2e test is the MVP-mode contract for the whole walking skeleton.
Output: Installable `sift` package (`uv run sift --help` works), clean ruff/pyright, one deliberately failing e2e test, network-guarded test suite.
</objective>

## Phase Goal

**As a** support engineer with a directory of raw diagnostic logs, **I want to** turn it into a queryable case of canonical, deterministic events, **so that** nothing is dropped silently and every event can later be cited as evidence.

(Mapped mechanically from the ROADMAP Phase 1 Goal line: "A user can turn a directory of ordinary logs into a queryable case of canonical, deterministic events — nothing dropped silently".)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md
@SPEC.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Classes/functions: `sift.cli.app` (Typer instance), CLI command functions `new`, `ingest`, `show`, `analyze`, `report`, `eval_` (registered as `eval`), `doctor`; test `test_walking_skeleton_happy_path`; conftest fixtures `_isolate_dirs`, `_no_network`.
CLI surface: subcommands `new <case-name> --input <dir> [--adapter glob=name]...`, `ingest <case>`, `show <case> <what>`, `analyze`, `report`, `eval`, `doctor`.
Files: `pyproject.toml`, `.python-version`, `.gitignore`, `LICENSE`, `src/sift/__init__.py`, `src/sift/cli.py`, `tests/conftest.py`, `tests/test_cli.py`.
(The full-phase artifact inventory across all five plans is recorded in SKELETON.md.)

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — approve the six PyPI installs</name>
  <read_first>.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (section "Package Legitimacy Audit")</read_first>
  <action>Present the package legitimacy audit to the developer before any `uv add` runs. All six packages carry a formal [SUS] verdict from the legitimacy seam; every flag traces to the seam's missing PyPI download telemetry, not genuine risk — identities were verified against the PyPI JSON API and official source repos, and all six are named in SPEC.md §9's boring-tech allowlist.</action>
  <what-built>Nothing yet — this gates the dependency installs in Task 2. Packages to approve: typer 0.27.0, pydantic 2.13.4, zstandard 0.25.0 (runtime); pytest 9.1.1, ruff 0.15.22, pyright 1.1.411 (dev).</what-built>
  <how-to-verify>
    1. Open the audit table in 01-RESEARCH.md ("Package Legitimacy Audit"): every [SUS] reason is "unknown-downloads" (seam has no PyPI telemetry); typer additionally "too-new" because 0.27.0 released 2026-07-15 (package itself dates to 2019).
    2. Spot-check any package at https://pypi.org/project/typer/ , https://pypi.org/project/pydantic/ , https://pypi.org/project/zstandard/ , https://pypi.org/project/pytest/ , https://pypi.org/project/ruff/ , https://pypi.org/project/pyright/ — confirm official repos (fastapi/typer, pydantic/pydantic, indygreg/python-zstandard, pytest-dev/pytest, astral-sh/ruff, microsoft/pyright).
    3. All six appear in SPEC.md §9's allowed-technology list.
  </how-to-verify>
  <acceptance_criteria>Developer has explicitly approved the six packages (or substituted alternatives) before Task 2 executes any install command.</acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or name any package to exclude/replace</resume-signal>
</task>

<task type="auto">
  <name>Task 2: uv project scaffold with quality gates and 7-subcommand CLI surface</name>
  <files>pyproject.toml, .python-version, .gitignore, LICENSE, src/sift/__init__.py, src/sift/cli.py</files>
  <read_first>SPEC.md (§5.8 CLI design, §7 repository layout, §9 conventions), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Standard Stack, Pattern 1, Code Example 1), CLAUDE.md (commands, conventions)</read_first>
  <action>Create the uv src-layout project (D-01: Typer CLI). pyproject.toml: `[project]` name "sift", version "0.1.0", `requires-python = ">=3.12"`, `license = "Apache-2.0"`, dependencies typer, pydantic, zstandard (install via `uv add typer pydantic zstandard`); dev group pytest, ruff, pyright (`uv add --dev pytest ruff pyright`); `[project.scripts]` entry `sift = "sift.cli:app"`. Tool config: `[tool.pytest.ini_options]` with `testpaths = ["tests"]`; `[tool.ruff]` with `target-version = "py312"` and `src = ["src"]`; `[tool.ruff.lint]` select including E, F, I, UP, B and DTZ (flake8-datetimez — guards the naive-datetime pitfall from RESEARCH Pitfall 2); `[tool.pyright]` with `typeCheckingMode = "strict"`, `venvPath = "."`, `venv = ".venv"`, include src and tests (STACK.md recommends strict from day one; prefer targeted per-line ignores over lowering the mode). Add LICENSE with the full Apache-2.0 text and .gitignore covering .venv/, __pycache__/, *.pyc, .pytest_cache/, .ruff_cache/, dist/. src/sift/__init__.py defines `__version__ = "0.1.0"`. src/sift/cli.py: one `typer.Typer(no_args_is_help=True)` app named `app` with seven flat commands per SPEC §5.8 — `new(case_name: str, input: Annotated[str, typer.Option("--input")], adapter: Annotated[list[str], typer.Option("--adapter")] = [])`, `ingest(case: str)`, `show(case: str, what: str)`, `analyze`, `report`, `doctor`, and `eval_` registered via `@app.command("eval")` to avoid shadowing the builtin. Use the Annotated option style throughout (pyright-strict friendly). ALL seven bodies are stubs for now: print a British-English one-liner naming when the command arrives ("analyze arrives in Phase 4 (M4)"; new/ingest/show say "implemented later in this phase") and `raise typer.Exit(1)`. No business logic in cli.py yet.</action>
  <verify>
    <automated>uv sync && uv run sift --help && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run sift --help` exits 0; its output contains all seven command names: new, ingest, analyze, report, show, eval, doctor
    - `uv run sift analyze` exits 1 with a message containing "Phase"
    - pyproject.toml contains `sift = "sift.cli:app"` and `requires-python = ">=3.12"` and a `[tool.pyright]` table with `typeCheckingMode = "strict"`
    - `uv run ruff check` exits 0; `uv run pyright` exits 0
    - LICENSE file exists and contains "Apache License"
  </acceptance_criteria>
  <done>Clean checkout + `uv sync` yields a working `uv run sift --help` showing the complete CLI-01 subcommand surface; both quality gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Test isolation infrastructure and the RED walking-skeleton e2e test</name>
  <files>tests/conftest.py, tests/test_cli.py</files>
  <read_first>src/sift/cli.py (created in Task 2), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Security Domain — socket-guard fixture; Code Example 1 — CliRunner testing), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-04 case location)</read_first>
  <action>tests/conftest.py: two autouse fixtures. `_isolate_dirs(tmp_path, monkeypatch)` sets XDG_DATA_HOME and XDG_CONFIG_HOME to fresh subdirectories of tmp_path and deletes every SIFT_-prefixed env var, so no test can ever read or write the real home directory (D-04 paths derive from these). `_no_network(monkeypatch)` patches socket.socket.connect to raise RuntimeError with a message pointing at the zero-network-in-tests rule (RESEARCH Security Domain recommendation — makes CLAUDE.md's rule mechanical from day one; Phase 3 will relax it for loopback only). tests/test_cli.py: `test_walking_skeleton_happy_path` — build an input dir under tmp_path containing app.log with three ISO 8601 timestamped lines (mixed severities ERROR/WARN/INFO in the message text) plus one indented continuation line under the second entry; use typer.testing.CliRunner to invoke, in order: `new demo --input <dir>` (expect exit 0), `ingest demo` (expect exit 0, stdout contains "app.log" and a coverage percentage), `show demo events` (expect exit 0, stdout contains three 16-char lowercase-hex event IDs and the ERROR message text). This test MUST FAIL at the end of this plan — the command bodies are stubs exiting 1. It is the MVP walking-skeleton contract; plan 01-02 turns it green. Do not mark it xfail/skip; commit it red per the RED-commit convention. Note: conftest.py is owned by this plan — wave-3 plans add fixtures locally in their own test files, never here.</action>
  <verify>
    <automated>! uv run pytest tests/test_cli.py::test_walking_skeleton_happy_path -x -q && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli.py -x -q` collects and RUNS the test (assertion/exit-code failure, NOT a collection or import error) and exits non-zero
    - tests/conftest.py contains two fixtures with `autouse=True`
    - Running the suite with a deliberate socket.create_connection call in a scratch test raises RuntimeError (spot-check then remove the scratch test)
    - `uv run ruff check` and `uv run pyright` exit 0 (test files are typed)
  </acceptance_criteria>
  <done>RED e2e test committed; test isolation (XDG + network guard) active for every test in the suite from day one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> local venv | third-party code enters the build environment via `uv add` |
| developer shell -> CLI args | untrusted argument strings enter the process (handled in later plans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pypi installs (uv add) | high | mitigate | Blocking human legitimacy checkpoint (Task 1) for all six [SUS]-flagged packages; exact versions pinned in uv.lock |
| T-01-05 | Information Disclosure | any module (network egress) | high | mitigate | No HTTP dependency installed this phase; autouse socket-guard fixture makes network use fail every test run (Task 3) |
</threat_model>

<verification>
- `uv sync && uv run sift --help` from a clean checkout works (walking-skeleton "deployment" = local full-stack run command)
- `uv run ruff check` and `uv run pyright` exit 0
- `uv run pytest -q` exits non-zero with exactly the walking-skeleton e2e test failing (expected RED)
</verification>

<success_criteria>
- Seven subcommands visible in `sift --help` (CLI-01 surface)
- RED e2e test defines INGST-01's happy path for plan 01-02
- Test isolation (XDG redirect + socket guard) autouse for the whole suite
- Developer approved the package installs at the legitimacy checkpoint
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-01-SUMMARY.md` when done
</output>
