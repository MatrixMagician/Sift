---
phase: 01-skeleton-event-contract-genericlog-adapter
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/sift/models.py
  - src/sift/config.py
  - src/sift/store.py
  - src/sift/cli.py
  - src/sift/adapters/__init__.py
  - src/sift/adapters/base.py
  - src/sift/adapters/genericlog.py
  - tests/test_models.py
  - tests/test_store.py
  - tests/test_cli.py
autonomous: true
requirements: [INGST-01, INGST-02]
user_setup: []

must_haves:
  truths:
    - "`sift new <case> --input <dir>` then `sift ingest <case>` on the ISO fixture produces canonical Event records with deterministic 16-hex IDs; event_id('app.log', 12345) == 'f7fdcb4b3de90265' (INGST-01, golden value pinned by research)"
    - "A second `sift ingest` run on an unchanged case adds zero new events (INGST-02)"
    - "Deleting the case directory under $XDG_DATA_HOME/sift/cases/<name>/ deletes the case entirely (D-04)"
    - "query_events returns a deterministic order: ts (NULLs last), then source_file, then line_start — equal-ts events have a specified, stable output order"
    - "`sift show <case> events` prints the stored events (walking-skeleton read path)"
    - statement: "All event inserts and the coverage meta write for one ingest run occur inside a single SQLite transaction, so an interrupted ingest leaves either the complete result or nothing — never a partial state that breaks idempotent re-run"
      verification: backstop
  artifacts:
    - path: src/sift/models.py
      provides: "frozen Event dataclass (SPEC §5.1 verbatim) + canonical event_id()"
      contains: "class Event"
    - path: src/sift/adapters/base.py
      provides: "Adapter Protocol (SPEC §5.2 verbatim), open_bytes() decompression seam, ParseStats"
      contains: "class Adapter(Protocol)"
    - path: src/sift/store.py
      provides: "CaseStore with PRAGMA user_version migrations, INSERT OR IGNORE idempotency, deterministic query_events"
      contains: "class CaseStore"
    - path: src/sift/adapters/genericlog.py
      provides: "genericlog v0: ISO 8601 byte-offset streaming parse, continuation grouping, unknown fallback"
      contains: "class GenericLogAdapter"
    - path: tests/test_models.py
      provides: "event_id golden-value + frozen-contract tests"
      contains: "f7fdcb4b3de90265"
    - path: tests/test_store.py
      provides: "idempotency + schema + ordering tests"
      contains: "test_reingest_idempotent"
  key_links:
    - from: src/sift/adapters/genericlog.py
      to: src/sift/models.py
      via: "constructs Event via models.event_id(source_file, byte_offset)"
      pattern: "event_id\\("
    - from: src/sift/cli.py
      to: src/sift/store.py
      via: "ingest wraps insert_events + coverage meta in CaseStore.transaction()"
      pattern: "transaction\\(\\)"
    - from: src/sift/cli.py
      to: src/sift/adapters/__init__.py
      via: "ingest resolves adapter per file through the registry"
      pattern: "detect|REGISTRY"
  prohibitions:
    - statement: "event_id must never incorporate case_id, wall-clock time, randomness, or any input beyond (source_file, byte_offset) — determinism and citations depend on it"
      status: flagged-unverified
    - statement: "`sift ingest` must never silently skip a file in the input directory — every file appears in the per-file report, failed files as loud errors with a non-zero exit"
      status: flagged-unverified
---

<objective>
Freeze the two permanent contracts (Event schema, Adapter protocol) and build the thinnest end-to-end slice that turns plan 01-01's RED walking-skeleton test GREEN: minimal config (D-04 paths), SQLite store with `PRAGMA user_version = 1` (D-03), genericlog v0 (ISO 8601, UTF-8, continuation grouping per D-06), and real `new`/`ingest`/`show events` command bodies.

Purpose: After this plan a user can create a case, ingest a plain log, and query deterministic events — the walking skeleton stands. The Event schema and Adapter protocol frozen here are the contract every later phase builds on; treat them as the phase's highest-stakes review.
Output: Green e2e test, idempotent ingest, per-file coverage line printed, all quality gates clean.
</objective>

## Phase Goal

**As a** support engineer with a directory of raw diagnostic logs, **I want to** turn it into a queryable case of canonical, deterministic events, **so that** nothing is dropped silently and every event can later be cited as evidence.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md
@SPEC.md
@src/sift/cli.py
@tests/test_cli.py
</context>

## Artifacts this phase produces

Classes: `sift.models.Event` (frozen dataclass, 16 fields per SPEC §5.1), `sift.adapters.base.Adapter` (Protocol), `sift.adapters.base.ParseStats`, `sift.store.CaseStore`, `sift.adapters.genericlog.GenericLogAdapter`.
Functions: `sift.models.event_id(source_file, byte_offset)`, `sift.adapters.base.open_bytes(path)`, `sift.adapters.base.read_head(path)`, `sift.store.validate_case_name(name)`, `sift.store.case_db_path(data_dir, name)`, `sift.config.load_config(flag_overrides)`, `sift.adapters.get(name)`, `sift.adapters.detect(...)` (v0 stub), `sift.adapters.genericlog.to_utc(dt, override_tz)`.
Model/config types: `sift.config.SiftConfig` (Pydantic; `data_dir` field this plan).
Adapter instance attributes (contract, recorded in SKELETON.md): `GenericLogAdapter.input_root`, `GenericLogAdapter.tz_overrides`, `GenericLogAdapter.last_stats`.
Constants: `GZIP_MAGIC`, `ZSTD_MAGIC`, `SNIFF_BYTES = 65536` in base.py.
DB objects: tables `events`, `meta`; index `idx_events_ts`; `PRAGMA user_version = 1`. Meta keys: `input_dir`, `created_at`, `parse_coverage`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the contracts — Event schema, event_id, Adapter Protocol, decompression seam</name>
  <files>src/sift/models.py, src/sift/adapters/__init__.py, src/sift/adapters/base.py, tests/test_models.py</files>
  <read_first>SPEC.md (§5.1 Event schema — copy field-by-field verbatim; §5.2 Adapter Protocol — copy verbatim), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 3 canonical event_id, Code Examples 2 and 3, Pattern 2 registry)</read_first>
  <behavior>
    - event_id("app.log", 12345) returns exactly "f7fdcb4b3de90265" (research-verified golden value)
    - event_id output is always 16 lowercase hex chars; ("a1", 1) and ("a", 11) produce different IDs (NUL-separator disambiguation)
    - Event is immutable: assigning to a field raises dataclasses.FrozenInstanceError
    - open_bytes on a gzip-compressed file yields the identical byte stream as the plain file (magic bytes 1f 8b), likewise zstd (28 b5 2f fd, read_across_frames)
  </behavior>
  <action>models.py: the frozen Event dataclass exactly as SPEC §5.1 — fields in that order: event_id, case_id, ts (datetime | None), ts_confidence, source, source_file, line_start, line_end, severity, component, thread, session, message, attrs (dict[str, str]), raw. Module docstring marks the schema FROZEN after Phase 1 (breaking change policy per CONTEXT integration notes). `event_id(source_file: str, byte_offset: int) -> str` implements sha256 over source_file + NUL byte + str(byte_offset), utf-8 encoded, hexdigest truncated to 16 chars (RESEARCH Pattern 3 — the NUL separator prevents concatenation ambiguity); docstring records that source_file is the case-relative POSIX path (compressed file's own path for .gz/.zst per D-07) and byte_offset is 0-based on the DECOMPRESSED stream. adapters/base.py: the Adapter Protocol verbatim from SPEC §5.2 (`name: str`, `sniff(self, path: Path) -> float`, `parse(self, path: Path, case_id: str) -> Iterator[Event]`) — frozen after this phase to enable Phase 5 parallelism; `open_bytes(path) -> io.BufferedIOBase` per RESEARCH Code Example 3 (GZIP_MAGIC b"\x1f\x8b" -> gzip.open handles concatenated members; ZSTD_MAGIC b"\x28\xb5\x2f\xfd" -> zstandard ZstdDecompressor().stream_reader with read_across_frames=True wrapped in io.BufferedReader — Pitfall 3; else plain open); `read_head(path) -> bytes` returning the first SNIFF_BYTES (65536) of DECOMPRESSED content (Pitfall 4: never sniff compressed bytes); `ParseStats` dataclass with fields path (str), total_bytes (int), unknown_fallback_bytes (int), event_count (int), notes (list[str], for D-05 timezone-assumption disclosures), and a `coverage` property returning 1.0 when total_bytes == 0 else 1 - unknown_fallback_bytes/total_bytes (Pattern 7). adapters/__init__.py: `REGISTRY: dict[str, Adapter]` (insertion-ordered), registering one GenericLogAdapter instance under "genericlog"; `get(name)` raising KeyError with the list of known names; a v0 `detect` that returns the genericlog instance unconditionally — plan 01-04 implements the full sniff algorithm (INGST-03), keep the signature it will need: detect(path: Path, relpath: str, overrides: dict[str, str]) -> Adapter. Registry design honours the SPEC §5.2 self-containment rule: adapter #5 = new module + one registration line. tests/test_models.py covers the behaviours above.</action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_models.py asserts `event_id("app.log", 12345) == "f7fdcb4b3de90265"` and passes
    - models.py contains `class Event` decorated with `@dataclass(frozen=True)` and all 16 SPEC §5.1 fields
    - base.py contains `class Adapter(Protocol)` with the two SPEC §5.2 method signatures unchanged
    - base.py contains `read_across_frames=True`
    - ParseStats.coverage returns 1.0 for a zero-byte file (unit test)
  </acceptance_criteria>
  <done>Contracts exist, golden event_id pinned by test, registry seam ready for plan 01-04 and Phase 5 adapters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CaseStore — migrations, idempotent inserts, deterministic queries</name>
  <files>src/sift/store.py, tests/test_store.py</files>
  <read_first>src/sift/models.py (Event fields — column mapping), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 6 schema DDL and migration runner, Security Domain — path traversal and SQL injection rows), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-03, D-04)</read_first>
  <behavior>
    - Opening a fresh case.db applies migration 1: events + meta tables exist, PRAGMA user_version == 1
    - insert_events called twice with the same events returns N then 0; total row count stays N (INGST-02)
    - query_events returns rows ordered by ts (NULLs last), source_file, line_start
    - validate_case_name accepts "case-1.2_x", rejects "..", ".", "", "a/b", "../../evil" with ValueError
    - Storing an Event with ts=None round-trips as None; aware datetimes round-trip via ISO 8601 strings
  </behavior>
  <action>store.py owns ALL SQL in the codebase (single reviewable SQL owner — T-02-02 mitigation): parameterised `?` placeholders only, no f-string SQL. `validate_case_name(name)`: must match a strict allowlist of ASCII letters, digits, underscore, dot and hyphen, and must not be empty, "." or ".." (T-02-01 path traversal); `case_db_path(data_dir, name)` returns data_dir/"cases"/name/"case.db" and asserts the resolved path stays inside data_dir/"cases" (D-04). `CaseStore` class: constructor opens sqlite3 with PRAGMA journal_mode=WAL and synchronous=NORMAL, then runs the migration runner — read PRAGMA user_version, apply numbered migration functions greater than current inside a transaction, set the new version (RESEARCH Pattern 6, ~20 lines; Phase 1 has exactly migration 1). Migration 1 DDL: the events table and meta table exactly as RESEARCH Pattern 6 (events: event_id TEXT PRIMARY KEY, case_id, ts TEXT nullable, ts_confidence with CHECK in exact/inferred/missing, source, source_file, line_start, line_end, severity with CHECK in fatal/error/warn/info/debug/unknown, component, thread, session, message, attrs TEXT DEFAULT '{}', raw; index idx_events_ts on ts; meta: key TEXT PRIMARY KEY, value TEXT). Leave a one-line comment in store.py pointing at STORE-02 for the Phase 2 raw-compression migration (Open Question 1 disposition). API: `transaction()` contextmanager (BEGIN IMMEDIATE ... commit/rollback) so cli ingest can wrap all files' inserts plus the coverage meta write atomically (backstop truth); `insert_events(events)` using executemany INSERT OR IGNORE, returning the number of NEWLY inserted rows via the connection total_changes delta (INGST-02 signal for "0 new events"); `query_events()` ordered by ts IS NULL, ts, source_file, line_start; `get_meta(key)`/`set_meta(key, value)`. Timestamps: store `e.ts.isoformat()` strings, NEVER pass datetime objects to execute — the sqlite3 default adapter is deprecated on 3.12+ (RESEARCH verified); parse back with datetime.fromisoformat on read. attrs serialised with json.dumps(sort_keys=True). tests/test_store.py covers the behaviours above including `test_reingest_idempotent`.</action>
  <verify>
    <automated>uv run pytest tests/test_store.py -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_store.py::test_reingest_idempotent -x` exits 0
    - store.py contains `INSERT OR IGNORE` and `PRAGMA user_version`
    - A test asserts PRAGMA user_version == 1 after opening a fresh store
    - A test asserts validate_case_name raises ValueError for a parent-directory name
    - No SQL string in store.py is built with f-string interpolation of values (all data via `?` parameters)
  </acceptance_criteria>
  <done>Portable per-case SQLite store (D-03/D-04) with idempotent inserts and a migration runner Phase 2 extends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: genericlog v0 + CLI wiring — walking-skeleton test goes GREEN</name>
  <files>src/sift/config.py, src/sift/adapters/genericlog.py, src/sift/cli.py, tests/test_cli.py</files>
  <read_first>tests/test_cli.py (the RED test defines exact expected behaviour), src/sift/adapters/base.py, src/sift/models.py, src/sift/store.py (all from earlier tasks), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 4 byte-offset loop, Pattern 5 rows 1 and the grouping rule, Code Examples 4 and 5, Architectural Responsibility Map), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-05, D-06)</read_first>
  <behavior>
    - test_walking_skeleton_happy_path (plan 01-01) passes: new -> ingest -> show events end-to-end
    - An ISO-timestamped line starts an event; an indented/timestamp-less line appends to the preceding event (D-06)
    - A leading unparseable region becomes its own event with severity "unknown", ts None, ts_confidence "missing" (D-06)
    - A naive ISO timestamp yields aware UTC with ts_confidence "inferred"; an explicit-offset timestamp yields "exact" (D-05)
    - byte offsets are computed on the raw byte stream: the first event of a file has offset 0, subsequent offsets equal the cumulative byte length of prior lines including newlines
  </behavior>
  <action>config.py: Pydantic `SiftConfig` model with a `data_dir: Path` field (more fields arrive in plan 01-04 per D-08) and `load_config(flag_overrides: dict[str, object] | None = None) -> SiftConfig`: default data_dir = $XDG_DATA_HOME (fallback ~/.local/share) / "sift", overridden by SIFT_DATA_DIR env, then by non-None flag overrides (D-04; full flags>env>toml>defaults layering lands in plan 01-04). genericlog.py: `GenericLogAdapter` with name "genericlog"; instance configuration attributes set by the ingest orchestrator before parse (the Protocol signature is frozen, so per-run configuration travels on the instance — record in SKELETON.md): `input_root: Path | None`, `tz_overrides: dict[str, str]` (glob -> IANA name, D-05 mechanism — consumed for naive timestamps via first fnmatch match on the case-relative path), `last_stats: ParseStats | None` (set after each parse; how the orchestrator obtains coverage). v0 scope (plan 01-03 adds the full ladder/encodings/caps): `sniff` reads base.read_head, decodes utf-8 errors-replace, returns 0.1 if any line matches the ISO anchor regex else 0.0 (low-but-nonzero so genericlog never outcompetes a domain adapter — Pattern 2). `parse`: stream from base.open_bytes, split records on b"\n" at the BYTE level with a running offset counter — never .tell() on a text wrapper, never offsets from decoded text (Pitfall 1, the phase's cardinal sin); decode each record utf-8 errors-replace only after its offset is fixed; a line whose start matches the anchored ISO regex (four-digit year, dashes, T-or-space, HH:MM:SS, optional fractional/offset/Z — bounded candidate slice fed to datetime.fromisoformat, never an unanchored substring, Pitfall 5) starts a new event; other lines append to the current event (message grows, line_end advances, bytes accrue); `to_utc(dt, override_tz)` per RESEARCH Code Example 5 returning (aware-UTC datetime, confidence). severity: case-insensitive token scan after the timestamp — FATAL/CRIT/CRITICAL -> fatal, ERR/ERROR -> error, WARN/WARNING -> warn, INFO/NOTICE -> info, DEBUG/TRACE/FINE -> debug, no recognised token -> unknown (never fabricate a severity — RESEARCH A2, confirm at plan review). Event fields: source_file = path relative to input_root in POSIX form; event_id = models.event_id(source_file, first_byte_offset); line_start/line_end 1-based inclusive; message = record text with the matched timestamp prefix removed from the first line; raw = verbatim decoded record; component/thread/session None; attrs empty. Populate last_stats (unknown_fallback_bytes = bytes of events with ts None). cli.py: replace the new/ingest/show stub bodies. `new`: load_config, validate_case_name, create the case dir + CaseStore, set meta input_dir (absolute resolved path) and created_at (aware-UTC ISO string); if the input dir does not exist exit 1 with an error; if empty, warn but create (RESEARCH Open Q3). `ingest`: read meta input_dir, walk `sorted(input_dir.rglob("*"))` files only (deterministic order — Pitfall 6), per file: adapter = adapters.detect(path, relpath, overrides={}) (v0), set adapter.input_root and adapter.tz_overrides = {}, parse inside a try/except so one bad file never silently vanishes — record the error, print "ERROR <file>: <message>", continue with remaining files, final exit code 1 if any file failed; wrap ALL insert_events calls plus one set_meta("parse_coverage", json-object mapping source_file to total_bytes/unknown_fallback_bytes/event_count/coverage/notes) in a single store.transaction() (backstop truth); print per file: relative path, coverage percentage to one decimal, event count, new-event count (first-class output per CONTEXT specifics), and a summary line with total new events. `show`: what == "events" prints one row per query_events result — event_id, ts (or "-"), severity, source_file:line_start, first 120 chars of message; "clusters" and "hypotheses" exit 1 naming their arrival phase (2 and 4).</action>
  <verify>
    <automated>uv run pytest -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli.py::test_walking_skeleton_happy_path -x` exits 0 (GREEN)
    - Full suite `uv run pytest -q` exits 0; `uv run ruff check` and `uv run pyright` exit 0
    - A new test asserts: running ingest twice on the same case prints "0 new" (or equivalent) on the second run and the events row count is unchanged
    - `sift show <case> events` output contains 16-hex event IDs
    - ingest stdout contains the fixture filename and a coverage percentage
  </acceptance_criteria>
  <done>Walking skeleton complete: a user can create a case, ingest a plain ISO log into deterministic events, re-ingest idempotently, and read events back — from a clean checkout via uv run sift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> filesystem | case name and input dir come from the user and drive path construction |
| log file bytes -> parser/store | fully untrusted adversarial content enters parse and SQL layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | store.validate_case_name / case_db_path | high | mitigate | Strict allowlist on case names; resolve final path and assert containment inside data_dir/cases (Task 2) |
| T-02-02 | Tampering | store.py SQL layer | high | mitigate | store.py is the single SQL owner; parameterised `?` placeholders only, event/meta values never interpolated (Task 2) |
| T-02-03 | Denial of Service | genericlog parse memory | medium | mitigate | Streaming byte loop, no slurp; hard per-event caps land in plan 01-03 (D-06) |

Canon-referral breadcrumb (prohibition probe stage 2): path traversal and SQL injection are canon security items — handled here as T-02-01/T-02-02 mitigations, not minted as prohibitions.
</threat_model>

<verification>
- Full suite green: `uv run pytest && uv run ruff check && uv run pyright`
- Manual smoke (optional): `uv run sift new demo --input /tmp/somelogs && uv run sift ingest demo && uv run sift show demo events`
</verification>

<success_criteria>
- Walking-skeleton e2e test GREEN (INGST-01)
- Second ingest adds zero events, proven by test (INGST-02)
- Event schema and Adapter Protocol frozen, golden event_id pinned
- Case lives at $XDG_DATA_HOME/sift/cases/<name>/case.db (D-04), PRAGMA user_version = 1 (D-03)
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-02-SUMMARY.md` when done
</output>
