---
phase: 01-skeleton-event-contract-genericlog-adapter
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/sift/config.py
  - src/sift/cli.py
  - src/sift/adapters/__init__.py
  - tests/test_config.py
  - tests/test_adapters_detect.py
  - tests/test_cli.py
autonomous: true
requirements: [INGST-03, CLI-01]
user_setup: []

must_haves:
  truths:
    - "Config precedence resolves flags > SIFT_* env > ~/.config/sift/config.toml > defaults, proven by a precedence-matrix test (CLI-01, D-08)"
    - "Auto-detection runs every registered adapter's sniff on the first 64 KB of decompressed content; a unique highest confidence >= 0.5 wins; ties or all-below-threshold fall back to genericlog; `--adapter glob=name` overrides detection unconditionally (INGST-03)"
    - "Detection is deterministic: equal-confidence ties fall back to genericlog and registry iteration order is fixed insertion order"
    - "sniff operates on the first 65536 bytes of the DECOMPRESSED stream; all detection length/offset arithmetic is byte-based, never code points"
    - "An empty file detects as genericlog (fallback) and ingests to zero events without crashing; `sift ingest` on a case whose input dir is empty reports 0 files and exits 0; `sift new` warns on an empty input dir but creates the case"
    - "`sift show <case> events` strips control characters (other than newline and tab) from rendered message text — hostile log content cannot inject terminal escapes"
    - "Config timezones mapping (glob -> IANA name) reaches the adapter: ingest sets adapter.tz_overrides from the resolved config (D-05 mechanism exists in Phase 1)"
    - "An unknown adapter name in --adapter (or config) fails with an error listing the registered adapter names"
  artifacts:
    - path: src/sift/config.py
      provides: "SiftConfig (data_dir, timezones, adapters) + load_config layered precedence"
      contains: "timezones"
    - path: src/sift/adapters/__init__.py
      provides: "detect() with override-first + sniff-threshold algorithm; parse_adapter_overrides()"
      contains: "def detect"
    - path: tests/test_config.py
      provides: "precedence matrix + tz validation tests"
      contains: "def test_"
    - path: tests/test_adapters_detect.py
      provides: "threshold/tie/fallback/override detection tests"
      contains: "def test_"
  key_links:
    - from: src/sift/cli.py
      to: src/sift/config.py
      via: "commands call load_config with flag overrides"
      pattern: "load_config\\("
    - from: src/sift/cli.py
      to: src/sift/adapters/__init__.py
      via: "ingest resolves per-file adapter through detect() with merged overrides"
      pattern: "detect\\("
    - from: src/sift/config.py
      to: "~/.config/sift/config.toml"
      via: "tomllib.loads of the XDG config file when present"
      pattern: "tomllib"
  prohibitions: []
---

<objective>
Complete the CLI's contract surface: full D-08 config precedence (flags > `SIFT_*` env > config.toml > defaults) with a validated typed settings object, the INGST-03 sniff-based auto-detection algorithm with `--adapter glob=name` overrides, and CLI hardening (terminal-escape sanitisation, empty-input semantics, config-to-adapter timezone wiring).

Purpose: CLI-01 and INGST-03 are the phase's user-facing contract; the timezones mechanism must exist now so Phase 5's multi-node dsserrors cases can use it unchanged.
Output: Precedence matrix proven by tests; detection algorithm proven with a synthetic high-confidence adapter; hardened show/ingest output.
</objective>

## Phase Goal

**As a** support engineer with a directory of raw diagnostic logs, **I want to** turn it into a queryable case of canonical, deterministic events, **so that** nothing is dropped silently and every event can later be cited as evidence.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md
@SPEC.md
@src/sift/config.py
@src/sift/cli.py
@src/sift/adapters/__init__.py
</context>

## Artifacts this phase produces

Functions/types added: `sift.config.SiftConfig` gains `timezones: dict[str, str]` and `adapters: dict[str, str]` fields with ZoneInfo validation; `sift.config.load_config` gains toml + env layering; `sift.adapters.detect(path, relpath, overrides)` (real algorithm), `sift.adapters.parse_adapter_overrides(specs)`; `sift.cli._sanitise(text)`; `--data-dir` option on new/ingest/show commands.
Config keys: `data_dir`, `timezones` (glob -> IANA), `adapters` (glob -> adapter name); env var `SIFT_DATA_DIR`; config file `$XDG_CONFIG_HOME/sift/config.toml`.
Tests: `tests/test_config.py`, `tests/test_adapters_detect.py`, CLI hardening tests in `tests/test_cli.py`.

## Flagged Assumptions (edge probe: unresolved items surfaced, not dropped)

- **CLI-01 (unclassified probe):** the Phase-1 environment-variable surface is `SIFT_DATA_DIR` only — the `timezones`/`adapters` mappings are expressible via TOML and flags, which satisfies D-05/D-08's "mechanism must exist"; a generalised `SIFT_*` -> key mapping arrives when later phases add scalar config keys (RESEARCH Pattern 8 notes the env form can be deferred). Precedence is still proven across all four layers using data_dir.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full config precedence — flags > SIFT_* env > config.toml > defaults</name>
  <files>src/sift/config.py, tests/test_config.py</files>
  <read_first>src/sift/config.py (v0 from plan 01-02 — extend load_config, keep its signature), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 8, Code Example 4), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-08, D-04)</read_first>
  <behavior>
    - No config anywhere: data_dir == $XDG_DATA_HOME/sift
    - config.toml sets data_dir: toml value wins over default
    - SIFT_DATA_DIR set: env wins over toml
    - flag_overrides data_dir passed: flag wins over env
    - timezones = { "node1/*" = "Europe/Berlin" } in toml round-trips into SiftConfig.timezones
    - timezones value "Not/AZone" raises a validation error naming the bad zone
  </behavior>
  <action>Extend SiftConfig (plain Pydantic, D-08 — no pydantic-settings) with `timezones: dict[str, str] = {}` (glob -> IANA zone, the D-05 override mechanism) and `adapters: dict[str, str] = {}` (glob -> adapter name, same semantics as --adapter); add a field validator constructing zoneinfo.ZoneInfo for every timezones value so bad zone names fail at config time (RESEARCH: ZoneInfo validates for free). load_config layering per Code Example 4: start from a defaults dict (data_dir = XDG_DATA_HOME fallback ~/.local/share, joined with "sift"), merge config.toml via tomllib.loads if $XDG_CONFIG_HOME/sift/config.toml exists (fallback ~/.config), merge SIFT_DATA_DIR env, merge non-None flag_overrides last (later wins per key), then a single SiftConfig.model_validate. tests/test_config.py: the full precedence matrix from the behaviour block (monkeypatch env; write toml under the tmp XDG_CONFIG_HOME provided by the conftest isolation fixture), invalid-zone rejection, missing-toml tolerated, adapters mapping round-trip. Do not modify tests/conftest.py (owned by plan 01-01; plan 01-03 runs concurrently this wave).</action>
  <verify>
    <automated>uv run pytest tests/test_config.py -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py -x` exits 0 with at least 6 tests (four precedence layers + tz validation + adapters mapping)
    - config.py contains a tomllib import and no pydantic_settings import
    - A test proves each adjacent precedence pair (default<toml, toml<env, env<flag)
  </acceptance_criteria>
  <done>One resolved, typed settings object with the exact CLAUDE.md precedence chain, validated timezones/adapters mappings (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sniff-based auto-detection with threshold, fallback, and --adapter override</name>
  <files>src/sift/adapters/__init__.py, tests/test_adapters_detect.py</files>
  <read_first>src/sift/adapters/__init__.py (v0 detect stub — keep the signature), src/sift/adapters/base.py (read_head, Adapter Protocol), SPEC.md (§5.2 auto-detection rules), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 2 detection algorithm, Pitfall 8)</read_first>
  <behavior>
    - A registered dummy adapter whose sniff returns 0.9 wins detection for its file
    - Two adapters tied at the same max confidence: genericlog wins (tie rule)
    - All confidences below 0.5: genericlog wins (threshold rule)
    - Override {"*.special": "dummy"} beats a losing sniff score unconditionally; first matching glob in insertion order applies
    - parse_adapter_overrides(["*.log=genericlog"]) splits on the FIRST equals sign only; a glob containing an equals sign survives; an unknown adapter name raises ValueError listing registered names
    - An empty file yields genericlog (fallback) without error
  </behavior>
  <action>Implement `detect(path: Path, relpath: str, overrides: dict[str, str]) -> Adapter` per RESEARCH Pattern 2 (INGST-03): (1) first fnmatch match of relpath against overrides in insertion order returns that adapter unconditionally; unknown name -> ValueError naming the registered adapters; (2) otherwise call every REGISTRY adapter's sniff(path) — each adapter reads its own head via base.read_head, so detection always sees decompressed bytes (Pitfall 4) and is capped at SNIFF_BYTES (65536); (3) the unique maximum confidence wins when >= 0.5; (4) a tie at the maximum, or all scores below 0.5, falls back to REGISTRY["genericlog"]. Iteration is insertion order over the dict — document that determinism in the docstring. `parse_adapter_overrides(specs: list[str]) -> dict[str, str]`: split each spec on the first equals sign only (Pitfall 8), validate the name against REGISTRY, raise ValueError with the sorted known-name list on miss; keep this module typer-free (cli converts ValueError to a Typer error in Task 3). tests/test_adapters_detect.py: a local DummyAdapter class (sniff returns a fixed score, parse yields nothing) registered into REGISTRY by a fixture that restores the registry afterwards; cover every behaviour above.</action>
  <verify>
    <automated>uv run pytest tests/test_adapters_detect.py -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_adapters_detect.py -x` exits 0 with at least 6 tests
    - adapters/__init__.py contains `def detect` and `def parse_adapter_overrides`
    - A test proves the tie-at-max case returns genericlog
    - A test proves a glob containing an equals sign parses correctly
  </acceptance_criteria>
  <done>INGST-03 detection algorithm complete and deterministic; adding adapter #5 in Phase 5 remains one module + one registration line.</done>
</task>

<task type="auto">
  <name>Task 3: CLI wiring and hardening — config consumption, tz wiring, sanitised output, empty-input semantics</name>
  <files>src/sift/cli.py, tests/test_cli.py</files>
  <read_first>src/sift/cli.py (current new/ingest/show bodies from plan 01-02), src/sift/config.py and src/sift/adapters/__init__.py (Tasks 1-2 of this plan), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Security Domain — terminal escape row; Open Question 3)</read_first>
  <action>Wire the resolved config through every implemented command (D-08): add an `--data-dir` option (Annotated, default None) to new/ingest/show feeding flag_overrides into load_config — this makes the flags layer of CLI-01 observable end-to-end. ingest: merge adapter overrides as dict(config.adapters) updated by parse_adapter_overrides of any --adapter values stored at `new` time in meta (persist the raw --adapter list from `new` into a meta key named adapter_overrides so ingest reuses it; flags win over config); convert ValueError from parse_adapter_overrides into a Typer BadParameter-style failure listing valid names; set adapter.tz_overrides = config.timezones before parse (D-05 wiring — test by asserting the registry genericlog instance carries the mapping after an ingest run, and end-to-end: a naive-timestamp fixture under a glob-matched subdir converts to the expected UTC instant since v0 to_utc already consumes tz_overrides). show: route ALL user-content output (message text) through a `_sanitise(text)` helper that removes control characters below 0x20 except newline and tab, plus 0x7f (T-04-01 — hostile log bytes cannot inject terminal escapes; sanitise at render only, stored raw/message stay verbatim for citation fidelity). Empty-input semantics (RESEARCH Open Q3): `sift new` on an existing-but-empty input dir prints a warning and creates the case, exit 0; missing dir exits 1; `sift ingest` with zero files prints a zero-files line, exit 0. tests in test_cli.py: data-dir precedence through the CLI (flag beats SIFT_DATA_DIR env), an ESC byte (0x1b) planted in a fixture log line does not appear in show output while the message text does, empty-dir new/ingest behaviours, unknown adapter name in --adapter fails with the registered-name list, tz wiring end-to-end assertion.</action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_config.py tests/test_adapters_detect.py -x -q && uv run pytest -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli.py -x` exits 0 including a test asserting the 0x1b byte is absent from show output
    - A test proves `--data-dir` beats SIFT_DATA_DIR through CliRunner
    - A test proves ingest on an empty input dir exits 0 and reports zero files
    - A test proves config.timezones reaches the adapter and changes a naive timestamp's UTC value end-to-end
    - Full suite green
  </acceptance_criteria>
  <done>CLI-01 complete: seven subcommands, proven precedence chain, deterministic detection with overrides, hardened output, sane empty-input behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored log content -> terminal | untrusted bytes are echoed to the user's terminal by `sift show` |
| config.toml / env -> process | user-writable config drives paths and timezone lookups |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering/Spoofing | cli show rendering | medium | mitigate | _sanitise strips control chars (except newline/tab) at render time; stored text stays verbatim (Task 3) |
| T-04-02 | Tampering | config load (bad tz names, malformed toml) | low | mitigate | Pydantic validation + ZoneInfo construction at config time; tomllib parse errors surface as clear config errors, never silent defaults (Task 1) |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py tests/test_adapters_detect.py tests/test_cli.py -q` green
- Full gate: `uv run pytest && uv run ruff check && uv run pyright`
</verification>

<success_criteria>
- INGST-03: threshold, tie, fallback, and override rules all test-proven
- CLI-01: precedence matrix proven end-to-end through the CLI
- D-05 timezone mechanism wired config -> adapter and observable in event timestamps
- show output safe against terminal escape injection
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-04-SUMMARY.md` when done
</output>
