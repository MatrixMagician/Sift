---
phase: 09-mcm-episode-detection-memory-breakdown
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/sift/pipeline/mcm.py
autonomous: true
requirements: [MCM-01, MCM-02]
user_setup: []

must_haves:
  truths:
    - "detect_episodes(store.query_events()) returns exactly 1 episode on the Hartford slice, non-interactively (no prompts) — crit #1, MCM-01"
    - "the episode's lifecycle_signals include memory-status-low, emergency-offload-start, and emergency-offload-complete, each referencing a real event_id within the episode span — crit #2, D-02"
    - "the denial-time MemoryBreakdown parses Format A: cube_caches=27923, working_set=268502, mmf=365, other_memory=101682, iserver_virtual=410325 (MB-native), verbatim map has all 23 labels incl. the physical/virtual split — crit #3, MCM-02"
    - "MCM Settings parse from the nearest in-span Info Dump: SmartHeap Cache Releasable=true present AND Memory Reserve present (0Bytes not dropped) — crit #3, MCM-02"
    - "re-running detect_episodes on identical events yields byte-identical model_dump_json — crit #5, D-05"
  artifacts:
    - "src/sift/pipeline/mcm.py exposing detect_episodes(events: list[Event]) -> list[McmEpisode], plus frozen extra=forbid models McmEpisode, MemoryBreakdown, LifecycleSignal and the ported marker/regex constants"
  key_links:
    - "detect_episodes consumes store.query_events() output directly; NO re-sort — the SQL ORDER BY ts IS NULL, ts, source_file, line_start already is the D-06 order (store.py:567)"
    - "every episode boundary and lifecycle signal maps to an event_id re-parsed from event.raw (D-01) — the load-bearing citation provenance for Phase 11 (cited subset-of prompted subset-of store)"
  backstop:
    - statement: "a log ending with no State=normal (Hartford) -> open_truncated=True, recovery=None, episode not dropped, no exception raised — crit #4, D-07"
      verification: backstop
    - statement: "a missing lifecycle signal or absent breakdown is recorded absent (None / empty), never fabricated, never crashes the analyser — D-03"
      verification: backstop
    - statement: "a denial event with an empty detail block whose neighbouring event is a different source_file sets fragmented=True (not silently merged) — D-06"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT modify the frozen Event dataclass in src/sift/models.py"
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "MUST NOT add a store table, migration, or PRAGMA user_version bump (D-05 — pure function over stored events)"
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "MUST NOT emit any LLM call or any network/subprocess/file-write call from the analyser (only llm/ talks HTTP)"
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "MUST NOT enrich or modify src/sift/adapters/dsserrors.py (D-01 — re-parse event.raw instead)"
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "MUST NOT add any CLI surface / sift mcm command (that is Phase 10) and MUST NOT port prompt_event/prompt_window/window/attribution/report logic"
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "MUST NOT use set() or any unordered iteration in ordered output — determinism (crit #5); insertion-ordered dicts + the SQL order only"
      status: flagged-unverified
      verification: flagged-unverified
---

<objective>
Implement the deterministic, no-LLM MCM analyser `src/sift/pipeline/mcm.py` and
turn the 09-01 golden suite GREEN. Port the reference `analyze_dss8.py` core
(`prescan`, `parse_detail_block`, `parse_abbrev_block`, `_get`, the regex/marker
constants) to read stored `Event` rows via `event.raw` (D-01), and EXTEND it with
lifecycle-signal capture (D-02), open/truncated handling (D-07), the in-span
Info-Dump association fix (Q1), the `Memory Reserve` regex widening, and the
fragmentation guard (D-06).

Purpose: this is the deterministic numeric core of milestone v1.1 — figures are
computed here, never authored by a model. Every signal keeps its owning
`event_id` so Phase 11 can cite it (cited subset-of prompted subset-of store).

Output: `src/sift/pipeline/mcm.py` with `detect_episodes(events) -> list[McmEpisode]`
and the typed models `McmEpisode`, `MemoryBreakdown`, `LifecycleSignal`; the full
suite (`ruff check`, `pyright`, `pytest`) green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcm-episode-detection-memory-breakdown/09-CONTEXT.md
@.planning/phases/09-mcm-episode-detection-memory-breakdown/09-RESEARCH.md
@.planning/phases/09-mcm-episode-detection-memory-breakdown/09-PATTERNS.md

# Primary analog (mirror its purity contract, shape, UTC helpers, tie-break)
@src/sift/pipeline/salience.py
# Model + Event contract (read-only — frozen Event, extra=forbid convention)
@src/sift/models.py
# Vendored reference to port from (created in 09-01)
@docs/reference/analyze_dss8.py
# Golden contract this plan must satisfy (created in 09-01)
@tests/test_mcm.py

# Read-only alignment references (do NOT modify):
#   src/sift/store.py            (query_events at :563; ORDER BY at :567; get_events_by_ids :590)
#   src/sift/adapters/dsserrors.py (why Info Dumps are NOT pre-grouped: _MCM_START :69,:270; 256-line/64KB caps :47-48; no-ReDoS discipline :50)
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

NEW symbols in `src/sift/pipeline/mcm.py`:
- Public function: `detect_episodes(events: list[Event]) -> list[McmEpisode]`.
- Typed models (frozen, `extra="forbid"`): `McmEpisode`, `MemoryBreakdown`, `LifecycleSignal`.
- Ported constants (verbatim from docs/reference/analyze_dss8.py:38-66, with provenance comment): `TIMESTAMP_RE`, `SID_RE`, `OID_RE`, `SIZE_RE`, `SOURCE_RE`, `AVAIL_MCM_RE`, `HWM_RE`, `DENIAL_MARKER`, `SUCCESS_MARKER`, `NORMAL_MARKER`, `CURRENT_INFO_MARKER`, `MCM_SETTINGS_MARKER`, `DETAIL_LINE_RE`, a WIDENED `ABBREV_LINE_RE` (adds `Bytes`), `UNIT_TO_MB`, `to_mb`.
- New pinned lifecycle-marker constants (D-02, RESEARCH § Lifecycle Markers): the memory-status-low, emergency-offload-start, and emergency-offload-complete anchor substrings, plus the `MemoryStatusHandler()` family anchor.
- Internal ported helpers: `prescan`-equivalent over the event-id-carrying line stream, `parse_detail_block`, `parse_abbrev_block`, `_get`.

No new store schema (D-05), no adapter change (D-01), no CLI (Phase 10), no LLM feed (Phase 11).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port constants + typed models (MemoryBreakdown / LifecycleSignal / McmEpisode) with the Bytes regex fix</name>
  <files>src/sift/pipeline/mcm.py</files>
  <read_first>
    - docs/reference/analyze_dss8.py:38-66 (constants), :247-286 (parse_detail_block/parse_abbrev_block), :500-504 (`_get`)
    - src/sift/models.py:61-73 (Hypothesis — ConfigDict(extra="forbid") convention) and :17-35 (frozen Event fields the models reference)
    - src/sift/pipeline/salience.py:1-41 (module docstring / purity stance + local frozen-vocabulary constant pattern)
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-RESEARCH.md § "Pattern 2: Verbatim map + typed accessors (D-04)", § "Memory Breakdown" (23 labels + the ABBREV_LINE_RE Bytes gap), § "Lifecycle Markers (D-02)"
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-PATTERNS.md § "Typed-model analog"
  </read_first>
  <behavior>
    - MemoryBreakdown holds raw_map (label -> (value_mb, unit)) verbatim, current_memory_info, and mcm_settings; typed accessors resolve via a fuzzy `_get(substr)` substring lookup (ported from reference:500) returning cube_caches_mb=27923, working_set_mb=268502, mmf_mb=365, other_memory_mb=101682, iserver_virtual_mb=410325 on the Hartford detail block; missing label -> None (D-03).
    - The widened ABBREV_LINE_RE parses `Memory Reserve = 0 (0Bytes)` without dropping it (unit alternation includes Bytes; robust to a missing space) while still matching the existing (TB|GB|MB|KB) and unlimited/true/false forms.
    - Models are frozen + extra="forbid" and serialise deterministically via model_dump_json (no set(), insertion-ordered dicts, tuples).
  </behavior>
  <action>
    Create src/sift/pipeline/mcm.py with `from __future__ import annotations` and a
    module docstring mirroring salience.py's purity stance (typer-free, print-free,
    SQL-free, no I/O, no LLM; cite MCM-01/MCM-02, milestone v1.1, SPEC-less). Port
    the KEEP-list constants verbatim from docs/reference/analyze_dss8.py:38-66 as
    module constants with a comment citing the provenance line range — EXCEPT
    ABBREV_LINE_RE, which must be widened so the optional unit group accepts Bytes
    and tolerates a missing space before the unit (per RESEARCH § Memory Breakdown
    regex fix). Keep every pattern `^`-anchored with its required terminator (no
    ReDoS — mirror dsserrors.py:50).

    Add the pinned lifecycle-marker anchor constants from RESEARCH § Lifecycle
    Markers (D-02): the `Memory status changes to low` MCM-side banner, the
    `MsiSessionManager::MemoryStatusHandler()` family anchor, and the tail
    classifiers `Initiating emergency memory offload for Working Set` (offload
    start) and `Working set emergency offload completed` (offload complete). Quote
    them exactly; classify signal kind by the tail text.

    Define three Pydantic models with `model_config = ConfigDict(frozen=True,
    extra="forbid")` (match the Hypothesis convention, per RESEARCH Q3 for Phase-11
    serialisation): LifecycleSignal (kind, event_id, ts, text), MemoryBreakdown
    (raw_map, current_memory_info, mcm_settings, a private `_get(substr)` fuzzy
    lookup ported from reference:500, and typed read-only accessors
    cube_caches_mb / working_set_mb / mmf_mb / cube_growth_index_mb /
    other_memory_mb / iserver_virtual_mb / physical_total / iserver_physical_mb /
    other_processes_mb / smartheap_unused_pool_mb — each returning float | None via
    _get so label drift and absence are tolerated), and McmEpisode (denial_event_id,
    denial_ts, recovery_event_id, recovery kind or None, open_truncated, fragmented,
    hwm_bytes, avail_timeline, lifecycle_signals, breakdown | None). Exact field set
    is executor discretion within D-04/D-05, provided the golden accessors and the
    verbatim raw_map survive. Add the ported `parse_detail_block`, `parse_abbrev_block`,
    and `_get` as module functions now (they are pure text->dict transforms the
    Task-2 detect_episodes will call). Do NOT add detect_episodes yet.
  </action>
  <acceptance_criteria>
    - `src/sift/pipeline/mcm.py` defines `class MemoryBreakdown`, `class McmEpisode`, `class LifecycleSignal`, and functions `parse_detail_block`, `parse_abbrev_block`, `_get`.
    - `uv run python -c "import re; from sift.pipeline.mcm import ABBREV_LINE_RE as R; assert R.match('Memory Reserve = 0 (0Bytes)'), 'Bytes unit must parse'"` exits 0.
    - `uv run python -c "from sift.pipeline.mcm import MemoryBreakdown as M; b=M(raw_map={'Cube Caches In Memory':(27923.0,'MB')}, current_memory_info={}, mcm_settings={}); assert b.cube_caches_mb==27923.0 and b.mmf_mb is None"` exits 0 (fuzzy accessor + absence tolerance).
    - `uv run pyright src/sift/pipeline/mcm.py` reports 0 errors; `uv run ruff check src/sift/pipeline/mcm.py` clean.
    - No `set(` literal and no `import httpx`/`requests`/`socket`/`subprocess` in the module (determinism + zero-egress).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from sift.pipeline.mcm import ABBREV_LINE_RE as R, MemoryBreakdown as M; assert R.match('Memory Reserve = 0 (0Bytes)'); b=M(raw_map={'Cube Caches In Memory':(27923.0,'MB')},current_memory_info={},mcm_settings={}); assert b.cube_caches_mb==27923.0 and b.mmf_mb is None" && uv run pyright src/sift/pipeline/mcm.py && uv run ruff check src/sift/pipeline/mcm.py</automated>
  </verify>
  <done>Constants (with widened ABBREV_LINE_RE), lifecycle anchors, the three frozen models with fuzzy accessors, and the block parsers exist and type-check; the physical/virtual accessors and the Bytes fix are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port prescan over the event-id line stream + lifecycle capture + in-span Info-Dump association + detect_episodes (suite GREEN)</name>
  <files>src/sift/pipeline/mcm.py</files>
  <read_first>
    - docs/reference/analyze_dss8.py:112-238 (prescan: same-burst collapse, implicit recovery, EOF-open) — note lineno-based boundaries to re-express as stream indices carrying event_id
    - src/sift/store.py:563-568 (query_events + the ORDER BY that already is the D-06 order — do NOT re-sort)
    - src/sift/pipeline/salience.py:58-77 (_as_utc/_parse UTC helpers), :215 (deterministic tie-break sort discipline)
    - src/sift/adapters/dsserrors.py:69,:270 (why Info Dumps are NOT pre-grouped — scan event.raw for markers), :47-50 (256-line/64KB caps + no-ReDoS)
    - tests/test_mcm.py (the 8 golden assertions this task must satisfy)
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-RESEARCH.md § "Architecture Patterns" (data-flow diagram, _line_stream), § "Lifecycle timing nuance" (do NOT close on AvailableMCM climb), § "Open Questions" Q1 (in-span Info-Dump rule) and Q2 (close only on State=normal or a following denial)
  </read_first>
  <behavior>
    - detect_episodes filters events to source == "dsserrors", reconstructs a (line_text, event_id, source_file) stream by splitting each event.raw on newlines in the already-queried order (no re-sort), and runs the ported prescan over it — exactly 1 episode on the Hartford slice.
    - Episode boundaries carry event_id: denial_event_id is the event whose raw holds the denial banner; recovery is None on the fixture (no State=normal) and open_truncated is True (D-07).
    - Lifecycle scan tags memory-status-low, emergency-offload-start, emergency-offload-complete signals within the span, each referencing the owning event_id; absent signals are simply not present (D-03), never fabricated; AvailableMCM climb is recorded as an annotation, never as an episode close (Q2 / crit #4).
    - The breakdown is parsed from the denial event's raw (Format A) and the MCM Settings / Current Memory Info from the LAST Info Dump block at or before the denial banner within the episode span (Q1); if none in span, mcm_settings is empty (D-03).
    - fragmented=True when a denial event's detail block is empty and its neighbouring event has a different source_file (D-06). Two detect_episodes runs on the same events are byte-identical (D-05).
  </behavior>
  <action>
    Add detect_episodes(events: list[Event]) -> list[McmEpisode] to mcm.py,
    following the salience.rank_clusters shape: filter `e.source == "dsserrors"`,
    build the line stream `[(line, e.event_id, e.source_file) for e in dss for line
    in e.raw.split(chr(10))]` in the incoming order — do NOT re-sort (store.py:567
    already gives the D-06 order). Port prescan's state machine from
    docs/reference/analyze_dss8.py:112-238, re-expressing its `lineno` bookkeeping
    as stream indices while carrying the event_id of each denial/recovery/succeeded
    line so denial_event_id and avail_timeline entries reference real event_ids
    (D-01). Preserve the reference semantics EXACTLY: same-burst collapse within one
    second, implicit-recovery close ONLY on a following denial banner after
    intervening successes, and the open-episode-at-EOF branch -> set
    open_truncated=True, recovery_event_id=None (D-07). Do NOT treat an AvailableMCM
    climb-back or resumed successes as a close (RESEARCH § timing nuance / Q2).

    Extend prescan with the lifecycle scan (D-02): within each episode's span, scan
    the stream for the pinned anchors and emit LifecycleSignal(kind, event_id, ts,
    text) for memory-status-low, emergency-offload-start, and emergency-offload-
    complete (kind classified by tail text), plus record the AvailableMCM climb as
    a lifecycle annotation. Use event.ts for signal timestamps (already parsed) —
    do not re-match TIMESTAMP_RE across event boundaries.

    For each episode, parse the Format-A detail block from the denial event's raw
    via parse_detail_block, and associate MCM Settings / Current Memory Info by
    scanning BACKWARD from the denial banner for the last `Current Memory Info:` /
    `MCM Settings:` markers within the episode span, parsing them with
    parse_abbrev_block (Q1 rule). Build MemoryBreakdown; if the detail block is
    empty and the neighbouring event has a different source_file, set
    fragmented=True (D-06). Wrap parse in tolerate-absence guards so a garbled or
    absent block yields breakdown=None rather than raising (D-03). Keep the
    reference 60-line block cap. Return the list in stream order (deterministic; no
    set()). Run `uv run pytest tests/test_mcm.py` and iterate until all 8 pass,
    then `uv run pytest` + `uv run ruff check` + `uv run pyright` all green.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_mcm.py -x` exits 0 — all 8 golden assertions pass (single episode, lifecycle signals with event_ids, absent-signal tolerance, breakdown values, MCM settings incl. Memory Reserve, open_truncated, byte-identical determinism, fragmented flag).
    - `src/sift/pipeline/mcm.py` contains `def detect_episodes(` and it takes `events: list[Event]`.
    - re-running detect_episodes on the fixture events yields an identical `[e.model_dump_json() for e in episodes]` list (determinism assertion in test_determinism_byte_identical passes).
    - `uv run pytest` (full suite) exits 0; `uv run ruff check` clean; `uv run pyright` 0 errors.
    - `git grep -n 'CREATE TABLE\|user_version\|ALTER TABLE' src/sift/pipeline/mcm.py` returns nothing (no schema — D-05); no `sift mcm` CLI registration added.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mcm.py -x && uv run pytest -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <done>detect_episodes ports prescan over the event-id line stream with lifecycle capture, in-span Info-Dump association, open/truncated + fragmentation handling; the 8 golden tests and the full suite are green with ruff + pyright clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored dsserrors `event.raw` (originates from untrusted production log text) -> pipeline/mcm.py regex parser | Malformed/adversarial memory-dump lines and integer figures cross here; the analyser must parse defensively and never hang or crash. |
| pipeline/mcm.py -> caller (Phase 10/11, tests) | Pure return of typed models; no side effects, no I/O, no egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | DETAIL_LINE_RE `(.+?)` / ABBREV_LINE_RE on malformed dump lines | medium | mitigate | Keep every ported pattern `^`-anchored with a required terminator and non-nested quantifiers (mirror dsserrors.py:50); retain the reference 60-line block cap so a never-terminated block cannot spin. Input is already bounded by the adapter's 256-line/64 KB event caps. |
| T-09-02-02 | Denial of Service (availability) | parse_detail_block / parse_abbrev_block on absent or garbled breakdown | medium | mitigate | D-03/D-07 tolerate-absence: return breakdown=None / open_truncated=True and record signals absent; never raise. Covered by test_absent_signals_tolerated + test_open_truncated_episode. |
| T-09-02-03 | Tampering | integer parse of attacker-influenceable AvailableMCM / HWM / Size / memory figures | low | mitigate | Values are captured via `\d+`-anchored groups into Python ints (arbitrary precision — no overflow) and only recorded/compared, never used for allocation or indexing; a non-matching value is simply absent (D-03). |
| T-09-02-04 | Information disclosure | analyser egress | low | accept | Module makes zero network/subprocess/file-write calls (only llm/ talks HTTP); tests run under the conftest network guard. No egress path exists. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages — pure stdlib `re` + already-vendored Pydantic (RESEARCH Package Legitimacy Audit N/A). No supply-chain surface. |

No `high`-severity threats — nothing blocks at ASVS L1 / block_on=high. V5 (input validation) is the only applicable ASVS category and is mitigated by anchored regexes + the block cap.
</threat_model>

<verification>
- `uv run pytest tests/test_mcm.py -x` — all 8 golden assertions green (covers MCM-01, MCM-02, criteria 1-5, D-03/D-06/D-07).
- `uv run pytest` (full suite), `uv run ruff check`, `uv run pyright` — all clean (phase gate).
- Determinism: test_determinism_byte_identical proves byte-identical model_dump_json across two runs (crit #5, D-05).
- No schema / no CLI / no egress: grep guards in Task 2 acceptance criteria confirm D-05 and the prohibition list.
</verification>

<success_criteria>
`src/sift/pipeline/mcm.py` deterministically detects the single Hartford denial
episode with its full lifecycle signals (each event_id-cited), parses the
denial-time Format-A breakdown and the in-span MCM Settings (Memory Reserve
included), reports the episode open/truncated with recovery None, and is
byte-identical on re-run — with the full suite, ruff, and pyright green and no
store schema, adapter change, CLI, or LLM/network call introduced.
</success_criteria>

<output>
Create `.planning/phases/09-mcm-episode-detection-memory-breakdown/09-02-SUMMARY.md` when done.
</output>
