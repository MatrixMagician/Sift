---
phase: 07-evaluation-harness-golden-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: false
requirements: [EVAL-01]
must_haves:
  truths:
    - "`uv run python -c 'import yaml'` succeeds in the project venv"
    - "PyYAML is a declared runtime dependency pinned at 6.0.3 in pyproject.toml"
  artifacts:
    - "pyproject.toml (pyyaml==6.0.3 in [project.dependencies])"
    - "uv.lock (pyyaml resolved and locked)"
  key_links:
    - "truth.yaml parsing in Plan 02 (src/sift/eval/truth.py) imports yaml — this install must land first"
---

<objective>
Add PyYAML (the single new third-party dependency this phase needs, per D-10 / RESEARCH) so `truth.yaml` ground-truth files can be parsed. PyYAML is currently declared nowhere in pyproject.toml and `import yaml` fails in the venv (RESEARCH Pitfall 6). Because a package install crosses a supply-chain trust boundary, a blocking-human legitimacy checkpoint precedes the install (project precedent: Phase 1 "01-01 blocking-human legitimacy checkpoint").

Purpose: unblock all downstream truth.yaml parsing (EVAL-01/02) with a verified, pinned dependency.
Output: pyyaml==6.0.3 in pyproject.toml + uv.lock; `import yaml` works.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation-harness-golden-cases/07-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify PyYAML package legitimacy (supply-chain gate, T-07-SC)</name>
  <files>pyproject.toml</files>
  <what-built>
    Nothing yet — this gate precedes the install. RESEARCH performed the
    Package Legitimacy Audit: PyYAML is verdict SUS on `unknown-downloads`
    ONLY (a false-positive of the download-count probe, not a risk signal),
    and is dispositioned Approved. Evidence for the human to confirm:
      - Registry: PyPI, project page https://pypi.org/project/PyYAML/
      - Latest release 6.0.3 (2025-09-25); library ~15 years old
      - Source repo: https://github.com/yaml/pyyaml (pyyaml.org)
      - No postinstall script (postinstall: null)
      - CLAUDE.md explicitly sanctions PyYAML for `truth.yaml`, M7 only
      - It is among the most-downloaded PyPI packages (tens of millions/week)
  </what-built>
  <action>
    Blocking-human legitimacy gate — do NOT install until a human approves.
    Present the audit evidence above and pause for sign-off. This checkpoint is
    never auto-approvable (a package install crosses a supply-chain trust
    boundary, T-07-SC), even under workflow.auto_advance.
  </action>
  <how-to-verify>
    1. Open https://pypi.org/project/PyYAML/ and confirm the maintainer/repo
       is github.com/yaml/pyyaml and the latest version is 6.0.3.
    2. Confirm no unexpected recent maintainer/ownership change.
    3. Approve pinning `pyyaml==6.0.3` as a runtime dependency (M7-only use).
  </how-to-verify>
  <verify>
    <human-check>Human has opened pypi.org/project/PyYAML, confirmed repo github.com/yaml/pyyaml + version 6.0.3, and typed "approved".</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with the pinned install, or describe concerns to abort.</resume-signal>
  <done>A human has explicitly approved installing pyyaml==6.0.3 after reviewing the legitimacy evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add and pin PyYAML 6.0.3</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml [project.dependencies] block (lines ~6-16) — mirror the
      existing pinned style (e.g. `httpx==0.28.1`, `sqlite-vec==0.1.9`).
    - .planning/phases/07-evaluation-harness-golden-cases/07-RESEARCH.md
      "Standard Stack" + "Environment Availability" (PyYAML 6.0.3, install line).
  </read_first>
  <action>
    Run `uv add "pyyaml==6.0.3"` so PyYAML lands in [project.dependencies] as a
    runtime dependency (NOT a dev-group dep — the harness imports yaml at
    runtime) and uv.lock resolves it. Pin exactly to 6.0.3 to match the audited
    version (belt-and-braces per RESEARCH). Add a one-line British-English
    comment above the pin noting it is M7-only (truth.yaml parsing); when the
    stack drops to a Python floor with a stdlib YAML parser this dependency can
    be revisited. Do not add PyYAML type stubs unless pyright flags a missing
    stub — PyYAML ships inline types in 6.x.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; print(yaml.__version__)" &amp;&amp; uv run ruff check &amp;&amp; uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import yaml"` exits 0 and prints 6.0.3.
    - pyproject.toml [project.dependencies] contains `pyyaml==6.0.3`.
    - uv.lock records pyyaml 6.0.3.
    - `ruff check` and `pyright` remain clean (no new errors from the change).
  </acceptance_criteria>
  <done>pyyaml==6.0.3 is a pinned runtime dependency and `import yaml` succeeds; ruff+pyright clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → developer venv | A package install pulls third-party code that executes in the project environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | `uv add pyyaml` install | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying PyYAML on pypi.org/project/PyYAML + github.com/yaml/pyyaml before install; pin exact `pyyaml==6.0.3` (audited version); RESEARCH audit verdict Approved (SUS is a false-positive on missing download-count only); no postinstall script. Checkpoint is never auto-approvable. |
</threat_model>

<verification>
- `import yaml` resolves in the project venv.
- ruff + pyright clean.
- The legitimacy checkpoint was explicitly approved by a human before install.
</verification>

<success_criteria>
PyYAML 6.0.3 is a pinned runtime dependency; `import yaml` works; downstream Plan 02 can parse `truth.yaml`.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-harness-golden-cases/07-01-SUMMARY.md` when done.
</output>
