---
phase: 02-case-store-template-dedup
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/sift/cli.py
  - pyproject.toml
  - tests/perf/generate_synthetic.py
  - tests/perf/test_perf_ingest.py
  - tests/test_cli.py
autonomous: true
requirements: [STORE-01, CLI-03]
user_setup: []

must_haves:
  truths:
    - "A ~100 MB seeded synthetic log (generator script included in tests) ingests end-to-end — parse, store, template rebuild — in < 60 s on CPU via `uv run pytest -m perf` (M2 gate)"
    - "Each case is one portable directory: deleting `data_dir/cases/<name>/` removes the case entirely, and after a clean CLI run the case directory contains ONLY `case.db` — no `-wal`/`-shm` siblings — so deleting the file is deleting the case (ROADMAP STORE-01 criterion, Pitfall 4)"
    - "Creating a case whose name already exists exits 1 with an 'already exists' error — no silent overwrite (Phase 1 behaviour preserved at scale)"
    - "During ingest, progress feedback renders on stderr only; stdout gains no progress output and all existing stdout assertions keep passing (CLI-03)"
    - "Ingest streams events in batches via itertools.batched — a 100 MB single file never materialises all its Event objects in one list"
    - "The default test suite excludes perf tests (`addopts = \"-m 'not perf'\"`); the M2 gate runs explicitly via `uv run pytest -m perf`"
    - "The synthetic generator is deterministic: random.Random(seed) makes repeated generation byte-identical for the same seed and size"
    - statement: "An interrupted ingest leaves the store either fully updated or unchanged — the single BEGIN IMMEDIATE transaction spans all batches of all files, and WAL sidecars are checkpointed away on clean close so the case directory is the deletable unit"
      verification: backstop
  artifacts:
    - path: tests/perf/generate_synthetic.py
      provides: "seeded, importable AND __main__-runnable 100 MB synthetic-log generator (~20 template shapes with volatile tokens)"
      contains: "def generate"
    - path: tests/perf/test_perf_ingest.py
      provides: "@pytest.mark.perf gated < 60 s ingest test + portability assertion"
      contains: "pytest.mark.perf"
    - path: pyproject.toml
      provides: "perf marker registration + addopts exclusion"
      contains: "not perf"
  key_links:
    - from: src/sift/cli.py
      to: src/sift/store.py
      via: "batched inserts inside the existing single ingest transaction"
      pattern: "batched\\("
    - from: tests/perf/test_perf_ingest.py
      to: tests/perf/generate_synthetic.py
      via: "perf test imports the generator (pytest prepend import mode inserts tests/perf on sys.path)"
      pattern: "generate_synthetic"
  prohibitions:
    - statement: "MUST NOT introduce any network egress — rich progress rendering and the generator are pure-local; the autouse socket guard stays green across the whole suite including perf"
      status: unverified
      flagged: true
    - statement: "Progress rendering MUST NOT swallow or obscure per-file errors — the loud per-file ERROR/SKIP stdout lines from Phase 1 remain intact and visible (nothing disappears silently)"
      status: unverified
      flagged: true
---

<objective>
Prove the Phase 2 write path at production scale (M2 acceptance): convert ingest to batched
streaming with progress feedback on stderr (CLI-03), ship the deterministic 100 MB synthetic-log
generator plus a perf-marked < 60 s gate, and pin STORE-01 portability (one deletable case
directory, no WAL orphans). Runs in wave 2 deliberately — RESEARCH assumption A4 requires the
perf measurement early, not last.

Purpose: the user-visible capability is "ingest 100 MB without a silent hang" — a progress bar
on a real terminal, bounded memory, and a measured time budget.
Output: streaming ingest in cli.py, tests/perf/ suite, perf marker config, portability tests.
</objective>

## Phase Goal

**As an** engineer triaging a production incident offline, **I want to** ingest a large log
collection into one portable case file and see it collapse into inspectable template groups,
**so that** I can understand what the log contains at a glance — with zero LLM dependency and
nothing dropped silently.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-case-store-template-dedup/02-RESEARCH.md
@.planning/phases/02-case-store-template-dedup/02-PATTERNS.md
@.planning/phases/02-case-store-template-dedup/02-01-SUMMARY.md
@src/sift/cli.py
@pyproject.toml
@tests/test_cli.py
</context>

## Artifacts this phase produces (plan 02-02 share)

New symbols created by this plan (source-grounding: exclude from drift verification):

- `tests/perf/generate_synthetic.py` — module; symbols: `TEMPLATES`, `generate(path, target_mb, seed)`, `__main__` entry
- `tests/perf/test_perf_ingest.py` — perf-marked test module
- `pyproject.toml` — `[tool.pytest.ini_options]` gains `markers` (perf) and `addopts = "-m 'not perf'"`
- `src/sift/cli.py` — ingest gains `itertools.batched` streaming, a rich `Progress` on a `Console(stderr=True)`, and a running per-file event counter replacing the materialised list
- `pyproject.toml` `[project]` dependencies gains an explicit `rich` entry (already installed transitively via Typer — nothing new enters the environment)
- Tests: progress/portability/name-collision tests in `tests/test_cli.py`

## Flagged Assumptions (edge probe — surfaced, not dropped)

- **CLI-03 (unclassified edge probe item):** CLI-03's text covers ingest, embedding AND
  generation progress; this phase can only deliver the ingest leg (embedding/generation arrive
  in Phases 3–4). Assumption: per-phase scope is sufficient for this plan; REQUIREMENTS.md
  CLI-03 should only be ticked with a note, and the phase verifier must confirm the convention
  (RESEARCH open question 1).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generator + RED perf/portability/progress tests + perf marker config</name>
  <files>tests/perf/generate_synthetic.py, tests/perf/test_perf_ingest.py, tests/test_cli.py, pyproject.toml</files>
  <read_first>
    - .planning/phases/02-case-store-template-dedup/02-RESEARCH.md (generator sketch, Pitfall 5 — perf flake isolation, benchmark grounding)
    - tests/test_cli.py (CliRunner + FIXTURE_LOG idiom, _make_case helper, existing ingest stdout assertions)
    - pyproject.toml (current [tool.pytest.ini_options] — only testpaths today)
    - .planning/phases/02-case-store-template-dedup/02-01-SUMMARY.md (Template groups stdout line format, store method names)
  </read_first>
  <behavior>
    - generate_synthetic.generate(path, target_mb=1, seed=42) twice → byte-identical files; lines are ISO-timestamped and cycle ~20 template shapes with rng-filled volatile tokens (numbers, 0x-hex, 32-hex session ids, paths, IPs) so masking collapses them
    - perf test (marked perf): generate ~100 MB into tmp_path, CliRunner new + ingest, assert wall-clock elapsed < 60 s, assert exit 0, assert the case-directory listing after the command returns is exactly ["case.db"] (no -wal/-shm siblings, nothing else — file-deletion == case-deletion per the ROADMAP STORE-01 criterion), assert a `Template groups:` line is present
    - test_cli.py additions: portability — create + ingest a small case, assert after the ingest command returns (clean close) that the case-directory listing is exactly ["case.db"] (file-deletion == case-deletion, ROADMAP STORE-01 criterion), then shutil.rmtree(data_dir/cases/<name>), assert the path is gone and `sift show <name> events` now exits 1 with the does-not-exist error; name collision — `sift new` twice with the same name exits 1 containing 'already exists' (pin the existing Phase 1 behaviour as a Phase 2 acceptance); progress-on-non-TTY — ingest via CliRunner still exits 0 and stdout contains the existing per-file coverage lines and the Total line unchanged (rich must be disabled off-terminal; strict-xfail until task 2 only if the assertion depends on new behaviour — the non-TTY test should pass both before and after, so do NOT xfail it; it is a regression guard)
  </behavior>
  <action>
    Create tests/perf/generate_synthetic.py: fully typed, ruff/pyright clean, seeded via
    random.Random(seed), writing ISO-8601-timestamped lines cycling a module-level TEMPLATES list
    of ~20 realistic shapes (reuse the RESEARCH sketch: MCM contract denials with 0x codes and
    32-hex sessions, pool-exhaustion warnings with peer IPs, request-completed infos), until the
    file reaches target_mb mebibytes. Give it a `python tests/perf/generate_synthetic.py OUT MB
    [SEED]` __main__ path using sys.argv (no argparse needed). Create
    tests/perf/test_perf_ingest.py with @pytest.mark.perf on the < 60 s gate described in the
    behavior block, using time.perf_counter around the ingest invocation only (generation time
    excluded — the budget is the ingest contract); generate into tmp_path, never commit the
    artefact. Do NOT add an __init__.py under tests/perf (pytest prepend mode inserts the dir;
    import the generator as `import generate_synthetic` inside the test module). In
    pyproject.toml [tool.pytest.ini_options] add markers = ["perf: 100 MB-scale gates, run
    explicitly via -m perf"] and addopts = "-m 'not perf'" (Pitfall 5 — a later explicit -m on
    the command line overrides addopts). Add the test_cli.py portability, name-collision and
    non-TTY regression tests per the behavior block. The perf test must be runnable now
    (plan 02-01 already made ingest+dedup work), but do NOT execute the timed 100 MB gate in this
    task's verify — the `-m perf` run happens exactly once per plan, at the task 2 plan-level
    gate, keeping per-task verify feedback under 30 s. Prove the marker wiring here with a
    collect-only check instead.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run pytest -m perf --collect-only -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `markers` with a `perf` entry and `addopts` containing `not perf`
    - `uv run pytest -q` (default) collects and runs zero perf-marked tests
    - `uv run pytest -m perf --collect-only -q` collects exactly the perf-marked test(s) — marker gating proven without paying the 100 MB run; the timed gate runs once at task 2
    - tests/perf/generate_synthetic.py contains `def generate(` and `if __name__ ==`
    - Determinism test green: two generate() calls with the same seed produce identical bytes
    - Portability tests green: after clean close the case directory contains only case.db; rmtree of the directory leaves nothing and show exits 1
  </acceptance_criteria>
  <done>The M2 scale gate exists and is isolated behind the perf marker; the timed measurement runs once at the task 2 plan-level gate (still wave 2 — RESEARCH assumption A4 retired early in the phase).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batched streaming ingest + rich progress on stderr</name>
  <files>src/sift/cli.py, pyproject.toml, tests/test_cli.py</files>
  <read_first>
    - src/sift/cli.py (the ingest loop: single transaction block, the materialised `events = list(...)` call, per-file stats/coverage bookkeeping, event_count fallback)
    - .planning/phases/02-case-store-template-dedup/02-RESEARCH.md (Pattern 4 — batched streaming + progress, byte_offset/byte_len attrs contract, compressed-input caveat)
    - .planning/phases/02-case-store-template-dedup/02-PATTERNS.md (batched-ingest section: keep ONE transaction, running counter note)
    - tests/test_cli.py (which stdout strings existing tests assert — these must keep passing byte-for-byte on stdout)
  </read_first>
  <behavior>
    - All existing ingest stdout assertions unchanged; non-TTY regression test from task 1 still green; perf gate still < 60 s; memory-bounding proven structurally (no list() materialisation of a whole file's events on the ingest path)
  </behavior>
  <action>
    Declare rich explicitly in pyproject.toml [project] dependencies (it is already in uv.lock
    via typer standard — this adds no new package to the environment; note that in the commit
    message body). In cli.py ingest, replace the per-file materialised list with
    `for batch in itertools.batched(file_adapter.parse(path, case), 5000)` accumulating
    new_count += store.insert_events(batch) — still inside the ONE existing
    `with store.transaction():` (do NOT introduce per-batch transactions; all-or-nothing
    semantics are load-bearing). Replace the `len(events)` fallback with a running per-file
    parsed-event counter. Build a rich progress around the file loop: Console(stderr=True);
    Progress(..., console=console, transient=True, disable=not console.is_terminal) so non-TTY
    runs (CliRunner, CI, pipes) render nothing deterministically; total = sum of on-disk file
    sizes; for GenericLogAdapter on uncompressed files advance within-file using the last batch
    event's attrs byte_offset + byte_len (Phase 1 exposes both), clamped to the file size; for
    compressed inputs (.gz/.zst suffixes) and non-genericlog adapters advance the whole file size
    on file completion (decompressed offsets do not map to on-disk bytes — the honest simple
    choice per RESEARCH). Keep the Progress task description a STATIC string ('Ingesting') —
    never interpolate untrusted filenames into rich renderables (T-02-06 rich-markup/terminal
    injection; per-file names keep flowing through the existing _sanitise'd stdout prints). All
    per-file ERROR/SKIP/coverage prints and the Total/Template-groups lines stay on stdout
    exactly as before. Close the store on all paths as today. This task's verify carries the
    plan's single timed `-m perf` run (the plan-level M2 gate); if it fails the < 60 s budget on
    this machine, STOP and surface the measured seconds in the SUMMARY as a blocker instead of
    loosening the bound.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run pytest -m perf -x -q && uv run ruff check && uv run pyright</automated>
    <human-check>End-of-phase UAT: run `uv run sift ingest <case>` on the 100 MB file in a real terminal and observe a live progress bar on stderr (02-VALIDATION.md manual-only item — rich cannot be TTY-tested via CliRunner)</human-check>
  </verify>
  <acceptance_criteria>
    - src/sift/cli.py contains `batched(` and `Console(stderr=True)`
    - src/sift/cli.py ingest no longer materialises `list(file_adapter.parse` on the ingest path
    - Progress construction passes `disable=` derived from the console's terminal detection
    - pyproject.toml [project] dependencies contains `rich`
    - `uv run pytest -x -q` exits 0 (all existing stdout assertions green) and `uv run pytest -m perf -x -q` exits 0 in < 60 s
    - `uv run ruff check` and `uv run pyright` exit 0
  </acceptance_criteria>
  <done>Ingest streams in bounded-memory batches inside one transaction, shows live progress on a real terminal via stderr, and the 100 MB M2 gate is green — CLI-03 (ingest leg) and STORE-01 delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input files → ingest loop | untrusted bundle content and filenames drive batching and progress accounting |
| filenames → terminal renderables | file names may carry ESC bytes or rich markup metacharacters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Denial of Service | cli.py ingest memory | medium | mitigate | itertools.batched(…, 5000) streaming — no whole-file Event materialisation (task 2) |
| T-02-06 | Tampering/Spoofing | rich Progress renderables | medium | mitigate | static progress description; untrusted filenames never enter rich markup; names render only via existing `_sanitise` stdout prints |
| T-02-07 | Tampering (integrity) | WAL sidecars vs portability | low | mitigate | store.close() on all CLI paths checkpoints WAL; perf test asserts no -wal/-shm survive; deletable unit documented as the case directory |
| T-02-SC | Tampering | package installs | low | accept | no new package enters the environment — rich is promoted from transitive (typer) to explicit; versions already pinned in uv.lock (Phase 1 checkpoint) |

Canon-referral breadcrumb: terminal injection is canon — mitigated at T-02-06 and re-audited by
/gsd-secure-phase; not minted as a prohibition.
</threat_model>

<verification>
- Full gate: `uv run pytest && uv run ruff check && uv run pyright` all exit 0
- M2 scale gate: `uv run pytest -m perf` exits 0 with measured ingest < 60 s — run ONCE per plan, at the task 2 (final task) verify, never in earlier per-task verifies (record the actual seconds in SUMMARY)
- Default suite runtime stays in seconds (perf excluded via addopts)
- Portability: case-directory delete test green; after a clean close the case directory contains only case.db (no -wal/-shm siblings) — deleting the file is deleting the case
</verification>

<success_criteria>
- M2 acceptance line 1 proven: 100 MB synthetic log (generator in tests) ingests < 60 s on CPU
- STORE-01 complete: one portable case directory; deleting it deletes the case
- CLI-03 ingest leg complete: progress feedback on stderr, silent-hang eliminated, stdout contract intact
</success_criteria>

<output>
Create `.planning/phases/02-case-store-template-dedup/02-02-SUMMARY.md` when done, recording:
the measured 100 MB ingest seconds (A4 retirement evidence), the batch size, and the CLI-03
partial-scope note (embedding/generation progress deferred to Phases 3–4).
</output>
