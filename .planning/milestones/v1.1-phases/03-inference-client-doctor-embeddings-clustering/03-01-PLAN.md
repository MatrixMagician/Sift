---
phase: 03-inference-client-doctor-embeddings-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/sift/config.py
  - src/sift/cli.py
  - tests/test_config.py
  - tests/test_disk_full.py
autonomous: true
requirements: [LLM-01, CLUS-02]
must_haves:
  truths:
    - "uv run python -c 'import httpx, sqlite_vec, sklearn, numpy, respx' succeeds after this plan (four deps declared + installed)"
    - "config.toml [embeddings] and [clustering] sections load; a typo'd key under either raises ValueError (extra=forbid), never a silent default (per D-04, T-04-02 precedent)"
    - "embeddings.model default is None — NO baked-in embedding-model default (per D-03); base_url/timeout/retries/batch_size have tuned defaults"
    - "SIFT_EMBEDDINGS_BASE_URL / SIFT_GENERATION_BASE_URL (and the other scalar knobs) override config.toml but lose to CLI flags (precedence: flags > SIFT_* > toml > defaults)"
    - { statement: "WR-07: a SQLITE_FULL/SQLITE_IOERR raised mid-ingest aborts with DiskFullError, exits non-zero, and leaves zero events committed (transaction rolled back) — not misreported as one failed file", verification: backstop }
    - "clustering config exposes algorithm/min_cluster_size/min_samples/epsilon/distance_threshold so CLUS-02's agglomerative fallback is config-driven (D-04); min_samples default documents the sklearn +1 self-count"
    - "pytest 'live' marker is registered and excluded by default addopts alongside 'perf' (mirrors the perf marker pattern)"
  artifacts:
    - pyproject.toml
    - uv.lock
    - src/sift/config.py
    - src/sift/cli.py
    - tests/test_config.py
    - tests/test_disk_full.py
  key_links:
    - "config.py EmbeddingsConfig/ClusteringConfig/GenerationConfig -> consumed by llm/client.py (Plan 02) and pipeline/cluster.py (Plan 05)"
    - "cli.py DiskFullError -> raised in _ingest per-file handler, caught at the ingest command boundary -> typer.Exit(1)"
  prohibitions:
    - statement: "No vendor LLM SDK (openai / langchain / llamaindex / instructor) added to dependencies — hand-rolled httpx only"
      status: flagged-unverified
    - statement: "A malformed config file or unknown config key never silently falls back to defaults"
      status: flagged-unverified
    - statement: "Disk-full mid-ingest never disappears silently as a per-file parse error"
      status: flagged-unverified
---

<objective>
Lay the Phase-3 foundation: declare and install the four new dependencies, extend the
hand-rolled config precedence with the `[embeddings]`, `[clustering]` and generation
inference knobs, register the `live` pytest marker, and close the carried-forward WR-07
disk-full atomicity hole in the ingest loop.

Purpose: every downstream Phase-3 plan imports httpx / sqlite-vec / scikit-learn / numpy
and reads the new config sections; none can be built or tested until they exist. WR-07 is a
signed-off deferred fix from Phase 2 that is stdlib-only and belongs on the write path, so it
rides this foundation slice.

Output: `pyproject.toml` + `uv.lock` with 4 new deps and the `live` marker; `config.py` with
three new nested Pydantic config models and a generalised `SIFT_*` scalar env mapping; `cli.py`
with `DiskFullError` and fatal-SQLite-code reclassification in `_ingest`; two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-inference-client-doctor-embeddings-clustering/03-CONTEXT.md
@.planning/phases/03-inference-client-doctor-embeddings-clustering/03-RESEARCH.md
@.planning/phases/03-inference-client-doctor-embeddings-clustering/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare and install the four Phase-3 dependencies + register the live marker</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (existing [project.dependencies], [dependency-groups].dev, [tool.pytest.ini_options] markers + addopts — lines 7-14, 16-21, 30-37)
    - .planning/phases/03-inference-client-doctor-embeddings-clustering/03-RESEARCH.md ("Standard Stack" install block; "State of the Art" respx 0.23.1 note)
    - .planning/phases/03-inference-client-doctor-embeddings-clustering/03-PATTERNS.md ("pyproject.toml — 4 deps" section; "live" marker registration guidance)
    - .claude/CLAUDE.md Technology Stack (frozen versions — do NOT re-litigate)
  </read_first>
  <action>
    Run `uv add httpx==0.28.1 sqlite-vec==0.1.9 scikit-learn==1.9.0` (numpy arrives transitively via
    scikit-learn) and `uv add --dev respx` (resolves 0.23.1, tracking httpx 0.28). Confirm the four
    runtime entries land in `[project.dependencies]` and respx in `[dependency-groups].dev`, and that
    `uv.lock` is regenerated and staged. Add a `live` marker to `[tool.pytest.ini_options].markers`
    with a one-line description ("live: real-inference-server integration tests, run explicitly via
    -m live"), and change `addopts` from `-m 'not perf'` to `-m 'not perf and not live'` so the default
    suite excludes it exactly like `perf`. Do not touch any other pyproject section. Confirm
    `uv run python -c "import httpx, sqlite_vec, sklearn, numpy, respx"` succeeds.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/Sift && uv run python -c "import httpx, sqlite_vec, sklearn, numpy, respx; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A12 '\[project.dependencies\]' pyproject.toml` lists httpx, sqlite-vec, scikit-learn
    - respx appears under `[dependency-groups].dev`
    - `pyproject.toml` markers list contains a `live:` entry and `addopts` contains `not live`
    - `uv run python -c "import httpx, sqlite_vec, sklearn, numpy, respx"` prints ok (exit 0)
    - `uv.lock` is modified and staged
  </acceptance_criteria>
  <done>Four deps importable in the venv; live marker registered and excluded by default; uv.lock pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Add [embeddings], [clustering], generation config models + generalised SIFT_* env mapping</name>
  <files>src/sift/config.py, tests/test_config.py</files>
  <read_first>
    - src/sift/config.py (entire file, 61 lines — SiftConfig, load_config, _zones_must_exist validator, the ConfigDict(extra="forbid") idiom, the docstring flagging "a generalised SIFT_* -> key mapping arrives when later phases add scalar config keys" — THIS is that phase)
    - .planning/phases/03-inference-client-doctor-embeddings-clustering/03-PATTERNS.md ("src/sift/config.py" section — EmbeddingsConfig/ClusteringConfig field shapes)
    - .planning/phases/03-inference-client-doctor-embeddings-clustering/03-CONTEXT.md (D-03 config-only model identity; D-04 clustering defaults)
    - tests/test_config.py (existing precedence + extra-forbid tests, to mirror style)
    - .claude/skills python-dev conventions (list[int] / X | None hints, Google docstrings, 88-char lines)
  </read_first>
  <action>
    Add three nested `BaseModel` classes, each with `model_config = ConfigDict(extra="forbid")`:
    `GenerationConfig` (base_url: str default "http://localhost:13305/v1", model: str | None = None,
    timeout: float = 60.0, retries: int = 2, backoff_base: float = 0.5), `EmbeddingsConfig`
    (base_url, model: str | None = None per D-03 NO baked default, timeout, batch_size: int = 64),
    and `ClusteringConfig` (algorithm: str = "hdbscan", min_cluster_size: int = 2, min_samples: int = 1
    with a comment naming the sklearn self-count +1 vs standalone, epsilon: float = 0.0,
    distance_threshold: float = 0.3). Add them to `SiftConfig` as fields defaulting to instances.
    Extend `load_config` with a generalised scalar `SIFT_*` -> nested-key mapping (e.g.
    SIFT_GENERATION_BASE_URL, SIFT_EMBEDDINGS_BASE_URL, SIFT_EMBEDDINGS_MODEL,
    SIFT_GENERATION_MODEL, SIFT_EMBEDDINGS_BATCH_SIZE) merged into the `layers` dict AFTER toml and
    BEFORE flags, preserving the documented precedence (defaults -> toml -> env -> flags). Build the
    nested dict so pydantic validates the sub-models; keep the existing SIFT_DATA_DIR handling.
    Update the module docstring line that defers the generalised mapping. Do not add pydantic-settings.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/Sift && uv run pytest tests/test_config.py -x && uv run ruff check src/sift/config.py && uv run pyright src/sift/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class EmbeddingsConfig\|class ClusteringConfig\|class GenerationConfig' src/sift/config.py` returns 3
    - `src/sift/config.py` contains `model: str | None = None` for the embeddings model (no baked default)
    - A test in tests/test_config.py sets SIFT_EMBEDDINGS_BASE_URL and asserts it overrides a config.toml value but a `--` flag override still wins
    - A test asserts an unknown key under `[clustering]` raises ValueError (extra=forbid)
    - `uv run pytest tests/test_config.py -x` exits 0; ruff + pyright clean on config.py
  </acceptance_criteria>
  <done>Three config sections load with tuned defaults, no baked embedding-model default, and the SIFT_* scalar env layer sits between toml and flags.</done>
</task>

<task type="auto">
  <name>Task 3: WR-07 — reclassify SQLITE_FULL/IOERR as a fatal ingest abort</name>
  <files>src/sift/cli.py, tests/test_disk_full.py</files>
  <read_first>
    - src/sift/cli.py (_ingest, lines 149-331 — the per-file try/except at 277 "except Exception as exc"; the ingest command wrapper try/finally at 138-147; the _sanitise helper 42-61)
    - src/sift/store.py (transaction() 260-274 and savepoint() 276-300 — the IN-03 dead-transaction ROLLBACK guards that already tolerate an auto-rolled-back connection)
    - .planning/phases/03-inference-client-doctor-embeddings-clustering/03-RESEARCH.md (Pitfall 1 — full root cause + fatal-code detection; verified sqlite3.SQLITE_FULL=13, SQLITE_IOERR=10, exc.sqlite_errorcode)
    - .planning/phases/03-inference-client-doctor-embeddings-clustering/03-PATTERNS.md ("cli.py — WR-07 fix" section — catch-order note: sqlite3.Error before Exception)
  </read_first>
  <action>
    Define `class DiskFullError(Exception)` near the top of cli.py. In `_ingest`, insert a
    `except sqlite3.Error as exc:` handler BEFORE the existing generic `except Exception as exc:`
    (subclass ordering matters). In it, read `getattr(exc, "sqlite_errorcode", None)` and, when the
    code is `sqlite3.SQLITE_FULL` or `sqlite3.SQLITE_IOERR` or shares the IOERR low byte
    (`code & 0xFF == sqlite3.SQLITE_IOERR`), raise `DiskFullError` with a message that names the
    relative path (sanitised via _sanitise) and states no events were committed / transaction rolled
    back. Any other `sqlite3.Error` re-raises into the existing recoverable per-file handling (or
    falls through to the generic handler). In the `ingest` command wrapper, wrap the `_ingest` call in
    `try/except DiskFullError` that prints `f"Error: {_sanitise(str(exc))}"` and raises `typer.Exit(1)
    from None`, keeping the existing `finally: store.close()`. `sqlite3` is already imported.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/Sift && uv run pytest tests/test_disk_full.py -x && uv run ruff check src/sift/cli.py && uv run pyright src/sift/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class DiskFullError' src/sift/cli.py` returns 1
    - `grep -c 'except sqlite3.Error' src/sift/cli.py` returns at least 1, and it appears before the `except Exception` in _ingest
    - tests/test_disk_full.py forces a small `PRAGMA max_page_count` on a CaseStore, drives `_ingest` past that page budget, asserts `DiskFullError` is raised AND `store.query_events()` returns an empty list (zero events committed)
    - `uv run pytest tests/test_disk_full.py -x` exits 0; ruff + pyright clean on cli.py
  </acceptance_criteria>
  <done>A disk-full / I/O error mid-ingest aborts loudly with zero committed events and a non-zero exit, instead of being swallowed as one failed file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml / SIFT_* env -> SiftConfig | User/host-controlled scalar values (base_urls, params) cross into typed config |
| storage medium -> ingest loop | A full/failing disk raises SQLite errors that must not corrupt the atomicity guarantee |
| PyPI -> pyproject/uv.lock | Third-party package code enters the trust base |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | disk-full mid-ingest (WR-07) | high | mitigate | Task 3: fatal-code detection aborts with all-or-nothing preserved and honestly reported; test forces real SQLITE_FULL via PRAGMA max_page_count |
| T-03-02 | Tampering | malformed config.toml / unknown key | low | mitigate | ConfigDict(extra="forbid") on every model; malformed TOML raises ValueError naming the file (existing T-04-02 precedent) |
| T-03-03 | Spoofing | SIFT_* env injecting a hostile base_url | low | accept | env can set base_url but the LLM-02 SSRF guard (Plan 02) refuses non-local endpoints at client construction; config layer only stores the string |
| T-03-SC | Tampering | npm/pip supply chain (4 new deps) | high | mitigate | RESEARCH Package Legitimacy Audit approved all four; sqlite-vec is pre-accepted [SUS] with the store.py-confinement + BLOB/numpy escape hatch (STATE.md blocker); no new [ASSUMED]/[SLOP] packages, so no blocking-human checkpoint required |
</threat_model>

<verification>
- `uv run pytest` (full suite, minus perf/live) green
- `uv run ruff check` and `uv run pyright` clean
- `uv run python -c "import httpx, sqlite_vec, sklearn, numpy, respx"` succeeds
- WR-07: forced SQLITE_FULL yields DiskFullError + zero committed events
</verification>

<success_criteria>
Four deps installed and importable; three config sections with correct precedence and no baked embedding-model default; WR-07 disk-full abort proven by an automated test; live marker registered.
</success_criteria>

<output>
Create `.planning/phases/03-inference-client-doctor-embeddings-clustering/03-01-SUMMARY.md` when done.
</output>
