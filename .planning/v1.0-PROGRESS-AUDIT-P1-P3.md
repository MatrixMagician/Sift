---
milestone: v1.0
audit_type: progress (mid-milestone, scoped to completed phases)
scope_phases: [1, 2, 3]
milestone_phase_count: 8
audited: 2026-07-17
status: tech_debt
scores:
  requirements: 24/24        # P1–P3 assigned requirements satisfied
  phases: 3/3                # completed phases passing verification
  integration: 8/8           # cross-phase hand-offs wired
  flows: 1/1                 # offline pipeline new→ingest→analyze→show clusters
gaps:
  requirements: []           # none unsatisfied, none orphaned
  integration: []            # no blocking wiring gaps
  flows: []                  # primary offline pipeline complete end-to-end
tech_debt:
  - phase: 02-case-store-template-dedup
    items:
      - "WR-06: _sanitise passes \\n/\\t — hostile DB field/filename can forge output lines (spoofing, not terminal control). Recommended _sanitise_line helper; not yet added (only _sanitise exists)."
      - "WR-09: mask_version meta written (dedup.py:131) but never read — a case built under MASK_VERSION 1 renders stale groups silently after the bump to 2. Cross-version staleness warning recommended."
      - "WR-08: surrogate-escaped (non-UTF-8) filename raises UnicodeEncodeError with traceback + full rollback — loud/all-or-nothing preserved, but violates the no-traceback style goal. Partial: _sanitise strips Cf but Cs/surrogate path unconfirmed."
      - "IN-01/02/05/06/07: review info items deferred with reasons in 02-04-PLAN.md."
  - phase: 03-inference-client-doctor-embeddings-clustering
    items:
      - "WR-04: InferenceClient.models() egresses to the endpoint without re-running the SSRF guard (construction already asserts local — low risk). Open."
      - "WR-05: commit-time SQLITE_FULL escapes the WR-07 handler as a raw traceback (per-file handler wraps insert body only). Open; loud/all-or-nothing preserved."
      - "4 Info items in 03-REVIEW.md (advisory)."
  - phase: integration (cross-phase, this audit)
    items:
      - "analyze does not check template_groups_stale — show clusters warns on a crash-interrupted rebuild, but analyze would silently cluster a stale/partial group set. Consider mirroring the warning."
      - "show clusters --filter contains= matches signature only, not the LLM label — filtering by a label-only term returns zero rows once a cluster is labelled."
nyquist:
  compliant_phases: []
  partial_phases: [3]            # validated + nyquist_compliant:false (1 manual-only live-server test)
  not_validated_phases: [1, 2]   # status:draft — validate-phase never reconciled (coverage TODO, not a compliance failure)
  missing_phases: []
  overall: coverage_todo
notes: >
  Mid-milestone checkpoint. v1.0 spans 8 phases (M1–M8); only 1–3 are complete.
  This audit deliberately excludes Phases 4–8 (not started) from the FAIL/orphan gate.
  The real end-of-milestone audit runs after Phase 8 into .planning/v1.0-MILESTONE-AUDIT.md.
---

# v1.0 Progress Audit — Phases 1–3 (mid-milestone checkpoint)

**Scope:** the 3 completed phases of the 8-phase v1.0 milestone. Phases 4–8 are not started and are out of scope — they are *not* counted as gaps. This is a progress checkpoint, **not** the milestone completion gate (that runs after Phase 8).

**Verdict: `tech_debt`** — no blockers. Every completed phase passed verification, all 24 assigned requirements are satisfied with zero orphans, and all 8 cross-phase hand-offs are wired end-to-end. A handful of hardening-class items and two Nyquist coverage TODOs are worth tracking but block nothing.

## Phase Verification Roll-up

| Phase | Status | Score | Human sign-off | Code review |
|-------|--------|-------|----------------|-------------|
| 1 — Skeleton, Event Contract & genericlog | ✅ passed | 31/31 must-haves | 4 judgment-tier prohibitions resolved (01-UAT, 4/4) | 2 Crit + 7 Warn all fixed |
| 2 — Case Store & Template Dedup | ✅ passed | 21/24 (3 closed via UAT) | 6 UAT items signed off | 0 Crit; WR-06/08/09 carried as hardening |
| 3 — Inference Client, Doctor, Embeddings & Clustering | ✅ passed | 5/5 must-haves + WR-07 fix | Live-server UAT passed (Lemonade v10.4.0, dim 1024) | 0 Crit, 5 Warn — WR-01/02/03 fixed, WR-04/05 open |

All three carry `status: passed` in their VERIFICATION.md frontmatter. Every review round across all three phases landed **0 Critical**. The Phase-2 disk-full atomicity hole (its WR-07) was carried forward and **fixed & verified in Phase 3** (`DiskFullError`, test-backed).

## Requirements Coverage — 24/24 satisfied, 0 orphaned

3-source cross-reference (VERIFICATION table + SUMMARY frontmatter + REQUIREMENTS traceability):

| Phase | Requirements | Result |
|-------|-------------|--------|
| 1 | INGST-01/02/03/04/05/06/10/11, CLI-01 (9) | all SATISFIED |
| 2 | STORE-01/02/04*, CLUS-01, CLI-03* (5) | all SATISFIED (*STORE-04 & CLI-03 partial-scope, noted in REQUIREMENTS.md) |
| 3 | LLM-01/02/03/04, STORE-03, CLUS-02/03, RAG-05, CLI-02, EVAL-05 (10) | all SATISFIED |

- **No unsatisfied requirements** → the milestone FAIL gate does not trigger for the in-scope set.
- **No orphans** — every in-scope REQ-ID appears in a phase VERIFICATION.md with evidence.
- **Frontmatter reconciliation:** CLUS-03 and CLI-02 are absent from Phase-3 SUMMARY `requirements_completed` frontmatter but are confirmed SATISFIED with evidence in 03-VERIFICATION.md (lines 90, 92). Treated as satisfied; the SUMMARY frontmatter is the only stale artefact (cosmetic — worth a fix-up when convenient, not a gap).
- Partial-scope items (STORE-04 hypotheses leg → Phase 4; CLI-03 embedding/generation progress legs → Phases 3–4) are correctly annotated and their remainders map to future phases.

## Cross-Phase Integration — 8/8 wired

Integration checker traced every hand-off through real data flow (not import existence):

1. **adapters → store insert** (P1→P2): `detect → parse → insert_events` (INSERT OR IGNORE) — WIRED
2. **store events → template_groups** (P2): `rebuild_template_groups` post-commit, clears stale flag — WIRED
3. **template_groups → embed exemplars** (P2→P3): `query_template_groups → cluster_and_label → client.embed` — WIRED
4. **cluster → store persist** (P3): `ensure_vectors_table → upsert_vectors → replace_clusters → set_cluster_labels`, one atomic transaction — WIRED
5. **store clusters → show render** (P2→P1 CLI): `query_clusters`, label-or-signature fallback — WIRED
6. **Event contract + migrations 1→2→3 compose** — WIRED
7. **embedding-dim guard shares one meta path** (analyze writer ↔ doctor reader) — WIRED
8. **SSRF/loopback guard is the only client-construction seam** (doctor + analyze both go through `_assert_local`) — WIRED

**E2E offline flow `sift new → ingest → analyze → show clusters` is complete** — every hand-off passes real data, no dangling imports, no orphaned APIs (the one unused export, `store._blob_to_vec`, is the deliberately-reserved read half for Phase-4 KNN).

## Tech Debt (non-blocking — track, don't gate)

**Phase 2 (carried hardening):**
- **WR-06** — `_sanitise` passes `\n`/`\t`; line-forging via hostile DB field/filename. Recommended `_sanitise_line`; not yet added.
- **WR-09** — `mask_version` written but never read → silent stale groups across a MASK_VERSION bump.
- **WR-08** — surrogate-escaped filename → `UnicodeEncodeError` traceback + rollback (loud, all-or-nothing preserved; style-only). `Cf` stripped, surrogate path unconfirmed.
- **IN-01/02/05/06/07** — review info items, deferred with reasons.

**Phase 3 (open review warnings):**
- **WR-04** — `InferenceClient.models()` egresses without re-running the SSRF guard (construction already asserts local; low risk).
- **WR-05** — commit-time `SQLITE_FULL` escapes the WR-07 handler as a raw traceback.
- 4 Info items (advisory).

**Cross-phase (this audit):**
- `analyze` doesn't check `template_groups_stale` → could silently cluster a stale/partial group set (mirror the `show clusters` warning).
- `show clusters --filter contains=` matches `signature` only, not the LLM label → label-only terms return zero rows once labelled.

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Classification | Action |
|-------|---------------|--------|-------------------|----------------|--------|
| 1 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 1` (coverage TODO — validate-phase never reconciled) |
| 2 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 2` |
| 3 | exists | validated | false | PARTIAL | 10/10 automated + 1 manual-only live-server test (inherently un-automatable) |

P1/P2 `draft` is a **coverage TODO, not a compliance failure** (§5.5 / #2117) — running `validate-phase` will promote them and yield the real verdict. P3's PARTIAL is the single manual-only live-server check, already signed off via UAT.

## Bottom line

The completed third of v1.0 is in strong shape: **3/3 phases passed, 24/24 requirements satisfied, 8/8 integration hand-offs wired, offline pipeline complete end-to-end, 0 Critical findings in any review round.** Nothing blocks continued forward progress into Phase 4. The tracked debt is all hardening-class, and the only "coverage" items are the two un-run Nyquist validations for P1/P2.

---
*Progress audit generated 2026-07-17. Not the milestone completion gate — re-run `/gsd-audit-milestone` after Phase 8 for the full v1.0 audit.*
