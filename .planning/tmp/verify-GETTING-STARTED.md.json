{
  "doc_path": "docs/GETTING-STARTED.md",
  "claims_checked": 74,
  "claims_passed": 73,
  "claims_failed": 1,
  "failures": [
    {
      "line": 340,
      "claim": "Under `show events`: \"`file` and `contains` matches are literal substrings\"",
      "expected": "`contains` to be a valid events filter key",
      "actual": "cli.py _FILTER_KEYS['events'] = (severity, source, file, since, until, limit); `contains` is a clusters-only key, so `--filter contains=` on events exits 2"
    }
  ]
}
