---
phase: 14-perfmon-facts-into-sift-analyze-golden-eval-case
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - src/sift/pipeline/hypothesise.py
  - tests/test_perfmon_analyze.py
autonomous: true
requirements: [PERF-07]
must_haves:
  truths:
    - "D-01: hypothesise gains _apply_perfmon_block + _PERFMON_SLOT/_PERFMON_BLOCK_RE/_PERFMON_MARKER_RE mirroring the MCM trio verbatim; _assemble splices the perfmon block AFTER the MCM apply (order KB -> MCM -> perfmon)."
    - "D-05: _assemble unions the perfmon block's printed ids into prompted_ids (prompted_ids = set(event_ids) | mcm_ids | perfmon_ids) — the load-bearing inversion that makes perfmon facts citable (the inverse of the non-citable KB path); the perfmon block is built pre-generation inside hypothesise() at the MCM chokepoint, reusing the same events + mcm_analysis (no third decompress pass)."
    - "PERF-07: perfmon figures are injected as CITED evidence with cited ⊆ prompted ⊆ store preserved; the fact block is built before generation so an adversarial/hallucinating model cannot alter or invent a number (anti-hallucination test mirroring Phase 11)."
    - "D-02: the integration is strictly additive — golden-hash coverage for ALL FOUR presence combinations (neither / MCM-only / perfmon-only / both); the both-absent and MCM-only prompt hashes MUST equal their pre-phase values (the shipped triage_prompt_hash / no-MCM golden hash does not move for no-new-data cases), because each sentinel block is removed independently."
    - "PERF-07 [edge: idempotency]: re-running sift analyze on the same case is deterministic — identical case+config+model+seed yields a byte-identical prompt and thus an identical triage_prompt_hash (project determinism invariant / D-02); the perfmon block is a pure function of the store."
  truths_backstop:
    - statement: "PERF-07 [edge: concurrency]: sift is a single-process CLI over one per-case SQLite case.db; there is no concurrent-writer contract — one process per case is the operating assumption, and the perfmon injection adds no shared mutable state."
      verification: backstop
  artifacts:
    - "src/sift/pipeline/hypothesise.py: _apply_perfmon_block, _PERFMON_SLOT, _PERFMON_BLOCK_RE, _PERFMON_MARKER_RE, the perfmon_block kwarg on _assemble, the pre-generation perfmon build in hypothesise()"
    - "tests/test_perfmon_analyze.py: 4-combination byte-identity, anti-hallucination, and citable/fabricated-id tests appended to the 14-01 scaffold"
  key_links:
    - "the pre-generation build chokepoint in hypothesise() is where the eval harness (which calls hypothesise directly) exercises perfmon injection too — the same reason MCM was built there (PERF-08 depends on it)."
  prohibitions:
    - "perfmon facts rendered into the prompt but not unioned into prompted_ids (rendered-but-not-citable)"
    - "the both-absent or MCM-only prompt bytes changing from their pre-phase values"
    - "merging the perfmon and MCM sentinel blocks (couples their independent byte-identity guards)"
---

<objective>
Splice the perfmon fact block into the triage prompt at the same pre-generation
chokepoint as MCM, and union its printed ids into `prompted_ids` so perfmon facts
become citable evidence. Mirror Phase 11's `_apply_mcm_block` machinery verbatim.

Purpose: deliver PERF-07 — perfmon figures cited by `event_id` with
`cited ⊆ prompted ⊆ store` preserved and the no-data prompt byte-identical to
today's. The union is the one-line inversion (`| perfmon_ids`) that separates
citable evidence from the non-citable KB path.
Output: `hypothesise.py` splice + union, and the analyze integration tests
(4-combo byte-identity, anti-hallucination, citability).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-PATTERNS.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-03-SUMMARY.md
@.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _apply_perfmon_block + prompted_ids union + pre-generation build</name>
  <read_first>
    - src/sift/pipeline/hypothesise.py:80-106 (_MCM_SLOT/_MCM_BLOCK_RE/_MCM_MARKER_RE + _apply_mcm_block — copy verbatim, MCM->PERFMON, mirroring the trailing-\n capture EXACTLY per Pitfall 4)
    - src/sift/pipeline/hypothesise.py:220-272 (_assemble: add perfmon_block kwarg after mcm_block; splice after the MCM apply at line 250; union at line 271: | (perfmon_block[1] if perfmon_block else set()))
    - src/sift/pipeline/hypothesise.py:326-381 (hypothesise() body: hoist events = store.query_events() to a local, reuse it for analyse_mcm AND analyse_perfmon; build perfmon_block = render_perfmon_facts(analyse_perfmon(mcm_analysis, events)); thread perfmon_block=perfmon_block into _assemble)
    - src/sift/pipeline/perfmon.py:648 (analyse_perfmon signature: (analysis: McmAnalysis, events) -> PerfmonAnalysis) and src/sift/pipeline/perfmon_facts.py (render_perfmon_facts from 14-03)
    - src/sift/prompts/triage.md (the PERFMON sentinel block added in 14-03)
  </read_first>
  <files>src/sift/pipeline/hypothesise.py</files>
  <action>
    In hypothesise.py: add _PERFMON_SLOT, _PERFMON_BLOCK_RE, _PERFMON_MARKER_RE and _apply_perfmon_block
    by copying the MCM trio verbatim with MCM->PERFMON (identical remove-whole-block-when-absent semantics
    and trailing-newline capture). In _assemble: add a perfmon_block: tuple[str, set[str]] | None = None
    kwarg mirroring mcm_block; after the _apply_mcm_block call, add template = _apply_perfmon_block(template,
    perfmon_block[0] if perfmon_block else None); extend the prompted_ids union with
    | (perfmon_block[1] if perfmon_block else set()). In hypothesise(): hoist events = store.query_events()
    into a local, compute mcm_analysis = analyse_mcm(events, ...) once, build mcm_block from it AND
    perfmon_block = render_perfmon_facts(analyse_perfmon(mcm_analysis, events)) at the same chokepoint, and
    pass perfmon_block=perfmon_block into _assemble. No CLI change — cli.analyze already threads
    mcm_thresholds; perfmon is built internally. OPPORTUNISTIC (non-blocking, IN-03, only because you are
    editing these exact lines): the redundant re.DOTALL on the marker RE and the cosmetic double-newline at
    the MCM splice may be tidied in passing — do not expand scope beyond the lines you touch.
  </action>
  <acceptance_criteria>
    - render_perfmon_facts output ids appear in prompted_ids (source assertion at the union) — a perfmon-citing hypothesis passes the citation gate only via this union.
    - store.query_events() is called once in hypothesise() (no third decompress pass; source assertion).
    - a case with no perfmon data yields _apply_perfmon_block removing the whole block, leaving the prompt byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_perfmon_analyze.py -k "citable or byte_identical" -x</automated>
  </verify>
  <done>Perfmon facts are spliced after MCM and made citable via the prompted_ids union, built pre-generation from the reused events.</done>
</task>

<task type="auto">
  <name>Task 2: Analyze integration tests — 4-combo byte-identity, anti-hallucination, citability</name>
  <read_first>
    - tests/test_perfmon_analyze.py (the 14-01 scaffold: _ingest_perfmon_case, _offline_client/MockTransport, test_fixture_overlaps — append here)
    - tests/test_mcm_analyze.py:83-120 (MockTransport / autouse _no_network), :221-262 (anti-hallucination shape: drive the fake client to echo a WRONG figure, assert the verbatim pre-built block is in the prompt and the wrong figure is not), :327-379 (CliRunner end-to-end)
    - src/sift/pipeline/hypothesise.py:159-165 (_prompt_hash) — the four-combination byte-identity comparison keys off the assembled prompt / triage_prompt_hash
    - .claude/skills (TDD fail-fast rule: any of these tests that passes on first run must be counterfactually broken to prove it fails without the splice, then restored byte-identically before commit)
  </read_first>
  <files>tests/test_perfmon_analyze.py</files>
  <action>
    Append to tests/test_perfmon_analyze.py (mirroring test_mcm_analyze.py): (1) a 4-combination
    byte-identity test asserting the assembled prompt for neither / MCM-only / perfmon-only / both, and
    that the NEITHER and MCM-ONLY prompt hashes equal their pre-phase values (D-02 / Pitfall 4 — capture the
    shipped no-perfmon hash as the frozen baseline; a moved baseline is a regression, never edited away);
    (2) an anti-hallucination test driving the fake client to echo a WRONG counter value, asserting the
    verbatim render_perfmon_facts block is in the prompt and the wrong figure is not (prompt built before
    the reply); (3) a citability test asserting a hypothesis citing a printed perfmon id passes the citation
    gate while a FABRICATED perfmon id is flagged (cited ⊄ prompted). Reuse the 14-01 overlapping fixture so
    the perfmon-present cases carry real citable ids.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon_analyze.py` exits 0 with the 4 combos, anti-hallucination, and citability tests present.
    - The byte-identity test asserts the neither/MCM-only hashes equal a frozen pre-phase constant (source assertion), not merely equal to each other.
    - The fabricated-perfmon-id case yields citations_valid False for that row.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_perfmon_analyze.py -x</automated>
  </verify>
  <done>All four presence combos are byte-identity-guarded, the model cannot alter a pre-built figure, and only printed perfmon ids are citable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered fact block → LLM prompt | untrusted counter-derived text reaches the model; the citation gate is the trust control on what it may cite back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | model fabricating a perfmon figure or citation | high | mitigate | figures built pre-generation; cited ⊆ prompted gate (hypothesise.py:428-483) flags any id the block did not print; anti-hallucination + fabricated-id tests |
| T-14-08 | Tampering | prompt-drift regressing the shipped no-data baseline | high | mitigate | independent sentinel removal + 4-combination byte-identity guard freezing the pre-phase neither/MCM-only hashes |

No package installs this phase — no supply-chain (SC) checkpoint applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/sift/pipeline/hypothesise.py`: `_apply_perfmon_block`, `_PERFMON_SLOT`, `_PERFMON_BLOCK_RE`, `_PERFMON_MARKER_RE`, `perfmon_block` kwarg on `_assemble`, pre-generation perfmon build in `hypothesise()`
- `tests/test_perfmon_analyze.py`: 4-combination byte-identity, anti-hallucination, and citability/fabricated-id test functions (appended)
</artifacts_produced>

<verification>
- `uv run pytest tests/test_perfmon_analyze.py tests/test_perfmon_facts.py -q` green.
- Full gate: `uv run pytest` + `uv run ruff check` + `uv run pyright` clean (per-wave merge).
</verification>

<success_criteria>
Perfmon facts are cited by event_id with cited ⊆ prompted ⊆ store preserved; the
no-perfmon prompt is byte-identical to today's across all four presence combos; a
hallucinating model cannot alter or invent a figure.
</success_criteria>

<output>
Create `.planning/phases/14-perfmon-facts-into-sift-analyze-golden-eval-case/14-04-SUMMARY.md` when done
</output>
