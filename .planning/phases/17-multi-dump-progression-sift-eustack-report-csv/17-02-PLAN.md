---
phase: 17-multi-dump-progression-sift-eustack-report-csv
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/fixtures/eustack/progression/dump_alpha.txt
  - tests/fixtures/eustack/progression/dump_bravo.txt
  - tests/fixtures/eustack/progression/dump_charlie.txt
  - tests/fixtures/eustack/progression/dump_delta_nots.txt
  - tests/fixtures/eustack/progression/derive_progression_fixtures.py
  - src/sift/pipeline/eustack_progression.py
  - tests/test_eustack_progression.py
autonomous: true
requirements: [EUS-07, EUS-08]
must_haves:
  truths:
    - "D-01: when EVERY dump in the case carries a dump-time header timestamp, dumps are ordered by that timestamp and the report states the basis explicitly as ordered by dump-time timestamp."
    - "D-02: when ANY dump lacks a header timestamp, ordering falls back to sorted source_file order, that basis is stated explicitly, and a loud unverified-ordering flag is raised — progression still renders under the flag, it is never suppressed."
    - "D-02: no timestamp is ever invented or inferred for a dump that lacks one (ADR 0012 record-don't-apply precedent)."
    - "D-03: no operator --order override exists — the fallback is automatic-but-declared, not operator-authorised."
    - "D-08: with three or more dumps the analysis carries BOTH consecutive-pair step deltas and an overall first-to-last delta, so a population that grew then shrank is visible rather than cancelled out."
    - "D-09: signatures are ranked by absolute delta on an explicit total sort key, with newly-appeared and vanished signatures distinguishable; no cap is applied."
    - "D-10: progression is expressed purely as signature-population change; no per-thread-identifier claim, qualified or otherwise, is produced anywhere."
    - "D-11: adding dumps never changes which dump the classification and saturation figures come from — it remains the last dump only, never a union."
  artifacts:
    - tests/fixtures/eustack/progression/derive_progression_fixtures.py
    - tests/fixtures/eustack/progression/dump_charlie.txt
    - tests/fixtures/eustack/progression/dump_bravo.txt
    - tests/fixtures/eustack/progression/dump_alpha.txt
    - tests/fixtures/eustack/progression/dump_delta_nots.txt
    - tests/test_eustack_progression.py
  key_links:
    - "resolve_dump_order -> Event.ts / Event.ts_confidence on any one thread event per group (the adapter stamps the dump-time header value onto every thread event, so no preamble lookup and no second raw-text regex)"
    - "compute_progression join key -> SignatureGroup.frames (the full tuple, kept internal; the D-07 display projection happens in the renderer, not here)"
    - "OrderingFlag -> the PerfmonHazard record shape, deliberately NOT SaturationFlag (a dump either carries a timestamp or it does not — there is no threshold pair to grade against)"
  prohibitions:
    - statement: "The analysis must never claim, imply, or qualify a claim that a specific thread persisted, continued, or remained blocked between two dumps — thread-identifier reuse is measured (9 exited and 10 new within 60 seconds on an idle server), so per-thread continuity is not establishable from this evidence at all."
      status: unverified
      verification: "Assert that every progression-derived string the module emits is phrased at signature-population level, and that no emitted string carries a thread-identifier token or a per-thread continuity verb."
    - statement: "A dump that carries no header timestamp must never be assigned a synthesised, inferred, interpolated or file-mtime-derived timestamp in order to make ordering resolvable."
      status: unverified
      verification: "Assert that on the untimestamped fixture every resulting DumpSlice keeps ts None and ts_confidence missing, and that the module reads no clock and stats no file."
    - statement: "A committed test fixture must never contain a customer's real captured hostnames, environment identifiers, process identifiers or symbol text lifted verbatim from the out-of-repo reference capture."
      status: unverified
      verification: "Assert the fixture files are reproducible from the committed derivation script alone, and that their preamble labels them authored-not-observed."
---

<objective>
Generalise the ordering and progression layer from the tracer's N=1 case to N
dumps: resolve the ordering basis honestly (D-01 timestamp path, D-02 declared
filename fallback with a loud flag, never an invented timestamp), and compute
per-signature population deltas across the ordered sequence — consecutive-pair
steps plus an overall first-to-last figure (D-08), with appeared and vanished
signatures distinguished (D-09).

Purpose: EUS-07's second half and the whole of EUS-08 live here. This is the
only genuinely new computation in the phase — everything else renders or wires
what already exists. It lands before the rendering plan so 17-03 has real
multi-dump data to render rather than a shape guessed at.

Output: a synthetic three-plus-one dump fixture set with its provenance script,
the multi-dump branches of `src/sift/pipeline/eustack_progression.py`, and
`tests/test_eustack_progression.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-multi-dump-progression-sift-eustack-report-csv/17-CONTEXT.md
@.planning/phases/17-multi-dump-progression-sift-eustack-report-csv/17-RESEARCH.md
@.planning/phases/17-multi-dump-progression-sift-eustack-report-csv/17-PATTERNS.md
@.planning/phases/17-multi-dump-progression-sift-eustack-report-csv/17-01-SUMMARY.md
@src/sift/pipeline/eustack_progression.py
@src/sift/adapters/eustack.py
@docs/decisions/0012-perfmon-naive-timestamps.md
</context>

## Artifacts this phase produces

New symbols and files created by THIS plan:

**`tests/fixtures/eustack/progression/`** (new directory)
- `derive_progression_fixtures.py` — provenance script; module symbols `SIGNATURES`, `DUMPS`, `main()`
- `dump_charlie.txt` — dump at `2026-04-10T16:07:39+00:00` (earliest)
- `dump_bravo.txt` — dump at `2026-04-10T16:08:37+00:00` (middle)
- `dump_alpha.txt` — dump at `2026-04-10T16:09:35+00:00` (latest)
- `dump_delta_nots.txt` — same population as `dump_bravo.txt`, header timestamp line removed

**`src/sift/pipeline/eustack_progression.py`** (extended, symbols added)
- `ORDERING_UNVERIFIED_DIMENSION: str`
- `ORDERING_UNVERIFIED_MESSAGE: str`
- `def compute_progression(ordered_analyses, dump_slices) -> tuple[SignatureProgression, ...]`

**`tests/test_eustack_progression.py`** (new) — `_parse_progression_fixture`,
`_bundle_for`, `test_order_by_timestamp_ignores_filename_order`,
`test_order_fallback_flagged_when_any_dump_untimestamped`,
`test_order_fallback_still_renders_progression`,
`test_no_timestamp_is_invented`, `test_single_dump_needs_no_ordering`,
`test_step_and_overall_deltas_disagree_on_grew_then_shrank`,
`test_appeared_and_vanished_signatures`,
`test_unchanged_signature_has_zero_delta`,
`test_progression_ranking_is_a_total_order`,
`test_saturation_computed_on_last_dump_only`,
`test_no_per_tid_claim_in_progression_strings`

<flagged_assumptions>
Spec-less probe fallback is active — no `*-SPEC.md` exists, so the edge probe
returned `unclassified`/`unresolved` for every requirement. The two rows this
plan carries are surfaced here rather than silently dropped or auto-backstopped:

- **EUS-07 — `unclassified`, `unresolved`.** The probe could not classify the
  edge behaviour of "full analysis from a single dump, per-signature deltas with
  two or more". This plan ASSUMES the delta edge set is: a signature present in
  every dump (plain deltas), present only in the first (vanished), present only
  in the last (appeared), present in the middle only (both ends zero — modelled
  as a normal count sequence, NOT as two boolean flags fighting each other), and
  unchanged across all dumps (zero delta, excluded from the report's changed-only
  section but kept in the CSV). It ASSUMES `appeared` means the count was zero in
  the first dump and non-zero in the last, and `vanished` the converse — both
  DERIVED from the count sequence, never tracked as separate state. It does NOT
  assume anything about two dumps sharing an identical `source_file`, which is
  structurally impossible because `source_file` is the grouping dict key.

- **EUS-08 — `unclassified`, `unresolved`.** The probe could not classify the
  edge behaviour of "ordering basis stated, unresolvable ordering flagged". This
  plan ASSUMES exactly three ordering states exist: one dump (no ordering
  required, no flag), all dumps timestamped (D-01, no flag), and at least one
  dump untimestamped (D-02, filename basis, one flag). It ASSUMES the
  all-timestamped path still needs a tie-break — two dumps sharing an identical
  timestamp sort on `source_file` as the secondary key — because equal timestamps
  are a real capture outcome and an unstable sort would break D-13 byte-identity.
  It does NOT assume `ts_confidence == "inferred"` is distinguishable from
  `"exact"` for ordering purposes: both count as "carries a timestamp", because
  the eu-stack adapter only ever emits `"exact"` or `"missing"` for a dump header
  (verified by running the adapter over both committed fixtures). If a future
  adapter change emits `"inferred"`, this assumption is where to look first.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Synthetic multi-dump fixture set with a provenance script</name>
  <files>tests/fixtures/eustack/progression/derive_progression_fixtures.py, tests/fixtures/eustack/progression/dump_charlie.txt, tests/fixtures/eustack/progression/dump_bravo.txt, tests/fixtures/eustack/progression/dump_alpha.txt, tests/fixtures/eustack/progression/dump_delta_nots.txt</files>
  <read_first>
    - `tests/fixtures/eustack/derive_reference_capture_derivative.py` (whole file) — the authored-not-observed provenance discipline, the preamble comment style, and how a generated file declares its own derivation
    - `tests/fixtures/eustack/threaddump.txt` lines 1-12 — the exact on-disk shape: a dashed comment line, then the ISO-8601 dump-time line, then `PID <n> - <name>`, then `TID <n>:` headers with `#<N>  0x<ADDR>  <symbol>` frames
    - `src/sift/adapters/eustack.py` lines 53-67 — `_TID_RE`, `_FRAME_RE` and `_TS_RE`, so the authored files parse cleanly rather than landing in the unknown-fallback path
    - `src/sift/rules/eustack_roles.toml` (whole file) — the shipped 24-rule taxonomy, so the authored signatures resolve to real roles and subsystems rather than all collapsing into `unclassified`
    - `tests/test_cli.py` lines 1255-1280 — `_copy_fixture(..., only=...)`, which is why the new files go in a subdirectory and why the phase-5 e2e event counts must be re-checked after adding them
  </read_first>
  <action>
Author a deterministic multi-dump fixture set under a NEW subdirectory
`tests/fixtures/eustack/progression/` — not the `tests/fixtures/eustack/` root,
which other tests copy from.

Write `derive_progression_fixtures.py` as the single source of truth: a module
declaring a `SIGNATURES` mapping of short label to frame list, a `DUMPS`
declaration of filename, ISO timestamp or `None`, and a label-to-thread-count
mapping, then a `main()` that writes the four `.txt` files. Every generated file
opens with a preamble stating in plain words that it is deliberately authored,
not observed; that it is not a capture from any real system; and that it is
regenerated by running this script — the same discipline
`derive_reference_capture_derivative.py` established. Copy nothing from the
out-of-repo reference capture directory named in `17-CONTEXT.md`: it carries
customer environment identifiers and must never enter the repository.

Five signature populations, chosen so every D-08 and D-09 branch has real data
behind it. Use frame chains the shipped `eustack_roles.toml` classifies into
genuinely different roles — check each chain against the TOML before settling on
it, rather than assuming:
`warehouse` resolving to blocked-on-external with subsystem warehouse, via a
`CDSSQueryEngine::WaitUntilFinished` chain;
`parked` resolving to idle-parked, via an `MSIQTask::GetNextPreferredJob` chain;
`stable` resolving to running;
`newcomer` resolving to blocked-on-external with the HTTP subsystem, via a
`curl_multi_poll` chain;
`departing` resolving to idle-parked through a parking chain DIFFERENT from
`parked`'s, so the two collapse to two distinct signatures rather than one.

Per-dump thread counts, in chronological order:
`dump_charlie.txt` at `2026-04-10T16:07:39+00:00` — warehouse 3, parked 10, stable 4, departing 2;
`dump_bravo.txt` at `2026-04-10T16:08:37+00:00` — warehouse 7, parked 6, stable 4, newcomer 2;
`dump_alpha.txt` at `2026-04-10T16:09:35+00:00` — warehouse 5, parked 6, stable 4, newcomer 2.

Two properties of that table are load-bearing and must not be "tidied up".
First, the timestamp order charlie, bravo, alpha is the exact REVERSE of sorted
filename order alpha, bravo, charlie — that reversal is the only thing that lets
the D-01 test tell timestamp ordering apart from filename ordering. Second,
warehouse runs 3 then 7 then 5, so its consecutive step deltas are +4 and -2
while its overall first-to-last delta is +2 — the grew-then-shrank case D-08
exists for, and invisible if only the overall figure were reported.

`dump_delta_nots.txt` carries the identical population to `dump_bravo.txt` with
the ISO-8601 line omitted entirely — not blanked, not replaced with a placeholder
stamp. It is the D-02 trigger. Its filename sorts after `dump_charlie.txt`, so
the untimestamped pair still has a well-defined filename ordering to declare.

Give each thread a distinct identifier within its own dump, and deliberately
reuse some identifiers across dumps for threads carrying DIFFERENT signatures —
the real capture's measured reuse behaviour, and the concrete reason D-10 forbids
per-thread continuity claims. State that intent in the script's docstring so a
later reader does not "fix" the reuse as a bug.

Regenerate the four `.txt` files by running the script and confirm the committed
files are byte-for-byte what the script produces.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k phase5_e2e -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/eustack/progression/` contains exactly `derive_progression_fixtures.py`, `dump_alpha.txt`, `dump_bravo.txt`, `dump_charlie.txt`, `dump_delta_nots.txt`
    - Running `uv run python tests/fixtures/eustack/progression/derive_progression_fixtures.py` leaves `git status --porcelain tests/fixtures/eustack/progression` empty — the committed files are exactly the script's output
    - `grep -c "^2026-04-10T16:" tests/fixtures/eustack/progression/dump_alpha.txt` returns `1`; the same holds for `dump_bravo.txt` and `dump_charlie.txt`; the same grep against `dump_delta_nots.txt` returns `0`
    - Parsing each of the four files with `EustackAdapter` yields thread-event counts 17 (alpha), 19 (bravo), 19 (charlie), 19 (delta) and a `ts_confidence` of `exact` for the first three and `missing` for `dump_delta_nots.txt`
    - Running `analyse_eustack` over `dump_charlie.txt` yields 4 signatures, over `dump_bravo.txt` 4 signatures, and their `frames` tuples intersect in exactly 3 signatures (warehouse, parked, stable) — proving `departing` vanished and `newcomer` appeared
    - No signature in any of the four fixtures resolves to role `unclassified`
    - `grep -rniE "iserver-1|hnyjbci|castorserver" tests/fixtures/eustack/progression/` returns no match (no customer identifier from the out-of-repo capture)
    - Each generated `.txt` file's first line declares it authored and not a real capture
    - `uv run pytest tests/test_cli.py -k phase5_e2e -q` exits 0 — the new subdirectory did not perturb the existing e2e event counts
    - `uv run ruff check tests` exits 0
  </acceptance_criteria>
  <done>Four fixture dumps and their derivation script are committed, reproducible from the script alone, free of any customer identifier, classify into four non-unclassified signatures each, and encode both the reversed-filename-order property (D-01 discriminator) and the grew-then-shrank warehouse sequence (D-08 discriminator).</done>
</task>

<task type="auto">
  <name>Task 2: Dump ordering — D-01 timestamp basis, D-02 declared fallback and its loud flag</name>
  <files>src/sift/pipeline/eustack_progression.py, tests/test_eustack_progression.py</files>
  <read_first>
    - `src/sift/pipeline/eustack_progression.py` — `resolve_dump_order`, `ORDER_BASIS_SINGLE`, `ORDER_BASIS_TIMESTAMP`, `ORDER_BASIS_FILENAME`, `OrderingFlag` and `DumpSlice` as 17-01 left them, including the `NotImplementedError` this task replaces
    - `src/sift/pipeline/perfmon.py` lines 119-144 — `PerfmonHazard`: the structural-condition record shape (`dimension`, `severity` as a `Literal`, `message`) that `OrderingFlag` mirrors
    - `src/sift/pipeline/eustack.py` lines 486-521 — `SaturationFlag`, so it is clear WHY it is not reused here: its `warn` and `critical` are required fields and an ordering basis has no cut-point pair
    - `docs/decisions/0012-perfmon-naive-timestamps.md` (whole file) — the record-don't-apply precedent D-01 and D-02 follow
    - `src/sift/adapters/eustack.py` lines 164-224 — proof the dump-time header value is copied onto every thread event, which is why one thread event per group is a correct representative
    - `tests/test_perfmon.py` — the grouping/ordering unit-test shape this new test module mirrors
  </read_first>
  <action>
Replace the `NotImplementedError` branch 17-01 left in `resolve_dump_order` with
the full three-state D-01/D-02/single-dump resolution, and pin it with tests.

Pick each dump's representative by taking the FIRST event in the group for which
`event.thread is not None`, then reading `.ts` and `.ts_confidence` off it. Do
not look for a preamble event and do not re-scan `Event.raw` with a second
timestamp regex.

With one dump: return `ORDER_BASIS_SINGLE` and an empty flag tuple — there is no
ordering to verify, so raising a flag would be noise.

With two or more dumps where every representative has
`ts_confidence != "missing"` and a non-`None` `ts`: sort on the total key
`(ts, source_file)` and return `ORDER_BASIS_TIMESTAMP` with an empty flag tuple
(D-01). The `source_file` secondary term is load-bearing — two dumps can share a
timestamp, and an unstable order would break D-13 byte-identity on re-run.

With two or more dumps where ANY representative has `ts_confidence == "missing"`:
sort on `source_file` alone, return `ORDER_BASIS_FILENAME`, and return exactly
one `OrderingFlag` built from `ORDERING_UNVERIFIED_DIMENSION` and
`ORDERING_UNVERIFIED_MESSAGE` at severity `warn` (D-02). Do NOT assign, infer,
interpolate or filesystem-derive a timestamp for the untimestamped dump: its
`DumpSlice.ts` stays `None` and its `ts_confidence` stays `missing`, exactly as
the adapter reported it — the ADR 0012 record-don't-apply precedent. Do NOT
suppress or shorten the progression when this flag is present; the flag declares
the limitation, it does not withhold the evidence. Expose no operator-facing
ordering override of any kind — no CLI option, no function keyword argument, no
environment variable, in this module or in the CLI: D-03 makes the fallback
automatic-but-declared, and a defaulted override parameter is exactly the
mechanism by which a later caller would silently reintroduce operator-chosen
ordering.

`ORDER_BASIS_FILENAME` and `ORDERING_UNVERIFIED_MESSAGE` must both state in
British English that the ordering rests on sorted file names because at least one
dump carries no dump-time timestamp, and that the resulting order is therefore
unverified. Write them so a reader who sees only the message understands the
limitation without consulting the code.

Create `tests/test_eustack_progression.py` with a `_parse_progression_fixture`
helper that parses a named fixture from `tests/fixtures/eustack/progression/`
with `EustackAdapter` and a `_bundle_for` helper that concatenates several
fixtures' events and calls `analyse_eustack_bundle`. Add:
`test_order_by_timestamp_ignores_filename_order` asserting the alpha/bravo/charlie
trio resolves to charlie, bravo, alpha with basis `ORDER_BASIS_TIMESTAMP` and no
flags — and asserting explicitly that this differs from `sorted()` filename
order, so the test cannot pass by accident;
`test_order_fallback_flagged_when_any_dump_untimestamped` on the charlie plus
delta pair, asserting basis `ORDER_BASIS_FILENAME` and exactly one flag with
dimension `ORDERING_UNVERIFIED_DIMENSION`;
`test_order_fallback_still_renders_progression` asserting the same pair still
produces a non-empty `signatures` tuple under the flag;
`test_no_timestamp_is_invented` asserting the delta dump's `DumpSlice` keeps
`ts is None` and `ts_confidence == "missing"` after ordering;
`test_single_dump_needs_no_ordering` asserting `ORDER_BASIS_SINGLE` and zero
flags for one dump.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eustack_progression.py -k "order or timestamp" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' src/sift/pipeline/eustack_progression.py | grep -c "NotImplementedError"` returns `0` (comment-filtered, so explanatory prose cannot self-invalidate the gate)
    - `grep -v '^\s*#' src/sift/pipeline/eustack_progression.py | grep -Ec "datetime\.now|utcnow|\.stat\(|st_mtime|fromtimestamp"` returns `0` — no clock read and no filesystem-derived timestamp on any code path (D-02)
    - `uv run sift eustack --help` lists exactly the options `--format`, `--data-dir` and `--help` — no ordering-override option exists (D-03, asserted as CLI behaviour)
    - `resolve_dump_order` and `analyse_eustack_bundle` each accept no parameter whose name contains `order`, asserted via `inspect.signature` rather than a source grep (D-03)
    - `uv run pytest tests/test_eustack_progression.py -k order_by_timestamp -x` exits 0
    - `uv run pytest tests/test_eustack_progression.py -k order_fallback -x` exits 0, covering both the flag assertion and the still-renders assertion
    - `uv run pytest tests/test_eustack_progression.py -k no_timestamp_is_invented -x` exits 0
    - `uv run pytest tests/test_eustack_progression.py -k single_dump -x` exits 0
    - `OrderingFlag.model_fields` has keys exactly `dimension`, `severity`, `message` — no `warn` and no `critical` field (it is the structural-hazard shape, not the graded shape)
    - `uv run ruff check src/sift tests` exits 0 and `uv run pyright src/sift` reports 0 errors
  </acceptance_criteria>
  <done>All three ordering states resolve correctly and declare their basis: one dump needs none, an all-timestamped set orders by timestamp in an order provably different from filename order, and a set containing an untimestamped dump orders by file name, states that basis, raises exactly one unverified-ordering flag, still renders its progression, and leaves the untimestamped dump's timestamp genuinely absent.</done>
</task>

<task type="auto">
  <name>Task 3: Population deltas — consecutive steps, overall first-to-last, appeared and vanished</name>
  <files>src/sift/pipeline/eustack_progression.py, tests/test_eustack_progression.py</files>
  <read_first>
    - `src/sift/pipeline/eustack_progression.py` — `SignatureProgression`, `ProgressionAnalysis` and `analyse_eustack_bundle` as Tasks 1 and 2 of 17-01 and Task 2 above left them
    - `src/sift/pipeline/eustack.py` lines 432-471 — `analyse_eustack`'s per-signature grouping and its `groups.sort(key=lambda g: (-g.thread_count, g.frames))` explicit-total-order discipline
    - `src/sift/pipeline/eustack.py` lines 382-399 — `SignatureGroup`, in particular that `frames` is the tuple that identifies a signature across dumps
    - `src/sift/pipeline/perfmon.py` lines 578-666 — `_file_scope_groups`' determinism comment block on why neither unordered-set iteration nor the `Counter` convenience ranking helper may appear on an output path
    - `.planning/phases/17-multi-dump-progression-sift-eustack-report-csv/17-CONTEXT.md` — decisions D-08, D-09 and D-10 verbatim, and the population-level phrasing model in the `<specifics>` section
  </read_first>
  <action>
Implement `compute_progression` and wire it into `analyse_eustack_bundle`, so a
multi-dump case yields real per-signature population deltas.

Join signatures across dumps on the full `SignatureGroup.frames` tuple — the
natural key, and the only lossless one. The D-07 display fields (matched frame,
its index, leaf frame) are ambiguous as a join key and must not be used as one;
they are a rendering projection applied later, in 17-03.

For each signature appearing in ANY dump, build one `SignatureProgression` whose
`counts` tuple holds its thread count in every dump in resolved order, using `0`
for a dump where the signature is absent. Set `step_deltas` to the
consecutive-pair differences, one shorter than `counts`, and `overall_delta` to
the last count minus the first (D-08 — both, always, so a population that grew
then shrank shows +4 then -2 alongside an overall +2 rather than being flattened
to a single misleading figure). Derive `appeared` as first count zero and last
count non-zero, and `vanished` as first count non-zero and last count zero
(D-09); derive them from `counts`, never track them as independent state.

Populate the classification fields (`role`, `subsystem`, `pattern`,
`frame_index`, `reason`, `matched_frame`, `leaf_frame`) from the signature's
group in the LAST dump where it appears, so a vanished signature still carries a
meaningful classification instead of empty cells.

Sort `ProgressionAnalysis.signatures` on the explicit total key
`(-abs(overall_delta), -counts[-1], frames)` (D-09 — ranked by absolute delta,
with the frames tuple as the final tie-break so the order is total and the
output is byte-identical on re-run). A sort keyed on absolute delta alone is not
a total order over ties and would let two runs disagree.

Apply no cap to the signature set: D-09's changed-only filter is a RENDERING
concern that lands in 17-03, and the analysis model carries every signature so
the CSV can export the unchanged ones.

`PROGRESSION_SCOPE_NOTE` states, in British English, that every figure in this
section describes how a signature's thread POPULATION changed between dumps, and
that no claim is made about any individual thread persisting between dumps
because thread identifiers are reused. Ensure no string this module emits —
`PROGRESSION_SCOPE_NOTE`, `ORDERING_UNVERIFIED_MESSAGE`, `ORDER_BASIS_*`, or any
other module-level message — carries a thread-identifier token or a per-thread
continuity verb such as persisted, remained, stayed, or still blocked (D-10).

<!-- planner-discipline-allow: TID -->

Confirm D-11 held after the change: `bundle.analysis` and `bundle.saturation`
still come from the LAST dump alone, so the three-dump fixture's totals equal
`dump_alpha.txt`'s own 17 threads, not the 55-thread sum across dumps.

Add to `tests/test_eustack_progression.py`:
`test_step_and_overall_deltas_disagree_on_grew_then_shrank` asserting the
warehouse signature over charlie, bravo, alpha has `counts == (3, 7, 5)`,
`step_deltas == (4, -2)` and `overall_delta == 2`;
`test_appeared_and_vanished_signatures` asserting `newcomer` is `appeared` and
not `vanished`, and `departing` the converse;
`test_unchanged_signature_has_zero_delta` asserting `stable` has
`overall_delta == 0` and `step_deltas == (0, 0)` and is still present in
`ProgressionAnalysis.signatures`;
`test_progression_ranking_is_a_total_order` asserting two independent
`analyse_eustack_bundle` calls over the same events, one with the input event
list reversed, produce an identical `signatures` tuple;
`test_saturation_computed_on_last_dump_only` asserting
`bundle.analysis.total_threads` equals `dump_alpha.txt`'s own thread count and
not the sum across the three dumps (D-11);
`test_no_per_tid_claim_in_progression_strings` collecting every module-level
string constant plus every emitted `OrderingFlag.message`, asserting the set is
non-empty as a non-vacuity guard, and asserting none matches a word-boundary
thread-identifier token or any of the per-thread continuity verbs — the same
word-boundary, non-vacuity-guarded technique
`test_ownership_blind_vocabulary_absent_from_source_and_emitted_output`
already uses in `tests/test_eustack_rules.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eustack_progression.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_eustack_progression.py -x` exits 0 with at least 12 tests collected
    - `uv run pytest tests/test_eustack_progression.py -k grew_then_shrank -x` exits 0, pinning `counts == (3, 7, 5)`, `step_deltas == (4, -2)`, `overall_delta == 2`
    - `uv run pytest tests/test_eustack_progression.py -k "appeared or vanished" -x` exits 0
    - `uv run pytest tests/test_eustack_progression.py -k total_order -x` exits 0 — reversing the input event list does not change the output tuple
    - `uv run pytest tests/test_eustack_progression.py -k last_dump_only -x` exits 0 — `total_threads` is 17, not 55 (D-11)
    - `uv run pytest tests/test_eustack_progression.py -k no_per_tid_claim -x` exits 0, with the candidate string set asserted non-empty
    - `grep -v '^\s*#' src/sift/pipeline/eustack_progression.py | grep -Ec "most_common|for .* in set\("` returns `0` — no unordered iteration on an output path (comment-filtered)
    - `uv run pytest` exits 0
    - `uv run ruff check src/sift tests` exits 0 and `uv run pyright src/sift` reports 0 errors
  </acceptance_criteria>
  <done>A three-dump case yields per-signature `counts`, consecutive `step_deltas` and an `overall_delta` for every signature including the ones that appeared and vanished, ranked on a total order that survives input reordering; the classification and saturation figures still come from the last dump alone; and no string the module emits makes a per-thread continuity claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer thread dump -> case store -> progression model | untrusted C++ symbol text and file paths become `frames` tuples and `source_file` values used as sort keys and join keys |
| out-of-repo customer capture -> committed test fixture | a 2.4 MB capture carrying customer host and environment identifiers sits one `cp` away from the repository |
| dump header timestamp -> ordering claim | an absent timestamp is an opportunity to fabricate one and present a guess as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-07 | Information disclosure | `tests/fixtures/eustack/progression/*` | high | mitigate | Fixtures authored from `derive_progression_fixtures.py` alone and reproducible from it; acceptance criterion greps the committed files for the real capture's host and environment identifiers and requires zero matches |
| T-17-08 | Spoofing | `resolve_dump_order` D-02 branch | high | mitigate | No clock read, no `stat`, no `fromtimestamp` anywhere in the module (grep-asserted); an untimestamped dump keeps `ts is None` and `ts_confidence == "missing"`, and the declared basis plus the `warn` flag make the weaker evidence visible rather than laundering it into an apparent chronology |
| T-17-09 | Repudiation | progression narrative strings | high | mitigate | `PROGRESSION_SCOPE_NOTE` states the population-level scope; `test_no_per_tid_claim_in_progression_strings` word-boundary-asserts no emitted string carries a thread-identifier token or continuity verb, with a non-vacuity guard so a refactor cannot turn it green by emitting nothing |
| T-17-10 | Tampering | `frames` tuple as sort and join key | low | mitigate | `frames` is compared and sorted, never `eval`-ed, interpolated into a shell command, or used as a path segment; the tuple never leaves the process except through the 17-03 renderer's escaping |
| T-17-11 | Denial of service | signature count on a large real capture | low | accept | The reference capture collapses 3,902 threads to 93 signatures, and the join is a dict lookup per signature per dump; no cap is invented, because capping would silently drop evidence from a forensic artefact |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no external package — `17-RESEARCH.md` records that none is introduced and no `pyproject.toml` change is in scope, so the package-legitimacy checkpoint has no subject |
</threat_model>

<verification>
- `uv run pytest tests/test_eustack_progression.py -x` — EUS-07 and EUS-08 in full
- `uv run pytest tests/test_cli.py -k phase5_e2e` — the new fixture subdirectory did not perturb existing event counts
- `uv run pytest` — full suite
- `uv run ruff check` and `uv run pyright` — the project's own "done" gate
- Manual (deferred to phase verification): ingest the real two-dump capture named in `17-CONTEXT.md` into a scratch case and confirm the D-02 basis and flag appear, since that capture carries no header timestamp and cannot be committed
</verification>

<success_criteria>
- A two-dump case reports per-signature population deltas and which populations advanced (ROADMAP criterion 2, second half; EUS-07)
- Dump ordering states its basis explicitly, and an unresolvable ordering is flagged loudly rather than assumed, with no timestamp invented (ROADMAP criterion 3; EUS-08)
- Progression is expressed as signature-population change with no unqualified per-thread causal claim anywhere (ROADMAP criterion 4)
- `ruff check`, `pyright` and `pytest` all clean
</success_criteria>

<output>
Create `.planning/phases/17-multi-dump-progression-sift-eustack-report-csv/17-02-SUMMARY.md` when done
</output>
