---
phase: 11-mcm-facts-into-sift-analyze-golden-eval-case
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/sift/cli.py
  - eval/cases/mcm-denial/input/hartford_deny_slice.log
  - eval/cases/mcm-denial/truth.yaml
  - eval/cases/mcm-denial/README.md
  - tests/test_eval_cases.py
  - tests/test_mcm_analyze.py
autonomous: true
requirements: [MCM-06, MCM-07]

must_haves:
  truths:
    - "sift analyze threads config.mcm.thresholds into hypothesise (no new CLI flag, D-17) so operator threshold overrides reach the injected MCM facts, composing with --kb (MCM-06)"
    - "A 7th golden case eval/cases/mcm-denial/ exists (dsserrors denial slice + frozen truth.yaml + README); sift eval discovers it, ingests it via dsserrors auto-sniff, and scores it as a positive case (MCM-07)"
    - "sift eval exits non-zero when the MCM case regresses below eval/thresholds.toml (EVAL-03)"
  artifacts:
    - eval/cases/mcm-denial/input/hartford_deny_slice.log
    - eval/cases/mcm-denial/truth.yaml
    - eval/cases/mcm-denial/README.md
    - src/sift/cli.py
    - tests/test_eval_cases.py
  key_links:
    - "cli.analyze passes mcm_thresholds=config.mcm.thresholds — the same McmThresholdsConfig sift mcm uses (cli.py:1036) — no new flag (D-17)"
    - "truth.yaml is authored/frozen BEFORE any prompt tuning (frozen-truth rule): a regression must fail, never be edited to pass"
    - "The golden fixture auto-sniffs as dsserrors (Contract Request Failed / Info Dump / [*.cpp:NNNN]) so ingest needs no --adapter override"
---

<objective>
Complete MCM-06 at the CLI surface and land MCM-07's regression gate. Thread
`config.mcm.thresholds` into `hypothesise` from `cli.analyze` (automatic, additive, no new flag —
D-17), and add the 7th golden eval case `eval/cases/mcm-denial/` reusing the committed
`hartford_deny_slice.log` (D-18) with a frozen `truth.yaml`, so `sift eval` scores the MCM case and
exits non-zero on regression (EVAL-03).

Purpose: MCM-06 requires the fact injection to be reachable from the real `sift analyze` command
with configured thresholds; MCM-07 requires an MCM golden case wired into the regression gate.
The eval path already exercises injection (Plan 02 built it inside `hypothesise`); this plan proves
it end-to-end through the harness and threads config through the CLI.
Output: `cli.analyze` threading, the `mcm-denial` golden case, and extended eval-discovery tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mcm-facts-into-sift-analyze-golden-eval-case/11-CONTEXT.md
@.planning/phases/11-mcm-facts-into-sift-analyze-golden-eval-case/11-RESEARCH.md
@.planning/phases/11-mcm-facts-into-sift-analyze-golden-eval-case/11-PATTERNS.md
@.planning/phases/11-mcm-facts-into-sift-analyze-golden-eval-case/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: thread config.mcm.thresholds into cli.analyze → hypothesise</name>
  <read_first>
    - src/sift/cli.py:844-861 — the `hypothesise(...)` call in `analyze` (add one kwarg alongside `kb_context=`)
    - src/sift/cli.py:1013-1036 — the `mcm` command's `analyse_mcm(store.query_events(), config.mcm.thresholds)` — the exact value to reuse
    - src/sift/config.py:98-116 — `McmConfig.thresholds` / `SiftConfig.mcm`
    - src/sift/pipeline/hypothesise.py:285-297 — the `hypothesise` signature now carrying `mcm_thresholds` (Plan 02)
    - tests/test_mcm_analyze.py — extend with the CLI-level assertion (owned across waves; Plan 03 is Wave 3, sequential after Plan 02)
  </read_first>
  <files>src/sift/cli.py, tests/test_mcm_analyze.py</files>
  <behavior>
    - Test (e2e, MockTransport): a `sift analyze` run (via CliRunner, fake OpenAI-compatible client, `_no_network` active) over a dsserrors case containing the denial slice surfaces MCM facts — the run persists a hypothesis able to cite an MCM `event_id` (or the assembled prompt carries MCM `[evt:]` lines), and the run exits with the success/degraded contract (never a crash).
    - Test (config threading): the value passed to `hypothesise` as `mcm_thresholds` is `config.mcm.thresholds` — assert an operator override in `[mcm.thresholds]` reaches the analyser (e.g. a lowered cut-point changes a flag tier surfaced in the prompt), proving no hard-coded default is used at the CLI.
  </behavior>
  <action>
    In `src/sift/cli.py` `analyze`, add `mcm_thresholds=config.mcm.thresholds` to the existing
    `hypothesise(...)` call (the same object the `mcm` command passes to `analyse_mcm` at cli.py:1036).
    Add no new CLI option and no `--mcm`/`--no-mcm` flag (D-17 — automatic and additive; injection is
    driven by whether `analyse_mcm` returns episodes, already handled inside `hypothesise`). Keep the
    call composable with `--kb` (both may be present). Extend `tests/test_mcm_analyze.py` with the
    CLI-level e2e (CliRunner + `httpx.MockTransport`) asserting MCM facts surface through the real
    `analyze` command, and a config-threading assertion that an `[mcm.thresholds]` override reaches the
    injected facts. Do not alter the exit-code contract.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mcm_analyze.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cli.analyze` calls `hypothesise(..., mcm_thresholds=config.mcm.thresholds)`; no new CLI flag added (D-17).
    - Behaviour: an analyze run over the dsserrors denial case surfaces MCM facts and can cite an MCM event; an `[mcm.thresholds]` override reaches the analyser.
    - Test command: `uv run pytest tests/test_mcm_analyze.py -x` green; `uv run ruff check` + `uv run pyright` clean.
  </acceptance_criteria>
  <done>cli.analyze threads config.mcm.thresholds (no new flag); e2e + config-threading tests green; composes with --kb.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCM golden eval case (mcm-denial) + frozen truth.yaml + eval-discovery test</name>
  <read_first>
    - eval/cases/memory-watermark-cascade/truth.yaml — the exact truth.yaml shape + frozen-truth header to mirror
    - src/sift/eval/truth.py:30-33 — the `Truth` schema fields (root_cause, required_evidence, acceptable_keywords, expect_no_incident), parsed via `yaml.safe_load` + `Truth(extra="forbid")`
    - src/sift/eval/runner.py:71-199 — `_run_pipeline` (calls `hypothesise` directly → MCM injected for free) and `run_case` scoring
    - src/sift/cli.py:1134-1139 — `sift eval` discovers sorted dirs containing `truth.yaml`; gate exits non-zero on regression (cli.py:1181-1190)
    - tests/fixtures/mcm/hartford_deny_slice.log — the source slice to copy (denial banner at 2026-04-07 12:39:47.230 `IServer enters MCM denial state`; `AvailableMCM`/`HWM(PB)` grant lines; working-set figures)
    - tests/test_eval_cases.py — the existing case-discovery/scoring assertions to extend
  </read_first>
  <files>eval/cases/mcm-denial/input/hartford_deny_slice.log, eval/cases/mcm-denial/truth.yaml, eval/cases/mcm-denial/README.md, tests/test_eval_cases.py</files>
  <behavior>
    - Test: `sift eval` (or the runner's case-discovery) finds `mcm-denial` among the sorted cases and it contains a parseable `truth.yaml` (yaml.safe_load + Truth(extra="forbid"), no custom tags).
    - Test: the case ingests via dsserrors auto-sniff (no `--adapter` override) — the ingested events include the denial banner and grant lines.
    - Test: `run_case` scores the MCM case as a positive case (`expect_no_incident: false`, not `run_failed`); the four metrics are computed. Offline runs bind `httpx.MockTransport` (EVAL-05); a live `sift eval` is the operator's CI gate.
  </behavior>
  <action>
    Create `eval/cases/mcm-denial/`: copy `tests/fixtures/mcm/hartford_deny_slice.log` verbatim to
    `input/hartford_deny_slice.log` (D-18 — reuse the committed, validated slice; no new fixture, no
    synthetic authoring). Author `truth.yaml` FROZEN before any prompt tuning (frozen-truth rule — a
    regression must fail, never be edited to pass), mirroring
    `eval/cases/memory-watermark-cascade/truth.yaml`: `expect_no_incident: false`; `root_cause` prose
    naming the MCM working-set-driven denial; `required_evidence` regexes matched against the
    exemplars/MCM lines fed to the model — assert the working-set flag line (a working-set percentage
    pattern), `AvailableMCM`, and the denial timestamp `2026-04-07 12:39:47`; `acceptable_keywords`
    any-of {memory, MCM, working set, denial}. Write `README.md` describing the case origin (redacted
    Hartford deny slice) and its known deterministic figures. Extend `tests/test_eval_cases.py` to
    assert the case is discovered, ingests via dsserrors, and scores as a positive case (not
    `run_failed`). Parse only with `yaml.safe_load` — no custom YAML tags (T-07-01). Choose
    `required_evidence` regexes conservatively so they match the deterministic MCM fact lines the
    analyser now feeds the model, not incidental log text.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_cases.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `eval/cases/mcm-denial/{input/hartford_deny_slice.log, truth.yaml, README.md}` exist; `truth.yaml` validates under `Truth(extra="forbid")` via `yaml.safe_load`, `expect_no_incident: false`.
    - Behaviour: the case is discovered by `sift eval`, ingests via dsserrors auto-sniff, and scores as a positive case (not run_failed); `sift eval` exits non-zero if its scores regress below thresholds (EVAL-03).
    - Test command: `uv run pytest tests/test_eval_cases.py -x` green; then full-suite `uv run pytest` + `uv run ruff check` + `uv run pyright` clean (phase gate).
  </acceptance_criteria>
  <done>7th golden case wired + frozen; sift eval discovers/ingests/scores it and gates on regression; eval-discovery test green; full suite + lint + typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| truth.yaml (on-disk test fixture) → eval harness | The golden case's YAML is deserialised by the eval runner |
| ingested denial-slice log text → analyze prompt | The golden fixture's log text flows through the same MCM injection path under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Elevation (YAML RCE) | eval/cases/mcm-denial/truth.yaml parsing | high | mitigate | Parsed with `yaml.safe_load` + `Truth(extra="forbid")` only (T-07-01, already enforced in eval/truth.py); the new case introduces no custom YAML tags. |
| T-11-06 | Repudiation (vacuous gate) | truth.yaml frozen-truth authoring | medium | mitigate | truth.yaml authored before prompt tuning; the MCM case is a positive case, and the existing gate fails a vacuously-empty positive aggregate (ADR 0010). A regression must turn the suite red. |
| T-11-04 | Tampering | prompt-injection via crafted MCM log text (CLI path) | medium | mitigate | Same sanitised renderer + untrusted-data framing as Plan 01/02; the CLI change only threads a config object, adds no new untrusted surface. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase (RESEARCH audit: not applicable); no install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_mcm_analyze.py -x` — CLI threading e2e + config threading
- `uv run pytest tests/test_eval_cases.py -x` — MCM golden case discovered, ingested via dsserrors, scored positive
- Phase gate: `uv run pytest` full suite + `uv run ruff check` + `uv run pyright` clean; a live/offline `sift eval` run shows the MCM case scored (not run_failed)
</verification>

<success_criteria>
- `sift analyze` threads config.mcm.thresholds into the MCM injection (no new flag, D-17); composes with --kb (MCM-06)
- `eval/cases/mcm-denial/` exists with a frozen truth.yaml; `sift eval` discovers, ingests (dsserrors), and scores it, exiting non-zero on regression (MCM-07/EVAL-03)
</success_criteria>

## Artifacts this phase produces (Plan 03 scope)

- `eval/cases/mcm-denial/input/hartford_deny_slice.log` — copied golden fixture (D-18)
- `eval/cases/mcm-denial/truth.yaml` — frozen ground truth (root_cause, required_evidence, acceptable_keywords, expect_no_incident:false)
- `eval/cases/mcm-denial/README.md` — case description + known deterministic figures
- `src/sift/cli.py` — `analyze` threads `mcm_thresholds=config.mcm.thresholds` (no new flag)
- `tests/test_eval_cases.py` — MCM-case discovery/ingest/scoring assertions
- `tests/test_mcm_analyze.py` — CLI-level analyze e2e + config-threading assertions

<output>
Create `.planning/phases/11-mcm-facts-into-sift-analyze-golden-eval-case/11-03-SUMMARY.md` when done
</output>
