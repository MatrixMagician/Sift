---
phase: 05-domain-adapters-journald-dsserrors-eustack
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/sift/adapters/journald.py
  - tests/test_journald.py
  - tests/fixtures/journald/basic.json
  - tests/fixtures/journald/field_types.json
autonomous: true
requirements: [INGST-07]
must_haves:
  truths:
    - "journald parses journalctl -o json JSONL (one object per line) into one Event per line, mapping PRIORITY 0-7 to the 6-value severity set, _SYSTEMD_UNIT to component, _PID/_COMM to attrs, _SYSTEMD_INVOCATION_ID to session"
    - "__REALTIME_TIMESTAMP (microseconds since epoch) becomes an aware UTC ts with ts_confidence=exact; an entry missing it parses with ts=None, ts_confidence=missing, and its bytes still count as covered"
    - "A field value that is a JSON string, null, an array of byte-integers (binary), or a value-array (repeated field) all normalise correctly via _field_to_str — a MESSAGE with an embedded NUL never stored as a Python list repr"
    - "A line that fails json.loads (or is not a JSON object) becomes one severity=unknown event whose bytes count as unknown_fallback_bytes; PRIORITY outside 0-7 or missing maps to unknown (never violates the store severity CHECK)"
    - "Parse coverage on a fixture with a deliberate malformed-line region is bounded (>=95 and <100 percent) — non-vacuous; event byte spans partition the file (contiguous from 0, summing to total_bytes); event_id is byte-offset deterministic (plain and gzip variants identical)"
    - "sniff returns ~0.95 on a journald head (signature keys __REALTIME_TIMESTAMP/__CURSOR/_BOOT_ID) and 0.0 otherwise"
  artifacts:
    - src/sift/adapters/journald.py
    - tests/test_journald.py
    - tests/fixtures/journald/basic.json
    - tests/fixtures/journald/field_types.json
  key_links:
    - "JournaldAdapter(ConfigurableAdapter) — inherits input_root/tz_overrides/last_stats so cli.py reads real coverage"
    - "_field_to_str normaliser — the one journald-specific piece with no in-repo analog (Pitfall 1)"
---

<objective>
Build the journald adapter (INGST-07): the cheapest, fully-specified vertical slice. `journalctl -o json` emits one self-contained JSON object per line with an authoritative µs-epoch UTC timestamp — no multi-line grouping, no encoding ladder. The only genuinely new code is the `_field_to_str` value normaliser (a journald field can be a string, null, a byte-int array, or a value-array). Everything else clones the genericlog byte-offset / ParseStats / coverage discipline.

Purpose: prove the domain-adapter pattern end-to-end on the simplest format and deliver INGST-07.
Output: `JournaldAdapter`, `_field_to_str`, handcrafted JSONL fixtures, and a full test file.
</objective>

## Phase Goal (MVP user story)

**As an** engineer triaging a MicroStrategy or systemd incident on an air-gapped host, **I want to** run `sift ingest` over journald JSON exports, DSSErrors logs, and EU-stack thread dumps, **so that** real production diagnostics become canonical UTC-ordered events — with nothing dropped silently and multi-node causality never inverted.

**This plan's slice:** a systemd journal export becomes canonical, severity-mapped, UTC-stamped events — with binary/NUL-bearing MESSAGE fields handled and coverage measured for real.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-RESEARCH.md
@.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handcraft journald JSONL fixtures exercising PRIORITY 0-7, field-type edge cases, and a malformed region</name>
  <files>tests/fixtures/journald/basic.json, tests/fixtures/journald/field_types.json</files>
  <read_first>
    - .planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-RESEARCH.md (Pattern 2 mapping table, Pattern 3 field-value normalisation, Wave 0 Gaps fixture list)
    - tests/test_genericlog.py (write_log/run_parse/assert_span_partition harness at lines 24-63 — the fixture and helper style to mirror)
  </read_first>
  <action>
    Author two handcrafted JSONL fixtures (one JSON object per line, UTF-8, newline-separated — do NOT shell out to journalctl; SPEC §5.2 forbids it and tests are offline).
    basic.json: a spread of normal entries covering PRIORITY values 0 through 7 (so severity mapping is exercised end to end), each with `__REALTIME_TIMESTAMP` (numeric string, µs since epoch), `MESSAGE`, and a mix of `_SYSTEMD_UNIT` present/absent (kernel-style entry with no unit), `_PID`, `_COMM`, and `_SYSTEMD_INVOCATION_ID`; include one entry with no `__REALTIME_TIMESTAMP`; append one deliberately malformed line (not valid JSON) sized to keep coverage in the bounded >=95 and <100 band relative to the file.
    field_types.json: entries whose `MESSAGE` is (a) a normal string, (b) an array of byte-integers 0-255 including an embedded 0 (binary/NUL content), (c) a value-array of repeated strings, and (d) a `null` field; plus a large-field `null` case per the >4096-byte rule.
    Keep values sanitised and synthetic. British English in any prose.
  </action>
  <verify>
    <automated>uv run python -c "import json,pathlib;[json.loads(l) for l in pathlib.Path('tests/fixtures/journald/field_types.json').read_text().splitlines() if l.strip() and not l.strip().startswith('MALFORMED')]" && test -s tests/fixtures/journald/basic.json</automated>
  </verify>
  <acceptance_criteria>
    - Both fixtures exist and are non-empty; field_types.json's well-formed lines each parse as a JSON object; basic.json covers PRIORITY 0-7, a missing-timestamp entry, a missing-unit entry, and one malformed line.
  </acceptance_criteria>
  <done>Complete when every acceptance-criterion above holds and the task's verify command exits 0 (RED→GREEN→gate→commit).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build JournaldAdapter (sniff + parse + _field_to_str + severity) test-first to green</name>
  <files>src/sift/adapters/journald.py, tests/test_journald.py</files>
  <read_first>
    - src/sift/adapters/genericlog.py (byte-line/offset discipline genericlog.py:367-374; ParseStats usage genericlog.py:331,339-342,433,451; _severity exhaustive-map shape genericlog.py:75-102; finish() Event builder genericlog.py:339-365)
    - src/sift/adapters/base.py (ConfigurableAdapter, ParseStats, open_bytes, read_head — after 05-01)
    - src/sift/models.py (frozen Event fields; event_id at models.py:38-47)
    - src/sift/store.py (severity CHECK at store.py:150 — the 6-value set)
    - tests/test_genericlog.py (run_parse/assert_span_partition harness to copy for tests/test_journald.py)
    - .planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-RESEARCH.md (Code Example 1 PRIORITY map; Pattern 3 _field_to_str; Pitfalls 1,4,6)
  </read_first>
  <behavior>
    - PRIORITY→severity: 0/1/2→fatal, 3→error, 4→warn, 5/6→info, 7→debug; missing or out-of-range or non-numeric→unknown (assert the full 0-7 range plus an invalid value; never emit a value outside the store CHECK set).
    - __REALTIME_TIMESTAMP (µs string)→aware UTC datetime, ts_confidence=exact; entry lacking it→ts=None, ts_confidence=missing, bytes still covered (not unknown_fallback).
    - _SYSTEMD_UNIT→component (None when absent); _PID→attrs["pid"], _COMM→attrs["comm"]; _SYSTEMD_INVOCATION_ID→session.
    - _field_to_str: str→verbatim; None→None; list of ints→bytes(...).decode("utf-8", errors="replace") (embedded-NUL MESSAGE decodes, never a list repr); value-array→newline-joined normalised parts; int→str.
    - A line failing json.loads or not a JSON object→one severity="unknown", ts=None event; its bytes count as unknown_fallback_bytes.
    - Coverage on basic.json bounded >=95 and <100 (non-vacuous); assert_span_partition holds (contiguous byte spans from 0 summing to total_bytes); event_id identical for plain vs gzip-compressed copy of the same fixture (byte-offset determinism, Pitfall 6).
    - sniff: journald head with a signature key→~0.95; a non-journald/plain-text head→0.0.
  </behavior>
  <action>
    Create src/sift/adapters/journald.py as `class JournaldAdapter(ConfigurableAdapter)` with `name = "journald"`. Import json, UTC/datetime, Path, and ConfigurableAdapter/ParseStats/open_bytes/read_head from base, plus Event/event_id from models.
    parse(): open via open_bytes; split at the BYTE level on b"\n" (journald is UTF-8 so a plain split suffices — no UTF-16 unit alignment), `offset += len(bline)`, `event_id(relpath, line_offset)`; compute relpath from self.input_root exactly as genericlog.py:319-321. For each line json.loads in try/except; on success map fields per the behaviour list (route every mapped textual field through `_field_to_str`); on failure build a severity="unknown", ts=None event and add its bytes to stats.unknown_fallback_bytes. Populate attrs["byte_offset"]/["byte_len"] for the span-partition invariant. Set stats.total_bytes = offset and self.last_stats = stats at the end (mirror genericlog.finish accounting).
    Add the exhaustive `_PRIORITY_SEVERITY` dict + `_severity(priority)` returning "unknown" on TypeError/ValueError or out-of-range (Code Example 1), and `_field_to_str` (Pattern 3). Anchor sniff on json.loads of the first non-blank decoded head line and the presence of a journald signature key.
    Write tests/test_journald.py mirroring the test_genericlog.py harness (a local run_parse building a fresh JournaldAdapter with input_root set, plus assert_span_partition). RED→GREEN. Do NOT register the adapter here (Wave 3 owns adapters/__init__.py). Type hints everywhere; annotate JSON-shaped locals as `dict[str, object]` to satisfy pyright strict (reportUnknownVariableType). British English.
  </action>
  <verify>
    <automated>uv run pytest tests/test_journald.py -q && uv run ruff check src/sift/adapters/journald.py tests/test_journald.py && uv run pyright src/sift/adapters/journald.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_journald.py is green and covers: full PRIORITY 0-7 + invalid→unknown; µs-epoch→UTC exact and missing-timestamp→missing-but-covered; _SYSTEMD_UNIT/_PID/_COMM/_SYSTEMD_INVOCATION_ID mapping; _field_to_str string/null/int-array(with NUL)/value-array; malformed line→unknown event byte-accounted; bounded coverage (>=95 and <100); span partition; plain-vs-gzip event_id determinism; sniff 0.95 vs 0.0.
    - No emitted severity is outside fatal|error|warn|info|debug|unknown.
    - ruff + pyright clean; adapters/__init__.py is untouched.
  </acceptance_criteria>
  <done>Complete when every acceptance-criterion above holds and the task's verify command exits 0 (RED→GREEN→gate→commit).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted journald JSON bytes → parse() → Event → store/show | Log content is untrusted input treated strictly as data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | DoS | oversized single JSON line | high | mitigate | Byte-level line splitting inherits genericlog's force-split at MAX_EVENT_BYTES; a monster line cannot slurp unbounded memory |
| T-05-11 | Tampering/DoS | malformed / partial JSON line (SIGKILL-truncated export) | medium | mitigate | json.loads in try/except → severity="unknown" byte-accounted event; never crash (fail-soft, "nothing disappears") |
| T-05-12 | Tampering | binary/NUL or non-UTF-8 MESSAGE stored as list repr | medium | mitigate | _field_to_str decodes int-arrays via bytes(...).decode(errors="replace"); Pitfall 1 test asserts it |
| T-05-13 | Tampering | PRIORITY outside 0-7 violating the severity CHECK | high | mitigate | Exhaustive map defaults to "unknown"; test the full range + an invalid value (Pitfall 4) |
| T-05-14 | Tampering/Spoofing | ANSI/terminal escape in message echoed by sift show | medium | mitigate | Existing whole-line _sanitise at render (Phase 2 WR-01) covers the new fields — verified at the Wave-3 e2e slice |
| T-05-SC | Tampering | package installs | low | accept | Zero external packages — stdlib json/datetime only |
</threat_model>

<verification>
- `uv run pytest tests/test_journald.py -q` green; then the full-suite gate `uv run pytest && uv run ruff check && uv run pyright`.
</verification>

<success_criteria>
- INGST-07 satisfied at adapter level: journald JSONL → Events with correct severity/component/attrs/session/ts and non-vacuous coverage; the registration + e2e ingest slice lands in the Wave-3 integration plan.
</success_criteria>

<output>
Create `.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-03-SUMMARY.md` when done.
</output>
