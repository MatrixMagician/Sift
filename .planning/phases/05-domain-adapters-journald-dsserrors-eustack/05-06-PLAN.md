---
phase: 05-domain-adapters-journald-dsserrors-eustack
plan: 06
type: execute
wave: 3
depends_on: [05-03, 05-04, 05-05]
files_modified:
  - src/sift/adapters/__init__.py
  - tests/test_adapters_detect.py
  - tests/test_cli.py
autonomous: true
requirements: [INGST-07, INGST-08, INGST-09]
must_haves:
  truths:
    - "journald, dsserrors and eustack are registered in adapters.REGISTRY; detect() (unchanged) routes each fixture to its own adapter, each beats genericlog's 0.1, and no two domain adapters collide on the same fixture — SPEC §5.2 'new module + registration only' now holds"
    - "sift new → sift ingest → sift show events works end-to-end for each of the three formats: canonical events land in the store and are queryable"
    - "sift ingest prints the REAL per-file parse coverage for each domain adapter (a fixture with a deliberate unparseable region shows a value below 100%, never a fabricated 100.0%)"
    - "Re-running sift ingest on the same case adds zero new events for all three formats (idempotent, INGST-02 preserved for the new adapters)"
    - "Untrusted fields (message/session/thread/attrs) flow through the existing whole-line _sanitise on sift show, so terminal-escape content in a domain-adapter event cannot corrupt the terminal"
  artifacts:
    - src/sift/adapters/__init__.py
    - tests/test_cli.py
  key_links:
    - "adapters/__init__.py REGISTRY — the only file that changes to add all three adapters; detect() logic is NOT touched"
    - "e2e CliRunner ingest slice — proves the 05-01 non-vacuous coverage fix with REAL domain fixtures, not just the stub"
---

<objective>
Wire the three new adapters into the registry and prove the end-to-end vertical slice for each format via the CLI. This is the integration wave: it holds the only cross-adapter shared files (`adapters/__init__.py`, `tests/test_adapters_detect.py`, `tests/test_cli.py`) so the three adapter plans (05-03/04/05) stay file-disjoint and parallel. `detect()` already dispatches generically over `REGISTRY.values()` — only three registry lines are added; its logic is not touched.

Purpose: land INGST-07/08/09 end-to-end (sniff → parse → store → show) and prove the non-vacuous coverage fix with real domain fixtures.
Output: three registry entries, detect-routing tests, and CliRunner e2e ingest/idempotency/coverage tests per format.
</objective>

## Phase Goal (MVP user story)

**As an** engineer triaging a MicroStrategy or systemd incident on an air-gapped host, **I want to** run `sift ingest` over journald JSON exports, DSSErrors logs (and their rotated `.bak` siblings), and EU-stack thread dumps, **so that** real production diagnostics become canonical UTC-ordered events — with nothing dropped silently and multi-node causality never inverted.

**This plan's slice:** the three adapters become reachable through `sift ingest`, and each format's full round-trip (ingest → show, with real coverage and idempotency) is proven at the CLI boundary.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-RESEARCH.md
@.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three adapters and add detect-routing tests (no cross-collision, each beats genericlog)</name>
  <files>src/sift/adapters/__init__.py, tests/test_adapters_detect.py</files>
  <read_first>
    - src/sift/adapters/__init__.py (REGISTRY at __init__.py:15-17; detect() at __init__.py:54-85 — do NOT change detect() logic, only add registry lines)
    - tests/test_adapters_detect.py (DummyAdapter + registry save/restore fixture at lines 13-35 — extend, mirror existing cases)
    - src/sift/adapters/journald.py, dsserrors.py, eustack.py (the sniff() each exposes, from 05-03/04/05)
    - .planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-PATTERNS.md (__init__.py registry section)
  </read_first>
  <action>
    In adapters/__init__.py add the three imports (JournaldAdapter, DsserrorsAdapter, EustackAdapter) and three REGISTRY entries ("journald"/"dsserrors"/"eustack"). Do NOT modify detect(), parse_adapter_overrides, or SNIFF_THRESHOLD — the generic dispatch is the SPEC §5.2 invariant this phase finally makes true.
    Extend tests/test_adapters_detect.py: for each of the three phase-5 fixtures, assert detect() routes it to its own adapter, that its sniff score beats genericlog's 0.1, and that no two domain adapters both claim the same fixture (no cross-collision → unique max, so detect returns the intended adapter). Name the new cases so `pytest -k phase5` selects them (per 05-VALIDATION.md). British English.
  </action>
  <verify>
    <automated>uv run pytest tests/test_adapters_detect.py -q && uv run ruff check src/sift/adapters/__init__.py tests/test_adapters_detect.py && uv run pyright src/sift/adapters/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - REGISTRY contains genericlog + journald + dsserrors + eustack; detect() logic is byte-unchanged.
    - test_adapters_detect.py asserts each phase-5 fixture routes to its own adapter, beats genericlog, and no cross-collision occurs.
    - ruff + pyright clean.
  </acceptance_criteria>
  <done>Complete when every acceptance-criterion above holds and the task's verify command exits 0 (RED→GREEN→gate→commit).</done>
</task>

<task type="auto">
  <name>Task 2: CliRunner e2e ingest slices per format — real coverage (not 1.0), idempotent re-ingest, show + sanitise</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py (existing CliRunner ingest/show e2e patterns to mirror)
    - src/sift/cli.py (ingest coverage print at cli.py:362-365; _sanitise at cli.py:56; show command)
    - tests/fixtures/journald/, tests/fixtures/dsserrors/, tests/fixtures/eustack/ (the fixtures from 05-03/04/05)
    - .planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-RESEARCH.md (Pitfall 2 non-vacuous coverage; Validation Architecture E2E slice row)
  </read_first>
  <action>
    Extend tests/test_cli.py with a CliRunner e2e per format: `sift new` a case pointed at the fixture dir, `sift ingest`, then `sift show events`. Assert (a) canonical events are stored and shown for each format; (b) the ingest coverage line for the fixture carrying a deliberate unparseable region reports a value strictly below 100% — the real ParseStats.coverage, proving the 05-01 fix on real domain fixtures (not the stub); (c) a second `sift ingest` on the same case adds zero new events (idempotency, INGST-02); (d) an event whose message/attrs contain a terminal-escape byte is rendered through _sanitise on `sift show` (no raw ESC reaches stdout). Reuse the dsserrors multi-node fixture so node tagging is visible in show output. British English.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "journald or dsserrors or eustack" -q && uv run ruff check tests/test_cli.py && uv run pyright tests/test_cli.py</automated>
  </verify>
  <acceptance_criteria>
    - test_cli.py e2e proves, per format: new→ingest→show round-trip; real coverage below 100% on the unparseable-region fixture (never fabricated 100.0%); second ingest adds 0 new events; escape bytes sanitised on show.
    - Full suite `uv run pytest && uv run ruff check && uv run pyright` green (the SPEC §8 M5 gate).
  </acceptance_criteria>
  <done>Complete when every acceptance-criterion above holds and the task's verify command exits 0 (RED→GREEN→gate→commit).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter registry → sift ingest routing | A mis-registration could route a file to the wrong parser silently |
| domain-adapter event fields → sift show render | Untrusted message/attrs/session/thread must not reach the terminal unsanitised |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-40 | Repudiation | e2e ingest coverage report for domain adapters | high | mitigate | e2e asserts real coverage below 100% on an unparseable-region fixture — the fabricated-100% bug cannot regress unnoticed |
| T-05-41 | Tampering/Spoofing | terminal-escape content in domain-adapter fields echoed by sift show | medium | mitigate | e2e asserts the existing whole-line _sanitise (WR-01) covers the new fields — escape bytes never reach stdout raw |
| T-05-42 | Tampering | wrong-adapter routing (sniff collision) | medium | mitigate | detect-routing test asserts unique max per fixture and no cross-collision; genericlog stays the fallback |
| T-05-SC | Tampering | package installs | low | accept | Zero external packages this phase — stdlib only |
</threat_model>

<verification>
- `uv run pytest && uv run ruff check && uv run pyright` fully green — the SPEC §8 M5 "done" gate.
- All four Phase-5 roadmap success criteria are demonstrable: journald/dsserrors/eustack coverage + mapping (criteria 1-3, criteria 1-2 e2e here, adapter-level in 05-03/04/05), and criterion 4 (mixed-tz timeline, proven in 05-04).
</verification>

<success_criteria>
- INGST-07/08/09 land end-to-end: three registered adapters, generic detect routing, and CLI round-trips with real coverage and idempotency for every format.
</success_criteria>

## Artifacts this phase produces

New symbols and files introduced by this plan:

- `src/sift/adapters/__init__.py` — three new `REGISTRY` entries (`"journald"`/`"dsserrors"`/`"eustack"`) plus their imports (`detect()`, `parse_adapter_overrides`, `SNIFF_THRESHOLD` unchanged).
- `tests/test_adapters_detect.py` — new phase-5 detect-routing test cases (each fixture routes to its own adapter, beats genericlog's 0.1, no cross-collision; `pytest -k phase5`).
- `tests/test_cli.py` — new per-format CliRunner e2e tests (new→ingest→show round-trip, real coverage <100% on the unparseable-region fixture, idempotent second ingest, `_sanitise` on show).

<output>
Create `.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-06-SUMMARY.md` when done.
</output>
