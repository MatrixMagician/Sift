---
phase: 05-domain-adapters-journald-dsserrors-eustack
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [INGST-08, INGST-09]
must_haves:
  truths:
    - "The eustack format identity (elfutils eu-stack native vs JVM-style thread dump) is confirmed by the MicroStrategy domain authority BEFORE any eustack regex is frozen — this decides whether lock/blocked-on attrs exist at all"
    - "The dsserrors record line layout and SID token shape are confirmed against a real sanitised MicroStrategy sample BEFORE the dsserrors regexes freeze"
    - "A sanitised, network-free sample (or explicit confirmation of the RESEARCH assumed shapes A1/A2/A3) exists for dsserrors and eustack fixtures to be authored from"
  artifacts: []
  key_links:
    - "Checkpoint output gates 05-04 (dsserrors) and 05-05 (eustack) — their fixtures and regexes must match the confirmed shapes"
---

<objective>
Resolve the two `[ASSUMED]` format questions the RESEARCH flags as blocking (A2 dsserrors SID/line layout, A3 eustack eu-stack-vs-JVM) with the user, who is the MicroStrategy domain authority. dsserrors and eustack are proprietary formats whose exact byte layout varies by product version; their parser regexes must not be frozen from memory. This checkpoint gathers a user-confirmed sanitised sample (or explicit confirmation of the assumed shapes) so 05-04 and 05-05 can author fixtures and anchor regexes against ground truth.

Purpose: prevent 05-04/05-05 from shipping regexes tuned to a guessed layout, which would silently mis-extract SIDs/OIDs/lock-info.
Output: a recorded decision (format identity + token shapes) captured in this plan's SUMMARY for the downstream adapter plans to consume.
</objective>

## Phase Goal (MVP user story)

**As an** engineer triaging a MicroStrategy or systemd incident on an air-gapped host, **I want to** run `sift ingest` over journald JSON exports, DSSErrors logs (and their rotated `.bak` siblings), and EU-stack thread dumps, **so that** real production diagnostics become canonical UTC-ordered events — with nothing dropped silently and multi-node causality never inverted.

**This plan's slice:** pin the proprietary-format ground truth so the dsserrors and eustack slices parse the real thing, not an assumption.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-RESEARCH.md
@.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm dsserrors line layout / SID shape and eustack format identity before regexes freeze</name>
  <files>(none — human confirmation only; no source files change)</files>
  <action>
    Present the two assumed proprietary-format shapes below to the user (the MicroStrategy domain authority) and block until they confirm or correct them, or explicitly reply "use assumed shapes A1/A2/A3". Do NOT write any adapter regex or fixture in this plan — capture the confirmed decision in this plan's SUMMARY so 05-04 (dsserrors) and 05-05 (eustack) freeze against ground truth. This is a blocking human-verify gate and is never auto-approvable.
  </action>
  <what-built>
    RESEARCH pinned the reliable, version-stable structural tokens for both proprietary formats but flagged three assumptions that cannot be frozen from memory (05-RESEARCH.md Assumptions Log A1/A2/A3 and Open Questions 1-2):
    - dsserrors (INGST-08): the representative record line begins with an offset-bearing timestamp and carries `[SourceFile.cpp:NNNN]` source-location tokens, `0x` error codes, GUID-shaped OIDs, MCM `***** Start/End of Info Dump *****` block sentinels, bracketed severity tags, and a bracketed thread ID. The exact field ORDER and the SID token shape (`session`) are MicroStrategy-version-dependent and assumed.
    - eustack (INGST-09): assumed to be elfutils `eu-stack` native per-thread backtraces (`TID <n>:` headers, `#N 0xADDR symbol` frames) with NO lock info — the alternative is a JVM-style thread dump which DOES carry `parking to wait for` / `waiting to lock` / `locked` lock info. SPEC explicitly wants "blocked-on / lock info where present", so the format identity decides whether those attrs exist.
  </what-built>
  <how-to-verify>
    1. Confirm the eustack format identity: are the real MicroStrategy Intelligence Server artefacts elfutils `eu-stack` native backtraces (no lock info) or JVM-style thread dumps (rich lock info)? State which. If unsure, provide one sanitised thread-dump sample (secrets/hostnames/IPs/SIDs masked) — no live capture needed.
    2. Confirm the dsserrors record layout: paste or point to a sanitised `DSSErrors.log` snippet (a few normal lines + one MCM Info Dump block + a `.bak` sibling), OR confirm the RESEARCH representative layout (Pattern 4) is close enough to anchor token-based extraction. Confirm the SID token shape used for `session` (long hex / GUID-like / other).
    3. Confirm whether the MCM/thread multi-line blocks and node-from-directory tagging match your real bundles (node = first path component under the case input dir).
    4. If you cannot provide a sample right now, reply "use assumed shapes A1/A2/A3" — 05-04/05-05 will freeze against the RESEARCH representative layout, flag the assumption in the adapter docstring + ADR, and remain tolerant/token-anchored so a later fixture correction is a small change.
  </how-to-verify>
  <resume-signal>Reply with (a) eustack format = eu-stack | jvm, (b) dsserrors sample provided | use-assumed-shapes, and (c) SID token shape — or type "approved: use assumed shapes A1/A2/A3" to proceed on the RESEARCH defaults.</resume-signal>
  <done>Complete when the user has confirmed the dsserrors layout/SID shape and eustack format identity (or replied "use assumed shapes A1/A2/A3") and the decision is recorded in 05-02-SUMMARY.md for 05-04/05-05 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human sample → repository fixtures | A pasted "sanitised" sample could still carry real SIDs/hostnames/IPs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information disclosure | user-provided dsserrors/eustack fixture sample | medium | mitigate | Require the sample be SANITISED (mask hostnames/IPs/SIDs) before it enters tests/fixtures; 05-04/05-05 must not commit unmasked production data |
| T-05-SC | Tampering | package installs | low | accept | No packages installed; checkpoint is human-verification only |
</threat_model>

<verification>
- A recorded decision (eustack format identity, dsserrors layout/SID confirmation or explicit "use assumed shapes") exists in this plan's SUMMARY before 05-04/05-05 execute.
</verification>

<success_criteria>
- The eustack format identity is decided (eu-stack vs JVM) and the lock-info expectation is set.
- The dsserrors layout + SID shape are confirmed or explicitly defaulted to RESEARCH assumptions with the debt flagged.
</success_criteria>

<output>
Create `.planning/phases/05-domain-adapters-journald-dsserrors-eustack/05-02-SUMMARY.md` recording the confirmed format decisions for 05-04/05-05 to consume.
</output>
