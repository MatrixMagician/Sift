---
phase: 16-saturation-contention-signature-collapse
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - tests/test_eustack_rules.py
  - docs/decisions/0016-eustack-saturation-analysis.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EUS-03, EUS-04, EUS-05, EUS-06]
must_haves:
  truths:
    - "The healthy reference capture's MEASURED composition — 3,902 threads, 52 unclassified (1.33%), zero no-resolvable-frame, zero `__lll_lock_wait` matches — raises zero flags above `info` against the shipped defaults, which is D-09's gate on those defaults, not an aspiration."
    - "The committed derivative fixture is signature-preserving, not thread-weight-preserving: its cap policy inflates the unclassified thread share from 1.33% to 38.1%, so it cannot serve as the D-09 gate for the thread-weighted ratio flags, and the test that runs it says so in its own docstring rather than being silently loosened."
    - "No shipped default was chosen to make a fixture pass: raising `unclassified_thread_pct.warn` above 38.1% to accommodate the capped fixture is explicitly rejected and recorded as rejected."
    - "Every graded flag prints its raw computed value beside its configured warn and critical cut-points, on the same record, for all three flag families (Success Criterion 5, D-08)."
    - "The derivative fixture raises zero `no_resolvable_frame_pct` and zero `lock_convergence_count` flags above `info` — the two flags it CAN faithfully exercise (D-09)."
    - "Zero thread events, and an analysis where every role bucket is empty, yield a zero-valued result with all fields present and an empty flags tuple, never an exception."
    - "The lock convergence cut-points are recorded as uncalibrated: no real hung-server capture exists, so they are conservative round numbers awaiting evidence, not measured thresholds."
    - "All figures are computed model-free with no LLM call anywhere in the phase (D-12), and the phase adds zero dependencies."
    - "EUS-03, EUS-04, EUS-05 and EUS-06 are each closed at the library level; `sift eustack` rendering remains Phase 17's job and fact injection Phase 18's."
  artifacts:
    - "docs/decisions/0016-eustack-saturation-analysis.md"
    - "tests/test_eustack_rules.py :: test_measured_reference_composition_raises_zero_flags"
    - "tests/test_eustack_rules.py :: test_reference_derivative_zero_flags"
  key_links:
    - "EustackThresholdsConfig defaults -> analyse_saturation() -> SaturationFlag.severity: the D-09 gate runs the shipped defaults, not test-local overrides, so a future default change fails this test."
    - "ADR 0016 -> docs/decisions/: the five open questions research and pattern-mapping left unsettled are recorded where a future maintainer looks, not only in a planning artefact."
---

<objective>
Close Phase 16: prove D-09's zero-flags gate against the reference capture's
measured composition, prove Success Criterion 5's value-beside-threshold contract
across all three flag families, and record the phase's settled design decisions
in an ADR.

Purpose: D-09 is a verification gate on the chosen defaults — if a default fires
on a healthy capture, the default is wrong. That gate has a trap: the committed
CI fixture caps thread counts, so its unclassified share reads 38.1% against the
real capture's 1.33%. Loosening a default to accommodate the fixture would ship a
threshold calibrated against an artefact of the fixture's cap policy. This plan
handles that honestly instead.

Output: the two D-09 gate tests, the Success-Criterion-5 coverage test,
`docs/decisions/0016-eustack-saturation-analysis.md`, and EUS-03 through EUS-06
ticked in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-saturation-contention-signature-collapse/16-CONTEXT.md
@.planning/phases/16-saturation-contention-signature-collapse/16-03-SUMMARY.md
@src/sift/pipeline/eustack.py
@docs/decisions/0015-eustack-thread-role-taxonomy.md
</context>

## S-8 — The D-09 gate cannot run on the committed derivative fixture (settled at plan time)

Measured during planning, by running the shipped Phase 15 analyser over
`tests/fixtures/eustack/reference_capture_derivative.txt`:

| Figure | Committed derivative | Real reference capture | Ratio |
|---|---|---|---|
| total threads | 105 | 3,902 | — |
| unclassified threads | 40 | 52 | — |
| **unclassified THREAD share** | **38.10%** | **1.33%** | **28x inflated** |
| no-resolvable-frame threads | 0 (0.00%) | subset of the 52 | — |
| `blocked-on-lock` signatures | 0 | 0 | — |

The cause is structural, not a fixture defect: the derivative preserves all 93
signatures but caps thread counts at 1 per signature (5 for the three
highest-population ones). Forty of the 93 signatures are unclassified, and the
three capped signatures are all classified — so capping deflates the classified
population by roughly 3,000 threads while leaving the unclassified population
nearly intact. The fixture is faithful to signature composition and deliberately
unfaithful to thread weight.

Two resolutions were considered and one is rejected outright:

- **Rejected: raise `unclassified_thread_pct.warn` above 38.1%** so the fixture
  passes. That ships a threshold calibrated against a cap policy rather than
  against a server, and it makes the flag useless — rules drift would have to
  consume more than 40% of a real thread population before warning. A default
  chosen to make a test pass is the failure mode D-09 exists to prevent.
- **Rejected: re-derive a thread-weight-faithful fixture.** Only three of the 93
  raw per-signature thread counts (1,715 / 1,110 / 247) are recorded anywhere in
  the repository, so the remaining 90 cannot be reconstructed without the
  out-of-repo capture. A fixture built from invented weights would be exactly the
  "written to match the detector" artefact `.planning/research/PITFALLS.md`
  Pitfall 5 names.

**Settled: split the gate across two tests.**
`test_reference_derivative_zero_flags` runs the committed fixture and asserts
zero warn-or-critical flags for the two families the fixture CAN faithfully
exercise — `no_resolvable_frame_pct` (genuinely 0.0% there) and
`lock_convergence_count` (genuinely zero sites, because Rule 6 matches a healthy
capture zero times by design). It asserts the `unclassified_thread_pct` flag's
value equals 38.1 and states in its own docstring, with the arithmetic, that this
figure is a cap-policy artefact and is deliberately excluded from the gate.
`test_measured_reference_composition_raises_zero_flags` is the real D-09 gate: it
constructs an `EustackAnalysis` directly at the real capture's MEASURED
composition (3,902 threads, 52 unclassified, zero no-resolvable-frame, zero
`blocked-on-lock`) — figures taken from ADR 0015 and CONTEXT.md, not from the
detector — and asserts every flag grades `info` against the SHIPPED defaults.

Constructing the input model directly rather than synthesising 3,902 events is
the right level: `analyse_saturation()` is the unit under test and
`EustackAnalysis` is its public, frozen input contract. The numbers come from
measured reality upstream of the code being tested, so this is not a fixture
shaped to agree with its detector.

## Artifacts this plan produces

New artefacts (excluded from drift verification):

**Documentation**
- `docs/decisions/0016-eustack-saturation-analysis.md` (new ADR)

**Tests (`tests/test_eustack_rules.py`)**
- `test_reference_derivative_zero_flags`
- `test_measured_reference_composition_raises_zero_flags`
- `test_every_flag_family_prints_value_beside_threshold`

**Planning**
- EUS-03, EUS-04, EUS-05, EUS-06 ticked in `.planning/REQUIREMENTS.md`

<tasks>

<task type="auto">
  <name>Task 1: The D-09 zero-flags gate and Success Criterion 5 coverage</name>

  <files>tests/test_eustack_rules.py</files>

  <read_first>
    - `src/sift/pipeline/eustack.py` — `EustackAnalysis`, `SignatureGroup`, `SaturationFlag` and the complete `analyse_saturation()` as built across 16-01 through 16-03, so the constructed input matches the real field set exactly.
    - `src/sift/config.py` — `EustackThresholdsConfig`'s shipped defaults, which this gate must use unmodified; a test-local override would make the gate meaningless.
    - `docs/decisions/0015-eustack-thread-role-taxonomy.md` — the day-one coverage figures (98.67% thread / 56.99% signature, 52 unclassified threads of 3,902) that are the source of the constructed composition.
    - `.planning/phases/16-saturation-contention-signature-collapse/16-CONTEXT.md` § Specific Ideas — the reference-capture figures the phase is measured against.
    - `tests/test_eustack_rules.py` — the existing tests, so the new ones follow the same import and assertion style and reuse `_parse_derivative_fixture()`.
  </read_first>

  <action>
Add three tests to `tests/test_eustack_rules.py`. All three must call
`analyse_saturation()` with `EustackThresholdsConfig()` constructed with NO
arguments, so they gate the shipped defaults; a test-local threshold override
would silently disconnect the gate from what ships.

`test_reference_derivative_zero_flags` — run the committed derivative fixture
through `analyse_eustack()` then `analyse_saturation()`. Assert no
`no_resolvable_frame_pct` flag grades above `info`, and assert no
`lock_convergence_count` flag exists at all (the fixture yields zero lock sites,
because Rule 6 matches a healthy capture zero times by design). Assert the
`unclassified_thread_pct` flag IS present and its `value` equals 38.1. The
docstring must carry S-8's arithmetic in full: 40 unclassified threads of 105 in
the derivative versus 52 of 3,902 in the real capture, a 28-fold inflation caused
by the fixture's cap policy of 1 thread per signature (5 for the three
highest-population ones); that the fixture is signature-preserving and
deliberately not thread-weight-preserving; and that this flag family is therefore
excluded from the derivative's half of the D-09 gate and covered by
`test_measured_reference_composition_raises_zero_flags` instead. State explicitly
that raising the shipped default to accommodate the fixture was considered and
rejected, so a future reader does not "fix" this by loosening the threshold.

`test_measured_reference_composition_raises_zero_flags` — the real D-09 gate.
Construct an `EustackAnalysis` directly, with `SignatureGroup` rows summing to
the real capture's measured composition: 3,902 total threads of which 52 are
`unclassified` with `reason="matched-no-rule"` and `subsystem=None`, zero threads
carrying `reason="no-resolvable-frame"`, zero `blocked-on-lock` signatures, and
the remaining 3,850 threads spread across at least two classified subsystems
including a large `idle-parked` `job-queue` population — the ~3,400 parked pool
workers that must read as idle rather than saturated. Populate
`threads_by_role` and `signatures_by_role` with all five role keys present, since
`analyse_saturation()` indexes `threads_by_role["unclassified"]` directly. Run
`analyse_saturation()` with the shipped defaults and assert EVERY flag's
`severity` is `info` — zero flags at warn or critical, which is D-09's gate.
Assert the specific figures too: `unclassified_thread_pct` value is 1.3 (52 /
3,902 * 100 rounded to 1 dp), `no_resolvable_frame_pct` value is 0.0, and no
`lock_convergence_count` flag exists. The docstring must name ADR 0015 and
CONTEXT.md as the source of these figures — measured upstream of the code under
test, not reverse-engineered from it — and state that constructing the input
model directly is deliberate because `EustackAnalysis` is `analyse_saturation()`'s
public frozen input contract.

`test_every_flag_family_prints_value_beside_threshold` — Success Criterion 5
across all three families. Build one input that emits at least one flag of each
`dimension`: reuse the synthetic lock-convergence construction from 16-02 for
`lock_convergence_count`, and a mix carrying both residual reasons for the two
ratio flags. Assert the emitted set of `dimension` values covers all three. For
each flag assert four things: `value` is a `float`; `warn` and `critical` are
both populated and equal the corresponding `EustackThresholdsConfig` pair for
that `dimension`; `unit` is `"percent"` for the two ratio flags and `"threads"`
for the count flag; and `severity` equals `_grade(value, warn, critical)`
recomputed independently in the test, so a flag whose severity disagrees with its
own printed value and thresholds fails. Add an assertion that the flag list is
non-empty before iterating, so the test cannot pass vacuously.
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_rules.py -k "reference_derivative_zero_flags or measured_reference_composition or flag_family_prints_value" -x</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_eustack_rules.py -k "reference_derivative_zero_flags or measured_reference_composition or flag_family_prints_value" -x` exits 0 and collects exactly 3 tests.
    - `test_measured_reference_composition_raises_zero_flags` constructs `EustackThresholdsConfig()` with no arguments — it gates the shipped defaults.
    - Against the constructed 3,902-thread / 52-unclassified composition, every emitted flag has `severity == "info"`; the `unclassified_thread_pct` flag's `value` is 1.3.
    - Against the committed derivative fixture, no `lock_convergence_count` flag exists and the `no_resolvable_frame_pct` flag has `severity == "info"`.
    - `test_every_flag_family_prints_value_beside_threshold` asserts a non-empty flag list before iterating and covers all three `dimension` values.
    - `uv run pytest` exits 0 — full suite green.
    - `uv run ruff check` and `uv run pyright` both exit 0.
  </acceptance_criteria>

  <done>D-09's gate runs against the reference capture's measured composition with the shipped defaults and passes with margin, the derivative fixture's cap-policy artefact is documented rather than papered over, and every flag family is proven to carry value beside threshold.</done>
</task>

<task type="auto">
  <name>Task 2: ADR 0016 and requirement closure</name>

  <files>docs/decisions/0016-eustack-saturation-analysis.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `docs/decisions/0015-eustack-thread-role-taxonomy.md` — the immediately preceding ADR: its section structure, its tone, its habit of recording rejected alternatives and known limitations openly, and its statement of the permanent ownership-blind non-goal that ADR 0016 builds on rather than restates.
    - `docs/decisions/0013-dsserrors-qualified-mcm-sniff.md` — the qualified-name anchoring precedent D-04's namespace test follows.
    - `.planning/phases/16-saturation-contention-signature-collapse/16-01-PLAN.md` § "Settled design decisions" (S-1 through S-6), `16-02-PLAN.md` § S-7, and this plan's § S-8 — the eight decisions the ADR records.
    - `.planning/REQUIREMENTS.md` — the EUS-03..EUS-06 checkboxes and the Traceability table rows to update.
  </read_first>

  <action>
Write `docs/decisions/0016-eustack-saturation-analysis.md`, following ADR 0015's
structure and British-English register. It records the decisions Phase 16 settled
that research and pattern-mapping had left open, so a future maintainer finds
them in `docs/decisions/` rather than only in a planning artefact. Cover, each
with its rationale and its rejected alternatives:

S-1, extending `src/sift/pipeline/eustack.py` in place rather than adding a
sibling `eustack_saturation.py` — the `mcm.py`/`perfmon.py` whole-domain-in-one-
file precedent, and the second reason that matters more: keeping Phase 16 inside
`eustack.py` puts it under the shipped ownership-blind source guard with no
second edit site for a security-relevant invariant.

S-2, importing `mcm._grade` as-is with the `pyright: ignore[reportPrivateUsage]`
convention already established for `_condense_symbol`, rather than promoting it
to a shared home; note the verified absence of an import cycle.

S-3, minting one `SaturationFlag` for all three flag families rather than
reusing `mcm.DiagnosticFlag` for the two ratios. State the binding constraint
correctly: `DiagnosticFlag.value_pct` is locked as a ratio by a milestone
machine-independence invariant, so the lock-convergence thread count cannot go
in it; `perfmon.PerfmonHazard` is the in-repo precedent for minting a sibling
rather than bending the contract. Record the two reasons for going further than
the precedent and using ONE record for all three: Success Criterion 5 needs the
cut-points on the record, and a `DiagnosticFlag | SaturationFlag` union would
burden every downstream consumer. Record the deliberate absence of `event_ids`
and that resolving an aggregate figure to a citable event set is Phase 18's open
question.

S-4, the default cut-points with the arithmetic against the reference capture,
and the honest calibration status: the two ratio pairs rest on a single real
capture and the count pair has no calibration data at all because no real
hung-server capture exists.

S-5, the thread-weighted denominator for both ratio flags, and why: a long tail
of one-thread signatures is a curation backlog rather than an operational signal,
and the signature-weighted figure (43.01%) differs from the thread-weighted one
(1.33%) by more than an order of magnitude, so the choice is load-bearing.

S-6, flags emitted at every severity including `info` so a healthy value is
still printed, which makes "zero flags" mean "zero flags above `info`"; and a
signal with a zero denominator emitting no flag rather than a fabricated figure.

S-7, enforcing the three-term ownership-blind prohibition over emitted strings
while leaving the shipped whole-source grep scoped to the single term
REQUIREMENTS.md names, and why the shipped rules file's prose comment is
documentation rather than output.

S-8, the derivative fixture's 28-fold inflation of the unclassified thread share
and the two-test split of the D-09 gate, including both rejected resolutions.

Also record, in a "Known limitations" section mirroring ADR 0015's own habit:
the enclosing-frame denylist covers the two third-party namespaces measured in
the reference capture plus two defensive entries, so a C++ runtime namespace
outside those four would still be reported as a lock site; and that no per-pool
occupancy flag ships because EUSV2-03 is deferred and no authoritative source
exists for "N% busy = warning". Do not use any of the three D-05-prohibited terms
except where quoting REQUIREMENTS.md's own non-goal wording is unavoidable —
prefer writing around them, and check the shipped ownership-blind test still
passes afterwards since it reads REQUIREMENTS.md at runtime.

Then update `.planning/REQUIREMENTS.md`: tick the EUS-03, EUS-04, EUS-05 and
EUS-06 checkboxes and set their Traceability table rows to `Complete`. Note in
the ticking that closure is at the library level — the same level EUS-01 and
EUS-02 closed at in Phase 15 — with `sift eustack` rendering remaining Phase 17's
job and fact injection Phase 18's. Do not alter the Out of Scope table's
"Deadlock detection" row or its `the word "..."` phrasing: `tests/test_eustack_rules.py`
parses that exact phrasing at runtime to extract the forbidden term, so editing
it would break the shipped guard.
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_rules.py -k ownership_blind -x && uv run pytest</automated>
  </verify>

  <acceptance_criteria>
    - `docs/decisions/0016-eustack-saturation-analysis.md` exists and contains a section for each of the eight settled decisions S-1 through S-8 and a "Known limitations" section.
    - `uv run pytest tests/test_eustack_rules.py -k ownership_blind -x` exits 0 — the runtime extraction from REQUIREMENTS.md still finds its term after the edit.
    - `grep -c '^- \[x\] \*\*EUS-0[3456]\*\*' .planning/REQUIREMENTS.md` returns 4.
    - `.planning/REQUIREMENTS.md`'s Traceability table shows `Complete` for EUS-03, EUS-04, EUS-05 and EUS-06.
    - `uv run pytest` exits 0 — full suite green.
    - `uv run ruff check` and `uv run pyright` both exit 0.
  </acceptance_criteria>

  <done>The eight decisions Phase 16 settled are recorded in `docs/decisions/` where a maintainer will look for them, and all four phase requirements are closed at the library level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped `EustackThresholdsConfig` defaults -> operator's flag readings | A default calibrated against a test artefact rather than a server silently mis-grades every real capture |
| `.planning/REQUIREMENTS.md` prose -> the runtime forbidden-term extraction | A documentation edit can silently disarm a shipped security-relevant guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Repudiation | Shipped threshold defaults calibrated to a fixture rather than a server | high | mitigate | The D-09 gate runs `EustackThresholdsConfig()` with no arguments against the reference capture's MEASURED composition, so a default changed to make a test pass fails the gate. Raising `unclassified_thread_pct.warn` above the derivative's cap-policy-inflated 38.1% is recorded as an explicitly rejected resolution in both the test docstring and ADR 0016, so a future maintainer meets the reasoning before repeating the mistake. |
| T-16-10 | Tampering | Editing REQUIREMENTS.md's Out of Scope wording disarms the runtime term extraction | medium | mitigate | The `the word "..."` phrasing is left untouched by the requirement-ticking edit, and `uv run pytest tests/test_eustack_rules.py -k ownership_blind -x` is run after the edit as an acceptance criterion, so a disarmed guard fails immediately rather than at some later phase. |
| T-16-11 | Repudiation | A vacuously-green gate test asserting over an empty flag list | medium | mitigate | `test_every_flag_family_prints_value_beside_threshold` asserts the flag list is non-empty and that all three `dimension` values are covered before iterating, mirroring the eval gate's rule that a vacuous 1.0 is never a pass. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan — zero new dependencies (16-RESEARCH.md § Package Legitimacy Audit records the gate as not applicable). No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `uv run pytest` — full suite green, the phase gate per 16-VALIDATION.md.
- `uv run ruff check` and `uv run pyright` clean — CLAUDE.md's definition of "done".
- Manual, out-of-repo (16-VALIDATION.md § Manual-Only Verifications): run the
  analysis against the real capture at
  `/home/oliverh/Downloads/iserver1_stacks_1-minute_diff/` and confirm the named
  figures — ~3,400 parked pool workers reading as idle, 79 warehouse waits, 78
  HTTP waits, 3,902 threads collapsing to 93 signatures, and zero raised flags.
  CI asserts the shape; the real capture asserts the numbers.

<human-check>
After the full suite is green, run the analysis against the out-of-repo reference
capture and confirm all five reference figures. This is the only place the
absolute numbers can be verified, because the committed fixture caps thread
counts by design.
</human-check>
</verification>

<success_criteria>
- The reference capture's measured composition raises zero flags above `info`
  against the shipped defaults (D-09, Roadmap Success Criterion 5).
- Every graded flag prints its raw computed value beside its configured
  threshold, across all three flag families (Success Criterion 5, D-08).
- The derivative fixture's cap-policy artefact is documented in the test that
  meets it, and loosening a default to accommodate it is recorded as rejected.
- ADR 0016 records all eight settled decisions plus the phase's known
  limitations, including that no per-pool occupancy flag ships (D-07,
  EUSV2-03 deferred).
- EUS-03, EUS-04, EUS-05 and EUS-06 are closed at the library level; no CLI
  command, no rendering and no LLM call was added (D-12).
- Zero new dependencies.
- `ruff check`, `pyright` and `pytest` all clean.
</success_criteria>

<output>
Create `.planning/phases/16-saturation-contention-signature-collapse/16-04-SUMMARY.md` when done.
</output>
</content>
