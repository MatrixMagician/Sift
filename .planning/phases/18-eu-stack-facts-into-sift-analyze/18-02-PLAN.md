---
phase: 18-eu-stack-facts-into-sift-analyze
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/sift/pipeline/eustack_facts.py
  - src/sift/prompts/eustack_facts.md
  - tests/test_eustack_facts.py
autonomous: true
requirements: [EUS-10]

must_haves:
  truths:
    - "The fact block reports all four Phase 16 groupings — role composition, per-pool occupancy, lock-site convergence and external-wait concentration — each as a summary line carrying its own `[evt:]` exemplar citations (SC1, D-05)."
    - "A multi-signature aggregate unions every contributing signature's event pool BEFORE taking the three lowest `event_id`s, yielding exactly one exemplar triple per aggregate row (SC4, D-17)."
    - "Every aggregate figure quoted in the block resolves to a concrete `event_id` set whose members all exist in `store.query_events()` (SC4)."
    - "The sampling sentence beside each aggregate states both the number of exemplars cited and that aggregate's own true population, so the citation set is never read as an enumeration (D-03)."
    - "The per-signature listing renders at most `_MAX_SIGNATURES = 8` signatures, ranked most-populous-first, and states the count of further signatures not shown whenever any are dropped (D-06, D-07)."
    - "The cap is composition-independent: a signature is never force-included because it carries a saturation flag (D-08)."
    - "A healthy capture that raises zero saturation flags still renders a full, non-empty fact block — emptiness is gated on `bundle.analysis.total_threads == 0` alone."
    - "`src/sift/prompts/eustack_facts.md` still contains zero ASCII digits after the new sections are added (D-16)."
  prohibitions:
    - statement: "MUST NOT present a sampled exemplar citation set as if the full population had been enumerated — every aggregate figure that carries `[evt:]` exemplars also states the exemplar count and the true population size."
      category: transparency
    - statement: "MUST NOT silently drop signatures beyond the rendered cap — the count of signatures not shown is stated in the block whenever any are dropped."
      category: transparency
    - statement: "MUST NOT assert per-thread continuity, lock possession, or a wait-for-graph conclusion in any emitted string — eu-stack carries no per-thread identity link across dumps and no monitor-ownership edges, so such a claim is unsupportable by construction."
      category: safety
  flagged_assumptions:
    - "EDGE PROBE UNRESOLVED (1 of 1): the deterministic edge probe classified EUS-10 as `unclassified` / `unresolved`. Never auto-backstopped, never dismissed — carried forward from Plan 18-01. The nearest coverage for its most plausible missing category (an adjacency/cardinality edge on the one-to-many signature→event_id join) is `test_multi_signature_aggregate_unions_before_sampling` and `test_sampling_sentence_states_true_population` in this plan."
  artifacts:
    - src/sift/pipeline/eustack_facts.py
    - src/sift/prompts/eustack_facts.md
    - tests/test_eustack_facts.py
  key_links:
    - "`PoolOccupancy` / `LockSite` / `DependencyWait` carry no `frames` list, so the renderer re-groups `bundle.analysis.signatures` by `.subsystem` (pools, dependencies) and by `enclosing_application_frame(...)` (lock sites) — the same predicates `analyse_saturation` already applies."
    - "`bundle.analysis.signatures` is already sorted thread-count-descending by `analyse_eustack` (`eustack.py:453`), so the `_MAX_SIGNATURES` cap is a plain slice with no new sort."
    - "`_union_exemplars` from Plan 18-01 is the single D-17 entry point — pools, lock sites and dependencies all route through it."
---

<objective>
Expand the proven tracer slice out to the full deterministic surface: all four Phase 16
groupings as cited summary lines under D-17 union-then-sample-3 exemplar selection, plus the
capped, drop-disclosing per-signature listing.

Purpose: this is where success criterion 4 is actually earned — every aggregate figure the
block quotes must resolve to a real, verifiable `event_id` set, and the D-03 sampling sentence
must stay honest against each aggregate's own population, not a signature's.

Output: `render_eustack_facts` emits pool-occupancy, lock-site and dependency-wait lines
alongside the role composition, plus the `_MAX_SIGNATURES`-capped signature listing with an
explicit dropped-count statement; six new unit tests pin the contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-eu-stack-facts-into-sift-analyze/18-CONTEXT.md
@.planning/phases/18-eu-stack-facts-into-sift-analyze/18-PATTERNS.md
@.planning/phases/18-eu-stack-facts-into-sift-analyze/18-01-SUMMARY.md
@src/sift/pipeline/perfmon_facts.py
@docs/decisions/0016-eustack-saturation-analysis.md
</context>

<artifacts_this_phase_produces>
New in this plan (excluded from drift verification — none of these exist before Phase 18):

**Added to `src/sift/pipeline/eustack_facts.py`**
- Constant `_MAX_SIGNATURES = 8`
- Helpers `_pool_lines`, `_lock_site_lines`, `_dependency_lines`, `_signature_listing_lines`,
  `_sampling_sentence`

**Added to `src/sift/prompts/eustack_facts.md`**
- Section prose for the pool-occupancy, lock-site and dependency-wait groupings and for the
  capped signature listing (labels and prose only, zero ASCII digits)

**Added to `tests/test_eustack_facts.py`**
- `test_exemplar_ids_exist_in_store`, `test_sampling_sentence_states_true_population`,
  `test_multi_signature_aggregate_unions_before_sampling`,
  `test_signature_cap_states_dropped_count`, `test_zero_flag_capture_still_renders_block`,
  `test_block_byte_identical_on_rerun`

Symbols this plan CONSUMES but does not create (they already ship): `PoolOccupancy`,
`LockSite`, `DependencyWait`, `SaturationAnalysis`, `SignatureGroup`,
`enclosing_application_frame`, `UNKNOWN_LOCK_SITE`, `LOCK_FINDING_NOTE`, `signature_of`,
`group_dumps`, `sanitise`.
</artifacts_this_phase_produces>

<reversibility>
  <decision rating="costly">
    `_MAX_SIGNATURES = 8` and the four-grouping render order freeze the block's byte shape.
    Changing either later invalidates the frozen prompt-hash constants from Plan 18-01 and any
    Phase 19 golden fixtures built on this output. Costly, not one-way — no persisted artifact,
    no migration.
  </decision>
  <decision rating="reversible">
    Section wording, heading text and whether the four groupings render as separate sections or
    one table are explicitly Claude's discretion per CONTEXT.md and live entirely in the
    versioned template — changing them touches no Python.
  </decision>
</reversibility>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four Phase-16 groupings as cited aggregate lines, unioned before sampling (D-05, D-17)</name>

  <files>
src/sift/pipeline/eustack_facts.py,
src/sift/prompts/eustack_facts.md,
tests/test_eustack_facts.py
  </files>

  <read_first>
- `src/sift/pipeline/eustack_facts.py` — the module as shipped by Plan 18-01, especially
  `_union_exemplars`, `_signature_event_ids`, `_events_by_dump_in_order` and `_cite_prefix`.
- `src/sift/pipeline/eustack.py` lines 474-640 — `SaturationFlag`, `PoolOccupancy`, `LockSite`,
  `DependencyWait`, `SaturationAnalysis`, `analyse_saturation`'s own grouping predicates, and
  `enclosing_application_frame` (line 263) with its D-03/D-04 walk semantics.
- `src/sift/pipeline/perfmon_facts.py` lines 116-212 — the per-group line-emission and
  salient-subset selection shape.
- `src/sift/prompts/perfmon_facts.md` — the section prose and slot layout to mirror.
- `docs/decisions/0016-eustack-saturation-analysis.md` — the definitions of occupancy,
  ownership-blind lock-site convergence and external-wait concentration, so the rendered prose
  matches the analyser's own semantics rather than inventing a second vocabulary.
- `tests/test_perfmon_facts.py` — the unit-test module structure to mirror.
  </read_first>

  <behavior>
Tests written before the implementation, in `tests/test_eustack_facts.py`:
- `test_exemplar_ids_exist_in_store`: for a bundle built from a real ingested store, every id
  in the returned set is an `event_id` present in `store.query_events()`. Non-vacuity: the set
  is asserted non-empty first.
- `test_sampling_sentence_states_true_population`: for each aggregate line carrying an
  exemplar triple, the parenthetical states an exemplar count equal to the number of `[evt:]`
  tokens on that line and a population equal to that aggregate row's own thread count as read
  from the model (`PoolOccupancy.total_threads`, `LockSite.thread_count`,
  `DependencyWait.thread_count`) — not a contributing signature's count.
- `test_multi_signature_aggregate_unions_before_sampling`: construct a synthetic bundle whose
  one pool is backed by two signatures with disjoint, interleaved id ranges; assert the pool
  line carries exactly three `[evt:]` tokens, and that those three are the three lowest ids of
  the UNION — specifically, assert the result differs from the lowest-three-per-signature
  concatenation, so the test cannot pass under the rejected strategy.
- `test_zero_flag_capture_still_renders_block`: a bundle with `saturation.flags == ()` but
  `analysis.total_threads > 0` renders a non-empty block containing pool lines.
- `test_block_byte_identical_on_rerun`: rendering the same bundle twice returns identical text
  and an identical sorted id list.
  </behavior>

  <action>
Add the three remaining Phase 16 groupings as summary lines, all routed through the existing
`_union_exemplars` helper so D-17 holds uniformly.

**Per-pool occupancy (EUS-03).** For each `PoolOccupancy` row in
`bundle.saturation.pools`, resolve its contributing signatures by selecting every
`SignatureGroup` in `bundle.analysis.signatures` whose `.subsystem` equals the row's
`.subsystem` — including the `None` key for the single unclassified row, compared as `None`
and never stringified, because a literal "None" subsystem string could collide with a real one.
Union their frames tuples through `_union_exemplars`, then emit a line naming the subsystem,
the row's `busy_threads`, `idle_threads`, `total_threads`, `occupancy` and `signature_count`,
followed by the D-03 sampling sentence keyed on `total_threads`. Format `occupancy` to a fixed
decimal width so float repr noise cannot vary between runs.

**Lock-site convergence (EUS-04).** For each `LockSite` row in `bundle.saturation.lock_sites`,
resolve contributing signatures by calling `enclosing_application_frame(group.frames,
group.frame_index)` for every `blocked-on-lock` signature group and matching the returned site
against the row's `.site`, substituting the same `UNKNOWN_LOCK_SITE` sentinel the analyser
substitutes when the walk returns nothing — reuse the shipped constant, do not re-declare the
string. Union and cite as above. Emit `bundle.saturation.lock_finding_note` **once**, before or
after the lock-site lines, so the ownership-blind label appears exactly once per block by
construction. The emitted lines report only that threads were observed converging at a site;
they must not state or imply that any thread holds or owns a lock, nor that a wait-for graph
was derived — eu-stack carries no such edges. Mirror the banned-vocabulary discipline already
enforced by `tests/test_eustack_report.py` around line 410 (its `continuity_verbs` tuple) and
add an equivalent assertion over the strings this renderer emits.

**External-wait concentration (EUS-05).** For each `DependencyWait` row in
`bundle.saturation.dependencies`, resolve contributing signatures as the `blocked-on-external`
groups whose `.subsystem` equals the row's `.subsystem` — grouping on subsystem, never on the
matched pattern text, because two distinct rules can share one subsystem and must aggregate
into one row. Union, cite and emit with the sampling sentence keyed on the row's
`thread_count`.

**Saturation flags.** Emit each `SaturationFlag` in `bundle.saturation.flags` as a graded line
carrying its `dimension`, `severity`, `value`, `unit`, `warn` and `critical`, so the model sees
the computed figure beside its configured threshold. A flag has no event ids of its own; give
it the exemplar triple of the aggregate it grades, or omit the flag line entirely if no
exemplar can be resolved — never print an uncited figure.

Extract the repeated parenthetical into a single `_sampling_sentence(k: int, population: int)
-> str` helper so the D-03 wording is defined once and cannot drift between the four groupings.

Do NOT add a cap to any of these four groupings — D-06 caps only the per-signature listing, and
inventing an extra cap here would contradict the locked decision. The theoretical unboundedness
of the lock-site and dependency lists under a much wider rules file is recorded as accepted risk
`T-18-05` in the threat model, not silently capped.

Every subsystem, site, role, dimension and message string routes through `sanitise` before
interpolation (V5). Extend `src/sift/prompts/eustack_facts.md` with the section prose for the
new groupings — labels and wording only, **still zero ASCII digits anywhere in the file**.
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_facts.py -x -q</automated>
    <automated>uv run pytest tests/test_eustack_facts.py::test_multi_signature_aggregate_unions_before_sampling tests/test_eustack_facts.py::test_sampling_sentence_states_true_population -x -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_eustack_facts.py -x -q` exits 0 with at least 5 new test functions passing.
- `uv run pytest -q` (full suite) exits 0.
- `uv run ruff check` reports no findings and `uv run pyright` reports 0 errors.
- `python3 -c "import pathlib,sys; t=pathlib.Path('src/sift/prompts/eustack_facts.md').read_text(); sys.exit(1 if any(ch.isdigit() for ch in t) else 0)"` exits 0.
- `grep -c 'UNKNOWN_LOCK_SITE\|LOCK_FINDING_NOTE\|lock_finding_note' src/sift/pipeline/eustack_facts.py` returns at least 2 — the shipped constants are imported, not re-declared as literals.
- `grep -c 'def _sampling_sentence' src/sift/pipeline/eustack_facts.py` returns exactly 1 — the D-03 wording has a single definition site.
- `grep -c 'enclosing_application_frame' src/sift/pipeline/eustack_facts.py` returns at least 1 — the shipped walk is reused, not re-implemented.
- Rendering the reference-derivative fixture bundle twice produces byte-identical text (asserted by `test_block_byte_identical_on_rerun`).
- The rendered block for a zero-flag healthy fixture is non-empty and contains at least one pool line (asserted by `test_zero_flag_capture_still_renders_block`).
- `test_multi_signature_aggregate_unions_before_sampling` is proven fail-first: temporarily switching `_union_exemplars` to lowest-three-per-signature-concatenated makes it fail; revert after demonstrating.
  </acceptance_criteria>

  <done>
All four Phase 16 groupings render as cited summary lines; each aggregate's exemplar triple is
the three lowest ids of the unioned contributing pools; each carries a sampling sentence keyed
on its own population; every cited id exists in the store; the template still holds no authored
digit; the change is committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capped per-signature listing with an explicit dropped-count statement (D-06, D-07, D-08)</name>

  <files>
src/sift/pipeline/eustack_facts.py,
src/sift/prompts/eustack_facts.md,
tests/test_eustack_facts.py
  </files>

  <read_first>
- `src/sift/pipeline/eustack_facts.py` — the module as extended by Task 1.
- `src/sift/pipeline/perfmon_facts.py` lines 72-78 and 129-135 — the `_MAX_GROUPS = 8` constant
  with its `ponytail:` ceiling note and the cap-then-slice comment.
- `src/sift/pipeline/mcm_facts.py` — `_MAX_EPISODES = 8` and its severity-rank-then-slice shape.
- `src/sift/pipeline/eustack.py` around line 453 — the explicit total sort
  `(-thread_count, frames)` applied to `EustackAnalysis.signatures`, which is why this cap needs
  no new sort.
- `tests/test_perfmon_facts.py` — the cap-and-drop test shape.
  </read_first>

  <behavior>
Tests written before the implementation, in `tests/test_eustack_facts.py`:
- `test_signature_cap_states_dropped_count`: build a bundle carrying strictly more than eight
  signatures (the committed `reference_capture_derivative.txt` fixture holds 93). Assert the
  rendered block contains exactly eight per-signature rows, that the eight rendered are the
  eight highest by thread count, and that the block contains an explicit statement naming the
  number of further signatures not shown, whose figure equals `total_signatures - 8`.
- Same test asserts that a flagged signature ranked below the cut is NOT force-included — the
  eight rendered rows are determined by thread count alone (D-08).
- A parametrised case with exactly eight or fewer signatures asserts NO dropped-count sentence
  is emitted (the statement must not appear when nothing was dropped).
  </behavior>

  <action>
Add `_MAX_SIGNATURES = 8` to `src/sift/pipeline/eustack_facts.py`, immediately above the
per-signature listing helper, with a comment in the same shape as
`perfmon_facts._MAX_GROUPS`'s `ponytail:` note: a fixed ceiling matching
`mcm_facts._MAX_EPISODES` and `perfmon_facts._MAX_GROUPS`, one consistent ceiling across all
four fact modules, swappable for budget-aware trimming only if real cases demand it (D-06).

Render the per-signature listing as a plain slice `bundle.analysis.signatures[:_MAX_SIGNATURES]`
— no new sort. `analyse_eustack` already applies the explicit total order
`(-thread_count, frames)`, so a re-sort here would be a second ordering that could silently
drift from the analyser's. Do not add any composition-dependent selection: a signature carrying
a saturation flag is not force-included when it falls below the cut, because a
composition-dependent cap makes block length depend on case composition, which is the exact
determinism hazard D-08 rejects.

Each rendered signature row cites its own exemplar triple via `_union_exemplars` over a
single-element frames collection — reuse the same helper rather than a separate single-signature
path, so signature rows and aggregate rows can never disagree about which dump an exemplar came
from. Emit the row's `thread_count`, `role`, `subsystem`, `pattern`, `reason` and the matched
frame text (index into `group.frames` at `group.frame_index` when it is not `None`), each
`sanitise`d, followed by the D-03 sampling sentence keyed on that signature's own
`thread_count`.

Compute `dropped = bundle.analysis.total_signatures - len(selected)` and, when `dropped > 0`,
emit exactly one explicit statement naming that figure and the total — never a silent
truncation, per the project's nothing-disappears-silently invariant (D-07). When `dropped == 0`
emit no such statement at all, so a small case's block carries no misleading zero.

Extend `src/sift/prompts/eustack_facts.md` with the signature-listing section prose. The
dropped-count sentence's *figures* are interpolated by Python; only its surrounding wording may
live in the template, and the file must still contain **zero ASCII digits**.
  </action>

  <verify>
    <automated>uv run pytest tests/test_eustack_facts.py::test_signature_cap_states_dropped_count -x -q</automated>
    <automated>uv run pytest tests/test_eustack_facts.py tests/test_eustack_analyze.py -q</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_eustack_facts.py::test_signature_cap_states_dropped_count -x -q` exits 0.
- `uv run pytest -q` (full suite) exits 0.
- `uv run ruff check` reports no findings and `uv run pyright` reports 0 errors.
- `grep -c '_MAX_SIGNATURES = 8' src/sift/pipeline/eustack_facts.py` returns exactly 1.
- `grep -v '^\s*#' src/sift/pipeline/eustack_facts.py | grep -c 'signatures.*sort(\|sorted(bundle.analysis.signatures' ` returns 0 — the analyser's existing total order is reused, never re-derived (comment lines stripped first so the explanatory note about not re-sorting cannot trip the gate).
- `python3 -c "import pathlib,sys; t=pathlib.Path('src/sift/prompts/eustack_facts.md').read_text(); sys.exit(1 if any(ch.isdigit() for ch in t) else 0)"` exits 0.
- Rendering the 93-signature `reference_capture_derivative.txt` bundle produces exactly 8 signature rows and a dropped-count statement naming 85.
- Rendering a bundle with 8 or fewer signatures produces no dropped-count statement — asserted by the parametrised branch of the same test.
- `test_signature_cap_states_dropped_count` is proven fail-first: temporarily raising `_MAX_SIGNATURES` to 100 makes it fail; revert after demonstrating.
  </acceptance_criteria>

  <done>
The per-signature listing is capped at eight most-populous signatures with no re-sort and no
composition-dependent force-inclusion; dropped signatures are stated as dropped whenever any
exist and never mentioned when none do; the template still holds no authored digit; the change
is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rules-file `subsystem` / frame-derived `site` strings → prompt | Curator-controlled and log-derived text interpolated into an LLM prompt. |
| Store → aggregate exemplar resolution → `prompted_ids` | The one-to-many join that decides which ids become citable. |

## STRIDE Threat Register (ASVS L1; blocking severity: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Elevation of Privilege | `_pool_lines` / `_lock_site_lines` / `_dependency_lines` / `_signature_listing_lines` | high | mitigate | Every subsystem, site, role, pattern, matched-frame, dimension and message string routes through `sift.render._util.sanitise` before interpolation; the template's verbatim untrusted-data framing paragraph is unchanged. The dedicated `test_control_chars_sanitised` lands in Plan 18-03. |
| T-18-04 | Spoofing | `_union_exemplars` | high | mitigate | Only ids printed as `[evt:]` tokens enter the returned set; `test_exemplar_ids_exist_in_store` proves every one is a real stored `event_id`, and the pre-existing `_all_cited_within` gate rejects anything else. |
| T-18-07 | Information Disclosure | the D-03 sampling sentence | high | mitigate | A sampled citation set presented without disclosure would misrepresent a three-event sample as an enumerated population — a transparency failure the citation validator cannot detect. `_sampling_sentence` is a single-definition-site helper and `test_sampling_sentence_states_true_population` pins the exemplar count and each aggregate's own population figure. |
| T-18-08 | Information Disclosure | the `_MAX_SIGNATURES` cap | high | mitigate | Silently truncating the signature listing would let the model reason over a partial population believing it complete. `test_signature_cap_states_dropped_count` pins the explicit dropped-count statement and is proven fail-first. |
| T-18-05 | Denial of Service | uncapped pool / lock-site / dependency lists | medium | accept | D-06 caps only the per-signature listing. On real data these lists are small (a handful of subsystems; the healthy reference capture raises zero lock sites). A rules file wide enough to blow this up is out of scope for v1.3. Recorded here rather than silently capped, because inventing an extra cap would contradict the locked decision. |
| T-18-SC | Tampering | package-manager installs | n/a | accept | No package is installed in this phase; `18-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (never silently dropped)

Carried forward from Plan 18-01 — the deterministic edge probe returned exactly one applicable
row for EUS-10, classified `unclassified` / `unresolved`. It is neither auto-`backstop`ped nor
dismissed. This plan carries the nearest available coverage for its most plausible missing
category, an adjacency/cardinality edge on the one-to-many signature→event_id join:
`test_multi_signature_aggregate_unions_before_sampling` and
`test_sampling_sentence_states_true_population`.

No-silent-drop equality holds: 1 probe-surfaced item == 1 flagged assumption.
</flagged_assumptions>

<verification>
- `uv run ruff check` — no findings.
- `uv run pyright` — 0 errors.
- `uv run pytest` — full suite green, including the Plan 18-01 byte-identity gates (this plan
  changes the eu-stack block's bytes, so the EUSTACK-ONLY and ALL-THREE hashes shift; the three
  frozen no-eu-stack constants must be UNCHANGED — if they move, the block is leaking outside
  its sentinels).
- `uv run pytest tests/test_eustack_facts.py -q` — six or more new tests green.
- Both fail-first demonstrations (per-signature exemplar concatenation in Task 1, raised cap in
  Task 2) were run and reverted; record the observed failure output in the SUMMARY.
</verification>

<success_criteria>
- All four Phase 16 groupings appear in the block, each with `[evt:]` exemplars and a sampling
  sentence keyed on that aggregate's own population.
- A multi-signature aggregate's exemplar triple is provably the three lowest ids of the union.
- Every printed id exists in `store.query_events()`.
- The signature listing is capped at eight and states its dropped count when any are dropped.
- A zero-flag healthy capture still renders a full block.
- The template still contains zero ASCII digits.
- Lint, type check and full test suite clean.
</success_criteria>

<output>
Create `.planning/phases/18-eu-stack-facts-into-sift-analyze/18-02-SUMMARY.md` when done.
Record: the exact rendered block for the reference-derivative fixture (or its first ten lines),
its total character length, and the observed output of both fail-first demonstrations.
</output>
