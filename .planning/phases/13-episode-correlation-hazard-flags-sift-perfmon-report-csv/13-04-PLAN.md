---
phase: 13-episode-correlation-hazard-flags-sift-perfmon-report-csv
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - src/sift/pipeline/perfmon.py
  - tests/test_perfmon.py
autonomous: true
requirements: [PERF-05]

must_haves:
  truths:
    - "D-06: zero perfmon samples inside the resolved span raises the non-overlap hazard, never a silently empty trend table — zero-in-span IS the wrong-timezone / wrong-host / wrong-day symptom, so it is the loud flag rather than an absence of data."
    - "D-12: all three hazards are `PerfmonHazard` instances carrying `dimension`, `severity`, `message`, `event_ids` and an optional numeric `value`; `mcm.DiagnosticFlag` is neither reused nor relaxed."
    - "D-13: severities are fixed in code, not config — non-overlap is `critical`, always-zero `Total MCM Denial` is `warn`, counter-set drift is `warn`. These are categorical facts with no number to tune, so `mcm._grade` is deliberately not called."
    - "D-14: the always-zero `Total MCM Denial` hazard fires only when detected denials exist in the window; with no episodes there is no hazard, because without a denial to contradict a zero counter is just a zero counter."
    - "D-15: the counter-set-drift hazard reads the per-event `_DRIFT_ATTR` marker plan 13-03 adds to `attrs`, giving each flag real citable `event_ids`; drift is never re-detected at correlation time."
    - "D-16: `Total MCM Denial` is a reported flag only and is never used as a correlation input — no branch of span resolution, sample selection or figure computation reads it."
    - "The non-overlap hazard message names both time ranges — the span the log defined and the range the CSV actually covers — so the reader can diagnose which mismatch it is; and it states that the hazard covers time non-overlap only, not host identity, since `McmEpisode` events carry no host field to compare against."
    - "Hazards are emitted in a fixed code order with equal-severity hazards in a stable, explicitly-ordered sequence, so two runs produce identical hazard tuples."
    - "A group with no hazards carries an empty tuple rather than a sentinel, and an analysis with no episodes carries no always-zero hazard at all."
  prohibitions:
    - "The correlator must never present a fabricated or inferred alignment as a real one — where the artefacts do not overlap it must raise the non-overlap hazard rather than emitting figures computed from whatever samples happen to be nearby."
  artifacts:
    - src/sift/pipeline/perfmon.py
  key_links:
    - "`_DRIFT_ATTR` (plan 13-03) → the counter-set-drift hazard's evidence and `event_ids`"
    - "collision-qualified counter keys (plan 13-03) → the `Total MCM Denial` counter lookup"
    - "`PerfmonAnalysis.hazards` and `TrendGroup.hazards` → the hazard table in plan 13-05's renderer"
---

<objective>
Add the three PERF-05 hazards to `src/sift/pipeline/perfmon.py`: time non-overlap, always-zero
`Total MCM Denial`, and counter-set drift.

Purpose: this is the phase goal's second half — the engineer is warned loudly when the two artefacts
cannot honestly be joined. A silently empty trend table is the failure mode these hazards exist to
prevent.

Output: three hazard builders wired into `analyse_perfmon`, and the four PERF-05 unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-CONTEXT.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-RESEARCH.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-02-SUMMARY.md
@.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-03-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan (new symbols — not pre-existing):

| Symbol | File | Kind |
|--------|------|------|
| `HAZARD_NON_OVERLAP` | `src/sift/pipeline/perfmon.py` | module constant (dimension name) |
| `HAZARD_DENIAL_ALWAYS_ZERO` | `src/sift/pipeline/perfmon.py` | module constant (dimension name) |
| `HAZARD_COUNTER_SET_DRIFT` | `src/sift/pipeline/perfmon.py` | module constant (dimension name) |
| `MCM_DENIAL_COUNTER` | `src/sift/pipeline/perfmon.py` | module constant (the counter short name) |
| `_hazard_non_overlap` | `src/sift/pipeline/perfmon.py` | private function |
| `_hazard_denial_always_zero` | `src/sift/pipeline/perfmon.py` | private function |
| `_hazard_counter_set_drift` | `src/sift/pipeline/perfmon.py` | private function |
| `_find_counter_key` | `src/sift/pipeline/perfmon.py` | private function |

Created by OTHER plans in this phase (excluded from drift verification):
`PerfmonHazard`, `CounterTrend`, `TrendGroup`, `PerfmonAnalysis`, `analyse_perfmon`, `_numeric`,
`_resolve_span`, `_counter_trends`, `SLOPE_DP` (plan 13-02); `_DRIFT_ATTR`, `_NOTE_CAP`,
`_qualify_counter_names` (plan 13-03); `src/sift/render/perfmon_report.py` and its functions
(plan 13-05); `PerfmonFormat` and the `perfmon` CLI command (plan 13-06).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The non-overlap hazard (D-06, D-12, D-13)</name>
  <files>src/sift/pipeline/perfmon.py, tests/test_perfmon.py</files>
  <read_first>
    - src/sift/pipeline/perfmon.py — `PerfmonHazard`, `_resolve_span` and the sample-selection code
      from plan 13-02, and the D-04 failure hazard already emitted there, whose message and
      `event_ids` style this hazard matches.
    - src/sift/pipeline/mcm.py lines 638-660 — `compute_flags`, the flag-emission shape to mirror:
      bind the citation tuple once, guard before computing, append a fully-built frozen model,
      figure inline in the message.
    - src/sift/pipeline/mcm.py lines 609-624 — `_grade`. Read to confirm the divergence is
      deliberate; it is NOT called here because D-13's severities are categorical.
    - tests/test_perfmon.py — `ingest_perfmon_slice` and `log_boundary_event`, and the module
      docstring note recording that the shipped MCM log fixtures do not overlap the CSV slice.
    - .planning/phases/13-.../13-RESEARCH.md § Hazard Detection — the recommendation that the
      message name both ranges side by side, and the note that a wrong host whose clock overlaps
      will NOT trip this hazard because `McmEpisode` events carry no host field.
  </read_first>
  <behavior>
    - `non_overlap_hazard`: with a resolved span that contains zero perfmon samples, the group
      carries exactly one hazard with `severity == "critical"` and
      `dimension == HAZARD_NON_OVERLAP`, and `counters == ()`.
    - The hazard's `message` contains the span's start and end timestamps AND the first and last
      perfmon sample timestamps in the case, so the reader sees both ranges.
    - The hazard's `event_ids` cite the two span boundary event ids plus the first and last perfmon
      sample event ids.
    - A span that DOES contain samples raises no non-overlap hazard.
    - With zero perfmon events in the case at all, the hazard still constructs without raising —
      the message says the case carries no perfmon samples rather than naming an empty range.
  </behavior>
  <action>
    Define `HAZARD_NON_OVERLAP` as a module constant naming the dimension (for example
    `non_overlap`), alongside the other two dimension constants used by tasks 2 and 3.

    Add `_hazard_non_overlap(...)` building a `PerfmonHazard` with `severity="critical"` — the
    correlation is not trustworthy at all, which is why this one grades above the other two (D-13).
    Set `value=None`: there is no ratio here, which is precisely why `mcm.DiagnosticFlag` was not
    reused (D-12).

    Message, in British English, must name the span range and the CSV coverage range side by side so
    the mismatch is diagnosable — a message reading only "no overlap" is not actionable. It must also
    state that the hazard covers time non-overlap only and not host identity, because a wrong host
    whose clock overlaps produces in-span samples and will not trip it; multi-host correlation is
    deferred to PERFV2-02.

    Guard the empty-case branch before indexing: with zero perfmon events in the case there is no
    first or last sample to name, and `samples[0]` would raise `IndexError`. Produce a message
    stating the case carries no perfmon samples instead.

    Wire it into `analyse_perfmon`: after span resolution succeeds and sample selection returns an
    empty list, emit this hazard on the group and leave `counters` empty. Do NOT emit an empty trend
    table alongside it — zero-in-span is the loud flag, not an absence of data (D-06).

    Add the two tests from `<behavior>` to `tests/test_perfmon.py` as `test_non_overlap_hazard` and
    `test_non_overlap_hazard_with_no_perfmon_events`. Construct the non-overlapping span using
    `log_boundary_event` with timestamps outside the slice's 04/02-04/07 coverage.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perfmon.py -x -q -k non_overlap</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon.py -x -q -k non_overlap` passes with 2 tests collected.
    - `test_non_overlap_hazard` asserts `len(group.hazards) == 1`,
      `group.hazards[0].severity == "critical"`,
      `group.hazards[0].dimension == HAZARD_NON_OVERLAP`, and `group.counters == ()`.
    - The same test asserts the hazard message contains all four timestamps: the span start, the
      span end, the first perfmon sample and the last perfmon sample.
    - The same test asserts `len(group.hazards[0].event_ids) >= 4`.
    - `test_non_overlap_hazard_with_no_perfmon_events` completes without raising `IndexError` and
      asserts the message names the absence of samples.
    - A separate assertion confirms an OVERLAPPING span produces zero hazards with
      `dimension == HAZARD_NON_OVERLAP`.
    - `src/sift/pipeline/perfmon.py` contains no call to `mcm._grade`.
    - `uv run ruff check` clean; `uv run pyright` 0 errors; `uv run pytest` full suite green.
    - Fail-fast: if `test_non_overlap_hazard` passes on its first run, temporarily make the
      empty-sample branch return an empty hazard tuple, confirm the test fails, then restore the
      file byte-identically before committing.
  </acceptance_criteria>
  <done>A span with no samples raises a critical, diagnosable non-overlap hazard instead of an empty table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The always-zero Total MCM Denial hazard (D-14, D-16)</name>
  <files>src/sift/pipeline/perfmon.py, tests/test_perfmon.py</files>
  <read_first>
    - src/sift/adapters/dssperfmon.py `_short_counter_name` and `_qualify_counter_names` as landed
      by plan 13-03 — the counter-key format this lookup must match, including what a
      collision-qualified key looks like.
    - .planning/phases/13-.../13-03-SUMMARY.md — the decided qualified-key spelling. This lookup is
      written against it, which is why plan 13-03 is a hard dependency.
    - src/sift/pipeline/perfmon.py — `_numeric` and the trend code from plan 13-02.
    - tests/test_perfmon.py — `ingest_perfmon_slice`; the slice's `Total MCM Denial` column reads
      `0` on all 20 samples, matching the real file's single distinct value across all 13,596 rows.
    - .planning/REQUIREMENTS.md § Out of Scope — the row stating this counter is never a primary
      signal.
  </read_first>
  <behavior>
    - `mcm_denial_always_zero`: with at least one episode and every in-span sample's
      `Total MCM Denial` reading zero, exactly one hazard is raised with `severity == "warn"` and
      `dimension == HAZARD_DENIAL_ALWAYS_ZERO`.
    - `no_episodes_no_zero_hazard`: with zero episodes, no hazard of this dimension is raised
      anywhere in the analysis, even though the counter still reads zero throughout.
    - A single in-span sample reading non-zero suppresses the hazard entirely.
    - The zero test is numeric, not string-equality: a value of `"0.0"` counts as zero and a value
      of `"0.0000001"` does NOT, so a `v == "0"` shortcut is provably absent.
    - `_find_counter_key` resolves the counter under its bare short name AND under a
      collision-qualified key whose final backslash-separated segment matches, so the lookup
      survives plan 13-03's qualification.
  </behavior>
  <action>
    Define `MCM_DENIAL_COUNTER` as the module constant holding the counter's short name, and
    `HAZARD_DENIAL_ALWAYS_ZERO` as its dimension constant.

    Add `_find_counter_key(attrs_keys, short_name)` returning the matching key or `None`: try the
    exact bare short name first, then fall back to any key whose final backslash-separated segment
    equals `short_name`. This is what makes the lookup survive collision qualification. When more
    than one qualified key matches, take them in sorted order for determinism and consider the
    hazard fired only if EVERY matching key reads zero across the window — a genuinely
    non-zero instance must not be masked by a zero one.

    Add `_hazard_denial_always_zero(...)` with `severity="warn"` (D-13). Fire it only when the group
    has at least one in-span sample and at least one detected denial episode exists — with no
    episodes there is no denial to contradict, and firing on every healthy case trains the reader to
    ignore the flag that matters (D-14). Cite the denial event id plus the in-span sample event ids
    that were checked; set `value=0.0` as the observed reading.

    Compare numerically via `_numeric`, not by string equality against `"0"`. Add a comment stating
    why: `"0.0"` and `"-0"` are zero while `"0.0000001"` is not, and a string shortcut would get
    both wrong in different directions.

    Add a comment recording that this counter is a REPORTED FLAG only and is never a correlation
    input (D-16) — no branch of span resolution, sample selection or figure computation reads it.

    Add the three tests from `<behavior>` to `tests/test_perfmon.py` as
    `test_mcm_denial_always_zero`, `test_no_episodes_no_zero_hazard` and
    `test_mcm_denial_zero_test_is_numeric_not_string`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perfmon.py -x -q -k "mcm_denial or no_episodes_no_zero"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon.py -x -q -k "mcm_denial or no_episodes_no_zero"` passes with 3 tests collected.
    - `test_mcm_denial_always_zero` asserts exactly one hazard with `severity == "warn"` and
      `dimension == HAZARD_DENIAL_ALWAYS_ZERO`, and asserts its `event_ids` is non-empty.
    - `test_no_episodes_no_zero_hazard` asserts
      `[h for h in all_hazards if h.dimension == HAZARD_DENIAL_ALWAYS_ZERO] == []` for an analysis
      built from `McmAnalysis(episodes=())`.
    - `test_mcm_denial_zero_test_is_numeric_not_string` asserts the hazard fires for a sample
      reading `"0.0"` and does NOT fire for one reading `"0.0000001"`.
    - `_find_counter_key` resolves the counter under a collision-qualified key, asserted in a test
      using `write_collision_csv` or an equivalent synthetic `attrs` mapping.
    - `grep -n 'Total MCM Denial' src/sift/pipeline/perfmon.py` shows the constant definition and
      hazard code only — no occurrence inside `_resolve_span` or `_counter_trends`.
    - `uv run ruff check` clean; `uv run pyright` 0 errors; `uv run pytest` full suite green.
  </acceptance_criteria>
  <done>The always-zero hazard fires only against a real denial, using a numeric test and a qualification-proof lookup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The counter-set-drift hazard and deterministic hazard ordering (D-15, D-21)</name>
  <files>src/sift/pipeline/perfmon.py, tests/test_perfmon.py</files>
  <read_first>
    - src/sift/adapters/dssperfmon.py `_DRIFT_ATTR` as landed by plan 13-03 — the exact `attrs` key
      and the value format this hazard reads.
    - .planning/phases/13-.../13-03-SUMMARY.md — confirmation the marker key is in
      `_RESERVED_ATTRS`, which is what makes it trustworthy evidence.
    - src/sift/pipeline/perfmon.py — the two hazard builders from tasks 1 and 2, and
      `analyse_perfmon`'s current emission order.
    - src/sift/pipeline/mcm.py lines 930-955 — the `tuple(dict.fromkeys(...))` idiom and the
      explicit tie-breaker sort, the concrete meaning of D-21's no-`set` rule.
    - tests/_perfmon_fixtures.py — `write_drift_csv` from plan 13-01.
  </read_first>
  <behavior>
    - `counter_set_drift_hazard`: ingesting `write_drift_csv(tmp_path)` and correlating over a span
      covering the drifted rows raises exactly one hazard with `severity == "warn"` and
      `dimension == HAZARD_COUNTER_SET_DRIFT`, whose `event_ids` cite the drifted events.
    - A file with no drift raises no hazard of this dimension.
    - The hazard reads the `attrs` marker only. Removing the marker from the events (while leaving
      the underlying row widths ragged) suppresses the hazard — proving no re-detection happens at
      correlation time.
    - `hazards_deterministic_order`: two runs over the same inputs produce identical hazard tuples,
      including when two hazards share a severity.
  </behavior>
  <action>
    Define `HAZARD_COUNTER_SET_DRIFT` as the dimension constant.

    Add `_hazard_counter_set_drift(...)` with `severity="warn"` (D-13), scanning the in-span perfmon
    samples for the presence of `_DRIFT_ATTR` in `attrs` and citing every drifted event's
    `event_id`. Import the marker constant from `sift.adapters.dssperfmon` rather than redeclaring
    the string, so the two cannot drift apart.

    Set `value` to the count of drifted samples so the figure appears inline in the message.

    Do NOT re-derive drift from row widths, cell counts or counter-key set differences. Ingest
    already knew; re-detecting at read time makes disagreement with the adapter possible (D-15). Add
    a comment saying so, naming plan 13-03's marker as the single source of truth.

    Cap the cited `event_ids` at a bounded count with the message stating the total, mirroring the
    WR-02 reasoning: a file where every row drifted would otherwise put one id per row into a single
    hazard.

    Finalise deterministic hazard ordering: emit the three hazards in a fixed sequence in code, and
    where several hazards of one dimension exist, order their contents with an explicit sort and
    tie-breaker rather than relying on insertion or `set` iteration. Build every cited id tuple with
    `tuple(dict.fromkeys(...))` (D-21).

    Add the four tests from `<behavior>` to `tests/test_perfmon.py` as
    `test_counter_set_drift_hazard`, `test_no_drift_no_drift_hazard`,
    `test_drift_hazard_reads_marker_not_row_widths` and `test_hazards_deterministic_order`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_perfmon.py -x -q -k "drift or hazards_deterministic"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_perfmon.py -x -q -k "drift or hazards_deterministic"` passes with 4 tests collected.
    - `test_counter_set_drift_hazard` asserts one hazard with `severity == "warn"` and
      `dimension == HAZARD_COUNTER_SET_DRIFT`, and asserts every cited `event_id` belongs to an
      event whose `attrs` carries the marker.
    - `test_drift_hazard_reads_marker_not_row_widths` builds events with ragged underlying rows but
      the marker stripped from `attrs`, and asserts zero drift hazards — proving no re-detection.
    - `test_hazards_deterministic_order` runs `analyse_perfmon` twice and asserts the two
      `model_dump_json()` outputs are equal, with at least two hazards of equal severity present in
      the fixture so the tie-break path is actually exercised.
    - `src/sift/pipeline/perfmon.py` imports the drift marker constant from
      `sift.adapters.dssperfmon` rather than redeclaring its string value.
    - `uv run ruff check` clean; `uv run pyright` 0 errors; `uv run pytest` full suite green.
    - Fail-fast: if `test_hazards_deterministic_order` passes on its first run, temporarily replace
      one output tuple construction with iteration over a `set`, confirm the test becomes unstable
      or fails, then restore the file byte-identically before committing.
  </acceptance_criteria>
  <done>Drift is flagged from ingest-time evidence with citable ids, and hazard output is provably stable across runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `attrs` drift marker → hazard evidence | The marker is trusted as the sole drift record; its integrity depends on plan 13-03's `_RESERVED_ATTRS` protection |
| counter names → `Total MCM Denial` lookup | The lookup matches attacker-influenceable keys, so a crafted name could try to satisfy or evade it |
| hazard messages → operator terminal and report | Message text embeds timestamps and counter names originating in customer artefacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-FALSEJOIN | Spoofing | non-overlap hazard in `pipeline/perfmon.py` | high | mitigate | Zero in-span samples raises a `critical` hazard rather than an empty trend table (D-06), so a wrong-timezone, wrong-day or wrong-host artefact pairing can never be presented as a successful correlation. The message names both ranges so the operator can identify the mismatch. Verified by `test_non_overlap_hazard`. |
| T-13-EVADE | Tampering | `_find_counter_key` in `pipeline/perfmon.py` | medium | mitigate | The counter lookup resolves both the bare short name and any collision-qualified key whose final segment matches, and requires EVERY matching key to read zero before firing. A crafted duplicate counter therefore cannot mask a genuinely non-zero instance nor evade the flag by forcing qualification. Verified by the `_find_counter_key` assertion in task 2. |
| T-13-DRIFTTRUST | Tampering | drift hazard in `pipeline/perfmon.py` | medium | mitigate | The hazard reads only the `_DRIFT_ATTR` marker, which plan 13-03 placed in `_RESERVED_ATTRS` so a crafted counter name cannot forge or suppress it. Re-detection at correlation time is explicitly avoided, removing the possibility of adapter and correlator disagreeing about what drifted. Verified by `test_drift_hazard_reads_marker_not_row_widths`. |
| T-13-HAZDOS | Denial of Service | cited `event_ids` on the drift hazard | medium | mitigate | The cited id list is capped with the total stated in the message, mirroring the WR-02 note cap. Without it a file where every row drifted would place one id per row into a single hazard, reproducing the unbounded-growth problem in a new location. |
| T-13-HAZTEXT | Tampering | hazard `message` strings | low | transfer | Hazard text embeds customer-derived timestamps and counter names. Neutralisation is the renderer's and CLI's responsibility, not the pipeline's: `render.markdown._field` for Markdown cells, `ensure_ascii=True` for JSON (plan 13-05) and `cli._sanitise` before echo (plan 13-06). The pipeline tier stays print-free and format-free by convention. |

No package-manager installs occur in this plan, so no `T-13-SC` supply-chain checkpoint applies.
</threat_model>

<verification>
- `uv run pytest tests/test_perfmon.py -q` — the full correlator suite including 9 new hazard tests passes
- `uv run ruff check` clean
- `uv run pyright` 0 errors
- `uv run pytest` full suite green
</verification>

<success_criteria>
- A span with zero samples raises a `critical`, diagnosable non-overlap hazard naming both ranges.
- The always-zero `Total MCM Denial` hazard fires only when a detected denial exists, using a numeric test.
- The counter-set-drift hazard reads the ingest-time `attrs` marker and cites the drifted events.
- No hazard calls `mcm._grade`; all three severities are categorical literals fixed in code.
- Two runs over identical inputs produce identical hazard tuples.
</success_criteria>

<output>
Create `.planning/phases/13-episode-correlation-hazard-flags-sift-perfmon-report-csv/13-04-SUMMARY.md` when done
</output>
