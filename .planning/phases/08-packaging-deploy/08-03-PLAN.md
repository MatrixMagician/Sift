---
phase: 08-packaging-deploy
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - README.md
autonomous: false
requirements: [PKG-01, PKG-02]
must_haves:
  truths:
    - "README quickstart takes a stranger from `uv tool install` → backend setup → `sift doctor` → new/ingest/analyze/report → optional pdf extra, in that order (D-09)"
    - "README documents both backends: llama.cpp llama-server (Vulkan default on gfx1151, ROCm 7.2+ alternative) and Lemonade Server (:13305, embeddings-recipe caveat: llamacpp/flm only, not ONNX/OGA)"
    - "README documents the two-instance setup (SIFT_GENERATION_BASE_URL + a separate --embeddings server → SIFT_EMBEDDINGS_BASE_URL)"
    - "README documents the sift[pdf] extra and the Fedora `dnf install pango` note (ADR 0002); the core install stays system-dep-free"
  artifacts:
    - "README.md rewritten from the 2-line stub into the D-09 quickstart, British English throughout (D-10)"
  key_links:
    - "The embeddings-recipe caveat + `sift doctor` round-trip (else users hit the ONNX/OGA no-embeddings failure silently — RESEARCH Pitfall 4)"
    - "The two-instance env-var names (generation vs embeddings) — the load-bearing accuracy point (RESEARCH Pattern 3)"
---

<objective>
Deliver the documentation half of PKG-01/PKG-02 (D-09, D-10): rewrite the 2-line `README.md` stub into a quickstart that lets a stranger on Fedora go from clean checkout to first triage report using only the README — the phase's headline goal.

Purpose: the install path (08-01) and deploy files (08-02) are only usable if the README walks them accurately. The load-bearing accuracy points are the two-instance backend setup, the Lemonade embeddings-recipe caveat, the Vulkan/ROCm gfx1151 guidance, and the `sift[pdf]`/pango note — these are exactly where a stranger gets stuck.
Output: a rewritten `README.md`, verified by a human walkthrough checkpoint.
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `README.md` | docs (REWRITE) | replaces the 2-line stub with the D-09 quickstart; British English |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-packaging-deploy/08-CONTEXT.md
@.planning/phases/08-packaging-deploy/08-RESEARCH.md
@docs/decisions/0002-weasyprint-pdf-extra.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md into the D-09 quickstart</name>
  <read_first>
    - README.md (the current 2-line stub being replaced)
    - 08-RESEARCH.md (§Pattern 3 lines 184-185: two-instance backend + env-var names; Pitfall 4 lines 230-232: Lemonade embeddings-recipe caveat; §D-09 for the ordered section list)
    - docs/decisions/0002-weasyprint-pdf-extra.md (the `sift[pdf]` extra + pango system-dep — README documents this install path)
    - CLAUDE.md (British-English convention, stack facts: llama.cpp `llama-server`, Lemonade port 13305)
  </read_first>
  <files>README.md</files>
  <action>
    Replace `README.md` with a quickstart in this exact section order (D-09):
    (1) Install — `uv tool install .` from a clean checkout and `uv tool install git+https://…` from the repo (local + VCS only, D-01; no PyPI publish); note `sift --version` confirms the install (from 08-01). pipx-compatibility comes free from the standard wheel (PKG-01).
    (2) Start an inference backend — document TWO paths. (a) llama.cpp `llama-server`: Vulkan is the robust default on gfx1151 (SPEC §3), ROCm 7.2+ is the documented alternative; the `--embeddings`/`--embedding` flag makes a server embedding-only, so run TWO instances — a generation server and a separate `--embeddings` server — and point Sift at both via `SIFT_GENERATION_BASE_URL` and `SIFT_EMBEDDINGS_BASE_URL` (RESEARCH Pattern 3). (b) Lemonade Server on port 13305 (config-only, never assumed) — and name the embeddings-recipe caveat explicitly: `/v1/embeddings` works only for `llamacpp`/`flm`-recipe models, NOT ONNX/OGA (the common Strix Halo chat default), so tell users to run `sift doctor` which round-trips a real embedding call (RESEARCH Pitfall 4).
    (3) `sift doctor` — verify both endpoints before analysing.
    (4) First-case walkthrough — `sift new <case> --input <dir>` → `sift ingest` → `sift analyze` → `sift report`.
    (5) Optional PDF — `uv tool install 'sift[pdf]'` (or the extra) plus the Fedora `dnf install pango` note; the core install stays system-dep-free (ADR 0002).
    Add a short pointer to `deploy/` and ADR 0011 for the optional containerised (Podman Quadlet) deployment (created in 08-02) — one line, not a full deploy guide. British English throughout (D-10). Keep commands copy-pasteable and accurate; do NOT present `--i-know-what-im-doing` as a localhost convenience — it is a genuinely-remote break-glass only (T-08-01).
  </action>
  <acceptance_criteria>
    - `README.md` contains, in order, sections for: install (`uv tool install`), backend setup, `sift doctor`, the `new`/`ingest`/`analyze`/`report` walkthrough, and the `sift[pdf]` extra.
    - README names both `SIFT_GENERATION_BASE_URL` and `SIFT_EMBEDDINGS_BASE_URL` and states the two-instance requirement.
    - README names the Lemonade embeddings-recipe caveat (llamacpp/flm vs ONNX/OGA) and directs users to `sift doctor`.
    - README mentions Vulkan (default) and ROCm (alternative) for gfx1151, and `dnf install pango` for the pdf extra.
    - README contains no `--i-know-what-im-doing` presented as a localhost default.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pathlib; t=pathlib.Path('README.md').read_text(); assert all(s in t for s in ['uv tool install','SIFT_GENERATION_BASE_URL','SIFT_EMBEDDINGS_BASE_URL','sift doctor','sift analyze','pango']); low=t.lower(); assert 'vulkan' in low and 'rocm' in low and 'lemonade' in low; assert '13305' in t"</automated>
  </verify>
  <done>README.md is a working D-09 quickstart in British English covering install → backend (two-instance, Vulkan/ROCm, Lemonade caveat) → doctor → analyse → report → pdf extra, with a one-line deploy pointer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — walk the README quickstart</name>
  <files>README.md</files>
  <action>Pause for a human to read `README.md` end-to-end and confirm the quickstart is accurate and correctly ordered, per the `<how-to-verify>` steps below. This is a blocking checkpoint: do not mark the plan complete until the reviewer signals approval. README prose accuracy is not machine-checkable (VALIDATION.md Manual-Only), so a human sign-off is the gate.</action>
  <what-built>A rewritten `README.md` quickstart (install → backend setup → `sift doctor` → `new/ingest/analyze/report` → optional `sift[pdf]`), the phase's headline deliverable: a stranger on Fedora using only the README.</what-built>
  <how-to-verify>
    1. Open `README.md` and read it top to bottom as if you had never seen Sift.
    2. Confirm the section order is: install → backend setup → `sift doctor` → first-case walkthrough → pdf extra, with a one-line pointer to `deploy/` + ADR 0011.
    3. Confirm accuracy on the load-bearing points: the two-instance setup names both `SIFT_GENERATION_BASE_URL` and `SIFT_EMBEDDINGS_BASE_URL`; Lemonade's port is 13305 and the embeddings-recipe caveat (llamacpp/flm, not ONNX/OGA) is stated with a `sift doctor` pointer; Vulkan is the gfx1151 default and ROCm 7.2+ the alternative; the pdf extra shows `dnf install pango`.
    4. Confirm British English throughout and that `--i-know-what-im-doing` is not shown as a localhost convenience.
    5. (Optional, if a real Fedora box + backend are available) Follow the quickstart end-to-end from a clean checkout: `uv tool install .` → `sift doctor` → `new/ingest/analyze/report`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the inaccuracies to fix.</resume-signal>
  <verify><human-check>Reviewer confirms the README sections, ordering, and the load-bearing accuracy points (two-instance env vars, Lemonade embeddings-recipe caveat + port 13305, Vulkan default / ROCm 7.2+, `dnf install pango`) and signals approval.</human-check></verify>
  <done>A human has read the README end-to-end and approved it (or listed inaccuracies, which are fixed before completion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README commands → user shell | users copy-paste quickstart commands as-is |
| README pdf-extra instructions → user system | the documented `dnf install pango` touches the host system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | README backend/guard guidance | high | mitigate | README presents the guard-clean loopback recipe as the default and mentions `--i-know-what-im-doing` only as a genuinely-remote break-glass, never as a localhost convenience — the deploy docs must not normalise SSRF-guard bypass |
| T-08-03 | Tampering | README sift[pdf]/pango system-dep instructions | medium | mitigate | Document the single `dnf install pango` line per ADR 0002; the core install stays system-dep-free; no third-party install scripts are piped to a shell |
| T-08-05 | Tampering | README copy-paste commands | low | accept | Commands are standard uv / llama-server / dnf invocations with no secrets or elevation beyond the documented `dnf install pango` |
</threat_model>

<verification>
- Automated: `README.md` contains the required sections and the load-bearing identifiers (env-var names, 13305, Vulkan/ROCm, Lemonade, pango).
- Human: the checkpoint walkthrough confirms prose accuracy and ordering (VALIDATION.md Manual-Only: README prose is not machine-checkable).
</verification>

<success_criteria>
A stranger on Fedora can go from clean checkout to first triage report using only the README: install, two-instance backend setup (Vulkan/ROCm + Lemonade caveat), `sift doctor`, the full `new→ingest→analyze→report` walkthrough, and the optional pdf extra — approved at the human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/08-packaging-deploy/08-03-SUMMARY.md` when done.
</output>
