---
phase: 07-evaluation-harness-golden-cases
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_conftest_network_guard.py
  - tests/test_eval_judge.py
  - src/sift/eval/judge.py
autonomous: true
gap_closure: true
requirements: [EVAL-04]

must_haves:
  truths:
    - "`uv run pytest -m live tests/test_eval_judge.py::test_judge_live_round_trip` can open a socket to the configured endpoint (no longer RuntimeError-blocked by the `_no_network` guard)."
    - "The default (non-live) suite remains fully socket-blocked (zero network egress) — unchanged: every unmarked test still raises the network-forbidden RuntimeError on connect."
    - "`judge_case()` returns None (does not raise, does not change the exit code) when the model returns an empty-content / reasoning-only / finish_reason=length response."
  artifacts:
    - "tests/conftest.py — `_no_network` autouse fixture exempts `@pytest.mark.live` tests via `request.node.get_closest_marker`, unchanged for every other test."
    - "tests/test_conftest_network_guard.py — deterministic offline regression: guard active for unmarked tests, bypassed for a live-marked test using an in-process loopback listener (no real server)."
    - "tests/test_eval_judge.py — mocked reasoning-only/empty-content degrade test proving judge_case → None, exit unchanged."
  key_links:
    - "`_no_network` marker carve-out → applies ONLY to tests carrying the `live` marker (pyproject.toml:50-53); default suite (`-m 'not perf and not live'`) is untouched."
    - "judge_case → InferenceClient.chat: chat already raises ValueError on empty/whitespace content (client.py:306-307); judge_case already catches ValueError → None (judge.py:106-107). This plan proves that path, it does not re-route it."
---

<objective>
Close Phase 07 UAT Gap 1: the sole outstanding item is a live-model judge round-trip that is un-runnable because the autouse `_no_network` guard in `tests/conftest.py` blocks the loopback socket for EVERY test — including `@pytest.mark.live` integration tests, which exist precisely to reach the real inference server. The harness, metrics, gate, CLI, and judge are all frozen-correct (464 tests, ruff clean, pyright 0/0/0); this is a TEST-HARNESS defect plus a regression-proof of the judge's already-present degrade path.

Purpose: make the `live` marker mean what pyproject.toml says it means (real-inference-server integration), so `uv run pytest -m live tests/test_eval_judge.py::test_judge_live_round_trip` against Lemonade :13305 becomes a runnable acceptance step rather than a blocked one — WITHOUT weakening the zero-network invariant for the default suite by one inch.

Output: a marker-scoped exemption in `_no_network`, a deterministic offline regression test locking both halves of the invariant (blocked-when-unmarked, open-when-live), and a mocked reasoning-only degrade test proving the judge's never-crash/return-None contract against the exact real-model response shape (HTTP 200, empty `content`, populated `reasoning_content`, `finish_reason: "length"`) that mocks had never exercised.

Ponytail note: the empty-content root-cause fix already lives at the sole HTTP boundary — `InferenceClient.chat` raises ValueError on whitespace-only content (client.py:306-307) and `judge_case` already swallows ValueError into None (judge.py:106-107); `tests/test_hypothesise.py::test_malformed_generation_empty_content` already characterises this shape for the generation path. Task 3 is therefore expected to pass green as a lock-in regression; hardening `judge.py` is only warranted if the new test unexpectedly goes red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-evaluation-harness-golden-cases/07-UAT.md
@.planning/phases/07-evaluation-harness-golden-cases/07-VERIFICATION.md
@tests/conftest.py
@tests/test_eval_judge.py
@tests/test_doctor.py
@src/sift/eval/judge.py
@src/sift/llm/client.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): regression test locking both halves of the network-guard invariant</name>
  <files>tests/test_conftest_network_guard.py</files>
  <behavior>
    - Test A `test_default_suite_socket_guard_active` (UNMARKED): constructing `socket.socket(AF_INET, SOCK_STREAM)` and calling `.connect(("127.0.0.1", 13305))` raises RuntimeError (the zero-network guard is installed for ordinary tests). Passes BEFORE and after the fix.
    - Test B `test_live_marked_tests_bypass_socket_guard` (`@pytest.mark.live`): a fully in-process, deterministic, offline loopback proof — bind a listener to `("127.0.0.1", 0)`, `listen(1)`, read the OS-assigned port, then connect a client socket to that port and assert the connect SUCCEEDS (no RuntimeError). This uses only loopback to a listener created inside the test — no external server, no real egress. FAILS before the fix (guard blocks the connect → RuntimeError), PASSES after.
  </behavior>
  <action>Create tests/test_conftest_network_guard.py with the two tests described in the behaviour block. Test B must be self-contained: open a throwaway `socket.socket` listener bound to port 0 on 127.0.0.1, call listen, capture `getsockname()[1]`, then in a second socket connect to that port and assert it does not raise — close both sockets in a finally. Do NOT depend on Lemonade or any external process; this is an offline determinism proof of the marker carve-out, distinct from the real-server `test_judge_live_round_trip`. Mirror the `@pytest.mark.live` usage already established in tests/test_doctor.py and tests/test_render_pdf.py. Keep it type-clean under pyright strict (annotate socket variables; the module is on the tests execution-environment extraPaths already). This satisfies EVAL-04's live-judge acceptance by first proving, deterministically, that the marker gates the guard correctly.</action>
  <verify>
    <automated>uv run pytest tests/test_conftest_network_guard.py::test_default_suite_socket_guard_active -q && uv run pytest -m live tests/test_conftest_network_guard.py::test_live_marked_tests_bypass_socket_guard -q; echo "expect: A passes, B RED before the Task 2 fix"</automated>
  </verify>
  <done>Test A passes in the default suite (guard active). Test B is present, correctly `@pytest.mark.live`, deselected from the default suite (so `uv run pytest` stays green), and observably RED under `-m live` before Task 2. Commit `test(07-06): add network-guard marker-carve-out regression` — the default full gate (ruff + pyright + `uv run pytest`) is green because Test B is deselected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): exempt live-marked tests from the `_no_network` guard</name>
  <files>tests/conftest.py</files>
  <behavior>
    - After the fix, `_no_network` installs the socket block for every test EXCEPT those carrying the `live` marker.
    - Test B from Task 1 passes under `-m live`; Test A and the entire default suite remain socket-blocked and green.
  </behavior>
  <action>Add a `request: pytest.FixtureRequest` parameter to the `_no_network` autouse fixture and, as its first statement, early-return without patching when `request.node.get_closest_marker("live") is not None` — live tests genuinely need the real loopback socket to reach the configured inference endpoint. This is the laziest correct root-cause fix: one carve-out in the shared fixture makes the `live` marker behave as pyproject.toml already documents, so every live test (doctor, render_pdf, eval judge) gets consistent behaviour instead of each hand-rolling its own `_REAL_CONNECT` restore. Because `monkeypatch.setattr` is never called on the exempt path, there is nothing to revert — no generator needed. Scope fence: do NOT alter `_isolate_dirs`, do NOT touch the block message or behaviour for unmarked tests, and do NOT broaden the exemption beyond the `live` marker. Update the fixture docstring to state the live-marker carve-out and that the default suite stays fully blocked.</action>
  <verify>
    <automated>uv run pytest -m live tests/test_conftest_network_guard.py -q && uv run ruff check && uv run pyright && uv run pytest -q</automated>
  </verify>
  <done>Test B passes under `-m live`; Test A and the full default suite (`uv run pytest`) stay green with zero deselected-test regressions; ruff clean; pyright 0/0/0. Commit `fix(07-06): exempt @pytest.mark.live tests from the zero-network guard`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: prove the judge degrades to None on the real reasoning-only/empty-content response shape</name>
  <files>tests/test_eval_judge.py, src/sift/eval/judge.py</files>
  <behavior>
    - A mocked judge chat reply reproducing the exact real-model shape — `{"choices": [{"message": {"content": "", "reasoning_content": "<truncated thoughts>"}, "finish_reason": "length"}]}` — makes `judge_case()` return None (ungraded) rather than raising.
    - Routed through `sift eval --judge` on the single-case suite: exit code stays 0, no traceback, the judge column reports the ungraded marker (n/a), and the keyword gate is unaffected.
  </behavior>
  <action>In tests/test_eval_judge.py add `test_reasoning_only_judge_reply_degrades`, mirroring the existing `test_malformed_judge_reply_degrades_not_crashes` but with a handler that returns the reasoning-only body above (populated `reasoning_content`, empty `content`, `finish_reason: "length"`) for the judge chat call. Reuse the existing `_judge_handler`/`eval_handler`/`single_case_suite`/`patch_http` seam — do not add a new fixture module. Recognise the judge call by the `justification` property in its response_format schema, exactly as `_judge_handler` already does, so the fake stays robust to judge.md edits. Assert: `result.exit_code == 0`; `result.exception is None or isinstance(result.exception, SystemExit)`; the ungraded marker (`n/a`, case-insensitive) is in the output; and the run did not crash. This locks the never-crash/return-None contract (D-08) against the shape that only appears against a live reasoning model. Only if this test is unexpectedly RED, harden src/sift/eval/judge.py minimally at the existing degrade seam (the `except (httpx.HTTPError, ValueError)` around the chat+validate call) — but the expectation is GREEN, because InferenceClient.chat already raises ValueError on whitespace-only content and judge_case already catches it. Do NOT re-author prompts, thresholds, or golden cases; do NOT weaken the gate.</action>
  <verify>
    <automated>uv run pytest tests/test_eval_judge.py -k "reasoning_only or malformed_judge or low_judge or default_run_has_no_judge" -q && uv run ruff check && uv run pyright && uv run pytest -q</automated>
  </verify>
  <done>`test_reasoning_only_judge_reply_degrades` passes; the existing judge advisory/degrade tests stay green; full default suite green; ruff clean; pyright 0/0/0. Commit `test(07-06): lock judge degrade on reasoning-only empty-content reply` (or `fix(07-06): …` only if judge.py needed hardening). After this commit the original UAT command `uv run pytest -m live tests/test_eval_judge.py::test_judge_live_round_trip` is runnable against Lemonade :13305 — that live round-trip is the operator UAT sign-off (see success criteria), not part of the automated gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → localhost inference endpoint | Only `@pytest.mark.live` tests may cross it; every other test must be hard-blocked at `socket.connect`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Information Disclosure | `_no_network` carve-out over-broadening (an unmarked test egressing) | high | mitigate | Exemption keyed strictly on `request.node.get_closest_marker("live")`; Test A in tests/test_conftest_network_guard.py proves unmarked tests still raise on connect; default addopts `-m 'not perf and not live'` keeps live tests out of the standard run. |
| T-07-06-02 | Denial of Service | Judge crashing the eval run on a real reasoning-only reply | medium | mitigate | Deterministic mocked reproduction of the empty-content/`finish_reason=length` body proves judge_case → None and exit unchanged; degrade already enforced at InferenceClient.chat (ValueError) + judge_case catch. |
| T-07-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies added; this plan touches only test harness + a possible one-line judge guard. No package installs, so the legitimacy gate is not triggered. |
</threat_model>

<verification>
- `uv run pytest` (default suite): green, zero network egress, no new deselections beyond the two `live`-marked tests.
- `uv run pytest -m live tests/test_conftest_network_guard.py`: green (marker carve-out proven offline, no server needed).
- `uv run ruff check` clean; `uv run pyright` 0 errors / 0 warnings / 0 informations.
- Unmarked tests still raise the network-forbidden RuntimeError on `socket.connect` (Test A).
- Judge degrades to None (n/a, exit 0) on the reasoning-only/empty-content reply (Task 3 test).
</verification>

<success_criteria>
- The `_no_network` guard exempts ONLY `@pytest.mark.live` tests; the default suite is byte-for-byte as socket-blocked as before.
- `uv run pytest -m live tests/test_eval_judge.py::test_judge_live_round_trip` reaches the socket instead of raising RuntimeError (the Gap-1 blocker is gone).
- `judge_case()` returns None — never raises, never changes the exit code — on the empty-content / reasoning-only / finish_reason=length shape, proven by a deterministic mocked test.
- Operator UAT acceptance (runs outside the automated gate, requires Lemonade Server on 127.0.0.1:13305 serving a llamacpp/flm-recipe chat model): `uv run pytest -m live tests/test_eval_judge.py::test_judge_live_round_trip` passes — the real model returns a parseable JudgeScore in [0.0, 1.0] surfaced in the `--judge` column WITHOUT altering the exit code (advisory-only, D-08). Fail-fast first with `uv run sift doctor` to confirm the endpoint is up.
- All three atomic commits made; ruff + pyright + full pytest green before each.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-harness-golden-cases/07-06-SUMMARY.md` when done.
</output>
