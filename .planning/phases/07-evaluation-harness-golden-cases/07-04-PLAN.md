---
phase: 07-evaluation-harness-golden-cases
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - eval/cases/smtp-rejection-storm/input/mail.log
  - eval/cases/smtp-rejection-storm/truth.yaml
  - eval/cases/smtp-rejection-storm/README.md
  - eval/cases/thread-pool-exhaustion/input/app.log
  - eval/cases/thread-pool-exhaustion/truth.yaml
  - eval/cases/thread-pool-exhaustion/README.md
  - eval/cases/disk-full/input/system.log
  - eval/cases/disk-full/truth.yaml
  - eval/cases/disk-full/README.md
  - eval/cases/dependency-timeout-mixed-tz/input/node-a.log
  - eval/cases/dependency-timeout-mixed-tz/input/node-b.log
  - eval/cases/dependency-timeout-mixed-tz/truth.yaml
  - eval/cases/dependency-timeout-mixed-tz/README.md
  - eval/cases/negative-no-incident/input/app.log
  - eval/cases/negative-no-incident/truth.yaml
  - eval/cases/negative-no-incident/README.md
  - tests/test_eval_cases.py
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "The committed golden suite contains exactly 6 cases covering all five SPEC §6 exemplars plus the three ROADMAP-mandated shapes (quiet-cause, mixed-timezone, negative)"
    - "Each case ships input/ (sanitised synthetic artefacts), truth.yaml (frozen, schema-valid), and README.md"
    - "The negative case sets expect_no_incident: true and passes via negative_case_pass offline"
    - "The mixed-timezone case has input from ≥2 nodes with different UTC offsets"
    - "Every case's truth.yaml loads through the Truth model (extra=forbid) without error"
  artifacts:
    - "eval/cases/smtp-rejection-storm/{input/,truth.yaml,README.md}"
    - "eval/cases/thread-pool-exhaustion/{input/,truth.yaml,README.md}"
    - "eval/cases/disk-full/{input/,truth.yaml,README.md}"
    - "eval/cases/dependency-timeout-mixed-tz/{input/node-a.log,input/node-b.log,truth.yaml,README.md}"
    - "eval/cases/negative-no-incident/{input/,truth.yaml,README.md}"
    - "tests/test_eval_cases.py"
  key_links:
    - "All 6 case truth.yaml files parse via src/sift/eval/truth.load_truth (Plan 02)"
    - "Negative case exercises runner.run_case's expect_no_incident branch + negative_case_pass (Plan 02)"
    - "memory-watermark-cascade (authored Plan 02) is the quiet-cause carrier — this plan authors the remaining 5"
---

## Phase Goal

**As a** Sift maintainer, **I want to** run `sift eval` over a committed golden-case suite that scores hypothesis quality and fails when scores regress below `eval/thresholds.toml`, **so that** hypothesis quality is measurable and regression-gated rather than vibes-based.

This plan delivers the **breadth**: the full committed golden suite (EVAL-01, ROADMAP SC1). After it, `sift eval` runs real coverage — every SPEC exemplar and every mandated special shape.

<objective>
Author the remaining 5 golden cases so the suite is the complete 6 required by D-01, and validate that each parses and scores correctly offline (including the negative case's no-confident-hypothesis predicate).

Purpose: satisfy EVAL-01 / ROADMAP SC1 in full.
Output: 5 new golden-case directories + a suite-validation test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation-harness-golden-cases/07-CONTEXT.md
@.planning/phases/07-evaluation-harness-golden-cases/07-RESEARCH.md
@SPEC.md
@eval/cases/memory-watermark-cascade/truth.yaml
</context>

## Planner decision: the suite is exactly 6 cases (D-01 reconciliation)

07-RESEARCH A5 flags a 6-vs-7 ambiguity in D-01. Decision (binding for this plan): **6 cases**, because D-01's header says "Author 6" and the CONTEXT specifics say "lean 6". All five SPEC §6 exemplars are present AND all three ROADMAP-mandated shapes are covered without a 7th case by layering two shapes onto exemplars:

| # | Case dir | SPEC exemplar | Special shape carried |
|---|----------|---------------|-----------------------|
| 1 | memory-watermark-cascade (Plan 02) | memory-watermark cascade | **quiet-cause** (loud OOM symptom, quiet early watermark root) |
| 2 | smtp-rejection-storm | SMTP relay rejection storm | — |
| 3 | thread-pool-exhaustion | thread-pool exhaustion | — |
| 4 | disk-full | disk-full | — |
| 5 | dependency-timeout-mixed-tz | dependency-service timeout | **mixed-timezone** (D-01) |
| 6 | negative-no-incident | — (healthy) | **negative / no-incident** (D-04) |

This satisfies ROADMAP SC1 (≥5 incl. quiet-cause + mixed-tz + negative) and SPEC §6 (all five exemplars). Recorded in each README and the case #6 README notes the negative shape.

## Artifacts this plan produces
- 5 golden-case directories (smtp-rejection-storm, thread-pool-exhaustion, disk-full, dependency-timeout-mixed-tz, negative-no-incident), each with input/, truth.yaml, README.md
- `tests/test_eval_cases.py` (suite parse + negative-predicate validation)

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 positive golden cases (smtp, thread-pool, disk-full, dependency-timeout-mixed-tz)</name>
  <files>eval/cases/smtp-rejection-storm/input/mail.log, eval/cases/smtp-rejection-storm/truth.yaml, eval/cases/smtp-rejection-storm/README.md, eval/cases/thread-pool-exhaustion/input/app.log, eval/cases/thread-pool-exhaustion/truth.yaml, eval/cases/thread-pool-exhaustion/README.md, eval/cases/disk-full/input/system.log, eval/cases/disk-full/truth.yaml, eval/cases/disk-full/README.md, eval/cases/dependency-timeout-mixed-tz/input/node-a.log, eval/cases/dependency-timeout-mixed-tz/input/node-b.log, eval/cases/dependency-timeout-mixed-tz/truth.yaml, eval/cases/dependency-timeout-mixed-tz/README.md</files>
  <read_first>
    - eval/cases/memory-watermark-cascade/{input/app.log,truth.yaml,README.md} (Plan 02) — the exact fixture shape + frozen truth.yaml schema to replicate.
    - 07-RESEARCH.md §Golden-Case Authoring (269-278: per-adapter recipes; mixed-timezone recipe at 271; genericlog is the simplest adapter for positive cases).
    - SPEC.md §6 exemplar list.
    - src/sift/eval/truth.py (Plan 02) Truth model — the fields truth.yaml must satisfy (extra=forbid: no stray keys).
  </read_first>
  <action>
    Author each case as sanitised synthetic artefacts (no real hostnames/IPs —
    REPT-05 redaction deferred; hand-author clean). Use genericlog ISO-8601
    timestamped lines (tens-to-low-hundreds), British English in README.
    - smtp-rejection-storm: a burst of relay rejections (e.g. 4xx/5xx SMTP
      replies, greylisting/relay-denied) as the incident; truth.yaml root_cause
      names the relay rejection, required_evidence regexes target the reject
      codes/relay-denied lines, acceptable_keywords (smtp, relay, reject,
      queue), expect_no_incident: false.
    - thread-pool-exhaustion: rising active-thread counts, task-queue-full /
      rejected-execution lines; truth targets pool saturation; keywords
      (thread, pool, exhaust, queue).
    - disk-full: filesystem-full / ENOSPC / write-failed lines with a rising
      usage signal; truth targets disk exhaustion; keywords (disk, space,
      ENOSPC, full).
    - dependency-timeout-mixed-tz: TWO input files node-a.log and node-b.log
      with DIFFERENT explicit UTC offsets (e.g. node-a at +00:00, node-b at
      -05:00 or +05:30) so INGST-11's UTC normalisation is exercised; the
      incident is an upstream dependency-service timeout whose true causal order
      only holds once timestamps are normalised (the timeline must not invert).
      truth targets the dependency timeout; keywords (timeout, dependency,
      upstream, unavailable). README explicitly notes it is the mixed-timezone
      shape.
    Each truth.yaml is FROZEN ground truth (D-02) — author it to the planted
    scenario, not to whatever the model happens to say. Each README is one
    paragraph: scenario, planted root cause, special shape (if any).
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; import yaml; import glob; [yaml.safe_load(Path(p).read_text()) for p in glob.glob('eval/cases/*/truth.yaml')]"</automated>
  </verify>
  <acceptance_criteria>
    - All four case directories exist with input/, truth.yaml, README.md.
    - dependency-timeout-mixed-tz/input has ≥2 files with different UTC offsets visible in the timestamps.
    - Every truth.yaml parses via yaml.safe_load and carries root_cause + required_evidence + acceptable_keywords + expect_no_incident: false.
    - No real hostnames/IPs (synthetic only).
  </acceptance_criteria>
  <done>The four positive golden cases exist with input/+truth.yaml+README.md; dependency-timeout-mixed-tz has ≥2 nodes with differing UTC offsets; every truth.yaml parses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the negative case + suite-validation test</name>
  <files>eval/cases/negative-no-incident/input/app.log, eval/cases/negative-no-incident/truth.yaml, eval/cases/negative-no-incident/README.md, tests/test_eval_cases.py</files>
  <read_first>
    - src/sift/eval/metrics.py negative_case_pass + src/sift/eval/runner.py expect_no_incident branch (Plan 02).
    - tests/_eval_fixtures.py (Plan 02) — the fake-client handler + _patch_http seam; for the negative case the handler should return an EMPTY HypothesisSet (or all-low confidence) so the predicate passes offline.
    - 07-RESEARCH.md §Golden-Case Authoring negative recipe (273) and §Metric Recipes negative scoring (291); Pitfall 5 (351-353).
  </read_first>
  <behavior>
    - Every committed case's truth.yaml loads through load_truth without raising (schema-valid, no extra keys).
    - The suite contains exactly 6 case directories; the three special shapes are present (quiet-cause=memory-watermark, mixed-tz=dependency-timeout-mixed-tz, negative=negative-no-incident).
    - negative-no-incident: truth.yaml has expect_no_incident: true; running the offline harness over it (empty/all-low fake reply) yields negative_case_pass == True and does NOT contribute a keyword-miss to the hit@k aggregate.
  </behavior>
  <action>
    Author negative-no-incident: healthy/steady-state genericlog (info-level,
    no error bursts, routine heartbeats/health-checks). truth.yaml sets
    expect_no_incident: true, an empty or minimal required_evidence /
    acceptable_keywords, and a root_cause string stating no incident is present.
    README notes it is the negative shape and that a confident hypothesis here
    is a false positive. Author tests/test_eval_cases.py per the behaviours:
    iterate eval/cases/*/truth.yaml through load_truth (all valid); assert the
    suite dir count is 6 and the three special shapes are present; run the
    offline harness (MockTransport, empty/all-low reply) over the negative case
    and assert negative_case_pass is True and it is excluded from the positive
    aggregates. Keep the test socket-blocked (reuse the _eval_fixtures seam).
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_cases.py -q &amp;&amp; uv run ruff check &amp;&amp; uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - eval/cases/negative-no-incident/ exists with input/, truth.yaml (expect_no_incident: true), README.md.
    - `ls -d eval/cases/*/` lists exactly 6 directories.
    - tests/test_eval_cases.py passes: all 6 truth.yaml load; negative case passes its predicate offline; special shapes present.
    - Full `uv run pytest` (socket-blocked) + ruff + pyright green.
  </acceptance_criteria>
  <done>negative-no-incident (expect_no_incident: true) is committed; exactly 6 case dirs exist; tests/test_eval_cases.py passes (all truth.yaml load, negative predicate passes offline); full ruff+pyright+pytest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden truth.yaml → Truth loader | Committed YAML ground-truth files deserialised by the harness |
| required_evidence regexes → metric matcher | Committed regex patterns compiled/run against a bounded exemplar haystack |
| golden input/ logs → adapters | Synthetic log text crosses the ingest trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering / Elevation | truth.yaml parse (all 6 cases) | high | mitigate | Loaded exclusively via load_truth (yaml.safe_load + Pydantic extra=forbid, Plan 02) — no arbitrary-object construction; Task 2 test loads every committed truth.yaml through it |
| T-07-04 | Denial of Service | required_evidence regex (ReDoS) | low | accept | Golden truth.yaml files are in-repo, authored and reviewed (trusted), and matched against a small bounded top-N exemplar haystack; not runtime user input — ReDoS risk is low and accepted with a bounded-input note |
| T-07-03 | Tampering | golden input/ logs via adapters | low | accept | Synthetic, trusted fixtures parsed by the already-hardened adapter boundary; no new prompt-injection surface |
</threat_model>

<verification>
- `ls -d eval/cases/*/` shows exactly 6 directories.
- `uv run pytest tests/test_eval_cases.py` green; every truth.yaml loads; negative predicate passes offline.
- `uv run ruff check` + `uv run pyright` clean.
</verification>

<success_criteria>
The committed golden suite is complete (6 cases, all SPEC exemplars + quiet-cause + mixed-tz + negative), every truth.yaml is schema-valid and frozen, and the negative case scores correctly by its no-confident-hypothesis predicate. EVAL-01 / ROADMAP SC1 satisfied in full.
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-harness-golden-cases/07-04-SUMMARY.md` when done.
</output>
