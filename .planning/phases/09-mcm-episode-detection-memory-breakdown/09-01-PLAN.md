---
phase: 09-mcm-episode-detection-memory-breakdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/analyze_dss8.py
  - tests/fixtures/mcm/hartford_deny_slice.log
  - tests/test_mcm.py
autonomous: true
requirements: [MCM-01, MCM-02]
user_setup: []

must_haves:
  truths:
    - "tests/test_mcm.py holds the 8 golden assertions from RESEARCH Validation Architecture and fails ONLY because sift.pipeline.mcm is absent (RED for the right reason) — MCM-01, MCM-02"
    - "the Hartford slice fixture, ingested via DsserrorsAdapter, yields dsserrors events whose raw preserves the Format-A denial detail block and the Format-B Info Dump verbatim"
  artifacts:
    - "tests/fixtures/mcm/hartford_deny_slice.log — one denial episode: lead-up Contract Request Succeeded lines carrying AvailableMCM=/HWM(PB)=, the pre-denial Info Dump (Current Memory Info + MCM Settings incl. 'Memory Reserve = 0 (0Bytes)'), the denial banner + tab-indented detail block, the AvailableMCM climb-back succeeded lines, and the memory-status-low / offload-start / offload-complete lifecycle lines; NO State=normal"
    - "tests/test_mcm.py — 8 assertions plus an ingest helper (adapter.parse -> CaseStore.insert_events -> query_events)"
    - "docs/reference/analyze_dss8.py — vendored provenance copy of /home/oliverh/Downloads/analyze_dss8.py"
  key_links:
    - "the test ingest helper reuses the run_parse-style DsserrorsAdapter.parse + CaseStore.insert_events pattern (tests/test_dsserrors.py:22, tests/test_dedup.py:221) — no new ingest machinery"
  backstop:
    - statement: "fixture ends without State=normal so the open/truncated path (D-07) is exercised on realistic data, not just a synthetic edge"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT create or modify src/sift/pipeline/mcm.py in this plan (that is Wave 2 / 09-02 — GREEN)"
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "MUST NOT modify the frozen Event dataclass in src/sift/models.py or enrich src/sift/adapters/dsserrors.py (D-01)"
      status: flagged-unverified
      verification: flagged-unverified
---

<objective>
Author the RED half of this phase: the failing golden test suite, its Hartford
slice fixture, and the vendored reference script. This plan writes NO production
code — it establishes the executable acceptance contract that Wave 2 (09-02)
turns GREEN.

Purpose: the user's TDD convention (RED -> GREEN -> gate -> docs) and the phase
Validation Architecture both require the failing golden tests to exist first, so
the port in 09-02 is measured against a fixed, real-data contract rather than
authored to fit whatever the implementation happened to produce.

Output: `tests/test_mcm.py` (8 assertions + ingest helper),
`tests/fixtures/mcm/hartford_deny_slice.log` (a verbatim Hartford slice with one
denial episode), and `docs/reference/analyze_dss8.py` (provenance copy).

**This plan intentionally leaves the suite RED.** `sift.pipeline.mcm` does not
exist yet, so `tests/test_mcm.py` fails on import and pyright reports the missing
module. That is the correct end state for Wave 1 — the full green gate (ruff +
pyright + pytest) is met at the end of Wave 2 (09-02), not here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcm-episode-detection-memory-breakdown/09-CONTEXT.md
@.planning/phases/09-mcm-episode-detection-memory-breakdown/09-RESEARCH.md
@.planning/phases/09-mcm-episode-detection-memory-breakdown/09-PATTERNS.md

# Test analogs (read to mirror shape — do not modify)
@tests/test_dsserrors.py
@tests/test_salience.py
@tests/conftest.py

# Out-of-repo sources (NOT @-loadable — read by absolute path in-task):
#   /home/oliverh/Downloads/analyze_dss8.py            (vendor target)
#   /home/oliverh/Downloads/hartford/hartford_linux_deny_.log  (fixture source)
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

NEW files:
- `docs/reference/analyze_dss8.py` — vendored, non-executed provenance copy of the reference script (citable line anchors: prescan 112-238, parse_detail_block 247-267, parse_abbrev_block 270-286, `_get` 500-504, constants 38-66).
- `tests/fixtures/mcm/hartford_deny_slice.log` — trimmed verbatim Hartford deny slice, one full denial episode + Info Dump + lifecycle tail, no `State=normal`.
- `tests/test_mcm.py` — the 8 golden assertions (RED until 09-02) plus a fixture-ingest helper.

Consumed by 09-02 (implementation), Phase 10 (report/CSV), Phase 11 (analyze feed).
No production module, no store schema, no CLI wiring in this plan.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the reference script and cut the Hartford deny slice fixture</name>
  <files>docs/reference/analyze_dss8.py, tests/fixtures/mcm/hartford_deny_slice.log</files>
  <read_first>
    - /home/oliverh/Downloads/analyze_dss8.py (the script to vendor verbatim)
    - /home/oliverh/Downloads/hartford/hartford_linux_deny_.log (fixture source — denial banner confirmed at line 5843, 'Memory Reserve = 0 (0Bytes)' Info-Dump line at 5836, offload-complete at 5881, ZERO 'State=normal')
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-RESEARCH.md § "Memory Breakdown" and § "Validation Architecture > Wave 0 Gaps" (fixture contents spec)
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-PATTERNS.md § "tests/fixtures/mcm/hartford_deny_slice.log" (analog: tests/fixtures/node1/DSSErrors.log)
  </read_first>
  <action>
    Copy /home/oliverh/Downloads/analyze_dss8.py byte-for-byte to
    docs/reference/analyze_dss8.py (create docs/reference/ first). Do NOT edit or
    "clean up" the copy — it is a provenance record, never imported or executed.
    Add a one-line British-English provenance header ONLY if it can be placed
    without altering the original body; otherwise leave it untouched and record
    provenance in the plan SUMMARY instead.

    Cut tests/fixtures/mcm/hartford_deny_slice.log as a VERBATIM contiguous slice
    of the real Hartford log. First confirm the anchors by grep (per the
    validate-against-real-data discipline), then extract the contiguous region
    that runs from a few lines before the pre-denial Info Dump through the
    emergency-offload-complete line — approximately source lines 5790-5885. The
    slice MUST contain, in ascending timestamp order and byte-identical to source:
      - at least 3 `Contract Request Succeeded` lines BEFORE the denial banner,
        each carrying `AvailableMCM=` and `HWM(PB)=` (these feed hwm_bytes /
        avail_timeline in prescan);
      - the pre-denial Info Dump block: the timestamped `... Current Memory Info:`
        line + its continuation lines (System Total, High Watermark, etc.), and
        the timestamped `... MCM Settings:` line + its continuation lines
        INCLUDING the literal `Memory Reserve = 0 (0Bytes)` line and
        `SmartHeap Cache Releasable = true`;
      - the denial banner line containing `IServer enters MCM denial state`
        followed by its tab-indented Format-A detail block (the 23 `Label(UNIT):
        value` continuation lines: Total System Physical Memory(GB), Cube Caches
        In Memory(MB), Working Set Cache RAM Usage...(MB), MMF Virtual Memory
        Size(MB), Other Memory In Intelligence Server(MB), etc.) up to and
        including its `Note:` / `Working set includes` / `SmartHeap cache memory`
        terminator lines;
      - the AvailableMCM climb-back succeeded line(s) after denial
        (`AvailableMCM=0` at denial rising to `AvailableMCM=10221216831`);
      - the lifecycle tail: `Memory status changes to low (...)`
        (ContractManagerImplSH.cpp:81), the `MsiSessionManager::MemoryStatusHandler()
        : Memory status is low` line, `... Initiating emergency memory offload for
        Working Set due to low memory` (offload start), and `... Working set
        emergency offload completed` (offload complete, source line 5881).
    The slice MUST end WITHOUT any `State=normal` line (the real log has none —
    this preserves the D-07 open/truncated path). Keep bytes verbatim (including
    tabs) so DsserrorsAdapter groups the detail block into one event and the
    Info-Dump markers stay scannable in event.raw. Target under ~150 lines so it
    commits comfortably.
  </action>
  <acceptance_criteria>
    - `docs/reference/analyze_dss8.py` exists and its body byte-matches the source (diff of the two bodies is empty aside from any single provenance header line).
    - `grep -c 'IServer enters MCM denial state' tests/fixtures/mcm/hartford_deny_slice.log` returns 1.
    - `grep -c 'State=normal' tests/fixtures/mcm/hartford_deny_slice.log` returns 0.
    - `grep -F 'Memory Reserve = 0 (0Bytes)' tests/fixtures/mcm/hartford_deny_slice.log` matches (line survives into the fixture).
    - `grep -F 'Working set emergency offload completed' tests/fixtures/mcm/hartford_deny_slice.log` matches, and `grep -F 'Current Memory Info:'` and `grep -F 'MCM Settings:'` both match.
    - `grep -c 'Contract Request Succeeded' tests/fixtures/mcm/hartford_deny_slice.log` returns >= 3.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/reference/analyze_dss8.py && test "$(grep -c 'IServer enters MCM denial state' tests/fixtures/mcm/hartford_deny_slice.log)" = "1" && test "$(grep -c 'State=normal' tests/fixtures/mcm/hartford_deny_slice.log)" = "0" && grep -qF 'Memory Reserve = 0 (0Bytes)' tests/fixtures/mcm/hartford_deny_slice.log && grep -qF 'Working set emergency offload completed' tests/fixtures/mcm/hartford_deny_slice.log</automated>
  </verify>
  <done>Reference vendored; fixture is a verbatim single-episode Hartford slice with the Info Dump, Format-A detail block, lifecycle tail, and no State=normal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the RED golden test suite tests/test_mcm.py (8 assertions + ingest helper)</name>
  <files>tests/test_mcm.py</files>
  <read_first>
    - tests/test_dsserrors.py:22-34 (run_parse helper — DsserrorsAdapter.parse pattern to reuse)
    - tests/test_dedup.py:221-224 (CaseStore(tmp_path/"case.db") + store.insert_events pattern)
    - tests/test_salience.py:90-104 (call-twice determinism-assert shape to adapt to model_dump_json byte-identity)
    - tests/conftest.py (autouse tmp/network-guard fixtures already in force)
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-RESEARCH.md § "Validation Architecture > Phase Requirements -> Test Map" (the 8 named tests + expected values) and § "Memory Breakdown" (cube=27923, working_set=268502, mmf=365, other_memory=101682, iserver_virtual=410325, physical_total from GB=499, 23 labels)
    - .planning/phases/09-mcm-episode-detection-memory-breakdown/09-PATTERNS.md § "tests/test_mcm.py"
  </read_first>
  <behavior>
    - test_hartford_single_episode: detect_episodes(query) returns exactly 1 episode; its denial_event_id is a real event_id present in the store (MCM-01, crit #1). No prompts, no input().
    - test_lifecycle_signals: the episode's lifecycle signals include a memory-status-low, an emergency-offload-start, and an emergency-offload-complete signal; each references an event_id that exists in the store and falls within the episode span (MCM-01, crit #2, D-02).
    - test_absent_signals_tolerated: recovery is None (no State=normal in fixture); querying signals not present in the fixture yields absence, not a fabricated value or an exception (crit #2, D-03).
    - test_breakdown_values: the breakdown's typed accessors return cube_caches=27923, working_set=268502, mmf=365, other_memory=101682, iserver_virtual=410325 (all MB-native), and the verbatim map has all 23 Format-A labels incl. the physical/virtual split (Total System Physical Memory / Total System Virtual Memory) (MCM-02, crit #3).
    - test_mcm_settings_complete: mcm_settings contains `SmartHeap Cache Releasable` (=true) AND `Memory Reserve` (the 0Bytes line is NOT dropped) (MCM-02, crit #3).
    - test_open_truncated_episode: the single episode has open_truncated True and recovery None; detect_episodes did not raise and did not drop the episode (crit #4, D-07).
    - test_determinism_byte_identical: two detect_episodes calls on the same queried events produce byte-identical `[e.model_dump_json() for e in episodes]` (crit #5, D-05).
    - test_fragmented_flag: a synthetic 2-source-file case (a denial event with an empty detail block whose neighbouring event has a different source_file) sets fragmented True (D-06 design guard).
  </behavior>
  <action>
    Create tests/test_mcm.py with `from __future__ import annotations`. Import the
    yet-to-exist public surface from sift.pipeline.mcm: `detect_episodes`,
    `McmEpisode`, `MemoryBreakdown`, `LifecycleSignal` (the import is expected to
    fail at collection now — that is the RED state). Add a module-level
    `# pyright: ignore` on the import line only if needed to keep the file's own
    syntax parseable; do not stub the module.

    Write an ingest helper `_episodes_from_fixture(tmp_path, rel="hartford_deny_slice.log")`
    that parses tests/fixtures/mcm/hartford_deny_slice.log through a fresh
    DsserrorsAdapter (mirror tests/test_dsserrors.py:22 run_parse — set
    adapter.input_root, call list(adapter.parse(path, "case1"))), inserts the
    events into `CaseStore(tmp_path / "case.db")` via store.insert_events, and
    returns `detect_episodes(store.query_events())`. Reuse the FIXTURES-path
    convention (`Path(__file__).parent / "fixtures" / "mcm"`).

    Author the 8 tests named exactly as in <behavior> and RESEARCH's Test Map, one
    assertion cluster each, using tmp_path. Assert the pinned MB accessor values
    as integers/floats per RESEARCH; assert `len(breakdown.raw_map) == 23`; assert
    membership for the physical/virtual split labels and for `Memory Reserve` and
    `SmartHeap Cache Releasable` in mcm_settings. For determinism, compare the
    JSON lists for exact string equality. For the fragmented case, build two tiny
    in-memory events with different source_file values (do not add a store table)
    and call detect_episodes directly. Do NOT weaken any assertion to make it pass
    now — these must be RED until 09-02. NO network, NO input(), NO sleeps.
  </action>
  <acceptance_criteria>
    - tests/test_mcm.py defines exactly the 8 test functions named in <behavior>.
    - `uv run pytest tests/test_mcm.py -q` FAILS, and the failure is the missing module (grep of output shows `No module named 'sift.pipeline.mcm'` or an ImportError/collection error for that module) — i.e. RED for the right reason, not an assertion authored to already pass.
    - `grep -c 'def test_' tests/test_mcm.py` returns 8.
    - the file references `detect_episodes`, `McmEpisode`, `MemoryBreakdown`, and `LifecycleSignal` and the fixture path `fixtures/mcm/hartford_deny_slice.log`.
    - no `input(`, `requests`, `httpx`, or `socket` connect calls appear in the test file.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'def test_' tests/test_mcm.py)" = "8" && uv run pytest tests/test_mcm.py -q 2>&1 | grep -Eq "No module named 'sift.pipeline.mcm'|ModuleNotFoundError|ImportError"</automated>
  </verify>
  <done>tests/test_mcm.py holds the 8 golden assertions and the ingest helper; the suite is RED solely because sift.pipeline.mcm is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture file -> test-time DsserrorsAdapter | The slice is trusted local test data authored here; no runtime parse of external/untrusted input occurs in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Information disclosure | tests/fixtures/mcm/hartford_deny_slice.log (real host/SID/OID tokens) | low | accept | Local test fixture from real diagnostics; report redaction (REPT-05) is explicitly v2/out of scope. No egress — tests run under the conftest network guard. Accepted for a private-repo dev fixture. |
| T-09-01-02 | Tampering | docs/reference/analyze_dss8.py | low | accept | Vendored copy is non-executed and never imported; it cannot affect runtime behaviour. Provenance-only. |
| T-09-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (pytest already present, no new deps — RESEARCH Package Legitimacy Audit N/A). No supply-chain surface. |

No `high`-severity threats — nothing blocks at ASVS L1 / block_on=high.
</threat_model>

<verification>
- `test -f docs/reference/analyze_dss8.py` — reference vendored.
- Fixture greps: exactly 1 denial banner, 0 `State=normal`, `Memory Reserve = 0 (0Bytes)` and `Working set emergency offload completed` present, >= 3 `Contract Request Succeeded`.
- `grep -c 'def test_' tests/test_mcm.py` == 8.
- `uv run pytest tests/test_mcm.py -q` fails on the missing `sift.pipeline.mcm` module (intended RED). Full-suite green is NOT a gate for this wave — it is met at the end of 09-02.
</verification>

<success_criteria>
The failing golden contract exists and is executable: 8 named tests, a real
verbatim Hartford slice with the open/truncated (no-State=normal) shape, and the
vendored reference — all committed, with the suite RED solely for the absence of
the Wave-2 module.
</success_criteria>

<output>
Create `.planning/phases/09-mcm-episode-detection-memory-breakdown/09-01-SUMMARY.md` when done.
</output>
