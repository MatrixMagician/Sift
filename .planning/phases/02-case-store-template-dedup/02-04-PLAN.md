---
phase: 02-case-store-template-dedup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sift/store.py
  - src/sift/cli.py
  - src/sift/pipeline/dedup.py
  - tests/test_cli.py
  - tests/test_store.py
  - tests/test_dedup.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [STORE-01, STORE-04, CLI-03]
user_setup: []

must_haves:
  truths:
    - "A file whose parse fails mid-stream after >=1 inserted batch (e.g. a truncated .gz raising EOFError past 5000 events) contributes exactly ZERO event rows; after the run, sum(template_groups.count) == count(events) == sum(parse_coverage event_counts) (CR-01)"
    - "Every DB-sourced field on both show paths — event_id, ts, severity, template_id, count, severity_max, first_ts, last_ts and every exemplar id, not just message/source_file/template — passes through _sanitise before reaching the terminal; a non-list exemplar_event_ids JSON renders sanitised instead of crashing ' '.join with a raw traceback (WR-01, T-04-01)"
    - "Duplicate --filter keys exit 2 with a loud error naming the duplicated key — never silent last-wins (WR-05, fail-loud prohibition)"
    - "REQUIREMENTS.md carries partial-scope notes on CLI-03 ('ingest leg delivered Phase 2; embedding/generation legs Phases 3-4') and STORE-04 ('events+clusters targets delivered Phase 2; hypotheses target Phase 4') on both the checkbox lines and the traceability rows"
  artifacts:
    - path: src/sift/store.py
      provides: "per-file savepoint contextmanager with a code-constant name; non-list exemplar JSON guard in query_template_groups; ROLLBACK guards that never mask the original error; migration notice on stderr"
      contains: "_SAVEPOINT_INGEST_FILE"
    - path: src/sift/cli.py
      provides: "savepoint-wrapped per-file ingest body; whole-line _sanitise on both show paths; duplicate-filter-key rejection; guarded case open (exit 1, no traceback); stale-groups warning"
      contains: "duplicate filter key"
    - path: src/sift/pipeline/dedup.py
      provides: "letter-required bare-hex mask (pure-decimal runs fall through to <NUM>); MASK_VERSION 2; stale-flag clear inside the rebuild transaction"
      contains: "MASK_VERSION = 2"
    - path: tests/test_cli.py
      provides: "truncated-gz mid-stream accounting test; hostile-bytes fixture test; duplicate-key exit-2 test; corrupt-db exit-1 test; stale-warning test"
    - path: .planning/REQUIREMENTS.md
      provides: "partial-scope notes on CLI-03 and STORE-04"
  key_links:
    - from: src/sift/cli.py
      to: src/sift/store.py
      via: "per-file ingest body (detect+parse+insert) runs inside store.savepoint() so a failed file rolls back to zero rows while the outer all-or-nothing transaction survives"
      pattern: "store\\.savepoint\\("
    - from: src/sift/cli.py
      to: src/sift/pipeline/dedup.py
      via: "ingest sets template_groups_stale=1 inside the event transaction; rebuild_template_groups clears it inside the rebuild transaction; show clusters warns when set"
      pattern: "template_groups_stale"
---

<objective>
Close the four verifier gaps from 02-VERIFICATION.md (status: gaps_found, 20/24): CR-01
write-path accounting integrity (per-file savepoint), WR-01 complete render-time sanitisation
on both show paths, WR-05 duplicate --filter key rejection, and the missing REQUIREMENTS.md
partial-scope notes. Cheap ride-alongs from 02-REVIEW.md land inside the gap tasks they are
adjacent to: WR-02 (graceful show failure on corrupt/read-only case.db + migration notice),
WR-03 (stale-groups detection), WR-04 (bare-hex mask decimal fix), IN-03 (ROLLBACK masking),
IN-04 (TOCTOU stat).

This is surgical gap closure — plans 02-01..02-03 are executed and their scope is NOT
re-planned. The seven existing plan prohibitions remain in force; nothing here weakens them.

Purpose: flip the four failed/partial truths to verified so Phase 2 passes re-verification.
Output: savepoint-hardened ingest, fully sanitised show paths, fail-loud duplicate filters,
honest traceability record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-case-store-template-dedup/02-VERIFICATION.md
@.planning/phases/02-case-store-template-dedup/02-REVIEW.md
@src/sift/store.py
@src/sift/cli.py
@src/sift/pipeline/dedup.py
</context>

## Artifacts this phase produces (plan 02-04 share)

New symbols created by this plan (source-grounding: exclude from drift verification):

- `src/sift/store.py` — `_SAVEPOINT_INGEST_FILE` module constant (the savepoint name);
  `CaseStore.savepoint()` contextmanager; non-list guard on `json.loads` of
  `exemplar_event_ids` inside `query_template_groups`; try/except OperationalError guards
  around the ROLLBACK statements in `_migrate` and `transaction()` (and the savepoint's
  rollback path); one-line stderr notice in `_migrate` when a migration actually applies
- `src/sift/cli.py` — per-file ingest body wrapped in `store.savepoint()`; whole-line
  `_sanitise` wrapping on both show render paths; duplicate-key `ValueError` in
  `_parse_filters` (new CLI behaviour: exit 2 on a repeated --filter key); `sqlite3.Error`
  catch in `_case_store` (new CLI behaviour: exit 1 with message, no traceback, on
  corrupt/read-only case.db); `template_groups_stale` warning on `show clusters` (stderr);
  defensive per-file `stat()` in the sizes pass
- `src/sift/pipeline/dedup.py` — `MASK_VERSION = 2`; bare-hex mask alternative requiring at
  least one hex letter; `template_groups_stale` cleared to "0" inside the rebuild transaction
- New meta key: `template_groups_stale` ("1" = events committed but groups not yet rebuilt)
- Tests: `test_ingest_truncated_gz_mid_stream_contributes_zero_rows` (tests/test_cli.py),
  hostile-bytes fixture test extending the T-04-01 family, duplicate-filter-key exit-2 test,
  corrupt-case-db exit-1 test, stale-warning test, pure-decimal mask test (tests/test_dedup.py)
- `.planning/REQUIREMENTS.md` — partial-scope notes on CLI-03 and STORE-04 (checkbox lines
  and traceability rows)

## Deferred ride-alongs (explicitly not planned, one-line reasons)

- IN-01 (severity vocabulary in four places): cross-cutting refactor of models.py plus three
  consumers — not adjacent to any gap task; carry to a later phase.
- IN-02 (migration 2 fetchall): one-time v1→v2 upgrade bounded by Phase-1 case sizes; no gap
  task touches `_migration_2`.
- IN-05 (dir-symlink silence): new file-collection behaviour + test, outside every gap task's
  edit region; carry forward.
- IN-06 (ZstdError context in query_events): latent — no CLI path reads `raw` until Phase 3;
  fix when `query_events` gains its first caller.
- Human-verification items (TTY progress, idle-machine perf re-run, migration-concurrency and
  interrupted-ingest backstops, prohibition sign-off): preserved in
  `behavior_unverified_items`; they survive to end-of-phase UAT and are NOT planned here.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — per-file savepoint so a failed file contributes zero rows (+ WR-03 stale flag, IN-03, IN-04)</name>
  <files>src/sift/store.py, src/sift/cli.py, src/sift/pipeline/dedup.py, tests/test_cli.py</files>
  <read_first>
    - src/sift/store.py (the `_migrate` BEGIN IMMEDIATE runner at lines 229-242 and the `transaction()` contextmanager at 244-254 — the savepoint mirrors their shape; note the existing "PRAGMA cannot take ? parameters; version is an internal migration number, never user data" comment convention for code-constant SQL interpolation)
    - src/sift/cli.py (the `_ingest` per-file loop, lines 199-296: the symlink skip, the try body from `adapters.detect` through the batched insert loop, the except branch that records `event_count: 0`, and the post-transaction `rebuild_template_groups` call)
    - src/sift/pipeline/dedup.py (rebuild_template_groups' closing `with store.transaction():` block at lines 121-123 — the stale-flag clear goes inside it)
    - tests/test_cli.py (test_ingest_corrupt_compressed_file_fails_loudly_but_continues at line 185 — the detect-time-failure sibling of the new mid-stream test; CliRunner idioms)
  </read_first>
  <behavior>
    - Test 1 (the CR-01 pin, RED before the fix): build an input dir with one small good log file and one large truncated gzip. Generate ~20000 distinct timestamped log lines, gzip them, then truncate the compressed bytes to ~60% so gzip raises EOFError mid-stream. First, directly iterate the genericlog adapter's parse() over the truncated file inside pytest.raises, counting yielded events — assert the count exceeds 5000 BEFORE the exception, pinning the fixture as a mid-stream (not detect-time) failure that crosses at least one insert batch. Then run `sift new` + `sift ingest` via CliRunner and assert: exit code 1 with the "file(s) failed to parse" line; an ERROR line naming the truncated file; zero rows in events whose source_file is the truncated file's relpath; the good file's events all present; parse_coverage meta records event_count 0 for the failed file; and the three-way accounting identity holds — sum of template_groups counts == total events row count == sum of parse_coverage event_counts.
    - Test 2 (WR-03): after a successful ingest, `template_groups_stale` meta is "0" and `show clusters` prints no staleness warning; when the flag is manually set to "1" via set_meta (simulating a crash between event commit and rebuild), `show clusters` emits a warning line on stderr containing "stale" and "sift ingest" while still rendering the groups on stdout.
  </behavior>
  <action>
    In store.py add a module-level constant `_SAVEPOINT_INGEST_FILE = "ingest_file"` and a
    `@contextmanager def savepoint(self, name: str = _SAVEPOINT_INGEST_FILE) -> Generator[None]`
    method on CaseStore: execute `SAVEPOINT {name}`; on BaseException execute
    `ROLLBACK TO {name}` then `RELEASE {name}` and re-raise; on success execute
    `RELEASE {name}`. The name is interpolated into SQL text — annotate with the same
    code-constant-never-user-data comment convention as the PRAGMA user_version line, and add
    the noqa S608 marker if ruff flags it. IN-03 ride-along: wrap the bare
    `self._conn.execute("ROLLBACK")` in `_migrate` and in `transaction()` — and the savepoint's
    ROLLBACK TO/RELEASE pair — in try/except sqlite3.OperationalError: pass, so a dead
    transaction never masks the original exception in the traceback.

    In cli.py `_ingest`, wrap the per-file try body (from `adapters.detect(path, relpath,
    overrides)` through the end of the batched insert loop) in `with store.savepoint():` — the
    existing outer `except Exception` branch keeps its exact shape and continues to record
    `event_count: 0`, which the savepoint now makes TRUE (failed file rolls back to zero rows;
    the outer all-or-nothing BEGIN IMMEDIATE transaction at line 201 survives; note SQLite
    savepoints nest inside an open transaction natively). WR-03 ride-along: inside the event
    transaction, next to the parse_coverage set_meta call, add
    `store.set_meta("template_groups_stale", "1")`; in dedup.py's rebuild_template_groups, add
    `store.set_meta("template_groups_stale", "0")` inside the existing rebuild transaction
    beside the mask_version write. In the show clusters branch of cli.py, before rendering,
    read the flag and when it equals "1" print a single warning line to stderr (import sys;
    print with file=sys.stderr): "Warning: template groups are stale (last ingest did not
    complete); re-run 'sift ingest'". IN-04 ride-along: replace the sizes dict comprehension
    (cli.py line 184) with a loop that tries `path.stat().st_size` and records 0 on OSError —
    a file vanishing between rglob and stat then fails loudly in the per-file loop instead of
    aborting the whole run with a traceback.

    Write the tests from <behavior> first and watch Test 1's zero-rows/accounting assertions
    fail against current HEAD (rows from the truncated file are committed today); then apply
    the fix and watch them pass. Do not modify the existing detect-time corrupt-archive test.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - src/sift/store.py contains `_SAVEPOINT_INGEST_FILE` and `def savepoint(`
    - src/sift/cli.py contains `with store.savepoint():` inside the per-file ingest body
    - tests/test_cli.py contains a test asserting >5000 events yield before the mid-stream failure AND zero rows from the failed file AND sum(template_groups.count) == count(events) == sum(parse_coverage event_counts)
    - src/sift/pipeline/dedup.py sets `template_groups_stale` to "0" inside the rebuild transaction; cli.py sets it to "1" inside the event transaction; show clusters warns on stderr when it reads "1"
    - `uv run pytest -q`, `uv run ruff check`, `uv run pyright` all exit 0
  </acceptance_criteria>
  <done>A mid-parse failure after one or more inserted batches contributes exactly zero event rows, coverage accounting is consistent three ways, and a crash between event commit and rebuild is detected (stale warning) instead of silently rendering wrong counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 whole-line sanitisation + exemplar guard, WR-05 duplicate-key rejection, WR-02 graceful open failure</name>
  <files>src/sift/cli.py, src/sift/store.py, tests/test_cli.py, tests/test_store.py</files>
  <read_first>
    - src/sift/cli.py (`_sanitise` at lines 40-59; the show clusters render at 417-423 and show events render at 428-435 — the exact f-strings to wrap; `_parse_filters` at 315-370 — the allowlist check after which the duplicate check goes; `_case_store` at 62-72)
    - src/sift/store.py (`query_template_groups` at 377-404 — the `json.loads(r[6])` that needs the non-list guard; `CaseStore.__init__` at 221-227 and `_migrate` at 229-242 for the WR-02 notice)
    - tests/test_cli.py (test_show_clusters_strips_terminal_escapes at line 488 — the T-04-01 test shape to extend; test_show_events_unknown_filter_key_exits_2_listing_keys at 601 — the exit-2 test shape)
  </read_first>
  <behavior>
    - Test 1 (WR-01 store-level fixture): open a case's store directly and UPDATE template_groups planting hostile bytes — an ESC (\x1b) plus CSI sequence in first_ts, and an exemplar_event_ids JSON array whose entries contain ESC and a bidi override (\u202e); also plant ESC bytes in an event row's event_id, ts and severity columns. Run `show clusters` and `show events`; assert exit 0 and that the captured output contains no \x1b and no \u202e anywhere — not just in the message/template fields.
    - Test 2 (WR-01 non-list guard): UPDATE one template_groups row setting exemplar_event_ids to a non-array JSON value (e.g. the JSON string "hostile" or a JSON object). `show clusters` exits 0, renders the row with the value visible-but-sanitised, and emits no traceback.
    - Test 3 (WR-05): `show <case> events --filter severity=error --filter severity=warn` exits 2 with an error message containing "duplicate filter key" and 'severity'. Same for a repeated clusters key.
    - Test 4 (WR-02): write garbage bytes (b"not a sqlite database") over case.db, then `show <case> events` exits 1 with an "Error: cannot open case" message and no Python traceback in output.
  </behavior>
  <action>
    In cli.py rewrite both show render paths to sanitise the COMPLETE rendered line: keep the
    existing per-field truncation/newline-collapsing where it exists, but wrap each print's
    whole f-string in `_sanitise(...)` so event_id, ts, severity, template_id, count,
    severity_max, first_ts, last_ts and every exemplar id are covered in one move (smallest
    diff per WR-01's fix sketch; the count/line_start ints format before sanitisation, which
    is fine). For the exemplars line use `' '.join(map(str, ...))` inside the `_sanitise`
    wrapper.

    In store.py `query_template_groups`, guard the `json.loads(r[6])` result where it is still
    typed Any (pyright strict forbids a redundant isinstance on the list[str]-typed dataclass
    field later): if the loaded value is not a list, wrap it as a single-element list; coerce
    all elements via str() so TemplateGroup.exemplar_event_ids honestly satisfies list[str]
    against tampered JSON (numbers, nested objects). The tampered value stays visible to the
    operator and render-time sanitisation strips its hostile bytes.

    In cli.py `_parse_filters`, immediately after the allowlist membership check (before any
    value validation), raise `ValueError(f"duplicate filter key {key!r}; each key may appear
    once")` when the key is already present in the filters dict. The existing except branch in
    show already converts ValueError to a sanitised exit-2 error — no other change needed.

    WR-02 ride-along: in cli.py `_case_store`, wrap the `CaseStore(db_path)` construction in
    try/except sqlite3.Error, printing `Error: cannot open case {case!r}: {sanitised exc}` and
    raising typer.Exit(1) from None (import sqlite3 in cli.py). In store.py `_migrate`, when a
    migration is about to apply, print a one-line notice to stderr (import sys):
    "note: migrating case.db to schema v{version}" — so `show` never rewrites v1 evidence
    files silently. Existing stdout-contract tests stay untouched because the notice is
    stderr-only and fires only when a migration actually runs.

    Write the four tests first (Tests 1-3 RED against HEAD), then implement. Keep the existing
    test_show_clusters_strips_terminal_escapes green throughout.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - In cli.py both show paths print via `print(_sanitise(f"..."))` — every DB-sourced field passes through _sanitise, verified by the hostile-bytes fixture test asserting the captured show output contains no raw ESC byte for values planted in first_ts, exemplar_event_ids, event_id, ts and severity
    - A non-array exemplar_event_ids JSON renders without a traceback (exit 0), guard living in store.py query_template_groups
    - `sift show <case> events --filter severity=error --filter severity=warn` exits 2 and the message contains "duplicate filter key"
    - Garbage-bytes case.db: `show` exits 1 with "Error: cannot open case" and no traceback text in output
    - store.py `_migrate` prints the migration notice to stderr when applying; `uv run pytest -q && uv run ruff check && uv run pyright` all exit 0
  </acceptance_criteria>
  <done>Truth 24 flips: all rendered cluster/event text passes through _sanitise; tampered case.db bytes cannot drive the terminal or crash show; duplicate filter keys fail loudly per the prohibition; show fails gracefully on corrupt/read-only evidence media and never migrates silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-04 decimal-safe hex mask (MASK_VERSION 2) + REQUIREMENTS.md partial-scope notes</name>
  <files>src/sift/pipeline/dedup.py, tests/test_dedup.py, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/sift/pipeline/dedup.py (the _MASK alternation at lines 22-32 — only the hex alternative changes; the ordering comment and MASK_VERSION at line 16)
    - tests/test_dedup.py (existing mask token-class tests and the ReDoS test shape — the new assertions join that family)
    - .planning/REQUIREMENTS.md (lines 29 and 65 — the STORE-04 and CLI-03 checkbox lines; lines 132 and 134 — their traceability rows)
  </read_first>
  <behavior>
    - Test 1: mask("id 1234567890123 end") == "id &lt;NUM&gt; end" (13-digit epoch-millis is NUM, not HEX)
    - Test 2: mask("retried 12345678 times") == "retried &lt;NUM&gt; times" (8 pure digits — the boundary WR-04 names)
    - Test 3: a letter-containing 8+ char hex run (e.g. "deadbeef01") still masks to &lt;HEX&gt;; 0x-prefixed hex still masks to &lt;HEX&gt;
    - Existing mask tests (32-hex SID fixtures, ReDoS timing) stay green unchanged
  </behavior>
  <action>
    In dedup.py replace the bare-hex alternative in _MASK with the review's letter-required
    form: the hex group becomes 0[xX][0-9a-fA-F]+ OR a word-bounded run matched only when two
    bounded lookaheads hold — (?=[0-9a-fA-F]{8,}\b) for length/charset and (?=[0-9]*[a-fA-F])
    requiring at least one hex letter — so pure-decimal 8+ digit runs (epoch seconds/millis,
    large ids) fall through to the num alternative instead of shattering templates by
    magnitude. Both lookaheads are bounded linear scans; extend the existing no-ReDoS comment
    (T-02-03) to say so. Bump MASK_VERSION to 2 — groups recompute from store on next ingest,
    and the mask_version meta write in rebuild_template_groups already records it; no
    migration needed. Add the three mask assertions from <behavior> to tests/test_dedup.py.

    Traceability notes in .planning/REQUIREMENTS.md (use Edit, scoped — never rewrite the
    file): on line 29 append to the STORE-04 bullet the note " *(partial scope: events+clusters
    targets delivered Phase 2; hypotheses target Phase 4)*"; on line 65 append to the CLI-03
    bullet " *(partial scope: ingest leg delivered Phase 2; embedding/generation legs Phases
    3-4)*". In the traceability table change row 132 STORE-04 status from "Complete" to
    "Complete (partial scope: events+clusters Phase 2; hypotheses Phase 4)" and row 134 CLI-03
    from "Complete" to "Complete (partial scope: ingest leg Phase 2; embedding/generation
    Phases 3-4)". These are the exact notes 02-02/02-03 SUMMARYs claimed to have written
    (verification gap 4) — the phase's own convention, now actually landed.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run ruff check && uv run pyright && grep -c "partial scope" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - src/sift/pipeline/dedup.py contains `MASK_VERSION = 2` and the hex alternative requires a hex letter for non-0x runs
    - tests/test_dedup.py asserts pure-decimal 8- and 13-digit runs mask to the NUM placeholder and letter-containing hex still masks to the HEX placeholder
    - `grep -c "partial scope" .planning/REQUIREMENTS.md` returns 4 (two checkbox notes + two traceability rows)
    - All existing dedup tests (including the 32-hex SID fixtures and reduction ratio) pass unchanged; `uv run pytest -q && uv run ruff check && uv run pyright` exit 0
  </acceptance_criteria>
  <done>Templates no longer shatter on numeric magnitude; the traceability record honestly states what Phase 2 delivered versus what Phases 3-4 still owe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tampered case.db → operator terminal | this phase's declared trust boundary (the reason _decode_raw carries a zstd-bomb cap): every TEXT column in a shared case file is attacker-controlled and flows to the terminal via show |
| input bundle → write path | a truncated/corrupt archive must not corrupt the store's accounting record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | cli.py show render paths (all DB-sourced fields) | high | mitigate | WR-01: wrap complete rendered lines in _sanitise on both paths; non-list exemplar JSON guard in store.query_template_groups; store-level hostile-bytes fixture test (T-04-01 class, ASVS L1 output-encoding) |
| T-02-12 | Tampering | store.py write-path accounting (CR-01) | high | mitigate | per-file SAVEPOINT so a failed file contributes zero rows; three-way accounting identity pinned by test |
| T-02-13 | Tampering | savepoint SQL identifier interpolation | low | mitigate | name comes only from the module constant _SAVEPOINT_INGEST_FILE, never user data (PRAGMA user_version precedent) |
| T-02-14 | Denial of Service | show on corrupt/read-only case.db | low | mitigate | WR-02: sqlite3.Error caught in _case_store → loud exit 1 message, no traceback; migration notice prevents silent evidence rewrite |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | no new packages in this plan |
</threat_model>

<verification>
- Full gate: `uv run pytest -q && uv run ruff check && uv run pyright` all exit 0
- Perf gate unaffected in principle (savepoint adds one statement per file); re-run `uv run pytest -m perf` on an idle machine remains the existing UAT item — do not treat a loaded-machine red as a regression (02-VERIFICATION precedent)
- The four failed/partial truths from 02-VERIFICATION.md flip: CR-01 accounting identity test green; hostile-bytes fixture test green; duplicate-key exit-2 test green; REQUIREMENTS.md notes present
- The seven existing plan prohibitions remain in force — in particular: stored raw/message stay verbatim (sanitisation is render-only; the savepoint and mask changes never touch stored evidence), and no network egress (no new imports beyond stdlib sys/sqlite3 in cli.py)
</verification>

<success_criteria>
- Re-running phase verification finds gaps 1-4 closed: a mid-stream parse failure contributes zero rows with consistent accounting; every DB-sourced field on both show paths is sanitised; duplicate --filter keys exit 2 loudly; CLI-03/STORE-04 carry partial-scope notes
- Ride-alongs WR-02/WR-03/WR-04/IN-03/IN-04 landed; IN-01/IN-02/IN-05/IN-06 explicitly deferred with reasons recorded in this plan
- Human-verification items untouched — they proceed to end-of-phase UAT as listed in behavior_unverified_items
</success_criteria>

<output>
Create `.planning/phases/02-case-store-template-dedup/02-04-SUMMARY.md` when done, recording:
the savepoint name constant and its nesting relationship to the outer ingest transaction, the
template_groups_stale meta-key contract (who sets, who clears, who reads), the MASK_VERSION 2
rationale (pure-decimal runs are NUM), and confirmation that the REQUIREMENTS.md notes now
exist at all four locations.
</output>
