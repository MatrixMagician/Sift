---
phase: 01-skeleton-event-contract-genericlog-adapter
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - docs/decisions/0001-typer-over-argparse.md
  - docs/decisions/0002-weasyprint-pdf-extra.md
  - docs/decisions/0003-hand-rolled-masking-over-drain3.md
  - src/sift/cli.py
  - tests/test_acceptance.py
autonomous: true
requirements: [INGST-01, INGST-02, INGST-05]
user_setup: []

must_haves:
  truths:
    - "Parse coverage >= 99% on the acceptance fixture, computed by the Pattern 7 formula and printed per-file by `sift ingest` (M1 acceptance, INGST-05)"
    - "The full M1 flow passes as one test: new -> ingest -> show events on the acceptance fixture, second ingest adds zero events (INGST-01, INGST-02)"
    - "event_id determinism across cases: ingesting the same input layout into two different cases yields identical event_id sets (identity is source_file + byte_offset only)"
    - "docs/decisions/ contains the three ADRs for the research-resolved SPEC §10 open questions: Typer over argparse, WeasyPrint behind the sift[pdf] extra, hand-rolled masking over drain3 (D-02)"
    - "`sift ingest --help` documents snapshot semantics: a case is one snapshot of artefacts; re-collect changed inputs into a new case"
    - "The phase gate is green: uv run pytest, uv run ruff check, uv run pyright all exit 0 (SPEC §8 M1 gate — no Phase 2 work while red)"
  artifacts:
    - path: docs/decisions/0001-typer-over-argparse.md
      provides: "ADR: Typer 0.27.x over argparse (D-01 rationale)"
      contains: "Typer"
    - path: docs/decisions/0002-weasyprint-pdf-extra.md
      provides: "ADR: WeasyPrint behind a sift[pdf] optional extra, implementation deferred to Phase 6"
      contains: "WeasyPrint"
    - path: docs/decisions/0003-hand-rolled-masking-over-drain3.md
      provides: "ADR: hand-rolled volatile-token masking over drain3 (dormant, Python <= 3.11)"
      contains: "drain3"
    - path: tests/test_acceptance.py
      provides: "M1 acceptance test: >=99% coverage, idempotency, cross-case determinism"
      contains: "99"
  key_links:
    - from: tests/test_acceptance.py
      to: src/sift/cli.py
      via: "CliRunner exercising the full new -> ingest -> show flow"
      pattern: "CliRunner"
  prohibitions: []
---

<objective>
Close Phase 1: seed the three `docs/decisions/` ADRs required by SPEC §10 (D-02), document the case-snapshot semantics, and prove the M1 acceptance criteria with a dedicated acceptance test — >= 99% parse coverage on a realistic fixture, idempotent re-ingest, deterministic IDs — with the full quality gate green.

Purpose: SPEC §8 M1 is the milestone gate; nothing in Phase 2 starts while this is red. The ADRs make the research-resolved open questions auditable.
Output: Three ADRs, snapshot-semantics help text, passing acceptance suite, green gate.
</objective>

## Phase Goal

**As a** support engineer with a directory of raw diagnostic logs, **I want to** turn it into a queryable case of canonical, deterministic events, **so that** nothing is dropped silently and every event can later be cited as evidence.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md
@SPEC.md
@.planning/research/STACK.md
@src/sift/cli.py
</context>

## Artifacts this phase produces

Files: `docs/decisions/0001-typer-over-argparse.md`, `docs/decisions/0002-weasyprint-pdf-extra.md`, `docs/decisions/0003-hand-rolled-masking-over-drain3.md`, `tests/test_acceptance.py`.
Functions/tests: `test_acceptance_coverage_99`, `test_acceptance_idempotent_reingest`, `test_acceptance_cross_case_determinism` (names indicative); updated `ingest` command help text (snapshot semantics).

## Flagged Assumptions (edge probe: unresolved items surfaced, not dropped)

- **INGST-02 (unclassified probe):** idempotency is defined for the SAME snapshot of artefacts (RESEARCH Open Question 2 / PITFALLS 10). Changed input dirs are out of contract: new files simply add events; RENAMED files add duplicate events — an accepted, documented limitation (cross-collection identity is v2 territory). Disposition: document in `sift ingest --help` (Task 1); no code change.

<tasks>

<task type="auto">
  <name>Task 1: Seed docs/decisions/ ADRs and snapshot-semantics documentation</name>
  <files>docs/decisions/0001-typer-over-argparse.md, docs/decisions/0002-weasyprint-pdf-extra.md, docs/decisions/0003-hand-rolled-masking-over-drain3.md, src/sift/cli.py</files>
  <read_first>SPEC.md (§10 open questions), .planning/research/STACK.md (rationale for all three decisions), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-02), src/sift/cli.py (ingest command docstring/help)</read_first>
  <action>Write the three ADRs (D-02) in a consistent lightweight format — Status / Context / Decision / Consequences — in British English: 0001 records Typer 0.27.x over argparse (D-01: active project, typed params pyright checks, ergonomic subcommands, on the SPEC §9 boring-tech list; cost: one dependency); 0002 records WeasyPrint behind a `sift[pdf]` optional extra with URL fetching disabled, implementation deferred to Phase 6 (answers SPEC §10 question 2; reportlab rejected for Markdown-fidelity reasons per STACK.md); 0003 records hand-rolled volatile-token masking over drain3 for Phase 2's template dedup (drain3 dormant since 2022, supports Python <= 3.11 only; project floor is 3.12). Each ADR cites the SPEC §10 question it answers and the research date (2026-07-16). Update the ingest command's help/docstring in cli.py to state the snapshot contract: a case is one snapshot of artefacts — re-collect changed inputs into a new case; new files in the dir add events, renamed files produce duplicate events (documented limitation). Do not alter any command behaviour.</action>
  <verify>
    <automated>ls docs/decisions/0001-typer-over-argparse.md docs/decisions/0002-weasyprint-pdf-extra.md docs/decisions/0003-hand-rolled-masking-over-drain3.md && uv run sift ingest --help | grep -i snapshot && uv run pytest -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - All three ADR files exist and each contains a "## Decision" heading
    - `uv run sift ingest --help` output contains the word "snapshot"
    - Full suite still green (help-text change only)
  </acceptance_criteria>
  <done>SPEC §10 decisions recorded in docs/decisions/ (D-02); INGST-02's scope limitation is user-visible documentation, not tribal knowledge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: M1 acceptance suite — >=99% coverage fixture, idempotency, cross-case determinism</name>
  <files>tests/test_acceptance.py</files>
  <read_first>SPEC.md (§8 M1 acceptance criteria), .planning/ROADMAP.md (Phase 1 success criteria 1-5), tests/test_cli.py (CliRunner patterns established in earlier plans), src/sift/adapters/genericlog.py (coverage semantics as built by plan 01-03)</read_first>
  <behavior>
    - The acceptance fixture ingests with printed per-file coverage >= 99.0%
    - Second ingest of the same case reports zero new events and leaves the row count unchanged
    - The same input layout ingested into two separate cases yields byte-identical sorted event_id lists
    - show events renders all fixture events with 16-hex IDs
  </behavior>
  <action>tests/test_acceptance.py builds a realistic acceptance fixture in tmp_path (fixture design is a Claude's-discretion area per CONTEXT): file one — roughly 200 ISO 8601 timestamped lines across all five severity tokens plus one 12-line stack trace under a single timestamp and a handful of indented continuation lines; give it a 2-line unparseable preamble whose byte size is under 1% of the file's total bytes (this pins coverage strictly between 99% and 100%, so the >= 99% assertion cannot pass vacuously via a trivially-clean file); file two — a gzip-compressed variant of a second small ISO log (proves compressed inputs flow through the acceptance path). Assertions via CliRunner: (1) parse the per-file coverage figures out of ingest stdout and assert file one's coverage is >= 99.0 and < 100.0; (2) run ingest a second time and assert the zero-new-events report plus an unchanged events count read through a direct CaseStore query; (3) ingest the identical input dir into a second case name and assert sorted event_id sets are equal across the two cases (identity excludes case_id — RESEARCH Pattern 3); (4) show events exit 0 with 16-hex IDs present. Keep fixture-builder helpers local to this file (conftest.py stays owned by plan 01-01). Finish by running the complete phase gate and fixing any residual ruff/pyright findings anywhere in the codebase — SPEC §8: no Phase 2 while M1 is red.</action>
  <verify>
    <automated>uv run pytest tests/test_acceptance.py -x -q && uv run pytest -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_acceptance.py -x` exits 0
    - The coverage assertion is bounded (>= 99.0 and < 100.0), proving the metric is real, not vacuous
    - Cross-case determinism assertion passes
    - `uv run pytest && uv run ruff check && uv run pyright` all exit 0 (M1 gate)
  </acceptance_criteria>
  <done>All five ROADMAP Phase 1 success criteria and the SPEC §8 M1 acceptance line are proven by automated tests; the milestone gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds documentation and tests only; no new code surface beyond help text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Repudiation | docs/decisions ADRs | low | accept | ADRs are the mitigation: decisions auditable in-repo rather than in chat history |
</threat_model>

<verification>
- `uv run pytest tests/test_acceptance.py -q` green
- Full M1 gate: `uv run pytest && uv run ruff check && uv run pyright` all exit 0
- All plan-level must_haves across 01-01..01-05 verifiable
</verification>

<success_criteria>
- ROADMAP Phase 1 success criteria 1-5 demonstrably true
- SPEC §8 M1 acceptance line passes: new/ingest/show events end-to-end on a fixture log, coverage >= 99%, idempotent re-ingest
- docs/decisions/ seeded per D-02
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-05-SUMMARY.md` when done
</output>
