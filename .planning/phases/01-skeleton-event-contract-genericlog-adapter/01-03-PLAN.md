---
phase: 01-skeleton-event-contract-genericlog-adapter
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/sift/adapters/genericlog.py
  - tests/test_genericlog.py
autonomous: true
requirements: [INGST-04, INGST-05, INGST-06, INGST-10, INGST-11]
user_setup: []

must_haves:
  truths:
    - "genericlog parses ISO 8601 (T/space, fractional, Z, ±HH:MM), syslog RFC3164, and epoch seconds/millis timestamps; continuation lines group into the preceding event (INGST-04)"
    - "A stack-trace fixture ingests as ONE event; a record exceeding 256 lines or 64 KB splits into a new severity-unknown continuation event instead of growing unbounded (INGST-06, D-06)"
    - "Unparseable regions become severity-unknown events with ts_confidence 'missing' — nothing dropped silently; per-file coverage = 1 - unknown_fallback_bytes/total_bytes (INGST-05)"
    - "Event byte spans partition every file: contiguous, non-overlapping, sum of spans equals total decompressed bytes including BOM and newlines (invariant test)"
    - "An empty file parses to zero events without error and reports coverage 1.0"
    - "gzip and zstd fixtures (including multi-member gzip and multi-frame zstd) yield the same messages, line numbers, byte offsets, and coverage as the identical plain-text file (INGST-10, D-07)"
    - "Naive timestamps normalise to UTC with ts_confidence 'inferred'; explicit-offset timestamps give 'exact'; a tz_overrides glob match applies the mapped IANA zone before conversion, and the assumption is disclosed in ParseStats.notes (INGST-11, D-05)"
    - "UTF-16LE/BE with BOM, cp1252, invalid-byte, and CRLF files all parse with correct byte offsets (encoding edge cases from PITFALLS checklist)"
    - statement: "Compressed-file ingest shares the single-transaction guarantee from plan 01-02; a corrupt or truncated compressed file raises a loud per-file error surfaced by the CLI report, never a silent skip or partial-file silence"
      verification: backstop
  artifacts:
    - path: src/sift/adapters/genericlog.py
      provides: "full timestamp ladder, encoding-aware byte-offset loop, caps, coverage, tz normalisation"
      contains: "MAX_EVENT_LINES"
    - path: tests/test_genericlog.py
      provides: "format/timezone/multiline/coverage/encoding/compressed test groups"
      contains: "def test_"
  key_links:
    - from: src/sift/adapters/genericlog.py
      to: src/sift/adapters/base.py
      via: "parse streams from base.open_bytes; stats reported via base.ParseStats"
      pattern: "open_bytes|ParseStats"
    - from: src/sift/adapters/genericlog.py
      to: src/sift/models.py
      via: "event_id computed from case-relative POSIX path and decompressed byte offset"
      pattern: "event_id\\("
  prohibitions:
    - statement: "The parser must never fabricate a severity or a timestamp — unrecognised severities stay 'unknown' and unparseable timestamps stay None/'missing'; guessing corrupts the evidence trail"
      status: flagged-unverified
---

<objective>
Grow genericlog v0 into the robust fallback parser SPEC §5.2 demands: the full timestamp ladder, encoding-aware byte accounting, D-06 safety caps, the parse-coverage metric that cannot trivially read 100%, compressed-input support (D-07), and UTC normalisation with `ts_confidence` and per-glob timezone overrides (D-05).

Purpose: This is the phase's genuinely custom component — determinism over raw bytes is the product requirement. Every later adapter and every citation depends on offsets being right.
Output: genericlog passes format, timezone, multiline, coverage, encoding, and compressed test groups; span-partition invariant holds on every fixture.
</objective>

## Phase Goal

**As a** support engineer with a directory of raw diagnostic logs, **I want to** turn it into a queryable case of canonical, deterministic events, **so that** nothing is dropped silently and every event can later be cited as evidence.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md
@src/sift/adapters/genericlog.py
@src/sift/adapters/base.py
</context>

## Artifacts this phase produces

Functions/constants added to `sift.adapters.genericlog`: timestamp ladder entries (ISO 8601, syslog RFC3164 with mtime year inference, epoch seconds/millis with plausibility window, Apache CLF), `MAX_EVENT_LINES = 256`, `MAX_EVENT_BYTES = 65536`, `EPOCH_MIN = 946684800`, `EPOCH_MAX = 4102444800`, BOM/encoding detection (utf-8-sig, utf-16-le, utf-16-be, cp1252 fallback), per-encoding newline byte patterns, per-file format lock-in fast path, cap-overflow split, ParseStats.notes timezone disclosures.
Tests: `tests/test_genericlog.py` with test groups matching `-k timezone`, `-k format`, `-k multiline`, `-k coverage`, `-k encoding`, `-k compressed`; local fixture-builder helpers (NOT in conftest.py — that file is owned by plan 01-01 and plan 01-04 runs in this same wave).

## Flagged Assumptions (edge probe: unresolved items surfaced, not dropped)

- **INGST-04 (unclassified probe):** the exact v1 timestamp format list is a Claude's-discretion area (CONTEXT). This plan adopts the RESEARCH Pattern 5 ladder including Apache CLF (assumption A1) — omitting CLF would only lower coverage on web-server logs.
- **INGST-06 (unclassified probe):** cap-overflow semantics follow D-06 verbatim (split into a new severity-unknown event); the overflow event carries ts None / ts_confidence "missing" so it counts as unknown-fallback for coverage. Severity for token-less timestamped lines stays "unknown" rather than "info" (assumption A2 — flagged for review, trivially changeable now, breaking later).
- **INGST-11 (unclassified probe):** year-less syslog timestamps infer the year from file mtime, minus one year when the result lands more than a day after mtime (December logs read in January — assumption A3, refines D-05).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Timestamp ladder + UTC normalisation with ts_confidence and tz overrides</name>
  <files>src/sift/adapters/genericlog.py, tests/test_genericlog.py</files>
  <read_first>src/sift/adapters/genericlog.py (v0 from plan 01-02 — extend, do not rewrite the byte loop), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 5 full ladder table, Pitfall 5 epoch false positives, Code Example 5), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-05)</read_first>
  <behavior>
    - "2026-07-16 14:02:03,123 ERROR x" and "2026-07-16T14:02:03Z x" and "2026-07-16T14:02:03+02:00 x" all parse; the Z/offset forms give ts_confidence "exact", the naive form "inferred"
    - "Jul  9 03:14:15 host proc: msg" parses via syslog rules; year inferred from file mtime; a syslog ts landing after mtime + 1 day gets the previous year; always "inferred"
    - "1752674523 msg" (epoch seconds) and "1752674523123 msg" (millis) parse as UTC "exact"; "99999 msg" and "9999999999999999 msg" do NOT parse as timestamps (plausibility window)
    - A line starting "20260716" is NOT treated as a timestamp (fromisoformat greediness guard)
    - With tz_overrides {"node1/*": "Europe/Berlin"}, a naive timestamp in node1/app.log converts from Berlin time to UTC, ts_confidence "inferred", and ParseStats.notes discloses the assumption
    - A naive timestamp with no override converts assuming UTC and notes disclose that assumption once per file
  </behavior>
  <action>Extend the ladder beyond ISO (D-05 semantics throughout): each ladder entry = anchored regex extracting a bounded candidate slice + a parse function. Order per RESEARCH Pattern 5: (1) ISO 8601 via datetime.fromisoformat on the regex-bounded candidate only; (2) syslog RFC3164 — regex for three-letter month, day, HH:MM:SS at line start, strptime "%b %d %H:%M:%S", year from the file's stat mtime with the minus-one-year rule when result exceeds mtime by more than one day (A3); (3) epoch — 10-digit (optional fraction) or 13-digit integer at line start followed by a delimiter, value bounded to [EPOCH_MIN 946684800, EPOCH_MAX 4102444800] (millis divided by 1000 must land in the same window), datetime.fromtimestamp with tz=timezone.utc, "exact"; (4) Apache CLF dd/Mon/yyyy:HH:MM:SS ±zzzz via strptime "%d/%b/%Y:%H:%M:%S %z", "exact" (A1). Per-file format lock-in: remember the ladder index of the first match and try it first on subsequent lines, falling back to the full ladder on a miss (deterministic fast path). Naive results route through to_utc: first fnmatch match of the file's case-relative POSIX path against tz_overrides (insertion order) picks the IANA zone via zoneinfo.ZoneInfo, else UTC; always "inferred". Append one disclosure note per file per assumption kind to ParseStats.notes, e.g. naming the assumed zone and the matching glob (D-05: assumptions disclosed in coverage output). Update sniff to count a match from ANY ladder entry (still returning 0.1/0.0). Tests: name the timezone tests so `-k timezone` selects them and format tests so `-k format` selects them; build fixture files with local helper functions inside tests/test_genericlog.py (do NOT touch tests/conftest.py — owned by plan 01-01, and plan 01-04 runs concurrently this wave).</action>
  <verify>
    <automated>uv run pytest tests/test_genericlog.py -k "timezone or format" -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_genericlog.py -k timezone -x` exits 0 with at least 3 tests collected
    - A test asserts every parsed Event.ts is timezone-aware UTC or None (Pitfall 2 enforcement)
    - A test proves the "20260716"-prefix line does not start a new event
    - A test proves the epoch window rejects out-of-range integers
    - A test asserts ParseStats.notes contains the assumed-timezone disclosure when tz_overrides matched
  </acceptance_criteria>
  <done>Full v1 timestamp ladder with D-05 UTC/ts_confidence semantics and glob timezone overrides, disclosed in stats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encoding-aware byte accounting, D-06 caps, and the coverage invariant</name>
  <files>src/sift/adapters/genericlog.py, tests/test_genericlog.py</files>
  <read_first>src/sift/adapters/genericlog.py (current byte loop), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 4 steps 2-5, Pattern 7 coverage formula, Pitfalls 1 and 7), .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-CONTEXT.md (D-06)</read_first>
  <behavior>
    - UTF-16LE-with-BOM, UTF-16BE-with-BOM, utf-8-sig, cp1252 (0xE9 byte), invalid-byte, and CRLF fixtures all parse; for each, sum of event byte spans == total file bytes
    - A 12-line Java-style stack trace under one timestamped line is ONE event with line_end - line_start == 11
    - A 300-line single record splits at 256 lines: first event capped, remainder becomes a new severity-unknown event (same for a >64 KB record)
    - Leading unparseable region (before the first timestamped line) is its own unknown event; coverage on a fixture with a known unparseable byte count equals the hand-computed value
    - Empty file: zero events, coverage 1.0, no exception
  </behavior>
  <action>Encoding layer per RESEARCH Pattern 4: sniff the first 4 DECOMPRESSED bytes for BOMs (utf-8-sig ef bb bf; utf-16-le ff fe; utf-16-be fe ff), else utf-8 with cp1252 as the per-record decode fallback; select the newline byte pattern by encoding (utf-8/cp1252 single LF byte; utf-16-le LF-then-NUL; utf-16-be NUL-then-LF); all splitting and offset arithmetic stays at the byte level, decode each record with errors-replace only after its offset and span are fixed (Pitfall 1). Byte accounting rules (Pitfall 7): BOM bytes belong to the first emitted event's span (or the leading unknown region if one exists); carriage-return bytes are counted in spans but stripped from decoded text only; every decompressed byte belongs to exactly one event. Caps (D-06): MAX_EVENT_LINES 256 and MAX_EVENT_BYTES 65536 — on breach, close the current event and open a NEW continuation event with severity "unknown", ts None, ts_confidence "missing" (bounded memory, T-03-01). Coverage (Pattern 7): unknown-fallback events are those with ts None; ParseStats.unknown_fallback_bytes sums their spans; coverage property already 1 - unknown/total with the empty-file 1.0 case. Add an explicit span-partition invariant helper used by tests: spans contiguous from 0, non-overlapping, summing to total_bytes. Tests: groups selectable via `-k multiline`, `-k coverage`, `-k encoding`; hand-compute expected coverage for the unparseable-region fixture; assert byte offsets of events in the UTF-16 fixture differ from the UTF-8 fixture (2-byte units) while messages are identical.</action>
  <verify>
    <automated>uv run pytest tests/test_genericlog.py -k "multiline or coverage or encoding" -x -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_genericlog.py -k multiline -x` exits 0 and includes a 256-line-cap test
    - `uv run pytest tests/test_genericlog.py -k coverage -x` exits 0 and includes the span-partition invariant asserted across ALL fixture encodings
    - A test asserts the empty-file case: zero events, coverage == 1.0
    - genericlog.py contains MAX_EVENT_LINES and MAX_EVENT_BYTES constants
  </acceptance_criteria>
  <done>Nothing-dropped-silently holds mechanically: every byte attributed, caps bound memory, coverage formula cannot trivially read 100%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compressed inputs — gzip/zstd with decompressed-stream offsets</name>
  <files>src/sift/adapters/genericlog.py, tests/test_genericlog.py</files>
  <read_first>src/sift/adapters/base.py (open_bytes — already handles both formats), src/sift/adapters/genericlog.py, .planning/phases/01-skeleton-event-contract-genericlog-adapter/01-RESEARCH.md (Pattern 4 step 1, Pitfalls 3 and 4, D-07 in user constraints)</read_first>
  <behavior>
    - app.log, app.log.gz (single member), app-multi.gz (two concatenated gzip members), app.log.zst (single frame), app-multi.zst (two concatenated frames) with identical logical content yield: identical event count, messages, line numbers, byte offsets, and coverage; only source_file (and therefore event_id) differs
    - A file named plain.log whose CONTENT is gzip bytes decompresses anyway (magic bytes, not extension)
    - A truncated/corrupt .zst file raises an exception from parse (surfaced by the CLI per-file error path from plan 01-02) — it does not return a partial silent result
  </behavior>
  <action>Ensure parse and sniff both route through base.open_bytes/base.read_head so compressed files sniff and parse on DECOMPRESSED content (Pitfall 4 — a gzipped syslog must sniff as genericlog). Confirm byte offsets refer to the decompressed stream and source_file records the compressed file's own case-relative path (D-07: keeps event_id deterministic and citation display consistent). Build all compressed fixtures inside tests/test_genericlog.py with stdlib gzip.compress and zstandard.ZstdCompressor().compress — multi-member gzip = two compress outputs concatenated; multi-frame zstd likewise (proves read_across_frames, Pitfall 3 — without it events would vanish silently); no system zstd tool needed (RESEARCH Environment Availability). Corrupt-input test: valid zstd magic followed by garbage bytes must raise (zstandard.ZstdError or ValueError) from the parse iterator. Name tests so `-k compressed` selects the group.</action>
  <verify>
    <automated>uv run pytest tests/test_genericlog.py -k compressed -x -q && uv run pytest -q && uv run ruff check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_genericlog.py -k compressed -x` exits 0 with at least 4 tests (gz, multi-member gz, zst, multi-frame zst)
    - A test asserts equality of per-event (byte_offset, line_start, message) tuples between the plain and each compressed variant
    - A test asserts the extension-vs-magic case (gzip content under a .log name decompresses)
    - A test asserts the corrupt-zst case raises
    - Full suite still green
  </acceptance_criteria>
  <done>gzip/zstd inputs ingest without manual decompression (INGST-10), multi-member/multi-frame safe, offsets deterministic on the decompressed stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial log bytes -> parser | attacker-controlled file content drives regexes, decoding, and memory allocation |
| compressed input -> decompressor | tiny input can expand enormously (bomb) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | open_bytes + parse loop (decompression bomb) | medium | mitigate | Streaming decompression, never materialise the whole stream; per-event 64 KB / 256-line caps bound memory (D-06); nothing written decompressed to disk |
| T-03-02 | Tampering | record decoding (invalid/hostile encodings) | low | accept | errors-replace decoding only after byte offsets are fixed — malformed bytes cannot corrupt identity or coverage; rendering-side sanitisation is T-04-01 in plan 01-04 |
</threat_model>

<verification>
- `uv run pytest tests/test_genericlog.py -q` green across all six test groups
- Full gate: `uv run pytest && uv run ruff check && uv run pyright`
</verification>

<success_criteria>
- INGST-04/05/06/10/11 behaviours proven by the per-requirement test commands in 01-VALIDATION.md
- Span-partition invariant holds on every fixture encoding
- Compressed variants byte-offset-identical to plain variants
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-event-contract-genericlog-adapter/01-03-SUMMARY.md` when done
</output>
