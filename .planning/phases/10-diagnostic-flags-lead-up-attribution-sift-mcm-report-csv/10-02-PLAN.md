---
phase: 10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/sift/config.py
  - src/sift/pipeline/mcm.py
  - tests/test_config.py
  - tests/test_mcm.py
  - tests/fixtures/mcm/hartford_deny_double.log
autonomous: true
requirements: [MCM-03]
must_haves:
  truths:
    - "compute_flags emits five graded diagnostic flags (working-set % of IServer virtual, other-processes % of physical, cube-cache/MMF coverage, SmartHeap releasability, system-free headroom), each a ratio part/whole*100, each severity in {info, warn, critical}"
    - "On the real Hartford slice the episode reads CRITICAL overall — working-set flag critical (65.4% of virtual), other-processes and headroom warn, cube/MMF and SmartHeap info (calibration anchor)"
    - "system_free_headroom_pct is graded DOWNWARD (lower % is worse): free=50% -> info, free=3% -> critical (inverted-metric special case)"
    - "Thresholds are documented constants in McmThresholdsConfig with defaults equal to the RESEARCH table; an absent [mcm.thresholds] block yields those defaults; a [mcm.thresholds] override changes the grading (config-only, precedence CLI>env>toml>defaults)"
    - "A typo'd key inside [mcm.thresholds] fails loudly (extra='forbid'), never silently dropped"
    - "Machine-independence: the ×2-scaled fixture yields byte-identical (dimension, severity, round(value_pct,3)) tuples AND identical window threshold_pct/request_count versus the original (success criterion #5)"
    - "An episode with an absent/empty breakdown (open/truncated, all accessors None) still returns flags without crashing — a dimension whose inputs are None simply emits no flag (nothing fabricated)"
  artifacts:
    - "src/sift/config.py — McmConfig + McmThresholdsConfig (+ ThresholdPair) with [mcm.thresholds] defaults; SiftConfig.mcm field"
    - "src/sift/pipeline/mcm.py — DiagnosticFlag model; compute_flags() pure function; _grade helper with inverted-direction support"
    - "tests/fixtures/mcm/hartford_deny_double.log — every absolute memory/byte token of hartford_deny_slice.log multiplied by 2 (integral)"
    - "tests/test_config.py + tests/test_mcm.py — threshold-config, flag-grading, Hartford-calibration, inverted-headroom and machine-independence cases"
  key_links:
    - "compute_flags reads the Phase-9 MemoryBreakdown typed accessors (working_set_mb, iserver_virtual_mb, other_processes_mb, physical_total, cube_caches_mb, mmf_mb, smartheap_unused_pool_mb) — no re-extraction from raw_map"
    - "compute_flags takes McmThresholdsConfig (config.mcm.thresholds) — the config→pipeline threshold injection"
  prohibitions:
    - "MUST NOT express any threshold or headline figure as an absolute GB — every flag cut-point and displayed value is a percentage of HWM/total (milestone-locked; the one machine-independence invariant)"
    - "MUST NOT expose a per-run CLI threshold flag — thresholds are config-only (D-12)"
    - "MUST NOT introduce a new dependency"
    - "MUST NOT invoke the LLM or the network"
    - "MUST NOT fabricate absent signals — a None breakdown input emits no flag, never a fabricated percentage"
    - "MUST NOT write to a store table or add a migration"
---

<objective>
Grade the reference's binary flag heuristics into the info/warn/critical model
D-12 asks for, and land the `[mcm.thresholds]` config section whose defaults are
calibrated (RESEARCH Deliverable 1) so the real Hartford episode reads CRITICAL —
driven by the correct root cause, working-set = 65.4% of IServer virtual. This is
MCM-03: five deterministic diagnostic flags, every cut-point a ratio (% of
HWM/total, never absolute GB), config-overridable but with no per-run CLI knob.
Also prove success criterion #5 (machine-independence) with a ×2-scaled fixture:
because every metric is `part/whole`, scaling all absolute figures leaves every
percentage and every flag tier byte-identical.

Purpose: the graded flags are the verdict layer of the `sift mcm` report; the
inverted headroom metric and the scaled-fixture proof are the two subtle
correctness risks this plan closes.
Output: `DiagnosticFlag` + `compute_flags` + `McmThresholdsConfig` (+ the
`[mcm.thresholds]` schema) + the machine-independence test and fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-RESEARCH.md
@.planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-PATTERNS.md
@src/sift/pipeline/mcm.py
@src/sift/config.py
@docs/reference/analyze_dss8.py
@tests/test_mcm.py
@tests/fixtures/mcm/hartford_deny_slice.log
</context>

## Artifacts this phase produces

This plan (10-02) produces:
- **`McmThresholdsConfig`** (in `src/sift/config.py`), frozen-style `ConfigDict(extra="forbid")`, fields with defaults from the RESEARCH Deliverable-1 table: `working_set_pct_virtual`, `other_processes_pct_physical`, `cube_pct_virtual`, `smartheap_pool_pct_virtual`, `system_free_headroom_pct` (each a `ThresholdPair(warn, critical)`), and scalar `mmf_pct_of_cube_low`.
- **`ThresholdPair`** helper model (`warn: float`, `critical: float`, `extra="forbid"`).
- **`McmConfig`** wrapper with `thresholds: McmThresholdsConfig` so the TOML section is literally `[mcm.thresholds]`; registered on `SiftConfig` as `mcm: McmConfig = McmConfig()`.
- **`DiagnosticFlag`** frozen model (in `mcm.py`): `dimension: str`, `severity: str`, `value_pct: float`, `message: str` (British English, % inline), `event_ids: tuple[str, ...]`.
- **`compute_flags(ep: McmEpisode, thresholds: McmThresholdsConfig) -> tuple[DiagnosticFlag, ...]`** + **`_grade(value_pct, warn, crit, *, invert=False)`** helper.
- **`tests/fixtures/mcm/hartford_deny_double.log`** — ×2-scaled fixture.

<tasks>

<task type="auto">
  <name>Task 1 (RED): scaled fixture + failing config-threshold, flag-grading, calibration, inverted-headroom and machine-independence tests</name>
  <files>tests/fixtures/mcm/hartford_deny_double.log, tests/test_config.py, tests/test_mcm.py</files>
  <read_first>
    - tests/fixtures/mcm/hartford_deny_slice.log (the source to scale ×2 — Format-A `Label(UNIT): value` block, Current Memory Info byte figures, succeeded-line `Size=`/`AvailableMCM=`/`HWM(PB)=`)
    - .planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-RESEARCH.md (Deliverable 1 threshold table + Hartford computed ratios: working-set 65.4%, other-proc 18.5%, headroom 9.85%; Deliverable 4 scaled-fixture design; Pitfall 2 inverted metric; Pitfall 3 non-integral scaling)
    - src/sift/pipeline/mcm.py:131-171 (MemoryBreakdown typed accessors compute_flags will read)
    - tests/test_config.py (existing config-precedence test patterns to mirror for [mcm.thresholds])
    - src/sift/config.py:23-90 (GenerationConfig/SiftConfig/extra="forbid" idiom + tomllib layering)
  </read_first>
  <action>
Create `tests/fixtures/mcm/hartford_deny_double.log` by multiplying EVERY absolute
memory/byte quantity in `hartford_deny_slice.log` by 2 (×2 guarantees integers —
Pitfall 3): the Format-A `...(MB): N`/`...(GB): N` detail values, the Current
Memory Info byte figures (System Total/Available, HWM, Low Watermark, I-Server
lines), and every succeeded-line `Size=`/`AvailableMCM=`/`HWM(PB)=`. Leave
timestamps, IDs, `Source=` names and non-memory tokens untouched. A tiny committed
generator or an inline regex transform is fine as long as the result is
auditable and parses the same number of breakdown labels as the original.

Add failing tests (imports of `DiagnosticFlag`/`compute_flags`/`McmThresholdsConfig`
are RED until Tasks 2–3):
- `tests/test_config.py::test_mcm_thresholds_defaults`: `load_config({})` yields
  `config.mcm.thresholds.working_set_pct_virtual.warn == 20` and `.critical == 40`
  (and the other rows per the RESEARCH table); an absent `[mcm.thresholds]` block
  gives these documented defaults.
- `tests/test_config.py::test_mcm_thresholds_override_and_typo`: a `config.toml`
  with `[mcm.thresholds]` overriding `working_set_pct_virtual` changes the value;
  a `[mcm.thresholds]` with an unknown key raises `ValueError` (extra='forbid').
- `tests/test_mcm.py::test_flags_five_dimensions`: `compute_flags(ep, thresholds)`
  over the Hartford slice returns five flags, each with a `dimension`, a `severity`
  in {info,warn,critical}, a `value_pct` that is a ratio, and `event_ids ==
  (ep.denial_event_id,)`.
- `tests/test_mcm.py::test_hartford_flags_calibration`: working_set flag severity
  == "critical" with value_pct ≈ 65.4; other-processes and system-free-headroom ==
  "warn"; cube/MMF and SmartHeap == "info"; overall (max tier) is critical.
- `tests/test_mcm.py::test_headroom_inverted_grading`: with synthetic breakdowns,
  system-free-headroom at 50% grades info and at 3% grades critical (downward).
- `tests/test_mcm.py::test_machine_independence_scaled`: the tuple of
  `(dimension, severity, round(value_pct, 3))` from `compute_flags` is EQUAL across
  `hartford_deny_slice.log` and `hartford_deny_double.log`, and `select_window`'s
  `threshold_pct`/`request_count` are equal across the two (flags/percentages
  invariant under scaling; absolute breakdown MB legitimately differ).
Cite D-12 (graded, config-only) and the % -not-GB milestone lock in docstrings.
  </action>
  <acceptance_criteria>
    - Source: `hartford_deny_double.log` exists; it parses to the SAME number of Format-A labels as the slice — assert in a test that `len(breakdown.raw_map)` is equal for both fixtures (23).
    - Behaviour: every scaled absolute token is exactly twice the original and integral (spot-checkable by reading the file).
    - Test: `uv run pytest tests/test_config.py -k mcm_thresholds tests/test_mcm.py -k "flags or headroom or machine_independence or calibration"` exits NON-zero — RED (missing `McmThresholdsConfig`/`DiagnosticFlag`/`compute_flags`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_config.py -k mcm_thresholds tests/test_mcm.py -k "flags or headroom or machine_independence or calibration"</automated>
  </verify>
  <done>Scaled ×2 fixture committed; config-threshold, flag-grading, Hartford-calibration, inverted-headroom and machine-independence tests exist and fail only for want of the implementation (RED); committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): McmThresholdsConfig + [mcm.thresholds] section</name>
  <files>src/sift/config.py, tests/test_config.py</files>
  <behavior>
    - Absent [mcm.thresholds] -> documented defaults (RESEARCH table).
    - [mcm.thresholds] override wins per field under standard precedence; unknown key raises ValueError (extra='forbid').
    - system_free_headroom_pct default is ThresholdPair(warn=20, critical=5) — the inverted metric stored as-is; direction handled in the grader (Task 3), not here.
  </behavior>
  <read_first>
    - src/sift/config.py:23-106 (GenerationConfig/EmbeddingsConfig/ClusteringConfig/SiftConfig/_ENV_SCALARS/load_config — the exact section-registration + layering to mirror)
    - .planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-RESEARCH.md (Deliverable 1 "Config schema (suggested)")
    - .planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-PATTERNS.md (config.py analog notes: nested {warn,critical} stay TOML/flag-only like timezones/adapters — do NOT add to _ENV_SCALARS)
  </read_first>
  <action>
Add `ThresholdPair(BaseModel)` (`ConfigDict(extra="forbid")`, `warn: float`,
`critical: float`), `McmThresholdsConfig(BaseModel)` (`extra="forbid"`) with the
six fields and defaults from the RESEARCH Deliverable-1 table
(`working_set_pct_virtual = ThresholdPair(warn=20, critical=40)`,
`other_processes_pct_physical = (10, 20)`, `cube_pct_virtual = (25, 40)`,
`mmf_pct_of_cube_low: float = 10`, `smartheap_pool_pct_virtual = (5, 15)`,
`system_free_headroom_pct = ThresholdPair(warn=20, critical=5)`), and
`McmConfig(BaseModel)` (`extra="forbid"`) with `thresholds: McmThresholdsConfig =
McmThresholdsConfig()`. Register `mcm: McmConfig = McmConfig()` on `SiftConfig`
(mirrors generation/embeddings/clustering at config.py:74-76). The `[mcm.thresholds]`
TOML table maps to `config.mcm.thresholds` through the existing `tomllib.loads`
layering — no loader change needed. Do NOT add the nested threshold mappings to
`_ENV_SCALARS` (scalars only; nested mappings stay TOML/flag-only per D-12). Run
the config tests to GREEN.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "class McmThresholdsConfig" src/sift/config.py` == 1; `SiftConfig` has an `mcm` field; `[mcm.thresholds]` keys are NOT present in `_ENV_SCALARS`.
    - Test: `uv run pytest tests/test_config.py -k mcm_thresholds` passes; the rest of `tests/test_config.py` stays green (precedence unbroken).
    - Gate: `uv run ruff check` and `uv run pyright` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_config.py -k mcm_thresholds && uv run pyright src/sift/config.py</automated>
  </verify>
  <done>[mcm.thresholds] config section lands with calibrated defaults, override + typo-loud behaviour; config tests GREEN; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): DiagnosticFlag + compute_flags (five dimensions, inverted headroom)</name>
  <files>src/sift/pipeline/mcm.py, tests/test_mcm.py</files>
  <behavior>
    - Five flags: working_set_pct_virtual, other_processes_pct_physical, cube_mmf (conditional), smartheap_releasable (conditional), system_free_headroom_pct.
    - Each value_pct = part/whole*100, rounded deterministically; None input -> flag omitted (never fabricated).
    - Hartford slice -> working_set critical (~65.4%), other-proc + headroom warn, cube/MMF + SmartHeap info.
    - system_free_headroom graded downward; ×2 scaled fixture -> identical (dimension,severity,round(value_pct,3)).
  </behavior>
  <read_first>
    - src/sift/pipeline/mcm.py:116-171 (MemoryBreakdown accessors) and :174-188 (McmEpisode)
    - .planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-RESEARCH.md (Deliverable 1 per-dimension cut-points + justification; Code Example "Flag computation"; Pitfall 2 inverted headroom)
    - docs/reference/analyze_dss8.py:79-85 and :507-624 (THRESHOLDS + _write_detail/_write_current_info heuristics being graded)
    - src/sift/config.py (McmThresholdsConfig from Task 2 — the injected thresholds type)
  </read_first>
  <action>
Add the `DiagnosticFlag` frozen model (`ConfigDict(frozen=True, extra="forbid")`)
with `dimension`, `severity`, `value_pct`, `message` (British-English one-liner
with the % inline), `event_ids: tuple[str, ...]`. Add `_grade(value_pct, warn,
crit, *, invert=False) -> str`: upward `critical if v>=crit elif v>=warn else
info`; when `invert=True` (headroom) the comparison flips (`critical if v<=crit
elif v<=warn else info`) so a LOW free-% is worst. Implement
`compute_flags(ep, t)`:
- `cite = (ep.denial_event_id,)` (the breakdown is parsed from the denial banner's
  block — D-16 provenance).
- working_set % of iserver_virtual → `working_set_pct_virtual`.
- other_processes % of physical_total → `other_processes_pct_physical`.
- cube/MMF conditional: cube_caches % of iserver_virtual graded against
  `cube_pct_virtual`, and MMF coverage of cube against `mmf_pct_of_cube_low` per
  the reference gate (only material when cubes dominate).
- SmartHeap releasability: `mcm_settings["SmartHeap Cache Releasable"]` — releasable
  → info regardless of pool size; else grade pool % of virtual against
  `smartheap_pool_pct_virtual`.
- system-free-headroom from Current Memory Info (`System Available`/`System Total`)
  graded with `invert=True`.
Guard every `None`/zero-denominator (accessor returns `float | None`) BEFORE
dividing — a missing input emits NO flag (nothing fabricated, D-03). Round
`value_pct` deterministically (e.g. 1 dp for display; the machine-independence
test rounds to 3 dp). Keep the function pure. Run the Task-1 flag/calibration/
headroom/machine-independence tests to GREEN.

Note the % -not-GB rule: `message` and `value_pct` are always percentages; never
emit an absolute-GB figure as a flag headline (milestone lock). Do not put any
acceptance-gated literal token verbatim in a code comment.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "def compute_flags" src/sift/pipeline/mcm.py` == 1; the module still imports no typer/sqlite3/httpx/csv (I/O-free); `compute_flags` reads only `MemoryBreakdown` accessors + `mcm_settings`/`current_memory_info`, never `raw_map` string-matching directly.
    - Behaviour: on `hartford_deny_slice.log` the working-set flag is critical (value_pct within 0.2 of 65.4); overall max tier critical; on an open/truncated episode with empty breakdown compute_flags returns without raising (flags for None-input dimensions omitted).
    - Test: `uv run pytest tests/test_mcm.py -k "flags or headroom or machine_independence or calibration"` passes (GREEN); full `uv run pytest tests/test_mcm.py` green.
    - Gate: `uv run ruff check` and `uv run pyright` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mcm.py -k "flags or headroom or machine_independence or calibration" && uv run ruff check && uv run pyright</automated>
  </verify>
  <done>Five graded % -based flags computed deterministically; Hartford calibrates CRITICAL; inverted headroom correct; ×2 scaled fixture yields identical flag tiers/percentages (criterion #5); MCM-03 satisfied; ruff+pyright+test_mcm clean; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dsserrors breakdown text → flags | Untrusted memory-dump numbers/labels re-parsed into ratios |
| config.toml → thresholds | Local user-authored `[mcm.thresholds]` values injected into the grader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | inverted headroom grading | medium | mitigate | Direction-aware `_grade(invert=True)` with an explicit unit test (free=50%→info, free=3%→critical); a copy-paste upward comparison would mis-grade high headroom as critical — the test is the guard |
| T-10-05 | Tampering | absolute-GB leak into a flag headline | high | mitigate | Every `value_pct`/`message` is a ratio; the machine-independence test (×2 scaled → identical tuples) fails loudly if any flag depends on an absolute figure — the milestone-locked invariant is mechanically enforced |
| T-10-06 | Denial of Service | zero/None denominators in ratio maths | low | mitigate | Guard every `MemoryBreakdown` accessor for `None`/zero before dividing; a missing input omits the flag rather than raising |
| T-10-07 | Tampering | malformed `[mcm.thresholds]` config | low | mitigate | `extra='forbid'` on `McmThresholdsConfig`/`ThresholdPair` makes a typo'd key fail loudly at load, never silently default (T-04-02 precedent) |
| T-10-SC | Tampering | dependency installs | low | accept | No package installs — stdlib + already-installed Pydantic only |
</threat_model>

<verification>
- `uv run pytest tests/test_mcm.py tests/test_config.py` — flags, calibration, inverted headroom, machine-independence, thresholds all green.
- `uv run ruff check` and `uv run pyright` clean.
- Manual: confirm no CLI threshold flag exists and no absolute-GB value appears in any flag `message`/`value_pct`.
</verification>

<success_criteria>
- Five deterministic % -based diagnostic flags graded info/warn/critical (MCM-03); Hartford reads CRITICAL on the correct driver.
- Thresholds are config-only (`[mcm.thresholds]`, D-12) with calibrated defaults; no per-run CLI knob.
- ×2-scaled fixture proves machine-independence (success criterion #5): identical flags + percentages + window threshold.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostic-flags-lead-up-attribution-sift-mcm-report-csv/10-02-SUMMARY.md` when done.
</output>
