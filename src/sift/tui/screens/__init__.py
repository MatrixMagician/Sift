"""Screen modules for the case browser.

One module per screen family: ``base`` (shared bindings + guarded reads),
``error`` (R012 failure surfaces), ``help_overlay`` (R013). T03/T04 add the
hypothesis, evidence, cluster and timeline screens alongside.
"""
